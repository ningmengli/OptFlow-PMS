# 00_项目核心规则_认识论（元规则）

> **位置**：项目元规则层（高于具体业务规则、ID 规则、UI 规则）
> **作用**：约束所有 FLOW-* 侦察报告、页面证据文档的评级方法论
> **生效时间**：2026-09-01 起
> **来源**：老板明确指令

---

## 🔴 核心原则

### 生产数据质量 ≠ 系统业务规则

生产环境中的历史数据可能存在以下情况：
- 员工未按规定流程操作
- 字段漏填
- 处方未录入
- 商品信息不完整
- 历史数据遗留
- 状态未按标准流程推进
- 不同员工使用习惯不同

---

## ⚠️ 严格禁止的推论

| 错误推论 | 为什么错 |
|---|---|
| ❌ 某条数据为空 → 推断系统没有该功能 | 页面有该字段 = 系统有该功能；数据空 = 员工没填 |
| ❌ 某条数据异常 → 直接认定系统存在 Bug | 系统功能/规则可能正确；只是这条数据有问题 |
| ❌ 员工没按规范操作 → 把实际结果当成标准业务流程 | 实际结果只是数据问题，不是规则 |
| ❌ 单条数据观察 → 推断系统业务规则 | 必须多维证据交叉验证 |

---

## ✅ 标准业务流程的判定方法

标准业务流程必须通过 **多维证据交叉验证**：

| 维度 | 证据来源 |
|---|---|
| 1. 页面结构 | 字段定义 / 按钮存在 / Tab 分类 / URL hash 命名 |
| 2. 多个真实样本 | ≥3 条独立样本验证一致性 |
| 3. 状态流转 | 同一实体在多个时间点的状态推进 |
| 4. ID 血缘 | 跨页面同一 ID 的字段对应关系 |
| 5. 业务规则 | 页面文案、字段约束、流程提示、副标题说明 |

**任一维度不满足 → 保持 E/F，不得强行定性。**

---

## 📊 异常/数据/规则的分级标准

每条观察/结论必须区分以下 4 类：

| 类别 | 含义 | 评级 |
|---|---|---|
| **A. 系统功能/结构事实** | 页面有什么字段 / 按钮 / Tab / 状态机 = 系统具备该功能 | **A**（直接观察即可）|
| **B. 标准业务规则** | 流程应该怎么走 / 字段应该怎么填 = 来自多维证据交叉验证 | **B/D**（需多维证据）|
| **C. 生产数据质量问题** | 某条数据当前为空/异常 = 员工没填/历史遗留 | **F**（实际数据状态）|
| **D. 系统疑似异常** | 通过多维证据仍无法用"员工操作/数据问题"解释 = 真正可能 Bug | **D**（需多维证据）|

### 关键判断规则
- **页面存在某字段 = A 级系统功能事实**（不依赖数据是否填写）
- **某条数据为空 = F 级数据问题**（不否定 A 级功能事实）
- **单条数据异常 ≠ D 级系统异常**（D 级需要多维证据）
- **无法区分 A/B/C/D 时 = 保持 E/F**（不强行定性）

---

## 🔁 对历史文档的影响

### 需要重新评估的旧结论（举例）
| 文档 | 旧评级 | 新评级（按本规则）|
|---|---|---|
| 33_PAGE009_李伊_已完成加工详情验证.md - 处方表全空 = "A 异常" | A 异常 | **F 生产数据问题**（页面有处方表 = A 功能；李伊处方数据空 = F 数据问题）|
| 33_PAGE009_李伊_已完成加工详情验证.md - PAGE-008 搜不到李伊 = "重大矛盾" | A 矛盾 | **F 数据问题或视图差异**（PAGE-008/PAGE-009 数据范围可能不同 = 不否定系统 ID 桥可能性）|
| 31_PAGE009_加工链异常专项验证.md - 陶可馨无加工记录 = 业务规则 | 业务规则 | **F 数据问题**（陶可馨商品确实无加工 = 数据问题，不是规则禁止）|

### 历史文档修订原则
- 不得删除历史记录（保留原始观察）
- 在原文档基础上**追加"按本规则重评"**章节
- 新观察/新结论直接套用本规则

---

## 📝 评级使用规范

### 升级路径（从观察 → 证据 → 规则）
```
F（单点观察）
  ↓ + 多维证据交叉
D（疑似异常，需多维验证）
  ↓ + 完整多维证据
A/B（系统事实 / 标准规则）
```

### 禁止的跳跃
- ❌ F 直接升 A（单条数据 → 系统事实）
- ❌ F 直接升 B（单条数据 → 标准规则）
- ❌ 1 个样本 → 多样本验证（必须 ≥3 独立样本）

### 允许的保持
- ✅ 现象 = F，规则保持 = E
- ✅ 找不到证据 = F
- ✅ 矛盾无法解释 = E（保持）

---

## 🛡️ 强制复核清单

每次提交 FLOW-* 文档前必须复核：

- [ ] 每条 A 级都有**页面结构**或**多样本**支撑？
- [ ] 每条 B 级都有**多维证据交叉**？
- [ ] 每条 D 级都排除了"员工操作/数据问题"？
- [ ] 每条 F 级都没有被强行升 A/B？
- [ ] 异常/数据/规则已正确分类到 A/B/C/D 四类？
- [ ] 无法区分时保持 E/F？

---

## 📌 应用范围

本规则适用于：
- ✅ 所有 PAGE-* 26 项证据文档
- ✅ 所有 FLOW-* 侦察报告
- ✅ 所有 ID 血缘专项回测
- ✅ 所有加工链异常专项验证
- ✅ 所有未来新增的页面/流程侦察

本规则**不适用**于：
- ❌ 系统源代码分析（属于代码层证据）
- ❌ API 接口直接调用验证（属于协议层证据）

---

## 引用方式

在文档中引用本规则时使用：

> 「按项目核心规则 00_项目核心规则_认识论.md，本观察属于 [A/B/C/D] 类。」

---

## 关键补充（2026-09-01 老板追加）

### 「员工没有按规范使用系统」本身也是一种数据质量变量

**禁止推论模式**：
```
❌ 页面没有记录 → 系统没有这个功能
```

**正确逻辑**：
```
页面没有记录
   ↓
可能是：
   - 没有发生（业务未触发）
   - 员工没有操作（漏填）
   - 员工操作错误（填错位置/填错数据）
   - 数据没有保存（提交失败/页面崩溃）
   - 查询条件问题（默认日期范围/搜索条件）
   - 权限问题（账号无权限/角色限制）
   - 系统 Bug（数据丢失/同步失败）
   ↓
在没有进一步证据前，只能标 F
```

### 强制适用

所有 PAGE-* / FLOW-* 文档的观察 → 在写入 A/B 级结论前必须自检：

1. 该观察是否依赖**数据值**？是 → 可能是数据问题
2. 该观察是否依赖**页面结构**？是 → 可以是 A 级功能事实
3. 该观察是否依赖**单条样本**？是 → 至少保持 E，需要 ≥3 样本才能升 B
4. 该观察是否**排除**了员工操作/数据问题？否 → 不能升 D



---

## 文件元信息

- **路径**：`E:\C\minimax\OptFlow PMS\00_项目核心规则_认识论.md`
- **层级**：项目元规则（统领所有业务规则/ID 规则/UI 规则）
- **生效**：2026-09-01
- **来源**：老板明确指令
- **影响范围**：所有 FLOW-* 文档的评级方法论
