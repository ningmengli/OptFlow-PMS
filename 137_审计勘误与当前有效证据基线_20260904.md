# 审计勘误与当前有效证据基线

> 建立日期：2026-09-04
>
> 目的：不改写历史侦察报告，记录 2026-09-04 对当前文档库、`controller.js` 与原系统只读页面的复核结果。本文是后续架构与开发讨论的当前入口；历史报告仍保留其形成当日的证据语境。

---

## 1. 使用规则

1. 历史文件中的事实只在其证据范围和形成时间内使用。
2. 本文列出的勘误优先于被点名报告的全局结论。
3. 未纳入 Git 的原始材料不能仅凭“报告已 Git 封口”视为可复现证据。
4. 页面可见事实、前端调用点事实、组件内部实现事实必须分级记录，不能相互替代。

---

## 2. 当前证据资产状态

| 项目 | 当前状态 | 审计结论 |
|---|---|---|
| Markdown 文档 | 143 个已由 Git 跟踪 | 文档版本可追溯 |
| 原始前端材料 | `controller.js` 与 9 个 HTML 为未跟踪文件 | 不能由提交独立复现 |
| `controller.js` | 2,194,196 字节 | 本次代码调用点审计的直接来源 |
| `controller.js` SHA-256 | `F40589FF0C56DE60BF4785A0B6C5B0A33519B75A30A15D988B0E1BDCF0A19433` | 后续复核必须比对此哈希或记录新的哈希 |
| 原系统页面 | 2026-09-04 已在超级管理员会话中只读打开 | 可继续用于补齐 UI、字段、状态与路由证据；不用于替代缺失的 bundle / Factory 内部源码 |

**Git 封口边界**：S1-76 的 Markdown 提交已固定；它不等同于其引用的原始 JavaScript 已固定。

---

## 3. S1-74 至 S1-76 审计勘误

### 3.1 S1-76 ObjectFactory 实例数

**勘误结论**：`deliveryInputCtrl` 内有 **6 个** `new ObjectFactory()` 调用，不是 7 个；6 个实例与 6 个 API 一一对应。

| 调用行 | Factory 变量 | API |
|---|---|---|
| 3835 | `getCenterListFactory` | `getMedicalProductMachineCenterVoList.json` |
| 3840 | `getStatusDeliveryFactory` | `statProductDeliveryStatusOfCashflow.json` |
| 3847 | `getMedicalRecordDeliveryFactory` | `getCashflowDeliveryVo.json` |
| 3884 | `getDeliveryListFactory` | `getCanBeDeliverySkuInListOfProduct.json` |
| 3965 | `createOrderFactory` | `sendMedicalProductToMachineCenter.json` |
| 4006 | `saveStockFactory` | `saveMedicalProductStockBatch.json` |

S1-76 将 `showAddModal` 内的 `getCenterListFactory` 重复计数。以下结论仍可保留：这 6 个 API 均由 ObjectFactory 调用，当前 controller 范围没有这 6 个 API 的直接 `$http` 调用。

### 3.2 不能从“零调用”推出“方法不存在”

S1-76 已确认 ObjectFactory 内部定义不在当前 `controller.js` 范围。故以下表述必须区分：

| 原报告表述 | 当前有效表述 | 等级 |
|---|---|---|
| ObjectFactory 没有 `search()` | 当前 controller 未观察到 ObjectFactory `.search()` 调用 | A（调用点）/ F（组件能力） |
| ObjectFactory 没有 `nextPage()` | 当前 controller 未观察到 ObjectFactory `.nextPage()` 调用 | A / F |
| ObjectFactory 没有 `clearAndSetIndex()` | 当前 controller 未观察到该调用 | A / F |
| ObjectFactory 没有 `items` / `count` | 当前 controller 未观察到 ObjectFactory 实例读取这些属性 | A / F |

因此，S1-76 中关于这些成员“完全不存在”的能力性结论撤销；仅保留调用点未观察到的事实。

### 3.3 S1-74 / S1-75 ListFactory 全局参数模式

对当前 `controller.js` 的 406 个 `new ListFactory(...)` 词法调用作括号与字符串安全解析后：

| 参数数量 | 数量 | 当前有效结论 |
|---|---:|---|
| 4 参数 | 392 | 常见模式，不是全量模式 |
| 3 参数 | 14 | 反例存在，不能宣称全部 4 参数 |

第二参数也并非恒为 `0`。例如第 2354、4195、15996 行使用 `pageStart`，第 54388 行使用 `$scope.idxNum * 10`。因此 S1-74 / S1-75 的“406 次均为四参数”“参数二均为 0”“100% 一致”均撤销为全局结论。

可保留的窄结论：各自列出的具体 Controller 调用（例如 `machineOrderListCtrl` 的四参数调用）仍可按原始行号单独作为 A 级调用点事实使用。

### 3.4 其他措辞边界

- `result.object / result.list 100% 跨 Controller 一致`：缺少完整、可复核的实例-属性关联统计，降为“已在列举调用点观察到”。
- S1-76 将第 3962 行标成“ListFactory 间接”，不正确；该行消费的是 `getCenterListFactory.result.list`，而该变量是 ObjectFactory 实例。
- Factory 的内部请求构造、响应回填、属性定义、分页算法继续维持 F，直到取得对应 vendor bundle 或定义源码。

---

## 4. 阶段 0 有效完成件映射

52 号报告是 2026-09-01 的阶段启动快照，其中的 0/17、0/1、待侦察状态不得再作为当前状态引用。下表提供计划编号与实际完成件的唯一映射。

| 52 号计划项 | 当前有效完成件 | 状态 |
|---|---|---|
| PAGE-809 管理员 | 53_PAGE809_管理员_26项.md | 已完成 |
| PAGE-810 操作证书 | 74_PAGE810_操作证书_26项.md | 已完成；54 号为占位件 |
| PAGE-801 门店 | 55_PAGE801_门店_26项.md | 已完成 |
| 9I.1 公司信息 | 68_9I.1_公司信息_26项.md | 已完成；56 号为占位件 |
| 9A.1 挂号收费 | 64_9A.1_挂号收费_26项.md | 已完成；57 号为占位件 |
| 9A.2 检查收费 | 65_9A.2_检查收费_26项.md | 已完成；58 号为占位件 |
| 9A.3 物资收费 | 66_9A.3_物资收费_26项.md | 已完成；59 号为占位件 |
| 9A.5 品牌 | 60_9A.5_品牌_26项.md | 已完成 |
| 9A.6 供应商 | 61_9A.6_供应商_26项.md | 已完成 |
| 9A.7 仓库 | 62_9A.7_仓库_26项.md | 已完成 |
| 9A.8 加工中心 | 63_9A.8_加工中心_26项.md | 已完成 |
| 9I.2 会员类型 | 69_9I.2_会员类型_26项.md | 已完成；旧 64 号同号文件不是本项完成件 |
| 9I.3 会员来源 | 70_9I.3_会员来源_26项.md | 已完成；旧 65 号同号文件不是本项完成件 |
| 9I.4 会员卡充值设置 | 71_9I.4_会员卡充值设置_26项.md | 已完成；旧 66 号同号文件不是本项完成件 |
| 9I.10 收银台设置 | 67_9I.10_收银台设置_26项.md | 已完成 |
| 9I.11 支付方式 | 72_9I.11_支付方式_26项.md | 已完成；旧 68 号同号文件不是本项完成件 |
| 9I.17 报表默认日期设置 | 73_9I.17_报表默认日期设置_26项.md | 已完成；旧 69 号同号文件不是本项完成件 |
| `/clinic/settings` | 75_clinicSettings_独立应用参数设置_26项.md | 已完成；旧 70 号同号文件不是本项完成件 |

**当前进度口径**：17 个 S1 计划项均已有完成件；独立应用参数设置已有完成件。完成侦察不自动等于全部 P0 决策已经解除，需依据每个完成件的 E/F 项与老板决策另行判定。

---

## 5. 当前可用与不可用基线

### 可直接引用

- 指向具体页面、具体代码行或具体原始样本的 A 级事实。
- 本文第 3.1 的 6 个 deliveryInputCtrl API 调用点。
- 本文第 4 节的当前有效完成件映射。
- 原系统首页可见的模块入口，仅作为 UI 入口事实。

### 不可直接引用为开发实现依据

- 未提供 Factory 定义源码时的 Factory 内部行为。
- “全仓 100% 一致”但未提供可复核解析清单的全局统计。
- 52、50、51、08 等历史总览中的“待侦察 / 0 进度 / 暂缓编码”状态，除非在新的决策台账中重新确认仍然有效。
- 同号文件名本身；引用必须同时使用完整文件名和章节/行号。

---

## 6. 下一步控制项

1. 将 `controller.js` 与 HTML 原始材料纳入 Git，或建立受版本控制的证据清单（路径、获取时间、大小、SHA-256）。
2. 为 S1-74、S1-75、S1-76 建立勘误引用，不改历史正文也不再引用其被撤销的全局结论。
3. 新建“P0 决策与剩余 E/F 台账”：逐项写明证据完成、老板已决、仍阻塞、可延后，替代 52/50/51 中的历史快照状态。
4. 使用已登录原系统以只读方式补齐具体页面的字段、枚举、默认值、校验与权限事实；任何写操作均需另行确认。
5. 取得 vendor bundle / Factory 定义源码前，不实现或断言 ObjectFactory、ListFactory 的内部算法。

---

## 7. 审计结论

阶段 0 的页面侦察文件已具备可用的完成件集合，但总控文档、编号映射与 Factory 调用协议存在需要优先纠正的断裂。后续开发只能以“可复核的具体调用点 + 当前有效完成件映射 + 单独的 P0 决策台账”为基线，不以历史快照或未经验证的全局 100% 结论为基线。
