# P0 决策与剩余 E F 台账

> 建立日期：2026-09-04
>
> 依据：137 号《审计勘误与当前有效证据基线》、53/55/60-75 号阶段 0 完成件、S1-74 至 S1-76 调用协议勘误。
>
> 目的：用可关闭的决策和验证项替代历史报告中累计式的“56 个 P0”口径。本文不改写历史文件。

---

## 1. 闸门规则

仅当未知项会直接改变以下任一内容时，才保留为 P0：

- 一期范围；
- 技术主键、租户隔离、核心外键；
- 收费、余额、发货、加工等核心闭环的不可逆业务规则；
- 角色权限或审计的安全边界。

页面样式、排序、弹窗、字段数据库命名、未使用的遗留 ID、单个页面的未知交互，默认是 P1 或 P2；不能因为原报告中累计出现而自动成为 P0。

---

## 2. 当前结论

| 结论 | 状态 |
|---|---|
| 17 个 S1 页面和 `/clinic/settings` 是否已有完成件 | 是，映射见 137 号 §4 |
| 是否可把历史“56 个 P0”直接当成当前阻塞数 | 否，存在重复、粒度不一致和已完成侦察项 |
| 是否可立即开始编码 | 否，先完成本台账中的 P0 决策与验证关口 |
| 是否要继续无差别侦察全部菜单 | 否，只补本台账列出的 P0 验证项；其余按对应实施阶段再做 |

---

## 3. P0 老板决策台账

| ID | 决策 | 当前建议 | 关闭条件 | 状态 |
|---|---|---|---|---|
| D-01 | 一期范围：患者维护、营销管理、筛查机构、高级报表的取舍 | 患者维护和筛查机构纳入；营销暂缓；高级报表定 S3，先做基础报表 | 老板给出范围确认 | 待决策 |
| D-02 | 新系统主键策略 | 使用独立技术主键；原系统的已验证 ID 作为业务字段，不以推测的 legacy ID 建主键 | 老板确认该策略 | 待决策 |
| D-03 | 审计与操作记录 | 一期建立最小审计记录：操作者、时间、动作、对象、前后值摘要 | 老板确认是否一期必做及保留期限 | 待决策 |
| D-04 | S1 系统设置的一期交付范围 | 已完成侦察的 S1 配置纳入一期；证书管理可按需、非核心闭环实现 | 老板确认是否要求 PAGE-810 完整 1:1 实现 | 待决策 |
| D-05 | 原系统 Factory 的复刻目标 | 不复刻黑盒 ObjectFactory/ListFactory 内部；新系统按已验证 API 业务契约实现 | 老板确认“业务复刻”而非“前端内部实现复刻” | 待决策 |

---

## 4. P0 原系统只读验证台账

| ID | 需核验的事实 | 现有证据 | 只读核验路径 | 关闭条件 | 状态 |
|---|---|---|---|---|---|
| V-01 | 门店、公司、管理员之间的实际关联字段及作用域 | PAGE-801 列表未显示 `hospitalId`；PAGE-809 显示所属医院但角色/部门不完整 | PAGE-801 修改页、PAGE-809 角色维护页；只查看，不保存 | 明确新系统是否需要“租户 → 门店 → 管理员成员关系”及其最小字段 | 待核验 |
| V-02 | 角色与权限枚举 | PAGE-809 仅观察到部分角色和“查看”权限 | PAGE-809 角色维护页；只读取权限项 | 得到一期所需角色、权限编码或确认采用新系统权限字典 | 待核验 |
| V-03 | 支付方式到收费记录、余额扣减的实际链路 | 9I.11 已有支付方式字典；PAGE-007 有收费证据；具体 ID 桥不足 | 打开既有收费记录及详情，只观察已有数据，不发起收费/退款 | 明确付款方式的展示值、业务值及余额支付是否落入同一收费记录 | 待核验 |
| V-04 | 收银最低折扣权限的语义 | 9I.10 可见 50% 样本，含义未完整验证 | 9I.10 只读查看说明、既有收费详情的折扣展示 | 明确“最低折扣”是下限、上限还是审批阈值 | 待核验 |
| V-05 | `/clinic/settings` 对预约与排班的实际影响 | 五个配置字段可见；跨页影响未验证 | 进入预约排班页面，只读比较可用时段与配置 | 明确一期预约、排班需要消费哪些参数 | 待核验 |

**安全规则**：V-01 至 V-05 仅允许导航、读取、展开或打开已有数据；不点击保存、启用/停用、删除、退款、发货、扣减或新增按钮。

---

## 5. 已降级为 P1 或 P2 的事项

| 类别 | 典型事项 | 处理时点 |
|---|---|---|
| 遗留字段名与遗留 ID | `companyId`、`hospitalId`、`brandId`、`supplierId`、`depotId`、`feeItemId` 等未直接显示的原系统字段名 | 仅在数据迁移或对接需要时补证；新系统可先用独立主键与明确关系 |
| 配置页面交互细节 | 下拉完整枚举、picker 行为、取消按钮、排序、分页 | 对应页面开发前 |
| 非核心支付规则 | 挂账、医保/商保对接、积分细节、自定义支付方式编辑 | 支付扩展或对接阶段前 |
| 多门店与跨应用实现细节 | 配置同步、session 共享、夜间门诊开关行为 | 预约排班和多门店阶段前 |
| 报表默认日期细节 | 4 个 select 枚举、跨年校验、临时改日期 | 报表阶段前 |
| Factory 内部源码 | 请求封装、响应回填、分页算法 | 仅在要求前端内部 1:1 复刻时升级为 P0 |

---

## 6. 进入架构设计的门槛

以下项目关闭后，输出《一期数据库与业务架构最终设计》；在此之前不进入正式编码：

1. D-01 至 D-05 完成老板决策；
2. V-01、V-02、V-03 完成，或老板明确采用新系统规则并记录与原系统的差异；
3. V-04、V-05 至少完成“是否影响一期核心规则”的判定；
4. 原始前端材料建立版本清单（路径、大小、SHA-256、获取时间），或纳入 Git；
5. 137 与本文被列为当前有效入口，历史总报告不再承担实时状态职责。

---

## 7. 推荐执行顺序

1. 老板先确认 D-01 至 D-05。
2. 进行 V-01、V-02：一次只读会话完成组织、门店、管理员与权限边界。
3. 进行 V-03、V-04：一次只读会话完成收费、支付、余额和折扣规则核验。
4. 进行 V-05：一次只读会话完成预约排班参数影响核验。
5. 建立原始材料版本清单。
6. 输出并评审《一期数据库与业务架构最终设计》。

---

## 8. 当前下一动作

**下一动作：V-01 + V-02。** 在已登录原系统中只读查看门店修改页、管理员角色维护页，记录组织层级、管理员所属门店、角色与权限的可见事实；不保存任何内容。

