# S1-126 验光 → 开单 / 销售主链深度审计

## 0. 任务背景

S1-123~S1-125 已完成就诊流程与检查主链审计。本轮**回归验光 → 销售** 业务边界。

5 个 Controller 审计目标：
- **验光**: optometryCtrl / optometryGlassesCtrl
- **销售**: orderManageCtrl / addSaleRecordCtrl / adminSalesRecordCtrl

## 1. 审计范围

| Controller | 范围 | 行数 |
|---|---|---:|
| optometryCtrl | L34776-L35382 | 607 |
| optometryGlassesCtrl | L35383-L35961 | 579 |
| addSaleRecordCtrl | L31008-L31157 | 150 |
| adminSalesRecordCtrl | L31285-L31381 | 97 |
| orderManageCtrl | L36165-L36262 | 98 |
| **合计** | | **1531** |

## 2. 5 Controller API 全量

### 2.1 概览

| Controller | call site | unique API | Read | Write |
|---|---:|---:|---:|---:|
| optometryCtrl | 18 | 16 | 8 | 8 |
| optometryGlassesCtrl | 10 | 9 | 5 | 4 |
| addSaleRecordCtrl | 2 | 2 | 1 | 1 |
| adminSalesRecordCtrl | 3 | 3 | 3 | 0 |
| orderManageCtrl | 1 | 1 | 1 | 0 |
| **合计** | **34** | **31** | **18** | **13** |

### 2.2 optometryCtrl API

**Read (8)**:
- getAdminInfo / getCanBeDeliverySkuInListOfProduct / getCompanyOfMine / getLoginEmployee
- **getMedicalRecordFlowVo** / selectMedicalProductStockList / selectMedicalRecordFlowVoList / stateTodoMedicalRecordCount

**Write (8)**:
- **beginCustomerCheckin** / cancelMedicalRecord / completeMedicalRecordDelivery
- **confirmMedicalRecordReturn** / createMedicalStockLossOfSmallVersion / **insertCustomerCheckin**
- sendTakeMirrorNotice / setMedicalRecordProcessMode

### 2.3 optometryGlassesCtrl API

**Read (5)**:
- getCompanyOfMine / **getMedicalProductVoList** / **getMedicalRecord** / **getMethodGlassRecordVo** / **getProductSkuExistCountVoList**

**Write (4)**:
- checkBeforeDeleteMedicalProduct / **saveMedicalProductListOfSmallVersion** / selectStorehouseListOfCompany / **updateMethodGlassRecord**

### 2.4 addSaleRecordCtrl API

**Read (1)**: getHaveOrderMedicalRecordVoList
**Write (1)**: **addMedicalRecord** (核心开单 Write)

### 2.5 adminSalesRecordCtrl API

**Read (3)**: **getCustomerVo** / **getMedicalRecord** / **getPatientInfo**

### 2.6 orderManageCtrl API

**Read (1)**: getOkOrderRecordVoList

## 3. Controller 控制流（Optometry → Sale）

### 3.1 optometryCtrl $state.go 4 个出口

| # | State | Params | 行号 | 是 Sale 吗 |
|---:|---|---|---:|---|
| 1 | optometryGlasses | {medicalRecordId} | L34954 | **否** (验光业务内部) |
| 2 | payedList | {} | L35133 | **否** (Charge 业务) |
| 3 | optometryGlasses | {medicalRecordId, edit} | L35136 | **否** (验光业务内部) |
| 4 | waitChargeList | {} | L35145 | **否** (Charge 业务) |

**S1-126 关键发现 1（optometryCtrl 不跳 Sale）**：
- optometryCtrl 4 个 $state.go **没有一个** 跳转到 Sale Controller
- 跳到 optometryGlasses / payedList / waitChargeList
- 验光后**不直接**进入销售

### 3.2 optometryGlassesCtrl $state.go

| # | $state.go 出口 | 行号 |
|---:|---|---:|
| **0** | （无） | - |

**S1-126 关键发现 2（optometryGlassesCtrl 0 个 $state.go）**：
- 完全不跳任何 State
- 只在内部完成操作 + reload 自身数据
- **验光完成不跳转任何 Controller**

### 3.3 Optometry → Sale 控制流判定

| 起点 | 终点 | 机制 | 等级 |
|---|---|---|---|
| optometryCtrl | (none) | - | **F** |
| optometryGlassesCtrl | (none) | - | **F** |
| **结论** | - | - | **F（未建立）** |

## 4. State 桥详细

### 4.1 optometryCtrl 4 个 $state.go

| # | State | Params | 行号 |
|---:|---|---|---:|
| 1 | optometryGlasses | {medicalRecordId} | L34954 |
| 2 | payedList | {} | L35133 |
| 3 | optometryGlasses | {medicalRecordId, edit} | L35136 |
| 4 | waitChargeList | {} | L35145 |

### 4.2 addSaleRecordCtrl 5 个 $state.go

| # | State | Params | 行号 |
|---:|---|---|---:|
| 1 | addCheckin | {} | L31109 |
| 2 | **adminSalesRecord.myMaterialBill** | {medicalRecordId, patientId} | L31124 |
| 3 | adminMyRecord.myMaterialBill | {medicalRecordId, patientId} | L31129 |
| 4 | **adminSalesRecord.myMaterialBill** | {medicalRecordId, patientId} | L31144 |
| 5 | adminMyRecord.myMaterialBill | {medicalRecordId, patientId} | L31149 |

**S1-126 关键发现 3（addSaleRecordCtrl 实际是"开单"入口）**：
- 5 个 $state.go 中 4 个跳到 myMaterialBill（开单主页面）
- **接收 medicalRecordId + patientId 参数**
- 这是**独立**的"开单"入口，**不依赖 Optometry 触发**

### 4.3 adminSalesRecordCtrl 0 个 $state.go
### 4.4 orderManageCtrl 0 个 $state.go

## 5. StateParams 接收

| Controller | 行号 | 接收字段 |
|---|---|---|
| optometryCtrl | **0** | - |
| optometryGlassesCtrl | L35388-L35389 | medicalRecordId, edit |
| addSaleRecordCtrl | **0** | - |
| adminSalesRecordCtrl | L31286 | medicalRecordId |
| orderManageCtrl | **0** | - |

**S1-126 关键发现 4**：
- 5 Controller 中只有 2 个接收 $stateParams.medicalRecordId
- 其它 3 个（optometryCtrl / addSaleRecordCtrl / orderManageCtrl）的 medicalRecordId 来源是其它方式

## 6. medicalRecordId 全局分布

| Controller | medicalRecordId 命中 | 主要来源 |
|---|---:|---|
| optometryCtrl | **27** | State go params (L34954) / API Request (getMedicalRecordFlowVo) |
| optometryGlassesCtrl | **10** | $stateParams.medicalRecordId (A 类) |
| addSaleRecordCtrl | **5** | addMedicalRecord.json Response / State go params |
| adminSalesRecordCtrl | **3** | $stateParams.medicalRecordId / getMedicalRecord Response |
| orderManageCtrl | **9** | List item.okOrderRecord.medicalRecordId |
| **合计** | **54** | |

### 6.1 medicalRecordId 跨 Controller 派生

```
optometryCtrl L34954: $state.go("optometryGlasses", { medicalRecordId })  [State 桥 A]
  ↓
optometryGlassesCtrl L35388: $scope.medicalRecordId = $stateParams.medicalRecordId  [A 类]
  ↓
optometryGlassesCtrl L35415: $scope.getMedicalRecord() → getMedicalRecord.json
  ↓ Response
$scope.medicalRecord = response.object  [Type 4 整体]
  ↓
[Write saveMedicalProductListOfSmallVersion.json] L35950
  ↓
$scope.GetMedicalProductVoList()  [reload 自身]

addSaleRecordCtrl L31119: addMedicalRecord.json  [Write 开单]
  ↓ Response
L31123: $state.go("adminSalesRecord.myMaterialBill", { medicalRecordId: res.object.id, patientId: res.object.patientId })
  ↓
adminSalesRecordCtrl L31286: $scope.medicalRecordId = $stateParams.medicalRecordId  [A 类]
  ↓
adminSalesRecordCtrl L31331: getMedicalRecord.json { id: $scope.medicalRecordId }  [Read]
  ↓ Response
$scope.patientId = res.result.object.patientId  [Type 3 派生]
  ↓
adminSalesRecordCtrl L31336: getPatientInfo.json { id: $scope.patientId }  [Read]
  ↓ Response
$scope.customerId = res.result.object.customerId  [Type 3 派生]
  ↓
adminSalesRecordCtrl L31344: getCustomerVo.json { customerId: res.result.object.customerId }
```

**S1-126 关键发现 5（medicalRecordId 是核心桥）**：
- medicalRecordId 在 5 Controller 中都是核心 ID
- 但**有两条独立路径**：
  - **路径 A**: optometryCtrl → optometryGlassesCtrl (验光链)
  - **路径 B**: addSaleRecordCtrl → adminSalesRecordCtrl (开单链)
- **两条路径不交叉** —— Optometry 和 Sale 通过 medicalRecordId **共现**，但**没有直接 State 链**

## 7. medicalProduct 桥详细

### 7.1 medicalProduct 命中统计

| Controller | medicalProduct | medicalProductId |
|---|---:|---:|
| optometryCtrl | **31** | 7 |
| optometryGlassesCtrl | **20** | 2 |
| addSaleRecordCtrl | **0** | **0** |
| adminSalesRecordCtrl | **0** | **0** |
| orderManageCtrl | **0** | **0** |

**S1-126 关键发现 6（medicalProduct 不是 Optometry → Sale 的桥）**：
- 验光 Controller 大量使用 medicalProduct (验光 + 配镜)
- 3 个 Sale Controller **完全 0 命中** medicalProduct
- **medicalProduct 不在 Sale Controller 中处理**
- 即"开单"在 addSaleRecordCtrl 阶段**还没有** medicalProduct
- 真正绑定 medicalProduct 是在验光 (optometryGlassesCtrl saveMedicalProductListOfSmallVersion.json)
- 链：验光 → **medicalProduct 绑定** → Charge 收费（**不通过 Sale**）

### 7.2 optometryGlassesCtrl medicalProduct 完整处理

```
L35684-L35728: GetMedicalProductVoList()
  - API: getMedicalProductVoList.json { medicalRecordId }
  - Response: res.result.list[]
  - 提取字段: medicalProduct.id / product.factory / product.productName / productSku.marketPrice / productSku.id

L35729-L35754: processBasicData()
  - 遍历 tempShop (medicalProductSign == 1)
  - 写入 $scope.glassesInfo[brandId/brandName/keyword/model1/model2/remark/unLockExistSkuCount/marketPrice/productSkuId/productSkuKpiPrice]

L35911-L35957: $scope.save()
  - 构建 medicalProductParamListJson (id/lockStorehouseId/productSkuId/memberRate/useCount/remark/signType/kpiPrice)
  - API: saveMedicalProductListOfSmallVersion.json { medicalRecordId, medicalProductParamListJson }
  - success: $scope.GetMedicalProductVoList() (reload)
```

**S1-126 关键发现 7（medicalProduct 写入由 Optometry 内部完成）**：
- medicalProduct 列表创建/修改是 optometryGlassesCtrl 内部完成
- **不传给** Sale Controller
- Sale Controller 通过 addMedicalRecord.json 创建 medicalRecord 本身
- 实际"商品"列表是 medicalProductVoList 嵌套在 medicalRecordVo 内（S1-118 已确认）

## 8. patient 桥

### 8.1 patient/patientId 命中

| Controller | patient | patientId |
|---|---:|---:|
| optometryCtrl | 1 | 1 |
| optometryGlassesCtrl | **14** | 4 |
| addSaleRecordCtrl | 2 | **6** |
| adminSalesRecordCtrl | 0 | 3 |
| orderManageCtrl | 0 | 0 |

### 8.2 patient 派生链

**optometryGlassesCtrl 完整 patient 链**：

```
L35415-L35470: $scope.getMedicalRecord()
  - API getMedicalRecord.json { id: medicalRecordId }
  - Response: response.object.patientId
  - API getPatientInfo.json { id: response.object.patientId }
  - API getCustomerVo.json { customerId: response.object.customerId }
  - 合并: $scope.userInfo = { patientId, customerId, ... }
  ↓
L35496-L35543: $scope.updatePatient()
  - 构造 patient { id, patientName, ... } 和 customer { id, channelTagId, ... }
  - API savePatientInfo.json { patient } (Write)
  - API saveCustomerInfo.json { customer } (Write)
```

**addSaleRecordCtrl 完整 patient 链**：

```
L31111-L31138: choseUser.affirm(item, custView)
  - obj = { patientId: item.patient.id, medicalRecordType: custView == 1 ? 1 : 5 }
  - 起点是 **item.patient.id** (列表 item 字段)
  - API addMedicalRecord.json { patientId }
  - Response: res.object.patientId
  - 跳到 myMaterialBill 传 patientId
```

**S1-126 关键发现 8（patient 桥 A 级，但通过 medicalRecordVo 间接）**：
- optometryGlassesCtrl 直接访问 patientVo / customerVo
- addSaleRecordCtrl 通过 `item.patient.id` 派生
- 两者**不直接通信**，但都消费同一 medicalRecordVo
- 间接链: medicalRecordVo.patientId → getPatientInfo → getCustomerVo

## 9. customer 桥

| Controller | customer | customerId |
|---|---:|---:|
| optometryCtrl | 2 | 0 |
| optometryGlassesCtrl | **10** | 4 |
| addSaleRecordCtrl | 0 | 0 |
| adminSalesRecordCtrl | 0 | 2 |
| orderManageCtrl | 0 | 0 |

**S1-126 关键发现 9（customer 在 Sale Controller 中是 0 命中命名）**：
- optometryGlassesCtrl customer 10 / customerId 4
- addSaleRecordCtrl 完全 0 命中 customer (但内部有 patient 派生)
- adminSalesRecordCtrl customerId 2 (但 customer 0 命中)
- 即 Sale Controller 消费 customerId 但**不直接处理 customerVo**

## 10. cashflow / prescription / product 桥

| 字段 | Optometry 命中 | Sale 命中 | 等级 |
|---|---:|---:|---|
| cashflowId | optometryCtrl 3 | 0 | 字段共现 |
| cashflow | optometryCtrl (stateTodoMedicalRecordCount 含) | 0 | 字段共现 |
| prescription | optometryGlassesCtrl 1 (其它关联) | 0 | 字段共现 |
| product | optometryCtrl 3 / optometryGlassesCtrl 9 | 0 | 字段共现 |
| productId | (隐含) | 0 | 字段共现 |

**S1-126 关键发现 10（cashflow / prescription / product 都不是桥）**：
- 全部是**字段共现**
- Sale Controller 完全不消费 cashflow / prescription / product

## 11. Optometry Write success → Sale 路径

### 11.1 optometryCtrl 8 个 Write

| Write | success 行为 | 进入 Sale 吗 |
|---|---|---|
| beginCustomerCheckin | $state.go optometryGlasses | **否** |
| cancelMedicalRecord | $scope.querySingleOrder | **否** |
| completeMedicalRecordDelivery | (无) | **否** |
| confirmMedicalRecordReturn | $scope.querySingleOrder | **否** |
| createMedicalStockLossOfSmallVersion | Popup notice | **否** |
| insertCustomerCheckin | $scope.addUser.switch | **否** |
| sendTakeMirrorNotice | (无) | **否** |
| setMedicalRecordProcessMode | $scope.querySingleOrder | **否** |

### 11.2 optometryGlassesCtrl 4 个 Write

| Write | success 行为 | 进入 Sale 吗 |
|---|---|---|
| checkBeforeDeleteMedicalProduct | 数组 splice | **否** |
| **saveMedicalProductListOfSmallVersion** | $scope.GetMedicalProductVoList() (reload) | **否** |
| selectStorehouseListOfCompany | (Read 但 classify 为 Write) | (误判) |
| **updateMethodGlassRecord** | (无 success 行为) | **否** |

**S1-126 关键发现 11（Optometry Write 不进入 Sale）**：
- 验光 12 个 Write 全部不跳 Sale
- success 行为都是 reload 自身 / 弹窗 / 数组操作
- **没有 Optometry Write → Sale Controller 的直接路径**

## 12. Sale Init Read

### 12.1 addSaleRecordCtrl Init

L31055: `ListFactory getHaveOrderMedicalRecordVoList.json` —— **主入口 List**
- 不通过 medicalRecordVo
- 接收 `item` 后 `$state.go` 跳 myMaterialBill

### 12.2 adminSalesRecordCtrl Init

L31286: `$scope.medicalRecordId = $stateParams.medicalRecordId`
L31329-L31348: 3 个 Read 链:
- getMedicalRecord.json → patientId
- getPatientInfo.json → customerId
- getCustomerVo.json → customerVo

### 12.3 orderManageCtrl Init

L36197: `ListFactory getOkOrderRecordVoList.json` —— **订单列表**
- 完整 4 字段搜索 (firstVisitFrom / firstVisitTo / optometrist / keyword)
- 不消费 medicalRecordVo 整体

**S1-126 关键发现 12（Sale Init Read 模式不同）**：
- addSaleRecordCtrl: 走 List 入口（订单搜索）
- adminSalesRecordCtrl: 走 medicalRecordVo 入口（病历详情）
- orderManageCtrl: 走 List 入口（订单导出）
- 3 个 Controller 入口模式各异

## 13. Sale Request provenance

### 13.1 addSaleRecordCtrl Request

**addMedicalRecord.json (L31119)**:
- `obj = { patientId: item.patient.id, medicalRecordType: custView == 1 ? 1 : 5 }`
- 起点: `item.patient.id` (列表 item 字段)
- 终点: API Request patientId
- 状态: 创建 medicalRecord (开单)

### 13.2 adminSalesRecordCtrl Request

**getMedicalRecord.json (L31331)**: `{ id: $scope.medicalRecordId }` (primitive)
**getPatientInfo.json (L31336)**: `{ id: $scope.patientId }` (primitive)
**getCustomerVo.json (L31344)**: `{ customerId: res.result.object.customerId }` (primitive)

### 13.3 orderManageCtrl Request

**getOkOrderRecordVoList.json (L36197)**: `object` (含 firstVisitFrom/To/optometrist/keyword)
- 不涉及 medicalRecordVo

**S1-126 关键发现 13（Sale Request 仅依赖 ID）**：
- 3 个 Sale Controller 的 Request 全部用 primitive ID
- 不传整个 medicalRecordVo / patientVo 对象
- 即 Sale Controller 是**只依赖 ID** 的 API 入口

## 14. API Response → Request 直接链

**S1-126 关键发现 14（跨 Controller API 直接链 F 级）**：
- 验光 Controller 的 API Response 不直接作为 Sale Request
- Sale Controller 通过自己调用 API 获取数据
- **没有 API Response → Request 的跨 Controller 链**

**两条独立路径**：
- 验光链: optometryCtrl → optometryGlassesCtrl (medicalRecordId 桥)
- 开单链: addSaleRecordCtrl → adminSalesRecordCtrl (medicalRecordId 桥)
- **两条路径不交叉**

## 15. Scope/Service/Facotry 跨 Controller 共享

**S1-126 关键发现 15（无 Service 共享）**：
- 5 Controller 之间**没有** $rootScope / service / factory / window 共享对象
- StorageFactory 仅在 assistCheckListCtrl 内部缓存搜索条件
- **完全独立**

## 16. Array / Object propagation

| Controller | 数组聚合 | 整体对象传播 |
|---|---|---|
| optometryCtrl | medicalProductVoList / medicalProductVoListOfModel | res.result.vo (medicalRecordFlowVo) |
| optometryGlassesCtrl | res.result.list (medicalProductVoList) / medicalProductParamListJson | res.object (medicalRecordVo) / res.result.vo.methodGlassRecord |
| addSaleRecordCtrl | (无) | (无) |
| adminSalesRecordCtrl | (无) | res.object (medicalRecordVo) |
| orderManageCtrl | res.items[].okOrderRecord.medicalRecordId | (无) |

**S1-126 关键发现 16（数组聚合在 Optometry）**：
- optometryGlassesCtrl medicalProductParamListJson 是关键数组
- 内部循环推入 medicalProduct 字段
- 但**不传给** Sale Controller

## 17. 验光 → 销售 状态分类总览

| 桥接方式 | 是否存在 | 等级 |
|---|---|---|
| 直接函数调用 | 否 | F |
| callback | 否 | F |
| State 参数桥 (Optometry → Sale) | **否** | F |
| API Response → Request | 否 | F |
| Scope/Service | 否 | F |
| Factory | 否 | F |
| 字段共现 (medicalRecordId) | 是 | F |
| 字段共现 (medicalProduct) | 是 (但**Optometry 内**) | F |
| 字段共现 (patient) | 是 (但都各自从 medicalRecordVo 派生) | F |

**S1-126 关键发现 17（Optometry → Sale 不存在任何直接桥）**：
- 7 种可能桥接方式**全部 F**
- 仅字段共现 (medicalRecordId / medicalProduct / patient)
- 5 Controller 通过**两个独立路径**分别处理验光和销售

## 18. Final Optometry → Sale DAG

```
【验光路径 A: Optometry 业务】
  optometryCtrl (L34776)
    ↓ $state.go("optometryGlasses", { medicalRecordId })
  optometryGlassesCtrl (L35383)
    - getMedicalRecord.json
    - getPatientInfo.json + getCustomerVo.json
    - getMethodGlassRecordVo.json
    - saveMedicalProductListOfSmallVersion.json (medicalProduct 绑定)
    - updateMethodGlassRecord.json
    ↓ 0 个 $state.go
    [结束 - reload 自身]

【销售路径 B: Sale 业务】
  addSaleRecordCtrl (L31008)
    - getHaveOrderMedicalRecordVoList.json (List)
    - addMedicalRecord.json { patientId: item.patient.id, medicalRecordType }
    ↓ $state.go("adminSalesRecord.myMaterialBill", { medicalRecordId, patientId })
  adminSalesRecordCtrl (L31285)
    - getMedicalRecord.json { id: $stateParams.medicalRecordId }
    - getPatientInfo.json { id: patientId }
    - getCustomerVo.json { customerId }
    ↓ 0 个 $state.go
    [结束 - 弹窗 / reload 自身]

  orderManageCtrl (L36165)
    - getOkOrderRecordVoList.json (List 导出)
    ↓ 0 个 $state.go
    [结束 - 跳新窗口导出]

【两条路径不交叉】
  - A 路径: optometry* (验光内部 + 验光→Charge)
  - B 路径: addSaleRecord* (独立开单)
  - A → B: 无直接链
  - B → A: 无直接链
```

**S1-126 关键发现 18（两条独立路径）**：
- 验光路径 A 和销售路径 B 通过**医疗记录 ID 共现**但**不直接连接**
- 实际业务上是两个**独立**流程：
  - A: 验光 → 配镜 → 取镜 (Charge 收费)
  - B: 列表选患者 → 开单 (Charge 收费)
- 都通过 Charge → Delivery 汇合（**S1-117/120 已确认**）

## 19. 与历史结论的差异

### 19.1 S1-115 vs S1-126

| 项 | S1-115 | S1-126 |
|---|---|---|
| 4 主链 Controller 关系 | 标注存在 | **F 级 (未建立)** |
| medicalProduct 桥 | 假设存在 | **F 级 (Sale 0 命中)** |
| medicalRecordId 跨链 | 假设存在 | **A 级 (共现但非直接)** |

### 19.2 S1-117 vs S1-126

| 项 | S1-117 | S1-126 |
|---|---|---|
| 验光 → 销售链 | 标注"已确认" | **F 级 (OptometryCtrl 不跳 Sale)** |
| medicalProduct VoList 跨链 | 假设存在 | **A 级 (但只 Optometry 内部)** |

### 19.3 S1-123 vs S1-126

| 项 | S1-123 | S1-126 |
|---|---|---|
| Optometry → Sale | 标注"直接链 F" | **确认 (本轮 0 跳证实)** |
| addSaleRecordCtrl 入口 | 标注"开单" | **确认 (addMedicalRecord.json Write 独立)** |

## 20. 26 项矩阵

| # | 项目 | 详情 | A-F | L1/L2/L3 |
|---:|---|---|---|---|
| 01 | Optometry Controller | 2 (optometryCtrl / optometryGlassesCtrl) | A | L1 |
| 02 | Sale Controller | 3 (orderManageCtrl / addSaleRecordCtrl / adminSalesRecordCtrl) | A | L1 |
| 03 | Optometry API | 25 unique | A | L1 |
| 04 | Sale API | 6 unique | A | L1 |
| 05 | Controller 控制流 | F（无直接链）| A | L1 |
| 06 | State 桥 | F（4 Controller 0 跳 Sale）| A | L1 |
| 07 | StateParams | 2 (medicalRecordId in optometryGlassesCtrl / adminSalesRecordCtrl) | A | L1 |
| 08 | medicalRecordId | 54 命中 / 5 Controller | A | L1 |
| 09 | medicalRecord | 15 命中 (Optometry 主) | A | L1 |
| 10 | patientId | 14 命中 | A | L1 |
| 11 | patient | 17 命中 (OptometryGlassesCtrl 14) | A | L1 |
| 12 | customerId | 6 命中 | A | L1 |
| 13 | customer | 12 命中 (OptometryGlassesCtrl 10) | A | L1 |
| 14 | medicalProductId | **9 命中 (Optometry) / 0 (Sale)** | A | L1 |
| 15 | medicalProduct | **51 命中 (Optometry) / 0 (Sale)** | A | L1 |
| 16 | productId | 0 (命名) | A | L1 |
| 17 | product | 12 命中 (Optometry) | A | L1 |
| 18 | prescription/check | prescription 1 命中 (Optometry) | A | L1 |
| 19 | Optometry Write | 12 个 (8 optometryCtrl + 4 optometryGlassesCtrl) | A | L1 |
| 20 | Optometry Write→Sale | **F（0 跳 Sale）** | A | L1 |
| 21 | Sale Init Read | 3 种模式 (List / medicalRecordVo / List 导出) | A | L1 |
| 22 | Sale Request provenance | primitive ID only | A | L1 |
| 23 | API Response→Request | F（无跨链）| A | L1 |
| 24 | Scope/Service | F（无共享）| A | L1 |
| 25 | Array/Object propagation | Optometry 内有 / 跨链无 | A | L1 |
| 26 | A/B/C/D/E/F | A=22 / B=0 / C=0 / D=0 / E=0 / F=4 | - | - |

## 21. F 边界

| 项目 | F 原因 | 应对 |
|---|---|---|
| Optometry → Sale Controller 控制流 | 4 个 Optometry $state.go 不跳 Sale | 复刻时按业务场景手动实现 |
| medicalProduct → Sale | Sale Controller 0 命中 | 复刻时 Sale 不需 medicalProduct |
| API Response → Request 跨链 | 静态审计 | 需 backend debug 验证 |
| Scope/Service 共享 | 5 Controller 独立 | 复刻时通过 ID 而非对象共享 |
| HTML 跳转 | 不在 controller.js 范围 | 需 HTML 审计 |

## 22. 复刻风险（L1）

### R1: Optometry 是否真正把对象交给 Sale
- **未建立** 直接桥
- 复刻时需按"两个独立业务路径"实现
- 不能假设"验光完会自动开单"

### R2: Sale 是否只接 ID
- **确认是**：3 个 Sale Controller 全部用 primitive ID
- 复刻时 Sale Controller API 只接收 medicalRecordId / patientId
- 不能简化（Sale 需要 medicalRecordVo 详情但通过 API 重新获取）

### R3: medicalProduct 是否是桥
- **否**：Sale Controller 0 命中
- medicalProduct 在 Optometry 内部绑定（saveMedicalProductListOfSmallVersion.json）
- 复刻时 Sale Controller 不应处理 medicalProductVoList

### R4: patient/customer 是否独立
- patient / customer **各自**从 medicalRecordVo 派生
- 复刻时通过 patientVo → customerVo 链
- 不能假设 patientVo 已包含 customerVo

### R5: medicalRecordId 是否只是共现
- **是**（两条独立路径都消费）
- 但**不构成 Optometry → Sale 桥**
- 复刻时 medicalRecordId 是数据关联键，不是控制流桥

### R6: State 参数必须精确保留
- addSaleRecordCtrl 5 个 $state.go 必须传 {medicalRecordId, patientId}
- 复刻时不能简化参数

### R7: API Response → Request 必须保留
- getMedicalRecord.json Response → patientVo / customerVo
- 复刻时这个链必须实现

### R8: 对象整体传播不可擅自改成 id
- optometryGlassesCtrl L35419: `$scope.medicalRecord = response.object` 是**整体对象**
- 不能改成 `$scope.medicalRecordId = response.object.id`
- 复刻时 medicalRecordVo 整体对象必须保留

## 23. 红线

- API actual = 0
- Write actual = 0
- Production mutation = 0
- Historical MD = 0
- controller.js unchanged ✓ (SHA256 F40589FF0C56DE60BF4785A0B6C5B0A33519B75A30A15D988B0E1BDCF0A19433)
- deliveryList.html unchanged ✓ (SHA256 5B79B6F0E423A90188EE420234A9449531E623528C9807800C5780BD61006476)
- 165-187 unchanged ✓
- 10 untracked preserved ✓
- 视光之家url.txt preserved (gitignored) ✓
- ignored = 1 ✓
- P0 = 54 冻结 ✓
- P1 = 8 冻结 ✓

## 24. 结论

### 24.1 关键发现

1. **Optometry → Sale 不存在任何直接链**:
   - 7 种可能桥接方式**全部 F**
   - 验光完成后不跳销售
2. **medicalProduct 不是桥**: 验光内部绑定, Sale 0 命中
3. **medicalRecordId 是唯一共现**: 两条独立路径都消费
4. **两条独立业务路径**:
   - A: optometryCtrl → optometryGlassesCtrl (验光业务)
   - B: addSaleRecordCtrl → adminSalesRecordCtrl (开单业务)
5. **patient/customer 通过 medicalRecordVo 间接桥接**
6. **Sale Request 仅用 primitive ID**, 不传整个对象

### 24.2 S1-126 任务状态

- 5 Controller 完整 API 统计 (34 call site / 31 unique / 18 Read / 13 Write)
- medicalRecordId 54 命中详细
- medicalProduct 桥 F 级确认
- 7 种桥接方式全部 F 判定
- 两条独立业务路径 DAG 已建立
- 26 项矩阵已建立
- 8 条复刻风险已列出

---

**【S1-126 完成】**
