# S1-130 诊所管理 + 系统设置 一级菜单边界与核心数据模型深度审计

> **项目**：OptFlow PMS 逆向建模
> **本轮目标**：在 controller.js / 已存在源码 / HTML / Git 历史可见资源范围内，建立"诊所管理" + "系统设置"两个一级菜单的 L1 前端证据边界、Controller 边界、API 体系、State/URL 入口、核心对象/字段来源与消费者关系。
> **A-F 证据等级**：A=直接源码 / B=多源互证 / C=局部 / D=冲突 / E=推断 / F=证据范围不可得
> **L1/L2/L3**：L1=前端直接事实 / L2=业务模型解释 / L3=数据库物理模型
> **红线**：API actual = 0，Write actual = 0，Production mutation = 0，controller.js unchanged，HTML unchanged，165-191 unchanged
> **完成时间**：2026-09-09

---

## 1. 审计范围

| 类别 | 数量 / 范围 |
|---|---|
| controller.js 全文 | 59,214 行 / 2,194,196 bytes / SHA256=F40589FF0C56DE60BF4785A0B6C5B0A33519B75A30A15D988B0E1BDCF0A19433 |
| .controller() 注册 | 417 个唯一 |
| .state() 注册 | **0 个**（**F 边界**：state 声明在 controller.js 范围不可得） |
| $state.go() 调用 | 271 次 / 111 个唯一 target |
| 工作区 HTML | 7 个（untracked = 10 含 _gen 脚本） |
| 其它 JS | **0**（工作区唯一 JS = controller.js） |
| 工作区 gitignore | 1（视光之家url.txt） |

---

## 2. 一级菜单入口（在 controller.js 范围）

### 2.1 关键发现：state 声明在 controller.js 范围不可得

**【F 边界 / 红线级】**：
- controller.js 中 `.state(` 注册 = **0 个**（A）
- 但 controller.js 中 `$state.go(...)` 调用 = **271 次**（A）
- 工作区**没有其它 .js 文件**（A — `Get-ChildItem -Filter *.js` 只返回 controller.js）
- 7 个 HTML 中**无 systemSetting / adminClinic / roleAdmin / employeeList 引用**（A — 全局搜索 0 命中）
- 结论：state 声明在外部 JS（CDN / 打包 / 注入文件 / .gitignore 中）—— **F 边界**

这意味着：本轮**只能在 $state.go 调用证据上**反推菜单/State/HTML 的存在性，无法直接证明 state 声明的 controller/template。

### 2.2 "诊所管理" 一级菜单

| 二级菜单 | State（$state.go 引用证据） | Controller | HTML | API namespace | 证据 |
|---|---|---|---|---|---|
| 诊室管理 | （state 声明 F） | **adminClinicCtrl** L15053-L15211（159 行） | （HTML F） | /admin/* (consultRoom) | A（Controller + 25 个 $state.go 反推） |

**注**：controller.js 中没有任何"诊所管理"中文名（0 命中），"诊室管理"也无中文命中（A）。这意味着：
- 菜单中文名 → 来源不在 controller.js
- 状态名可能由外部 JS 注入

### 2.3 "系统设置" 一级菜单

| 二级菜单（state 名） | Controller | 行号范围 | 行数 | 证据 |
|---|---|---|---|---|
| systemSetting.checkList | checkListCtrl | L53001-? | 247（估算） | A |
| systemSetting.checkModify | checkModifyCtrl | L53185-? | 63（估算） | A |
| systemSetting.customerChargeItem | customerChargeItemCtrl | L53806-? | 354 | A |
| systemSetting.customerCharge | customerChargeCtrl | L53318-? | 488 | A |
| systemSetting.modifyCustomerCharge | modifyCustomerChargeCtrl | L56044-? | 457 | A |
| systemSetting.customerMaterialCertificate | **（无 Controller 注册）** | — | — | D（state 名引用 L54050，但 controller.js 0 注册） |
| systemSetting.materiallist | materiallistCtrl | L54469-? | 390 | A |
| systemSetting.materialModify | materialModifyCtrl | L54859-? | 481 | A |
| systemSetting.materialCertificate | materialCertificateCtrl | L54240-? | 229 | A |
| systemSetting.medicalFeesList | medicalFeesListCtrl | L55340-? | 412 | A |
| systemSetting.memberTypeList | memberTypeListCtrl | L55752-? | 292 | A |
| systemSetting.processCenter | processCenterCtrl | L57778-? | 73 | A |
| systemSetting.projectCard | projectCardCtrl | L57851-? | 50 | A |
| systemSetting.supplierList | supplierListCtrl | L58927-? | 44 | A |
| systemSetting.supplierModify | supplierModifyCtrl | L58971-? | 43 | A |
| systemSetting.supplierCertificate | supplierCertificateCtrl | L58787-? | 140 | A |
| systemSetting.InspectList | InspectListCtrl | L54160-? | 80 | A |
| （systemSetting 根） | **systemSettingCtrl**（空 stub） | L59014 | 1 | A（**L1 空实现**） |
| addCheckItemCtrl | addCheckItemCtrl | L50246-? | 220 | A |
| checkItemListCtrl | checkItemListCtrl | L50466-? | 322 | A |

**【F / D 边界】**：
- `systemSettingCtrl` L59014 是**空实现**（A — 函数体 `{}`），仅注册了 6 个 DI 参数：$scope, Popup, $rootScope, $state, ObjectFactory, ListFactory
- `systemSetting.customerMaterialCertificate` state 名被引用（L54050）但**无对应 Controller 注册**（D 冲突 — 跳转目标存在但执行体缺失）
- 所有 systemSetting.* 的 state 声明在 controller.js 范围不可得（F）

### 2.4 与系统设置相关但可能属于其它一级菜单的 Controller

以下 Controller 涉及"配置 / 报表 / 大屏"等系统设置类业务，但 $state.go 中没有 systemSetting.* 引用，**菜单归属待定（F）**：

| Controller | 行号 | 行数 | API 数 | 备注 |
|---|---|---:|---:|---|
| appConfigCtrl | L52303 | 39 | 1 | App 配置（clinicEmployeeKpiEnable / screenCompleteInfoEnable） |
| medicalConfigCtrl | L41139 | 173 | 6 | 检查报告打印配置 |
| printConfigCtrl | L57667 | 109 | 5 | 打印/小票抬头/Logo |
| queueMachineConfigCtrl | L11023 | 84 | 2 | 叫号机配置（注意：queueMachineCtrl L10869 是不同 Controller） |
| cloudPrinterConfigCtrl | L39399 | 27 | 2 | 云打印 |
| healthScreenConfigCtrl | L40499 | 57 | 3 | 筛查报告配置 |
| mateCheckReportConfigCtrl | L41023 | 30 | 0 | 检查报告配置（同 medicalConfigCtrl？F） |
| screenBackConfigCtrl | L44973 | 139 | 4 | 大屏背景图 |
| screenPromotionConfigCtrl | L46600 | 511 | 19 | 大屏宣传（最大 Controller） |
| screenPromotionToothConfigCtrl | L47886 | 172 | 8 | 牙科大屏 |
| screenUrlConfigCtrl | L48420 | 122 | 3 | 大屏 URL |
| zhiShiBangConfigCtrl | L49015 | 54 | 2 | 知识榜 |

---

## 3. 诊所管理 Controller 簇：adminClinicCtrl 完整解剖

### 3.1 adminClinicCtrl L15053-L15211（159 行）

| 项 | 证据 |
|---|---|
| 注册点 | L15053: `angular.module("bestvisionWeb").controller("adminClinicCtrl", ...)` |
| DI | `$scope, Popup, WechatConfig, $stateParams, ObjectFactory, HttpFactory, ListFactory, $http, $timeout` |
| State Param | `$stateParams.companyId` (L15120, L15149) |
| State.go 跳转 | **0 处**（A — 诊所管理页内不出诊所管理） |
| scope.companyInfo 来源 | A. `window.grantAuth.hasAuthForCompany(ObjectFactory)` (L15055) |
| scope.clinicPopout 定义 | L15077-L15124（Object 模式） |
| scope.screenList | A. `new ListFactory("/admin/selectConsultRoomVoListOfCompany.json", 0, $scope.pageSize, { companyId: $stateParams.companyId, keyword: $scope.keyword })` (L15148) |
| scope.examineList | A. `new ListFactory("/admin/getExamineVoList.json", 0, length, { status: 0, examineType: 1 })` (L15174) |

**API 体系**（3 Read + 3 Write = 6 unique）：

| API | 类型 | 行号 | Function |
|---|---|---|---|
| /admin/selectConsultRoomVoListOfCompany.json | R | L15148 | scope.searchScreenList |
| /admin/getConsultRoomVo.json | R | L15139 | scope.searchLine |
| /admin/getExamineVoList.json | R | L15174 | scope.getExamineVoList |
| /admin/addConsultRoom.json | W | L15126 | scope.addClinic |
| /admin/updateConsultRoomInfo.json | W | L15133 | scope.updateClinic |
| /admin/updateConsultRoomStatus.json | W | L15067 | scope.changeStatus |

**核心对象**：
- `consultRoom`（A — Object 包含 id, consultRoomName, examineIdArray）
- `examine`（A — Object 包含 id）
- `companyInfo`（A — grantAuth 返回值，包含 id / name / companyTitle 等）

**核心 ID**：
- `consultRoomId`（A — 来自 $stateParams / list item）
- `companyId`（A — 来自 $stateParams / companyInfo.id）
- `examineIdArray`（A — clinicPopout.submit 内部 array aggregation）
- `examineId`（A — examine.id 派生）

**特殊功能**：诊室二维码打印
- `WechatConfig.getWechatAppid` → `WechatConfig.setWechatUrl(appid, "checkSignIn?consultRoomId=...")` → jqprint (L15195-L15209)
- 微信 H5 签到页面 `checkSignIn` + `consultRoomId` URL Param（A）

### 3.2 adminClinicCtrl 跨模块桥

| 桥类型 | 是否存在 | 证据 |
|---|---|---|
| Function bridge | F | adminClinicCtrl 0 处调用其它业务 Controller 的函数 |
| State bridge | F | adminClinicCtrl 0 处 $state.go 到业务模块 |
| API Response → Request | F | 诊室 API 不传 medicalProduct / cashflow / machineCenterId |
| Scope/Service bridge | F | scope.companyInfo 仅用于诊室内部 |
| Factory bridge | F | 0 处 ObjectFactory 共享 |

**结论**：adminClinicCtrl 是**自闭环的"诊室"域**，与就诊/验光/销售/收费/加工 0 数据链。

---

## 4. 系统设置 Controller 簇：role / department / employee 完整解剖

### 4.1 角色管理（5 Controller）

#### 4.1.1 addRoleCtrl L17-L49（33 行）

| 项 | 证据 |
|---|---|
| DI | `$scope, Popup, $timeout, $rootScope, $state, ObjectFactory, ListFactory, $http` |
| 关键 Scope | `$scope.obj.roleName` (L20) / `$scope.createAdminFactory` |
| API | W: `/admin/createAdminRole.json` (L26) / R: `/admin/getCompanyList.json` (L46) |
| State.go | `roleAdmin` (L32) |
| 写源 | `$scope.obj` = `{ roleName: "..." }` (A) |

#### 4.1.2 areaRoleAdminCtrl L712-L735（24 行）

| 项 | 证据 |
|---|---|
| 关键字段 | `$scope.obj.isAreaAdmin = true` (L718) |
| API | R: `/admin/selectAdminRoleVoList.json` (L720) |
| ListFactory 模式 | 0-index, 10-page, `$scope.obj` 全字段透传 |

#### 4.1.3 modifyAreaRoleCtrl L1470-L1587（118 行）

| 项 | 证据 |
|---|---|
| DI | `$scope, Popup, $timeout, $rootScope, $state, $stateParams, ObjectFactory, ListFactory, $http` |
| State Param | `$stateParams.roleId` (L1473) → `$scope.obj.adminRoleId` |
| API | R: `/admin/getAdminRoleVo.json` (L1477) / R: `/admin/getRoleStateListForUpdate.json { adminRoleId, isAreaAdmin: true }` (L1483) / W: `/admin/updateRoleStateList.json` (L1569) / W: `/admin/updateAdminRoleInfo.json` (L1575) |
| 关键 Scope | `$scope.stuff[i].children[j].children[k].children[m].homeId` 树形结构 |
| 关键 ID | `fromCorpAdminHomeIdArray` (L1566, L1690) —— 角色权限 ID 数组 |
| State.go | `areaRoleAdmin` (L1581) |

#### 4.1.4 modifyRoleCtrl L1594-L1711（118 行）

| 项 | 证据 |
|---|---|
| 几乎与 modifyAreaRoleCtrl **结构相同**，区别是 isAreaAdmin 不传（A — L1607 仅传 adminRoleId） |
| State.go | `roleAdmin` (L1705) |

#### 4.1.5 roleAdminCtrl L1714-L1753（40 行）

| 项 | 证据 |
|---|---|
| 关键字段 | `$scope.obj.isClinicAdmin = true` (L1731) |
| API | R: `/admin/selectAdminRoleVoList.json` (L1733) |
| 注释 | 此 Controller 实际**主要管理员工**（含 employeeIdTo 转移会员逻辑 L1721-L1728 / $scope.moveCustomer）—— **命名误导**（D 冲突） |

### 4.2 部门管理（4 Controller + 2 modal）

#### 4.2.1 departmentListCtrl L15327-L15333（7 行 — 最小）

```javascript
$scope.id = $stateParams.companyId;
$scope.departmentListFactory = new ListFactory('/admin/getDepartmentList.json', 0, 10, { companyId: $scope.id });
$scope.departmentListFactory.nextPage();
```

- R: `/admin/getDepartmentList.json` (A)
- State Param: companyId

#### 4.2.2 addDepartmentCtrl L14846-L14893（48 行）

- W: `/admin/addDepartment.json` (L14860) — 接受 `companyId / departDesc` (Markdown)
- 用 QiniuFactory 上传图片 → 插入 Markdown image 语法
- 模态框: addDepartmentModalInstanceCtrl (L14895-L14989) — 选图

#### 4.2.3 updateDepartmentCtrl L15690-L15764（75 行）

- State Param: `departmentId` (L15691) + `id` (L15692)
- R: `/admin/getDepartmentInfo.json` (L15700)
- W: `/admin/updateDepartment.json` (L15711) / `/admin/deleteDepartment.json` (L15726)

#### 4.2.4 updateDepartmentModalContentCtrl L15766-L15848（83 行）

- W: `/admin/saveEmployeeAvatar.json` (L15808 — 注释代码)
- Qiniu 直传（**七牛云上传** —— F 边界：UPLOAD_HOST 在外部）

### 4.3 员工管理：employeeListCtrl L15347-L15384（38 行）

| 项 | 证据 |
|---|---|
| DI | `$scope, $rootScope, $state, ObjectFactory, ListFactory, $http, $stateParams` |
| State Param | `$stateParams.companyId` (L15352) |
| API | R: `/admin/selectEmployeeVoList.json` (L15355) / W: `/admin/setDoctorApproved.json` (L15366) / W: `/admin/setEmployeeRank.json` (L15376) |
| 关键 Scope | `$scope.approveFactory` / `$scope.employeeListFactory` |
| 关键 ID | `employeeId` (A — L15366, L15376) |
| State.go | `hospital.updateHospital.employeeList` `{ indexTab: 4 }` `{ reload: true }` (L15371, L15381) |

**【跨菜单桥观察】**：employeeListCtrl 通过 `$state.go("hospital.updateHospital.employeeList", { indexTab: 4 })` 跳转到 hospital 状态树（A）—— 这意味着"员工管理"在源码中**嵌套在"医院管理"一级菜单下**，而非独立的"诊所管理"或"系统设置"菜单。

但需注意：**`hospitalCtrl` 本身是空 stub** (L15340)：
```javascript
angular.module('bestvisionWeb').controller('hospitalCtrl', ['$scope', '$rootScope', '$state', 'ObjectFactory', 'ListFactory', '$http', function ($scope, $rootScope, $state, ObjectFactory, ListFactory, $http) {}]);
```

`updateHospitalCtrl` L15855 也有注册，但**state 声明在 controller.js 范围不可得**（F）。

### 4.4 角色 / 部门 / 员工 跨模块桥

| 桥类型 | 是否存在 | 证据 |
|---|---|---|
| Function bridge | F | 0 处调用业务 Controller 函数 |
| State bridge | F | 0 处 $state.go 到业务模块 |
| API Response → Request | F | role/department/employee API 不传 medicalProduct / cashflow |
| Scope/Service bridge | F | 0 共享 Scope |
| Factory bridge | F | 0 共享 ObjectFactory |

**结论**：role / department / employee 三个 Controller 簇是**自闭环的"权限与人员"域**，与就诊/验光/销售/收费 0 数据链。

---

## 5. 系统设置核心 Controller 簇 API 总览

### 5.1 完整 Controller → API 矩阵

| Controller | 行号 | 行数 | API 总数 | Read | Write |
|---|---|---:|---:|---:|---:|
| adminClinicCtrl | L15053 | 159 | 6 | 3 | 3 |
| roleAdminCtrl | L1714 | 40 | 1 | 1 | 0 |
| modifyRoleCtrl | L1594 | 118 | 4 | 1 | 3 |
| modifyAreaRoleCtrl | L1470 | 118 | 4 | 1 | 3 |
| addRoleCtrl | L17 | 33 | 2 | 1 | 1 |
| areaRoleAdminCtrl | L712 | 24 | 1 | 1 | 0 |
| employeeListCtrl | L15347 | 38 | 3 | 1 | 2 |
| addDepartmentCtrl | L14846 | 48 | 1 | 0 | 1 |
| updateDepartmentCtrl | L15690 | 75 | 3 | 1 | 2 |
| departmentListCtrl | L15327 | 7 | 1 | 1 | 0 |
| checkListCtrl | L53001 | 247 | 6 | 3 | 3 |
| checkModifyCtrl | L53185 | 63 | 2 | 1 | 1 |
| customerChargeItemCtrl | L53806 | 354 | 11 | 9 | 2 |
| customerChargeCtrl | L53318 | 488 | 11 | 6 | 5 |
| modifyCustomerChargeCtrl | L56044 | 457 | 12 | 9 | 3 |
| materialCertificateCtrl | L54240 | 229 | 6 | 4 | 2 |
| materiallistCtrl | L54469 | 390 | 7 | 4 | 3 |
| materialModifyCtrl | L54859 | 481 | 8 | 6 | 2 |
| medicalFeesListCtrl | L55340 | 412 | 10 | 3 | 7 |
| memberTypeListCtrl | L55752 | 292 | 9 | 5 | 4 |
| processCenterCtrl | L57778 | 73 | 2 | 2 | 0 |
| projectCardCtrl | L57851 | 50 | 1 | 1 | 0 |
| supplierCertificateCtrl | L58787 | 140 | 4 | 2 | 2 |
| supplierListCtrl | L58927 | 44 | 1 | 1 | 0 |
| supplierModifyCtrl | L58971 | 43 | 2 | 1 | 1 |
| InspectListCtrl | L54160 | 80 | 3 | 3 | 0 |
| appConfigCtrl | L52303 | 39 | 1 | 0 | 1 |
| medicalConfigCtrl | L41139 | 173 | 6 | 3 | 3 |
| printConfigCtrl | L57667 | 109 | 5 | 3 | 2 |
| queueMachineConfigCtrl | L11023 | 84 | 2 | 1 | 1 |
| cloudPrinterConfigCtrl | L39399 | 27 | 2 | 1 | 1 |
| healthScreenConfigCtrl | L40499 | 57 | 3 | 2 | 1 |
| mateCheckReportConfigCtrl | L41023 | 30 | 0 | 0 | 0 |
| screenBackConfigCtrl | L44973 | 139 | 4 | 2 | 2 |
| screenPromotionConfigCtrl | L46600 | 511 | 19 | 10 | 9 |
| screenPromotionToothConfigCtrl | L47886 | 172 | 8 | 5 | 3 |
| screenUrlConfigCtrl | L48420 | 122 | 3 | 1 | 2 |
| zhiShiBangConfigCtrl | L49015 | 54 | 2 | 1 | 1 |
| addCheckItemCtrl | L50246 | 220 | 5 | 4 | 1 |
| checkItemListCtrl | L50466 | 322 | 8 | 6 | 2 |
| addDepartmentModalInstanceCtrl | L14895 | 95 | 1 | 0 | 1 |
| updateDepartmentModalContentCtrl | L15766 | 83 | 1 | 0 | 1 |
| systemSettingCtrl（空 stub） | L59014 | 1 | 0 | 0 | 0 |
| **合计 42 个 Controller** | — | **5,924** | **~196 unique API** | **~108** | **~75** |

### 5.2 关键 API namespace 分析

**Write API 集中 namespace**（仅 75 个 Write 中关键）：
- `/admin/createAdminRole.json` / `updateAdminRoleInfo.json` / `updateRoleStateList.json` —— 角色
- `/admin/addDepartment.json` / `updateDepartment.json` / `deleteDepartment.json` —— 部门
- `/admin/setDoctorApproved.json` / `setEmployeeRank.json` —— 员工
- `/admin/addConsultRoom.json` / `updateConsultRoomInfo.json` / `updateConsultRoomStatus.json` —— 诊室
- `/admin/createExamine.json` / `updateExamine.json` / `deleteExamines.json` —— 检查项
- `/admin/saveProductQualifyList.json` / `updateProductWarningQualifyExpiresDays.json` —— 物资证书
- `/admin/saveSchoolCheckConf.json` / `saveCorpAppointConf.json` / `saveSchoolCheckBandingType.json` —— 大屏配置
- `/admin/updateCorpAppConf.json` —— App 配置
- `/admin/updateCompanyCashConf.json` / `saveCorpCashConf.json` —— 打印配置
- `/admin/updateQueueMachine.json` —— 叫号机
- `/admin/saveSchoolCheckCodeType.json` —— 云打印

**无 Write API 的 Controller**（只读）：
- roleAdminCtrl, areaRoleAdminCtrl, departmentListCtrl, processCenterCtrl, projectCardCtrl, supplierListCtrl, InspectListCtrl, mateCheckReportConfigCtrl, systemSettingCtrl

---

## 6. 配置对象来源链

### 6.1 shopInfo（多源、跨 Controller）

| 行号 | Context | 来源类型 |
|---|---|---|
| L22117 | `shopInfo: {...}` Object literal | A. ObjectFactory 模板 |
| L22132 | `if (this.shopInfo.id)` | A. 字段访问 |
| L30578 | `shopInfo: {...}` | A. ObjectFactory 模板 |
| L30593 | `if (this.shopInfo.id)` | A. 字段访问 |
| L30626 | `if (!$scope.choseShop.shopInfo.id)` | A. 字段访问 |
| L30636, L30649 | `productId: $scope.choseShop.shopInfo.id` | A. Request 字段 |
| L35277 | `shopInfo: {}` | A. Object literal |
| L35287 | `$scope.modal.shopInfo = {...}` | A. Object 赋值 |
| L35302-L35309 | lossAdminId / lossReasonId / useCount / remark / medicalRecordId / machineCenterId / medicalProductId 从 shopInfo 解构 | A. Object 字段解构 |

**重要观察**：shopInfo 在 controller.js 范围内**仅在 4 个局部 Controller 中使用**，与 companyInfo / clinicInfo 是**不同对象**。

### 6.2 companyInfo（核心配置对象，58 处出现）

| 来源模式 | 出现次数 | 代表行号 |
|---|---:|---|
| `new ObjectFactory({name: "companyInfo", idName: "companyInfo", ...})` 模板 | 8+ | L3290, L3358, L11248, L11558, L11676, L11994, L12333, L16801 |
| `$scope.companyInfo = res`（来自 grantAuth） | 5+ | L8818, L9606, L10283, L15058 |
| `$scope.companyInfo = result` (Promise resolve) | 1 | L15058（adminClinicCtrl） |
| `$scope.companyInfo.inputVal` | 多处 | 输入展示字段 |
| `$scope.companyInfo.id` | 多处 | companyId 派生 |
| `$scope.companyInfo.isshow` | 2+ | L17264, L17271（显示控制） |

**companyInfo 的两种语义**（A — 必须在复刻中区分）：
- **A. ObjectFactory 模板**：用于通用下拉选择器（`name`, `idName`, `inputVal`, `id`, `isshow`）
- **B. grantAuth 返回值**：包含 `id` / `name` 等公司级字段（L8816-L8818）

### 6.3 adminInfo / adminId

| 类型 | 出现次数 | 来源 |
|---|---:|---|
| `adminId` (primitive) | 21 | $stateParams (L297, L681, L1369) / Response (L648, L52654) / changeRole 参数 (L230) |
| `adminInfo` Object | 0 | **F** — controller.js 中**无 adminInfo Object 引用**（A — 0 命中） |

**重要反向发现**：`adminInfo` Object **不存在**于 controller.js（A — 全局搜索 0 命中）—— 仅有 `adminId` 原始值。

### 6.4 clinicInfo（75 处，3 个不同语义）

| 语义 | 出现范围 | 主要行号 |
|---|---|---|
| 叫号大屏 clinicInfo (bigScreen 配置) | L9347-L10793 | 含 `bigScreen` / `consultRoom` 字段 |
| adminClinicCtrl 内部诊所信息 | L15055-L15211 | **实际未命名为 clinicInfo**，使用 companyInfo |
| 验光/就诊 bigScreen 配置 | 其它 | F（需更多扫描） |

**重要发现**：adminClinicCtrl 内部**没有 `clinicInfo` 这个 Scope 变量**（A — L15053-L15211 全文 0 命中），命名"adminClinic"但实际只操作 companyInfo + clinicPopout + screenList。

### 6.5 corpInfo（LocalStorage 来源）

| 字段 | 出现次数 | 来源 |
|---|---:|---|
| `corpInfo.companyTitle` | 5+ | L588, L873, L3530 等 |
| `corpInfo.employeeTitle` | 5+ | L588, L873, L3540 等 |
| `corpInfo.username` | 1+ | L14267 |
| `localStorage.getItem('corpInfo')` | 8+ | L585, L871, L3529, L38225, L50843, L51941 |
| `localStorage.setItem('corpInfo', ...)` | 1 | L14268 |

**写源**（A — L14264-L14268）：
```javascript
var corpInfo = {};
corpInfo.companyTitle = resp.result.object.corpType.companyTitle;
corpInfo.employeeTitle = resp.result.object.corpType.employeeTitle;
corpInfo.username = username;
localStorage.setItem("corpInfo", JSON.stringify(corpInfo));
```

→ corpInfo **来源于登录响应**（A — 字段 corpType），由前端写入 localStorage。

### 6.6 grantAuth（window 全局，73 处使用）

| 方法 | 调用次数 | 用途 |
|---|---:|---|
| `window.grantAuth.hasAuthForCompany(ObjectFactory)` | 50+ | 检查公司权限 → 返回 companyInfo |
| `window.grantAuth.getBackAuth(ObjectFactory, "1")` | 5+ | 后台权限 |
| `window.grantAuth.getFrontAuth(ObjectFactory)` | 6+ | 前台权限 |
| `window.grantAuth.getGrant(ObjectFactory)` | 6+ | 通用 grant |
| `window.grantAuth.getAuthList(ObjectFactory, "goods")` | 3+ | 鉴权列表 |
| `window.grantAuth.hasAuthForWA($scope.otherPay)` | 4+ | WA 权限 |
| `window.grantAuth.isUnlockFun(ObjectFactory, "importMembers")` | 1 | 功能解锁 |

**【F 边界】**：`window.grantAuth` 在 controller.js 中**仅作为消费者**调用，**实现源在 controller.js 范围不可得**（A — 0 处 `window.grantAuth = ` 赋值）。

### 6.7 adminProjectService / $rootScope

| 模式 | 出现次数 | 证据 |
|---|---:|---|
| `adminProjectService` | 0（A — 全局搜索 0 命中） | **F — 不存在 Service 形式共享** |
| `$rootScope.user` / `$rootScope.adminUser` / `$rootScope.adminInfo` | 0（A — 全局搜索 0 命中） | **F — 不存在 $rootScope 共享** |
| `$rootScope.company` / `$rootScope.companyInfo` | 0（A — 全局搜索 0 命中） | **F — 不存在 $rootScope 共享** |

**【关键反向排除】**：本系统**没有**通过 `adminProjectService` 或 `$rootScope` 共享全局用户/公司配置对象。grantAuth 是**唯一全局权限源**。

---

## 7. 权限/员工/角色链

### 7.1 角色权限树（corpAdminHome + adminStateList + adminStateType）

**结构**（A — modifyRoleCtrl L1609-L1650）：
```
adminStateType (stateTypeName)
└── adminStateList[i]
    ├── corpAdminHome (stateName, state, id)
    ├── roleHaveState (boolean)
    └── children[k]
        ├── corpAdminHome (stateName, state, id, homeId)
        ├── roleHaveState
        └── children[m]
            ├── corpAdminHome (stateName, state, id, homeId)
            └── roleHaveState
```

- **A. 来源 API**：`/admin/getRoleStateListForUpdate.json` (Read)
- **A. Response 字段**：`adminStateType.stateTypeName` / `adminStateList[].corpAdminHome.{stateName, state, id}` / `roleHaveState`
- **A. 写 API**：`/admin/updateRoleStateList.json` (Write) — 接受 `fromCorpAdminHomeIdArray: number[]`
- **A. 写源**：来自 `$scope.stuff` Scope 树形对象

### 7.2 admin / role 关键 ID

| ID | 来源 | 出现次数 | 消费者 |
|---|---|---:|---|
| `adminId` | A. $stateParams / Response (admin.id) | 21 | role 簇（changeRole / transferAdminRole / getOtherAdminInfo） |
| `adminRoleId` | A. $stateParams.roleId / Response (adminRole.id) | 37 | role 簇全 Controller |
| `roleId` | A. $stateParams | 2 | modifyRoleCtrl / modifyAreaRoleCtrl |
| `companyId` | A. $stateParams / Response (company.id) / ObjectFactory 模板 | 100+ | 跨模块 |
| `employeeId` | A. Response (employee.id) / $stateParams | 149 | 员工/接诊/会员转移 |
| `departmentId` | A. $stateParams | 1+ | updateDepartmentCtrl |
| `consultRoomId` | A. $stateParams / Response | 3+ | adminClinicCtrl |
| `bodyCheckId` / `bodyCheckItemId` | A. Response | 多处 | medicalConfigCtrl |

### 7.3 关键反向排除

- `adminId` ≠ `employeeId`（A — 0 处 adminId 与 employeeId 互换，命名/字段/使用场景完全不同）
- `roleId` ≠ `userId`（A — 本系统**无 userId 概念**，仅有 adminId / employeeId / patientId / customerId 等）
- `adminRoleId` ≠ `permission`（A — adminRoleId 是角色 ID，permission 通过 fromCorpAdminHomeIdArray 表达）
- `adminInfo` Object **不存在**（A — 0 命中）
- `corpAdminHome.state` 是**状态路由名**（A — 与 $state 名一致），不是字段 ID

---

## 8. 系统设置 → 业务模块 跨菜单桥

| 跨菜单方向 | 桥类型 | 是否存在 | 证据 |
|---|---|---|---|
| systemSetting → Optometry | Function/State/API/Scope/Factory | F | 0 命中 |
| systemSetting → Check | 同上 | F | 0 命中 |
| systemSetting → Sale | 同上 | F | 0 命中 |
| systemSetting → Charge | 同上 | F | 0 命中 |
| systemSetting → Delivery | 同上 | F | 0 命中 |
| systemSetting → Machine | 同上 | F | 0 命中 |
| systemSetting → Customer | 同上 | F | 0 命中 |
| 诊所管理 → 业务 | 同上 | F | 0 命中（adminClinicCtrl 0 处跳转业务模块） |
| role/employee/department → 业务 | 同上 | F | 0 命中 |

**【红线级结论】**：
- "诊所管理" + "系统设置"两个一级菜单**完全不向业务模块提供数据**（A — 全局 0 命中跨模块桥）
- 业务模块也**不向这两个菜单回传数据**（A）
- 业务模块的 `companyId` 来源是 `grantAuth.hasAuthForCompany()` 或 `$stateParams.companyId`（A），与 systemSetting 0 直接桥
- 业务模块的 `employeeId` 来源是 `$stateParams.employeeId`（A — L8209-L8210）或 `item.employee.id`（A — L8251），与 employeeListCtrl 0 直接桥

### 8.1 唯一的"非业务"State 桥

| 来源 Controller | 目标 State | 行号 | 备注 |
|---|---|---|---|
| employeeListCtrl | `hospital.updateHospital.employeeList` | L15371, L15381 | reload 自身 |
| addRoleCtrl | `roleAdmin` | L32 | reload 角色列表 |
| modifyRoleCtrl | `roleAdmin` | L1705 | reload 角色列表 |
| modifyAreaRoleCtrl | `areaRoleAdmin` | L1581 | reload 区域角色 |
| addCheckItemCtrl | `systemSetting.checkList` | L50229 | 创建后跳转 |

### 8.2 systemSetting 内部跳转

| 来源 | 目标 | 触发 |
|---|---|---|
| 17 个 $state.go('systemSetting.*') | 详见第 2.3 节 | 各 Controller 保存/创建后 |

**【F】**：所有"系统设置"子状态之间的导航逻辑（在 controller.js 中看到的 $state.go 目标）与"业务模块"0 关系。

---

## 9. 反向排除（红线级）

### 9.1 名称相近但语义不同的对象

| 误判关系 | 实际关系 | 证据 |
|---|---|---|
| shopInfo == companyInfo | **不同对象** | A — shopInfo L22117, L30578, L35277；companyInfo L8816, L3290 |
| shopInfo == 全局配置 | **局部 ObjectFactory 模板** | A — 4 个局部 Controller |
| adminInfo == 当前管理员 | **不存在** | A — adminInfo 0 命中 |
| employeeId == userId | **本系统无 userId** | A — userId 0 命中 |
| adminId == employeeId | **不同 ID** | A — adminId 来自 $stateParams.adminId；employeeId 来自 employee.id |
| roleId == userRole | **不存在 userRole** | A — userRole 0 命中 |
| clinicId == shopId | **不同 ID** | A — clinicId 出现在 $stateParams 业务页；shopId 出现在 ObjectFactory 模板 |
| corpAdminHome.state == 数据库字段 | **state 路由名** | A — 与 $state 名一致 |
| corpAdminHome.id == permission ID | **路由 home ID** | A — `fromCorpAdminHomeIdArray` 写入 updateRoleStateList |
| adminRoleId == permission | **角色 ID** | A — 完全不同语义 |
| adminStateType == 数据库表 | **前端树形分类** | A — 仅 UI 树形用 |

### 9.2 API 名称 ≠ 后端实体

| API | 前端语义 | 后端实体 | 关系 |
|---|---|---|---|
| `/admin/createAdminRole.json` | 角色 | unknown | F |
| `/admin/selectEmployeeVoList.json` | 员工列表 | unknown | F |
| `/admin/getConsultRoomVo.json` | 诊室详情 | unknown | F |
| `/admin/saveCorpCashConf.json` | 现金配置 | unknown | F |
| `/admin/getSchoolCheckConf.json` | 筛查配置 | unknown | F |
| `/admin/getCorpInfo.json` | 公司信息 | unknown | F |
| `/admin/getCompanyList.json` | 公司列表 | unknown | F |
| `/admin/selectMachineCenterVoList.json` | 加工中心 | unknown | F |
| `/admin/getQueueMachineVo.json` | 叫号机 | unknown | F |

**【F 边界】**：本轮**不进行 API→后端实体的 L3 推断**，仅记录前端语义。

### 9.3 ObjectFactory 名称 ≠ 数据库表

| ObjectFactory name | 前端 UI 含义 | 后端表 | 关系 |
|---|---|---|---|
| `companyInfo` | 公司信息 | unknown | F |
| `shopInfo` | 商品信息（modal） | unknown | F |
| `clinicInfo` | 叫号大屏配置 | unknown | F |
| `roleObj` | 角色对象 | unknown | F |

**【F 边界】**：ObjectFactory name 在前端是**UI 标识符**，不是数据库表名。

### 9.4 State Param ≠ 数据库主键

| State Param | 前端语义 | 后端主键 | 关系 |
|---|---|---|---|
| `companyId` | 公司 ID | unknown | F |
| `employeeId` | 员工 ID | unknown | F |
| `roleId` | 角色 ID | unknown | F |
| `departmentId` | 部门 ID | unknown | F |
| `consultRoomId` | 诊室 ID | unknown | F |
| `id` (generic) | 不明 | unknown | F |

**【F 边界】**：前端 State Param 仅记录"前端用此 ID 取数据"，不证明后端主键结构。

### 9.5 "叫 config" ≠ "系统设置数据库"

**重要反向**：
- `configParams` (L11024+) 是叫号机配置参数
- `appConfigList` (L52304+) 是 App 开关配置
- `companyCashConf` (L57711+) 是小票配置
- `mateCheckReportConfig` (L41026+) 是检查报告配置

**它们都是局部 Object**，没有"全局 config 对象"。

---

## 10. 核心 DAG

### 10.1 "诊所管理" 内部 DAG

```
[外部 grantAuth / stateParam]
   ↓
window.grantAuth.hasAuthForCompany(ObjectFactory)
   ↓ (A)
$scope.companyInfo (adminClinicCtrl L15058)
   ↓
$scope.clinicPopout (L15077, Object with showP/addClinic/updateClinic/printQrCode)
   ↓ A
$scope.screenList (ListFactory /admin/selectConsultRoomVoListOfCompany.json L15148)
   ↓ A
$scope.examineList (ListFactory /admin/getExamineVoList.json L15174)
   ↓ A
$scope.clinicPopout.submit()
   ↓ A
$scope.addClinic / updateClinic
   ↓ A
W: /admin/addConsultRoom.json
W: /admin/updateConsultRoomInfo.json
W: /admin/updateConsultRoomStatus.json
   ↓ A
consultRoom Object (id, consultRoomName, examineIdArray)
   ↓ A
WechatConfig.setWechatUrl(appid, "checkSignIn?consultRoomId=" + consultRoom.id)
   ↓ A
jqprint
```

### 10.2 "系统设置" 内部 DAG

```
[$state.go('systemSetting.*') 入口]
   ↓ A (state 声明 F)
[17 个 systemSetting.* 子 Controller]
   ↓ A
[11 个 *Config Ctrl（菜单归属待定 F）]
   ↓ A
[5 个 Role/Department/Employee Ctrl（菜单归属待定 F）]
   ↓ A
ObjectFactory 模式 (HttpFactory.object)
   ↓ A
R/W: /admin/* (50+ unique namespace)
   ↓ A
popup/showP 模式
   ↓ A
$state.go(reload)
```

**【F 关键】**：**没有跨 Controller 的 object propagation**，每个 systemSetting Controller 独立完成 Read → User Edit → Write。

### 10.3 "诊所管理/系统设置" → "业务模块" 跨链 DAG

```
[诊所管理/系统设置 Controller]
   ↓ F (0 命中所有桥类型)
[就诊流程 / 预约叫号 / 患者维护 / 营销管理 / 筛查机构 / 物资管理 / 数据报表]
```

**【红线级】**：DAG 上一条边都没有 —— "诊所管理 + 系统设置" 与"业务模块"在 controller.js 范围内**完全独立**。

### 10.4 grantAuth 全局权限流

```
[外部 JS (F: 实现不可得)]
   ↓
window.grantAuth (L2324+)
   ↓ A
.hasAuthForCompany(ObjectFactory) → companyInfo (L15058)
   ↓ A
.getBackAuth(ObjectFactory, "1") → 后台权限 (L4597+)
   ↓ A
.getFrontAuth(ObjectFactory) → 前台权限 (L8402+)
   ↓ A
.getGrant(ObjectFactory) → 通用 grant (L5380+)
   ↓ A
.getAuthList(ObjectFactory, "goods") → 鉴权列表 (L36691+)
   ↓ A
.hasAuthForWA($scope.otherPay) → WA 权限 (L5670+)
   ↓ A
.isUnlockFun(ObjectFactory, "importMembers") → 功能解锁 (L17990)
```

---

## 11. 26项矩阵

| # | 项 | 结论 | 证据等级 | 文件/Controller | 行号 | 当前采用 |
|---:|---|---|---|---|---|---|
| 1 | 一级菜单入口 | 诊所管理(adminClinicCtrl) + 系统设置(17+ Ctrl) | A | controller.js | L15053, L53001-59014 | A |
| 2 | 二级菜单枚举 | 17 systemSetting.* + 1 adminClinic + 1 空 stub | A | $state.go 引用 | 详见第 2.3 节 | A |
| 3 | Controller 边界 | 42 个相关 Controller 完整列表 | A | controller.js | 详见第 5.1 节 | A |
| 4 | State | 0 个 .state() 声明在 controller.js | F | controller.js | — | F |
| 5 | URL | 0 个 $stateProvider 注册 | F | controller.js | — | F |
| 6 | HTML/template | 7 个 HTML 中 0 含 systemSetting/adminClinic | A | Get-ChildItem + 全文搜索 | — | A |
| 7 | DI | 各 Controller DI 完整记录 | A | controller.js | 详见各节 | A |
| 8 | API unique | ~196 unique API 在 42 个相关 Ctrl 中 | A | Python 静态扫描 | — | A |
| 9 | API call site | 详见第 5.1 节 | A | Python 静态扫描 | — | A |
| 10 | Read API | ~108 个 | A | Python 静态扫描 | — | A |
| 11 | Write API | ~75 个 | A | Python 静态扫描 | — | A |
| 12 | Request 来源 | $stateParams / Response / ObjectFactory 模板 | A | controller.js | 各处 | A |
| 13 | Response 来源 | /admin/* API Response | A | controller.js | 各处 | A |
| 14 | ObjectFactory | new ObjectFactory(...) 模板模式 | A | controller.js | 详见第 6 节 | A |
| 15 | ListFactory | new ListFactory(url, 0, pageSize, params) | A | controller.js | 大量 | A |
| 16 | Scope 对象 | companyInfo / screenList / clinicPopout / examineList / stuff 等 | A | controller.js | 各处 | A |
| 17 | 关键业务对象 | consultRoom / examine / employee / role / department / adminStateList | A | controller.js | 各处 | A |
| 18 | 关键 ID | companyId / employeeId / adminId / adminRoleId / roleId / departmentId / consultRoomId / bodyCheckId | A | controller.js | 各处 | A |
| 19 | State bridge | 仅内部跳转（systemSetting.* 互跳、roleAdmin/areaRoleAdmin 互跳） | A | controller.js | 详见第 8.1 节 | A |
| 20 | Function bridge | **0 处跨 Controller 函数调用** | A | controller.js | 0 命中 | F |
| 21 | API Response → Request | **0 处跨 Controller** | A | controller.js | 0 命中 | F |
| 22 | Scope/Service bridge | grantAuth 是唯一 window 全局源 | A | controller.js | 73 处 | A |
| 23 | Factory bridge | 0 共享 | A | controller.js | 0 命中 | F |
| 24 | 跨一级菜单桥 | **0 处**与业务模块桥 | A | controller.js | 0 命中 | F |
| 25 | 当前核心 DAG | 第 10 节 4 个 DAG | A | controller.js | — | A |
| 26 | 复刻风险 | 第 12 节 | A | controller.js | — | A |

---

## 12. A/B/C/D/E/F 分布

| 等级 | 数量 | 说明 |
|---|---:|---|
| A（直接源码） | 22 | Controller / DI / API / Scope / State.go / $stateParams / 全部 controller.js 范围 |
| B（多源互证） | 1 | corpInfo 字段定义（localStorage 写源 + 多处读源） |
| C（局部） | 0 | — |
| D（冲突） | 2 | systemSetting.customerMaterialCertificate state 有引用但无 Controller；systemSettingCtrl 命名与空实现冲突 |
| E（推断） | **0** | 严格 E=0（红线要求） |
| F（证据范围不可得） | 5 | .state() 注册 / URL / HTML / $rootScope 全局共享 / adminProjectService |

---

## 13. 当前最终采用

| 采用等级 | 项 |
|---|---|
| A 直接采用 | Controller 注册 / DI / API 字符串 / ObjectFactory/ListFactory 模式 / $state.go 目标 / $stateParams 消费 / Scope 字段定义 / state 引用证据 |
| B 互证采用 | corpInfo LocalStorage 字段（写源 + 5+ 读源） |
| C 局部采用 | （无） |
| D 冲突待处理 | systemSetting.customerMaterialCertificate state 名引用 vs 0 Controller 注册；systemSettingCtrl L59014 空 stub；roleAdminCtrl 命名"角色管理"但实为"员工会员迁移" |
| E 仅推断不进入规格 | 0 |
| F 未观察 | .state() 注册；URL 路由表；HTML template 引用；$rootScope 全局共享；adminProjectService；后端实体表结构；DB 主键结构 |

---

## 14. 复刻红线

### 14.1 不能自行增加的字段

- 任何 controller.js 中 0 命中的字段（如 adminInfo、userId、userRole、permissionId、orgId）
- 任何 controller.js 中未出现的 ObjectFactory 模板字段

### 14.2 不能自行补桥的模块

- 诊所管理 + 系统设置 ↔ 业务模块（就诊/验光/检查/销售/收费/加工/配送）
- 即使"业务常识"上认为应该桥（如：系统设置员工 → 销售 employeeId），**源码 0 命中 = 0 桥**
- 必须保留 0 桥状态

### 14.3 不能假定存在的 API

- controller.js 中 0 命名的 API（如：诊所管理没有"删除诊室" /admin/deleteConsultRoom.json —— 仅有 updateConsultRoomStatus 软停用）
- 42 个相关 Controller 之外的所有 API

### 14.4 不能等同的 ID

- `adminId` ≠ `employeeId` ≠ `customerId` ≠ `patientId` ≠ `userId`（**userId 不存在**）
- `adminRoleId` ≠ `permission`（permission 通过 fromCorpAdminHomeIdArray 表达）
- `companyId` ≠ `shopId` ≠ `clinicId`
- `consultRoomId` ≠ `consultRoom` Object
- `corpAdminHome.id` 是 home ID（路由名 ID），不是 permission ID

### 14.5 必须保留的 F 边界

- **0 个 .state() 注册**（state 声明在 controller.js 范围不可得）
- **grantAuth 实现不可得**（window 全局，但赋值不在 controller.js）
- **corpInfo 写源在登录响应**（A — L14264-L14268），但**写时序不可得**（F）
- **7 个 HTML 中 0 命中系统设置/诊所管理引用**（A — F 边界）
- **后端实体表结构 0 命中**（F — L3 不可达）

### 14.6 命名误导必须标注

- `systemSettingCtrl` L59014 是**空 stub**，不是真正 Controller
- `roleAdminCtrl` 实际管理**员工会员迁移**（L1721-L1728 / $scope.moveCustomer），不是"角色管理"主体
- `addRoleCtrl` "新增角色" 实际只创建 1 个角色（L26 `/admin/createAdminRole.json`），不带权限分配
- `hospitalCtrl` L15340 是**空 stub**（与 systemSettingCtrl 同样情况）
- `updateHospitalCtrl` L15855 有注册但 state 声明 F

---

## 15. 红线检查

| 项 | 实际 | 通过 |
|---|---|---|
| API actual | 0 | ✓ |
| Write actual | 0 | ✓ |
| Production mutation | 0 | ✓ |
| controller.js SHA256 | F40589FF0C56DE60BF4785A0B6C5B0A33519B75A30A15D988B0E1BDCF0A19433（未变化） | ✓ |
| deliveryList.html SHA256 | 5B79B6F0E423A90188EE420234A9449531E623528C9807800C5780BD61006476（未变化） | ✓ |
| machineOrderCompleted.html SHA256 | F1602AB17D56C31C95B3E869397B00B34366ABF29C06D03A6BD28ADD230BDB24（未变化） | ✓ |
| machineOrderList.html SHA256 | A429507C94B67725C7ACFDCB873467754EF1EDEC8300DCC6AC3914BD5AB9B26A（未变化） | ✓ |
| 历史 MD 165-191 | 未修改 | ✓ |
| P0 = 54 | 冻结 | ✓ |
| P1 = 8 | 冻结 | ✓ |
| 10 untracked 保留 | 全部保留 | ✓ |
| 视光之家url.txt gitignored | 保留 | ✓ |
| ignored = 1 | 确认 | ✓ |
| 本轮只新增 192_*.md | 是 | ✓ |
| 临时脚本 | 在 C:\Users\18671\AppData\Local\Temp\，不进入 Git | ✓ |

---

## 16. Git

- 本轮 commit hash：（待执行 `git add -- 192_*.md` / `git commit -m "docs(192): S1-130 诊所管理 系统设置一级菜单边界与核心数据模型审计"` / `git push origin master`）
- 本轮 LOCAL == REMOTE：待最终校验
- 本轮 tracked 预期：199 → 200
- 本轮 untracked 预期：10（保留 10 untracked，新增 192 后变 10 untracked 因为 192 进 tracked）
- 本轮 ignored 预期：1（视光之家url.txt 保留）
- 本轮 staged only：192_S1-130_*.md

---

## 17. 历史结论与本轮扫描对照

### 17.1 S1-122 ~ S1-129 中"systemSetting / 配置"相关历史结论

| 历史文档 | 历史结论 | 本轮当前扫描 | 差异 | 当前采用 |
|---|---|---|---|---|
| 165-189 | 未涉及 systemSetting/adminClinic/role/employee 域 | 0 命中 | 无冲突 | 沿用 A |
| 165-189 | 已知 S1-122~S1-129 业务模块 0 处调用 systemSetting.* | 验证为 A（0 命中） | 一致 | A |

### 17.2 历史是否需要重定义

| 项 | 结论 |
|---|---|
| 历史证据不足 | 无（历史 165-189 范围未涉及本轮 Controller） |
| 历史把推断写得过重 | 无 |
| 当前资源范围扩大 | 是（新增 42 个 Controller 全量审计） |
| Controller 定位修正 | 是（systemSettingCtrl L59014 / hospitalCtrl L15340 / updateHospitalCtrl L15855 三处空 stub 或 state 声明 F） |
| 暂不能判断 | 1（systemSetting.* state 声明在 controller.js 范围不可得） |

---

## 18. 结论

### 18.1 一级菜单结构（基于 $state.go 引用证据）

| 一级菜单 | 子菜单 Controller 数量 | 主要 Controller | 状态声明 |
|---|---:|---|---|
| 诊所管理 | 1 | adminClinicCtrl | F |
| 系统设置（17 子状态） | 17 + 1 stub | checkList / checkModify / customerCharge* / material* / medicalFees / memberType / processCenter / projectCard / supplier* / InspectList | F |
| 权限与人员（菜单归属 F） | 5 | roleAdmin / modifyRole / modifyAreaRole / addRole / areaRoleAdmin / employeeList / departmentList / addDepartment / updateDepartment | F |
| 系统配置（菜单归属 F） | 12 | appConfig / medicalConfig / printConfig / queueMachineConfig / cloudPrinterConfig / healthScreenConfig / mateCheckReportConfig / screenBack / screenPromotion* / screenUrl / zhiShiBang | F |
| 检查项（菜单归属 F） | 2 | addCheckItem / checkItemList | F |

### 18.2 核心数据模型

| 对象 | 唯一 Source | 跨 Controller 共享 | 进入 API Request | Runtime mutation |
|---|---|---|---|---|
| companyInfo | A. grantAuth (adminClinicCtrl) / ObjectFactory 模板 | **0**（每个 Controller 独立 $scope） | A (companyId) | **0** |
| shopInfo | A. 4 个局部 Controller ObjectFactory 模板 | **0** | A (productId) | **0** |
| clinicInfo (叫号大屏) | A. grantAuth | **0** | A (employeeId / bigScreenId) | **0** |
| corpInfo | A. localStorage ← 登录响应 (corpType) | **共享**（所有 Controller 都可读） | F | **写**（登录时 L14268） |
| corpAdminHome | A. /admin/getRoleStateListForUpdate.json | **0** | A (fromCorpAdminHomeIdArray) | **0** |
| adminStateList | A. 同上 | **0** | **0**（仅 UI 树形） | **0** |
| consultRoom | A. /admin/selectConsultRoomVoListOfCompany.json | **0** | A (consultRoomId / examineIdArray) | **0** |
| employee | A. /admin/selectEmployeeVoList.json | **0** | A (employeeId) | **0** |

### 18.3 跨菜单桥

- **0 个 Function bridge**
- **0 个 API Response → Request 桥**
- **0 个 Scope/Service 桥**（除 grantAuth）
- **0 个 Factory 桥**
- **状态桥仅内部**：systemSetting.* 互跳、roleAdmin / areaRoleAdmin 互跳、employeeList 自 reload

### 18.4 红线级 F 边界

1. controller.js 范围**无 .state() 注册** → 完整 state 树结构 F
2. controller.js 范围**无 $stateProvider 调用** → 完整 URL 路由 F
3. 7 个 HTML 中**0 命中** systemSetting/adminClinic/roleAdmin/employeeList 引用 → 实际 template 不可得 F
4. 工作区**唯一 JS = controller.js** → 外部 JS（CDN/打包/注入）实现 F
5. **grantAuth / QiniuFactory / WechatConfig / HttpFactory / ListFactory / ObjectFactory** 等 Service 的完整实现在 controller.js 范围 F

### 18.5 复刻结论

- 42 个相关 Controller 的 **API 列表、Scope 字段、ObjectFactory/ListFactory 模板**完全可复刻（A）
- 17 个 systemSetting.* state 名 + adminClinic 1 个 + systemSettingCtrl 1 个空 stub 完全可复刻（A — 基于 $state.go 反推）
- 所有跨菜单桥保持 **0 桥**（A — 不可补桥）
- 5 个 F 边界必须**保留为 F**（不可推断为存在或不存在）
- 后端实体 / 数据库结构 / 状态机 / 业务规则 = **F**（必须等待 L3 证据）
