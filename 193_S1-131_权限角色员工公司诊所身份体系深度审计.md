# S1-131 权限 / 角色 / 员工 / 公司 / 诊所 身份体系深度审计

> **项目**：OptFlow PMS 逆向建模
> **本轮目标**：在 S1-130 基础上，深度拆解 grantAuth / corpInfo / companyId / adminId / adminRoleId / roleId / employeeId / departmentId / corpAdminHome / adminStateList / role / adminRole / employee / department 这一整套"权限/角色/员工/公司/诊所"前端身份体系
> **A-F 证据等级**：A=直接源码 / B=多源互证 / C=局部 / D=冲突 / E=推断 / F=证据范围不可得
> **L1/L2/L3**：L1=前端直接事实 / L2=业务模型解释 / L3=数据库物理模型
> **红线**：API actual = 0，Write actual = 0，Production mutation = 0，controller.js unchanged，HTML unchanged，165-192 unchanged
> **完成时间**：2026-09-09

---

## 1. 审计范围

| 类别 | 数量 / 范围 |
|---|---|
| controller.js 全文 | 59,214 行 / 2,194,196 bytes / SHA256=F40589FF0C56DE60BF4785A0B6C5B0A33519B75A30A15D988B0E1BDCF0A19433（未变） |
| grantAuth 7 个方法总调用 | **73 处**（A — 全量字符级） |
| corpInfo 出现 | **26 处**（A） |
| companyId 出现 | **100+ 处**（A） |
| corpId 出现 | **31 处**（A） |
| adminId 出现 | **21 处**（A） |
| adminRoleId 出现 | **37 处**（A） |
| roleId 出现 | **4 处 State Param**（A） |
| employeeId 出现 | **149+ 处**（A） |
| departmentId 出现 | **2 处**（A） |
| corpAdminHome 出现 | **46 处**（A） |
| adminStateList 出现 | **88 处**（A） |
| schoolId 出现 | **200+ 处**（A — **重大发现：schoolId ≠ companyId**） |
| customerId 出现 | **128 处**（A） |
| adminInfo Object | **0 处**（A — 不存在） |
| userId | **0 处**（A — 不存在） |
| permissionId | **0 处**（A — 不存在） |
| orgId | **0 处**（A — 不存在） |

---

## 2. grantAuth 全量审计

### 2.1 7 个方法精确调用统计（A — 字符级）

| 方法 | 调用次数 | 模式 | 返回值 | 副作用 |
|---|---:|---|---|---|
| `hasAuthForCompany(ObjectFactory)` | **32** | `window.grantAuth.hasAuthForCompany(ObjectFactory).then(function (res) {...})` | Promise<companyInfo> | 设置 `$scope.companyInfo = res` 或 `$scope.companyInfo = result` |
| `getBackAuth(ObjectFactory, "1")` | **8** | `grantAuth.getBackAuth(ObjectFactory, "1").then(function () {...})` | 副作用（无返回值消费） | 通常用于"获取后台权限" |
| `getBackAuth(ObjectFactory)` | **3** | 变体（无第二参数） | 同上 | 同上 |
| `getFrontAuth(ObjectFactory)` | **7** | `window.grantAuth.getFrontAuth(ObjectFactory).then(function (res) {...})` | Promise<frontAuth> | `$scope.frontAuth = res` 类 |
| `getGrant(ObjectFactory)` | **6** | `grantAuth.getGrant(ObjectFactory).then(function (obj \| _ref) {...})` | Promise<grantObj> | 解构 `{ adminId, schoolIdArray, ... }` |
| `getAuthList(ObjectFactory, "goods")` | **5** | `window.grantAuth.getAuthList(ObjectFactory, "goods").then(function (auth) {...})` | Promise<authList> | `$scope.authList = auth` |
| `getAuthList(ObjectFactory, "report.reportSale")` | **1** | 同上 | 同上 | 同上 |
| `getAuth($scope.authList, auth)` | **4** | `return window.grantAuth.getAuth($scope.authList, auth)` | boolean | 权限检查 |
| `hasAuthForWA($scope.otherPay)` | **4** | `if (!grantAuth.hasAuthForWA($scope.otherPay))` | boolean | 微信支付权限检查 |
| `isUnlockFun(ObjectFactory, "importMembers")` | **1** | `window.grantAuth.isUnlockFun(ObjectFactory, "importMembers").then(function (resolve) {...})` | Promise<boolean> | 功能解锁检查 |
| `isUnlockFun`（其他 4 个 unlock key） | **4** | 同上 | boolean | — |

### 2.2 isUnlockFun 5 个 unlock key 精确分类（A — 字符级）

| unlock key | 行号 | 上下文 |
|---|---|---|
| `importMembers` | L17990 | 会员导入 |
| `healthScreenConfig` | L40529 | 筛查报告配置 |
| `screenConditionBatch` | L45262 | 大屏条件批量 |
| `screenInStore` | L45976 | 大屏入库 |
| `addLens` | L54543 | 添加镜片 |

### 2.3 getAuthList 完整列表（A — 字符级）

| 调用点 | unlock key | 行号 |
|---|---|---|
| L36691 | "goods" | 商品权限 |
| L36828 | "goods" | 商品权限 |
| L37253 | "goods" | 商品权限 |
| L37604 | "goods" | 商品权限 |
| L37899 | "report.reportSale" | 销售报表权限 |
| L38013 | "goods" | 商品权限 |

### 2.4 grantAuth 调用点 Controller 簇分布

grantAuth 在 controller.js 73 处分布（按 Controller 簇粗略归类）：
- **业务页 Controller**（40+ 处）：包括 `getBackAuth`（支付/账单）、`getFrontAuth`（前台/会员页）、`getGrant`（多 Controller）
- **大屏 / 报表 Controller**（10+ 处）：`getFrontAuth` + `getAuthList("report.reportSale")`
- **诊所管理**（1 处）：`hasAuthForCompany` L15055
- **系统设置**（0 处）：role/employee/department 0 命中 grantAuth（A — 反向证据：systemSetting 域**不消费 grantAuth**）

### 2.5 grantAuth 实现源 F 边界

- controller.js 中**无 `window.grantAuth =` 赋值**（A — 0 命中）
- controller.js 中**无 `grantAuth.prototype` 或 `grantAuth = function` 定义**（A — 0 命中）
- **grantAuth 是外部 JS 注入的 window 全局对象**（F 边界 — 实现细节不可得）

### 2.6 第二个权限源：JudgeAuthClass（A — 字符级）

- **L17483**: `JudgeAuthClass.getAuth("isSuperAdmin").then(function (res) {...})` —— `isSuperAdmin` 是不同 ID
- 调用方：会员回收站相关 Controller
- **JudgeAuthClass 与 grantAuth 是平行权限源**（A — 两个不同入口）

### 2.7 grantAuth → 业务模块消费证据（A — 字符级）

| 业务 Controller | 调用 grantAuth 的方法 | 用途 |
|---|---|---|
| 接诊 / 验光 / 检查 | `hasAuthForCompany` / `getFrontAuth` | 初始化公司权限 + 前台权限 |
| 销售 / 收费 | `getBackAuth` / `getGrant` / `getAuthList("goods")` / `hasAuthForWA` | 后台权限 + 支付方式权限 + 商品权限 + 微信支付权限 |
| 报表 | `getAuthList("report.reportSale")` | 销售报表权限 |
| 报告 / 大屏 | `getFrontAuth` / `getAuthList` | 前台权限 + 鉴权列表 |
| 患者 / 会员 | `getFrontAuth` / `getGrant` | 前台权限 + 通用 grant |
| 库存 | `getGrant` / `hasAuthForCompany` | 通用 grant + 公司权限 |
| 叫号 | `hasAuthForCompany` | 公司权限 |

**结论**：grantAuth **真实跨业务模块**（与 S1-130 "systemSetting 0 跨业务"结论不同）—— 这是**唯一真正的"权限 → 业务"桥**。

---

## 3. corpInfo 全链路

### 3.1 写源（A — 唯一）

**L14263-L14268** 登录后写入：
```javascript
new ObjectFactory().saveOrQuery("/admin/getCorpTypeConfVo.json").then(function (resp) {
  var corpInfo = {};
  corpInfo.companyTitle = resp.result.object.corpType.companyTitle;
  corpInfo.employeeTitle = resp.result.object.corpType.employeeTitle;
  corpInfo.username = username;
  localStorage.setItem("corpInfo", JSON.stringify(corpInfo));
});
```

| 字段 | 来源 |
|---|---|
| `companyTitle` | A. `resp.result.object.corpType.companyTitle` |
| `employeeTitle` | A. `resp.result.object.corpType.employeeTitle` |
| `username` | A. 登录输入的 `username` 变量（L14224 `$scope.user.name`） |

### 3.2 读源（A — 8 处 LocalStorage + 字段访问）

8 处 `localStorage.getItem('corpInfo')` / `'corpInfo'`（含 1 个 setItem）：
- L585, L871, L3529, L14268(set), L38225, L50843, L51941
- L14263-L14269 set + 7 处 get

字段访问次数：
- `corpInfo.companyTitle` 13 处
- `corpInfo.employeeTitle` 8 处
- `corpInfo.username` 1 处
- `corpInfo.companyId / id / adminId / employeeId` **0 命中**（A — 反向排除）

### 3.3 corpInfo 使用场景（A — 字符级）

| 使用点 | 行号 | 用途 |
|---|---|---|
| 报损单选公司 | L585, L871, L873 | `$scope.supplierWord = "请选择" + companyTitle` |
| 找视光师 | L3540 | `stateName: "找" + corpInfo.employeeTitle` |
| 登录时写入 | L14264-L14268 | 完整来源链 |
| 报损单 modal 供应商 | L38225, L38247 | `name: '接诊' + corpInfo.employeeTitle` |
| 报损单 modal 公司 | L50843, L50845, L50859 | `Popup.notice('请选择' + companyTitle)` |
| 销售单 / 报损 | L51941, L51943 | 同上 |

**【关键】**：corpInfo **不进入任何 API Request**（A — 0 命中），**不进入 State**（A — 0 命中），**仅用于 UI 文案渲染**（A — 全部用于 `请选择XXX` / `接诊XXX` 字符串拼接）。

### 3.4 corpInfo 跨菜单范围（A — 字符级）

| 菜单 | 使用 | 证据 |
|---|---|---|
| 报损（Delivery / Stock Loss） | A | L585, L871, L3529, L38225, L50843, L51941 |
| 销售（Sale） | A | L51941, L51943 |
| 业务页通用 | A | 多个 Controller |

**跨菜单共享**：是（**所有 Controller 都可读** localStorage）

### 3.5 假阳性反向排除

- L29297 `HttpFactory.object('/admin/getStockConf.json').then(function (corpInfo) {...})` — 这是 **API 响应参数名 `corpInfo`**（不是 LocalStorage 的 corpInfo Object）—— **D 冲突**（同名不同源）
- L29299 `if (corpInfo && corpInfo.enableFeier == 1)` — 是库存配置响应

### 3.6 corpInfo 与 companyId / corpId 关系

| 关系 | 是否等同 | 证据 |
|---|---|---|
| corpInfo.companyTitle == companyInfo.name | **未直接证明** | A — 命名相近但未在同一行出现 |
| corpInfo 包含 companyId | **不存在** | A — corpInfo 只有 3 个字段（companyTitle / employeeTitle / username） |
| corpId == companyId | **未直接证明** | A — 命名风格不同；corpId 是 31 处 ID，companyId 是 100+ 处 ID；未在源码任何位置直接互证 |

---

## 4. companyId 身份链

### 4.1 companyId 8 个不同来源（A — 字符级）

| # | Source | 出现次数 | 代表行号 |
|---:|---|---:|---|
| 1 | `$stateParams.companyId` | 7 | L14849, L15120, L15149, L15311, L15329, L15352, L18258 |
| 2 | `res.result.object.company.id` / `res.company.id` | 5 | L420, L10992, L10994, L43424, L43426 |
| 3 | `localStorage.getItem("corpid")` | 1 | L1144 |
| 4 | `localStorage.setItem("corpid", corpId)` | 1 | L14239 |
| 5 | `ObjectFactory 模板 this.info.id` | 8+ | L3302, L3370, L10887, L11259, L11569, L11687, L12005, L12344 |
| 6 | `$scope.companyInfo.id` | 5+ | L9661, L9700, L10211, L10339, L10361, L11646 |
| 7 | `$scope.companyId = res.id`（业务页派生） | 多 | L8323, L8340, L9080, L9086, L9336-L9338, L9766, L10078, L10417 |
| 8 | `res.object.id` (getCompanyOfMine Response) | 1 | L14570 |

### 4.2 companyId Consumer 分布

| Consumer | 类型 | 行号 |
|---|---|---|
| `selectEmployeeVoList` (Request) | 列表 API | L1292, L8339, L9085, L15355 |
| `selectConsultRoomVoListOfCompany` (Request) | 列表 API | L15148 |
| `selectBandingCustomerList` (Request) | 列表 API | L1300 |
| `addClinic` / `updateClinic` (Request) | 写 API | L15126, L15133 |
| `addDepartment` (Request) | 写 API | L14860 |
| `getDepartmentList` (Request) | 列表 API | L15330 |
| `getHospitalInfo` (Request id) | 读 API | L50832 |
| `changeAdminRole` (Request) | 写 API | L648 |
| `getCompanyInfo` (chain) | 读 API | L14239 |
| `selectInYearListBySchoolPlan` (Request) | 列表 API | L19238, L19894 |
| 等等（~100 处） | — | — |

### 4.3 companyId 状态桥（A — 字符级）

`$stateParams.companyId` **直接被以下 Controller 消费**：
- `addDepartmentCtrl` L14849
- `adminClinicCtrl` L15120, L15149
- `departmentListCtrl` L15329
- `employeeListCtrl` L15352
- 等等

**companyId 跨 Controller 共享**：是（通过 State Param + URL）

### 4.4 companyId vs corpId 关系

| 维度 | companyId | corpId |
|---|---|---|
| 出现次数 | 100+ | 31 |
| 命名风格 | camelCase | lowercase |
| 主要场景 | URL / State / API | 登录 / 充值 / 报损 modal |
| localStorage key | N/A | "corpid" |
| 是否 = 同一 ID | **未直接互证**（F 边界） | 业务含义相同 |

**【F 边界】**：源码**无任何直接赋值 `companyId = corpId` 或反之**（A — 0 命中）。但根据：
- 业务含义都是"公司 ID"
- 命名空间相似
- 都写入 localStorage
- 都作为 company API 的入参

**B 级互证**（业务高度相关），但**A 级字符级未直接证明**。

### 4.5 companyId 关键反向排除

- **companyId ≠ clinicId**（A — 0 命中 `companyId === clinicId` 互换）
- **companyId ≠ shopId**（A — 0 命中）
- **companyId ≠ hospitalId**（A — L15235 `getHospitalInfo({ id: $scope.id })` 与 companyId 是不同 ID 路径）
- **companyId ≠ corpInfo.xxxId**（A — corpInfo 不含 companyId 字段）
- **companyId ≠ adminId**（A — 完全独立 ID 系统）

---

## 5. adminId / adminRoleId / roleId 独立树

### 5.1 adminId 树（A — 字符级）

#### 5.1.1 21 处精确分布

| Source | 行号 |
|---|---|
| `$stateParams.adminId` | L297, L681, L1369 |
| `res.result.object.admin.id` (角色修改时显示) | L420 附近 |
| `res.result.object.id` (createAdminRole Response) | L1052, L1412 |
| `_defineProperty({ adminId: adminId }, "adminId", adminId)` | L138 |
| `changeAdminRole` Request | L232, L648 |
| `transferAdminRole` Request | L262 |
| `getAdminStateListForUpdate` Request changeAdminId | L307 |
| `updateAdminStateList` Request changeAdminId | L383 |
| `getOtherAdminInfo` Request | L399, L406, L688 |
| `getAdminAreaLevelVo` Request | L1388 |
| 等等 | — |

#### 5.1.2 adminId Consumer
- `getOtherAdminInfo.json` (Read)
- `getAdminStateListForUpdate.json` (Read)
- `updateAdminStateList.json` (Write)
- `getAdminAreaLevelVo.json` (Read)
- `changeAdminRole.json` (Write)
- `transferAdminRole.json` (Write)

**全部在 role 簇 Controller 内部**（A — 0 跨业务模块）

### 5.2 adminRoleId 树（A — 字符级）

#### 5.2.1 37 处分布
- 主要在 role 簇 5 个 Controller
- `$scope.obj.adminRoleId` 是核心 Scope 字段
- L1052, L1412: 来自 createAdminRole Response
- L415: `res.result.object.adminRole.id` (Response)
- L648: changeAdminRole Request
- L823, L1483, L1607: getRoleStateListForUpdate Request
- L863: `if ($scope.roleObj.adminRole.superType == 10)` —— **关键字段 superType**

#### 5.2.2 adminRoleId 核心字段
- `id` (adminRole.id)
- `roleName` (adminRole.roleName)
- `status` (adminRole.status)
- `superType` (adminRole.superType) — 角色类型代码

#### 5.2.3 adminRole 实际语义
- L415: `res.result.object.adminRole.roleName` → 角色名
- L416: `res.result.object.adminRole.superType` → 角色类型
- L862: `if ($scope.roleObj.adminRole.superType == 10)` → **特殊判断 superType=10 是某种特殊角色**

### 5.3 roleId 树（A — 字符级）

#### 5.3.1 4 处精确 State Param

| 行号 | 上下文 |
|---|---|
| L1473 | `modifyAreaRoleCtrl`: `$scope.obj.adminRoleId = $stateParams.roleId;` |
| L1542 | `modifyAreaRoleCtrl` create(): `obj.adminRoleId = $stateParams.roleId;` |
| L1597 | `modifyRoleCtrl`: `$scope.obj.adminRoleId = $stateParams.roleId;` |
| L1666 | `modifyRoleCtrl` create(): `obj.adminRoleId = $stateParams.roleId;` |

#### 5.3.2 **关键发现：$stateParams.roleId ≡ adminRoleId**（A — 字符级直接证据）

- L1473 / L1597: `$scope.obj.adminRoleId = $stateParams.roleId` —— **直接赋值**
- L1542 / L1666: `obj.adminRoleId = $stateParams.roleId` —— **直接赋值**
- **结论**：URL 中的 `?roleId=` 实际就是 adminRoleId（命名不同，**实际同一 ID**）

### 5.4 三棵树之间关系

| 关系 | 是否等同 | 证据 |
|---|---|---|
| adminId ↔ employeeId | **不同 ID** | A — 0 互换，命名/字段/Consumer 完全不同 |
| adminId ↔ customerId | **不同 ID** | A — 0 互换 |
| adminId ↔ roleId | **不同 ID** | A — adminId 是用户 ID，roleId 是角色 ID |
| adminRoleId ↔ roleId | **同一 ID** | A — 4 处 State Param 直接赋值 |
| adminRoleId ↔ permission | **不是 ID** | A — 权限通过 fromCorpAdminHomeIdArray 表达 |
| adminRoleId ↔ roleName | **不同字段** | A — roleName 是字符串，adminRoleId 是 ID |

### 5.5 isClinicAdmin / isAreaAdmin（A — 字符级）

| 标志 | 4 处 / 2 处 | 含义 |
|---|---|---|
| `isClinicAdmin: true` | L213, L276, L1731, L52612 | 区分诊所管理员 vs 区域管理员 |
| `isAreaAdmin: true` | L718, L1483 | 区域管理员 |

**消费 API**：
- `getAdminList.json` 接受 `isClinicAdmin: true`（L276）
- `getRoleStateListForUpdate.json` 接受 `isAreaAdmin: true`（L1483）

**结论**：本系统**至少有 3 种管理员类型**：
- 平台管理员（无 isClinicAdmin / 无 isAreaAdmin）
- 诊所管理员（isClinicAdmin=true）
- 区域管理员（isAreaAdmin=true）

**【F 边界】**：superType=10 是什么角色？需要更多 API 响应证据（A — 1 处 L862）

---

## 6. employee / department 完整链路

### 6.1 employee 树（A — 字符级）

#### 6.1.1 employeeId 精确分类（149+ 处中关键 32 处）

| 类型 | 次数 | 代表行号 | Consumer |
|---|---:|---|---|
| `$stateParams.employeeId` | 2 | L8209, L8210 | 接诊 State Param |
| `item.employee.id` Response | 1+ | L8251, L8339 | 选员工 UI |
| `res.object.id` (getEmployeeVo Response) | 1+ | L34796, L8564 | 员工详情 |
| `res.employee.id` (getEmployeeInfo Response) | 1 | L49983 | 携带者信息 |
| `{ employeeId: id }` Request | 1 | L1300, L8564 | 列表/详情 API |
| `employeeIdFrom` (Request) | 1 | L1298, L1328 | moveCustomersOfEmployee |
| `employeeIdTo` (Request) | 4 | L1312, L1323, L1328, L1721 | moveCustomersOfEmployee |
| `setDoctorApproved` Request | 1 | L15366 | 医生认证 |
| `setEmployeeRank` Request | 1 | L15376 | 员工等级 |
| 业务页 (L8209-L8251 接诊选医生) | 多 | 多个 Controller | 验光/接诊/检查 |

#### 6.1.2 employee API 全集（A — 字符级）

| API | 类型 | 调用点 |
|---|---|---|
| `/admin/selectEmployeeVoList.json` | R | L1292, L8339, L9085, L9484, L15355 |
| `/admin/selectEmployeeVoListOfMyCompany.json` | R | L2614, L2649, L19434, L19452, L19470, L19575, L33593, L34127, L35557, L49695, L50122 |
| `/admin/getEmployeeVo.json` | R | L8564 |
| `/admin/selectBandingCustomerList.json` | R (含 employeeId) | L1300 |
| `/admin/moveCustomersOfEmployee.json` | W | L1328 |
| `/admin/setDoctorApproved.json` | W | L15366 |
| `/admin/setEmployeeRank.json` | W | L15376 |
| `/admin/getEmployeeList.json` | R (估计) | 多处 |
| `/admin/selectEmployeeVoRank.json` | R (估计) | — |

**【关键】**：有两套 employeeVoList API：
- `selectEmployeeVoList.json` —— 接收 companyId（L1292, L8339, L9085, L15355）
- `selectEmployeeVoListOfMyCompany.json` —— 接收 `mobile` / 无参数（L2614, L19434 等）— "我的公司"语义

**结论**：本系统**有两套员工查询 API**，业务含义不同。

### 6.2 department 树（A — 字符级）

#### 6.2.1 4 Controller + 4 API

| Controller | 行号 | API | 类型 |
|---|---|---|---|
| `addDepartmentCtrl` | L14846-L14893 | `addDepartment.json` | W |
| `departmentListCtrl` | L15327-L15333 | `getDepartmentList.json` | R |
| `updateDepartmentCtrl` | L15690-L15764 | `getDepartmentInfo.json` / `updateDepartment.json` / `deleteDepartment.json` | R + W + W |
| 模态: `addDepartmentModalInstanceCtrl` | L14895 | `saveEmployeeAvatar.json` (注释代码) | W (未调用) |
| 模态: `updateDepartmentModalContentCtrl` | L15766 | Qiniu 直传 | — |

#### 6.2.2 关键字段

- `$stateParams.departmentId` (L15691)
- `$stateParams.id` (L15692)
- `departDesc` (Markdown description) — 6 处
- `rank1` (L15705)
- 真实字段集合：`{ id, name, companyId, departDesc, rank1, ... }`

#### 6.2.3 department 与 employee 关系

- **department 树中 0 处包含 employee 字段**（A — 0 命中）
- **employee API 中 0 处包含 department 字段**（A — 0 命中）
- 0 处跨 API 桥

**结论**：department ↔ employee **0 直接关系**（A — F 边界）

---

## 7. corpAdminHome / adminStateList 完整树

### 7.1 树结构（A — 字符级）

```
adminStateType (stateTypeName)
└── adminStateList[i]
    ├── corpAdminHome
    │   ├── stateName (string)
    │   ├── state (string = 路由名)
    │   └── id (number = homeId)
    ├── roleHaveState (boolean)
    └── children[k]
        ├── corpAdminHome
        ├── roleHaveState
        └── children[m]
            ├── corpAdminHome
            └── roleHaveState
```

### 7.2 corpAdminHome.state 强证据 — 实际是 state 路由名（A — 字符级）

**L37791** (reportCtrl):
```javascript
$scope.setTab = function (tab, iten) {
  $scope.active = tab;
  $state.go(iten.children[0].corpAdminHome.state);  // ← state 路由名
  sessionStorage.setItem("reportActive", tab);
};
```

**L37795** (reportCtrl):
```javascript
$scope.getLinkCodeFactory.saveOrQuery("/admin/getChildrenLinkAndCode.json", { state: "report" });
$scope.isCheck = function (str) {
  if ($scope.getLinkCodeFactory.result.object && $scope.getLinkCodeFactory.result.object.childrenCode.indexOf(str) > -1) {
    return true;
  }
  return false;
};
```

**【关键】**：`corpAdminHome.state` **是 ui-router 的 state 名字符串**（A — 字符级 L37791 实际 `$state.go()` 调用）

### 7.3 corpAdminHome.id 强证据 — 是 home 节点 ID（A — 字符级）

**L1690** (modifyRoleCtrl.create):
```javascript
obj.fromCorpAdminHomeIdArray = homeIdArr;  // ← homeId 数组
```

**L1569 / L1693**: `updateRoleStateList.json` Request 接受 `fromCorpAdminHomeIdArray`

**L383**: `updateAdminStateList.json` Request 接受 `changeAdminId` + `fromCorpAdminHomeIdArray`

**结论**：`corpAdminHome.id` 是 **home 节点 ID**（不是 permission ID），与 `corpAdminHome.state` 配合使用。

### 7.4 getRoleStateListForUpdate 4 处精确分类

| 行号 | Controller | Request | 返回 |
|---|---|---|---|
| L504 | `addRoleCtrl` | `{ adminRoleId: id }` | roleStateList |
| L823 | `addRoleCtrl` | `{ adminRoleId: $scope.obj.adminRoleId }` | roleStateList |
| L1483 | `modifyAreaRoleCtrl` | `{ adminRoleId: $scope.obj.adminRoleId, isAreaAdmin: true }` | roleStateList (区域) |
| L1607 | `modifyRoleCtrl` | `{ adminRoleId: $scope.obj.adminRoleId }` | roleStateList |

**【关键差异】**：L1483 传 `isAreaAdmin: true`，与 L504/L823/L1607 不传 isAreaAdmin 形成**对照**—— 区域角色的权限树和普通角色可能不同。

### 7.5 roleHaveState 来源

- 来自 API Response（A — 字符级 24 处全部是 `arr[i].adminStateList[j].roleHaveState`）
- 写入 `$scope.stuff` 树形结构（A）
- UI 渲染：`selected: arr[i].adminStateList[j].roleHaveState`（A）
- 后续：进入 `fromCorpAdminHomeIdArray` 计算（A — 仅在 selected=true 的叶子节点）

**【关键】**：`roleHaveState` 是 API Response 字段，**不是前端计算**（A）

### 7.6 permissionId 是否存在？

- 全局搜索 `permissionId`：**0 命中**（A）
- 全局搜索 `permission` 作为独立字段（排除 permissionToReturn）：**0 命中**（A — permissionToReturn 是 coupon 字段 L12538 / L12908）
- 权限通过 `fromCorpAdminHomeIdArray: number[]` 表达（array aggregation）

**【F 边界】**：permissionId 在本前端代码范围**不存在**。后端数据库是否有 permission 表——F。

### 7.7 corpAdminHome 业务流

```
[登录后] admin 进入 "角色管理"
   ↓ A
GET /admin/selectAdminRoleVoList.json { isClinicAdmin: true }
   ↓ A
Response: 角色列表 (adminRoleVo)
   ↓ A
用户点"修改" → $state.go("modifyRole", { roleId: id })
   ↓ A
modifyRoleCtrl: $scope.obj.adminRoleId = $stateParams.roleId
   ↓ A
GET /admin/getAdminRoleVo.json { adminRoleId: $scope.obj.adminRoleId }
   ↓ A
Response: { adminRole: { id, roleName, status, superType } }
   ↓ A
GET /admin/getRoleStateListForUpdate.json { adminRoleId: $scope.obj.adminRoleId }
   ↓ A
Response: adminStateList 树 (stateTypeName, corpAdminHome, children, roleHaveState)
   ↓ A
UI 渲染树形权限结构 (selected = roleHaveState)
   ↓ A
用户勾选 → $scope.stuff[i].children[j].selected = true
   ↓ A
POST /admin/updateRoleStateList.json { adminRoleId, fromCorpAdminHomeIdArray: homeIdArr }
   ↓ A
POST /admin/updateAdminRoleInfo.json { adminRoleId, roleName, status }
   ↓ A
$state.go("roleAdmin")
```

---

## 8. role Controller 簇详细审计

### 8.1 5 个 Controller 完整对照表（A — 字符级）

| Controller | 行号 | 行数 | DI | API | State Param | State.go | 核心字段 | 异常 |
|---|---|---:|---|---|---|---|---|---|
| `addRoleCtrl` | L17-L49 | 33 | $scope, Popup, $timeout, $rootScope, $state, ObjectFactory, ListFactory, $http | W: createAdminRole / R: getCompanyList | N/A | roleAdmin | obj.roleName | 仅创建角色，不带权限分配 |
| `areaRoleAdminCtrl` | L712-L735 | 24 | 10 个（含 DateUtilFactory） | R: selectAdminRoleVoList | N/A | N/A | obj.isAreaAdmin=true | 列表 + isAreaAdmin 过滤 |
| `modifyAreaRoleCtrl` | L1470-L1587 | 118 | 10 个 | R/W: getAdminRoleVo / getRoleStateListForUpdate / updateRoleStateList / updateAdminRoleInfo | $stateParams.roleId | areaRoleAdmin | obj.adminRoleId = $stateParams.roleId | 区域角色，isAreaAdmin: true 传给 R API |
| `modifyRoleCtrl` | L1594-L1711 | 118 | 10 个 | R/W: getAdminRoleVo / getRoleStateListForUpdate / updateRoleStateList / updateAdminRoleInfo | $stateParams.roleId | roleAdmin | obj.adminRoleId = $stateParams.roleId | 普通角色，isAreaAdmin 不传 |
| `roleAdminCtrl` | L1714-L1753 | 40 | 10 个（含 DateUtilFactory） | R: selectAdminRoleVoList | N/A | N/A | obj.isClinicAdmin=true / employeeIdTo / moveCustomer | **命名冲突**：同时是角色列表 + 员工会员迁移 |

### 8.2 roleAdminCtrl 命名冲突深度审计（A — 字符级）

**L1714-L1753 完整内容**：
```javascript
$scope.modify = function () {
  if (!$scope.employeeIdTo) { Popup.notice("请选择迁移的会员"); return false; }
  Popup.confirm("确定将" + $scope.fromName + "的会员迁移到" + $scope.toName + "名下？", function () {
    $scope.moveCustomer();  // ← 实际是 moveCustomer
  });
};
$scope.obj.isClinicAdmin = true;
$scope.searchAdminList = function () {
  $scope.getAdminList = new ListFactory("/admin/selectAdminRoleVoList.json", 0, 10, $scope.obj);
  // ...
};
$scope.setStatus = function (status) { $scope.obj.status = status; $scope.searchAdminList(); };
$scope.searchAdminList();
```

**实际职责**：
- API: `/admin/selectAdminRoleVoList.json`（A）— 角色列表
- 函数: `$scope.moveCustomer`（A）— **员工会员迁移**（不在 Controller 内部定义，是从其他 Controller 共享？F 边界）

**【D 冲突】**：roleAdminCtrl **同时承担**：
- 角色列表展示
- 员工会员迁移触发

**可能解释**：
- A. 命名历史残留（原始是"角色管理"，后来加了"员工迁移"功能）
- B. Controller 复用（HTML 模板中可能同时使用 roleAdminCtrl + 部分模板）
- C. 实际业务包含"角色管理 + 员工迁移"两个 tab
- D. 当前证据范围**无法判断**（F）

**当前采用**：F（无法判断命名意图，但事实是 Controller 内部同时存在两套逻辑）

### 8.3 moveCustomer 跨 Controller 关系

- `moveCustomer` 第一次定义在 L1326（**L1319 调用**）
- 第二次调用在 L1727（roleAdminCtrl.modify）
- API: `/admin/moveCustomersOfEmployee.json` { employeeIdFrom, employeeIdTo }
- 实际是 **Controller 间的 Function 共享**（A — 但**没有显式 service/factory 桥**，仅靠同 scope）

**【F 边界】**：moveCustomer 是**直接函数定义在某个 Controller 中**，roleAdminCtrl 通过某种方式共享（可能是 HTML 父 Controller 链继承）

### 8.4 addRoleCtrl 异常

- 仅 33 行
- API: W: createAdminRole / R: getCompanyList
- **不调用 getRoleStateListForUpdate** —— 创建后**不带权限分配**
- 用户需要再"修改角色"才能分配权限

**结论**：addRoleCtrl 是**最小化的创建入口**，权限分配是 modifyRoleCtrl 的功能。

---

## 9. user / admin / employee / customer 反向排除

### 9.1 明确等同（A — 字符级直接证明）

| 关系 | 等同 | 证据 |
|---|---|---|
| `$stateParams.roleId` ≡ `adminRoleId` | **是** | A — L1473 / L1542 / L1597 / L1666 4 处直接赋值 |
| `corpInfo.username` ≡ 登录 username | **是** | A — L14267 `corpInfo.username = username` |
| `localStorage.username` ≡ `loginFactory.result.admin.nickname` | **是** | A — L14273 `localStorage.setItem("username", $scope.loginFactory.result.admin.nickname)` |
| `corpId` ≡ `localStorage.corpid` 值 | **是** | A — L14239 `localStorage.setItem("corpid", corpId)` |
| `corpId` (login) ≡ `companyId` (? 待定) | **B 级** | 业务含义相同；未直接互证 |

### 9.2 明确不同（A — 字符级反向证据）

| 关系 | 不同 | 证据 |
|---|---|---|
| `userId` ≠ 任何 ID | **不存在 userId** | A — 0 命中 |
| `userInfo` ≠ userId | **userInfo 是 sessionStorage 临时对象** | A — L35414 `$scope.userInfo = {}` |
| `currentUser` / `currentAdmin` | **不存在** | A — 0 命中 |
| `userObj` | **不存在** | A — 0 命中 |
| `adminInfo` Object | **不存在** | A — 0 命中；仅有 `getOtherAdminInfo` API 名 |
| `adminId` ≠ `employeeId` | **不同 ID** | A — 0 互换；命名/字段/Consumer 完全不同 |
| `adminId` ≠ `customerId` | **不同 ID** | A — 0 互换 |
| `adminRoleId` ≠ `permissionId` | **不同概念** | A — adminRoleId 是 ID，permission 通过 fromCorpAdminHomeIdArray 表达 |
| `adminRoleId.superType` ≠ `permissionId` | **不同字段** | A — superType 是分类代码 |
| `companyId` ≠ `clinicId` | **不同 ID** | A — 0 互换 |
| `companyId` ≠ `shopId` | **不同 ID** | A — 0 互换 |
| `companyId` ≠ `corpInfo.xxxId` | **不同对象** | A — corpInfo 不含 companyId 字段 |
| `corpAdminHome.id` ≠ `permissionId` | **不同 ID** | A — homeId 是路由 home 节点 ID，permission 不存在 |
| `roleHaveState` ≠ `permissionId` | **不同字段** | A — roleHaveState 是 boolean |
| `corpInfo.companyTitle` ≠ `companyInfo.name` | **未证明等同** | A — 不在同一行出现；命名相近但无语义互证 |
| `grantAuth` ≠ database permission table | **未证明** | A — grantAuth 是前端 window 全局，无任何后端证据 |
| `admin` ≠ `employee` | **不同概念** | A — admin 是后台管理员，employee 是公司员工（如视光师） |
| `customer` ≠ `employee` | **不同概念** | A — customer 是顾客（customerId 128 处），employee 是员工 |
| `orgId` / `organizationId` | **不存在** | A — 0 命中 |
| `permission` 独立字段 | **不存在**（除 permissionToReturn 是 coupon 字段） | A — 0 命中 |

### 9.3 无法证明（F 边界）

| 关系 | 状态 |
|---|---|
| `corpId` ≡ `companyId` | F（**未直接互证**；B 级业务高度相关） |
| `corpInfo.companyTitle` ≡ `companyInfo.name` | F（未在同一 Controller 出现） |
| 后端数据库 `permission` 表是否存在 | F（L3 不可达） |
| `corpAdminHome.state` 后端是否对应 state 字段 | F（前端只看路由名） |
| `superType=10` 的具体含义 | F（仅 1 处判断） |
| `roleAdminCtrl` 命名意图 | F（D 冲突 — 见第 8.2） |
| `moveCustomer` 在 roleAdminCtrl 中的来源 | F（无显式 Service 桥） |
| grantAuth 后端实现 | F（外部 JS 不可见） |
| `corpId` / `companyId` 是否存储在数据库 `company` 表 | F（L3 不可达） |
| `schoolId` 与 `companyId` 是否同一 ID | F（**未直接互证**；两者是不同 ID 系统 — schoolId 200+ 处属筛查机构，companyId 100+ 处属公司） |

---

## 10. 跨一级菜单权限桥（A-G 判定）

| 一级菜单 | Function | State | API Response → Request | Scope/Service | Factory | Object ref | 字段共现 |
|---|---|---|---|---|---|---|---|
| **就诊流程** | F (0 命中) | F | F | A (grantAuth.hasAuthForCompany / getFrontAuth / getGrant) | F | F | F |
| **预约叫号** | F | F | F | A (grantAuth.hasAuthForCompany) | F | F | F |
| **患者维护** | F | F | F | A (grantAuth.getFrontAuth / getGrant) | F | F | F |
| **营销管理** | F | F | F | A (grantAuth.getFrontAuth / getAuthList) | F | F | F |
| **筛查机构** | F | F | F | A (grantAuth.isUnlockFun("healthScreenConfig")) | F | F | F |
| **物资管理** | F | F | F | A (grantAuth.getGrant / hasAuthForCompany / getAuthList("goods")) | F | F | F |
| **数据报表** | F | F | F | A (grantAuth.getAuthList("report.reportSale") / getFrontAuth) | F | F | F |
| **诊所管理 (hospital)** | F | F | F | A (grantAuth.hasAuthForCompany) | F | F | F |
| **诊所管理 (adminClinic)** | F | F | F | A (grantAuth.hasAuthForCompany L15055) | F | F | F |
| **系统设置 (systemSetting.*)** | F | F | F | **F (0 命中 grantAuth)** | F | F | F |
| **权限/员工/部门 (role/employee/department)** | F | F | F | **F (0 命中 grantAuth)** | F | F | F |

### 10.1 关键发现

- **grantAuth 是唯一跨业务模块的权限桥**（A）
- 但 grantAuth 桥**仅是 Scope/Service 桥**（D 触发权限检查），**不是 Function / State / API Response → Request 桥**
- **systemSetting.* + role/employee/department 完全不消费 grantAuth**（A — 0 命中，重要发现）
- **业务页 Controller 通过 grantAuth 消费权限**（A — getFrontAuth / getGrant / hasAuthForCompany 等）

### 10.2 与 S1-130 结论关系

| S1-130 结论 | S1-131 验证 |
|---|---|
| "诊所管理 + 系统设置 0 桥到业务模块" | **部分修正**：诊所管理 adminClinicCtrl 通过 grantAuth 间接桥到业务模块（hasAuthForCompany 拉公司权限） |
| "systemSetting.* 0 桥" | **完全保持**：systemSetting.* 0 命中 grantAuth |
| "42 个 Controller 自闭环" | **修正**：role/employee/department Controller 自闭环，但 adminClinicCtrl 通过 grantAuth 与业务模块有间接权限关系 |

---

## 11. L1 / L2 / L3 分层

### 11.1 L1（前端直接事实）

- controller.js 59,214 行所有身份相关代码
- 7 个 grantAuth 方法的调用点、参数、返回值消费
- corpInfo LocalStorage 读写链路
- 8 种 companyId 源头
- adminId / adminRoleId / roleId 三棵树
- employeeId 32 处精确分类
- department 4 Controller + 4 API
- corpAdminHome 完整树结构
- 5 个 role Controller 的 DI / API / Scope / State.go

### 11.2 L2（基于 L1 的有限业务解释）

- 本系统有 3 种管理员（平台 / 诊所 / 区域） — A 级推断
- roleAdminCtrl 同时承担"角色列表"和"员工会员迁移" — A 级观察
- corpInfo 主要用于 UI 文案渲染 — A 级观察
- corpAdminHome.state 是 state 路由名 — A 级观察
- fromCorpAdminHomeIdArray 是"角色权限"提交 — A 级观察
- 登录态有 6 个 localStorage 字段 + 2 个 sessionStorage 字段 — A 级观察

### 11.3 L3（当前无证据的数据库/物理模型）

- 后端是否有 `permission` 表（permissionId 0 命中）
- 后端是否有 `company` 表 / 字段结构
- 后端是否有 `employee` 表 / 字段结构
- 后端是否有 `department` 表 / 字段结构
- 后端是否有 `admin` / `adminRole` 表
- 后端 FK 关系
- corpInfo / corpId / companyId 在数据库中是否同一字段
- superType=10 是什么角色类型
- `schoolId` 与 `companyId` 是否同一 ID

---

## 12. 身份 DAG（A-G）

### DAG A: corpInfo

```
[/auth/adminLogin.json] (login)
   ↓ A
$scope.loginFactory.result.token
   ↓ A
new ObjectFactory().saveOrQuery("/admin/getCorpTypeConfVo.json")
   ↓ A
resp.result.object.corpType
   ├── companyTitle
   └── employeeTitle
   ↓ A
corpInfo = { companyTitle, employeeTitle, username }
   ↓ A
localStorage.setItem("corpInfo", JSON.stringify(corpInfo))
   ↓ A
[后续 8+ Controller 读取]
   ↓ A
corpInfo.companyTitle / employeeTitle
   ↓ A
UI 字符串拼接（"请选择" + companyTitle / "找" + employeeTitle）
   ↓ F
[0 处进入 API Request / State / 持久化业务对象]
```

### DAG B: companyId

```
[5 个 Source 互不互证]
├── $stateParams.companyId (7 处, L14849, L15120, L15149, L15311, L15329, L15352, L18258)
├── res.result.object.company.id (5 处, L420, L10992, L10994, L43424, L43426)
├── localStorage.getItem("corpid") (1 处, L1144) ← B 级对应 corpId
├── ObjectFactory this.info.id (8+ 处, companyInfo 模板)
├── $scope.companyInfo.id (5+ 处)
├── $scope.companyId = res.id (业务页派生, 多处)
└── res.object.id (getCompanyOfMine, L14570)
   ↓ A
[进入 ~100 处 API Request]
   ├── selectEmployeeVoList { companyId }
   ├── selectConsultRoomVoListOfCompany { companyId }
   ├── selectBandingCustomerList { employeeId }
   ├── addClinic / updateClinic { ...companyId... }
   ├── addDepartment { companyId }
   ├── getDepartmentList { companyId }
   ├── getHospitalInfo { id } ← 实际就是 companyId (L50832)
   └── 等等
   ↓ F
[companyId 5 个 Source 是否同一 ID]
   - A 级：来源路径不同；未直接互证
   - B 级：业务含义都是"公司 ID"
   - F 边界：未直接证明
```

### DAG C: adminId / adminRoleId / roleId

```
$stateParams.adminId (3 处, L297, L681, L1369)
   ↓ A
$scope.object.adminId
   ↓ A
[3 个 API]
├── getOtherAdminInfo.json { adminId }
├── getAdminStateListForUpdate.json { changeAdminId: adminId }
├── updateAdminStateList.json { changeAdminId, fromCorpAdminHomeIdArray }
├── changeAdminRole.json { id: adminId, adminRoleId }
├── transferAdminRole.json { adminId }
└── getAdminAreaLevelVo.json { adminId }

$stateParams.roleId (4 处, L1473, L1542, L1597, L1666)
   ↓ A
$scope.obj.adminRoleId = $stateParams.roleId  ← 字符级直接赋值
   ↓ A
[A 级证明 $stateParams.roleId ≡ adminRoleId]
   ↓ A
[API]
├── getAdminRoleVo.json { adminRoleId }
├── getRoleStateListForUpdate.json { adminRoleId [, isAreaAdmin: true] }
├── updateRoleStateList.json { adminRoleId, fromCorpAdminHomeIdArray }
└── updateAdminRoleInfo.json { adminRoleId, roleName, status }
   ↓ F
[后端是否真的叫 adminRoleId 还是 roleId] — F
```

### DAG D: employeeId / departmentId

```
employeeId:
├── $stateParams.employeeId (2 处, L8209, L8210) ← A
├── item.employee.id (1+ 处, L8251) ← A (Response)
├── res.object.id / res.employee.id (3 处) ← A
├── { employeeId: id } (2 处) ← A
├── employeeIdFrom / employeeIdTo (5 处, 转移会员) ← A
├── setDoctorApproved { employeeId, approved } ← A
└── setEmployeeRank { employeeId, rank1 } ← A
   ↓ A
[API 集合]
├── selectEmployeeVoList.json { companyId } ← A
├── selectEmployeeVoListOfMyCompany.json { mobile } ← A (不同 API)
├── getEmployeeVo.json { employeeId } ← A
├── selectBandingCustomerList.json { employeeId } ← A
├── moveCustomersOfEmployee.json { employeeIdFrom, employeeIdTo } ← A
├── setDoctorApproved.json ← A
└── setEmployeeRank.json ← A

departmentId:
├── $stateParams.departmentId (1 处, L15691) ← A
├── $stateParams.id (1 处, L15692) ← A
├── res.result.object.id (getDepartmentInfo) ← A
└── [0 employee 字段] ← F
   ↓ A
[API 集合]
├── getDepartmentList.json { companyId } ← A
├── getDepartmentInfo.json { id } ← A
├── addDepartment.json ← A
├── updateDepartment.json ← A
└── deleteDepartment.json ← A
   ↓ F
[department ↔ employee 0 直接关系]
```

### DAG E: corpAdminHome / roleHaveState

```
/admin/getRoleStateListForUpdate.json { adminRoleId [, isAreaAdmin] }
   ↓ A
Response: adminStateList 树
   ├── adminStateType.stateTypeName
   ├── adminStateList[i]
   │   ├── corpAdminHome.{stateName, state, id}
   │   ├── roleHaveState (boolean)
   │   └── children[k]
   │       ├── corpAdminHome
   │       ├── roleHaveState
   │       └── children[m]
   │           ├── corpAdminHome
   │           └── roleHaveState
   ↓ A
$scope.stuff[i].children[j].homeId (采集 selected=true 的叶子)
   ↓ A
obj.fromCorpAdminHomeIdArray = homeIdArr
   ↓ A
POST /admin/updateRoleStateList.json { adminRoleId, fromCorpAdminHomeIdArray }
   ↓ A
$state.go("roleAdmin" / "areaRoleAdmin")

corpAdminHome.state 强证据:
   ↓ A
L37791: $state.go(iten.children[0].corpAdminHome.state)  ← state 路由名
```

### DAG F: grantAuth

```
[外部 JS] (F: 实现不可得)
   ↓
window.grantAuth
   ↓
[7 个方法]
├── hasAuthForCompany(ObjectFactory) → 32 处 → $scope.companyInfo
├── getBackAuth(ObjectFactory, "1") → 8 处 → 副作用
├── getBackAuth(ObjectFactory) → 3 处 → 副作用
├── getFrontAuth(ObjectFactory) → 7 处 → $scope.frontAuth 类
├── getGrant(ObjectFactory) → 6 处 → 解构 { adminId, schoolIdArray, ... }
├── getAuthList(ObjectFactory, "goods") → 5 处 → $scope.authList
├── getAuthList(ObjectFactory, "report.reportSale") → 1 处
├── getAuth($scope.authList, auth) → 4 处 → boolean
├── hasAuthForWA($scope.otherPay) → 4 处 → boolean
├── isUnlockFun(ObjectFactory, "importMembers") → 1 处 → boolean
├── isUnlockFun(ObjectFactory, "healthScreenConfig") → 1 处
├── isUnlockFun(ObjectFactory, "screenConditionBatch") → 1 处
├── isUnlockFun(ObjectFactory, "screenInStore") → 1 处
└── isUnlockFun(ObjectFactory, "addLens") → 1 处
   ↓ A
[Consumer 模块]
├── 接诊/验光/检查/销售/收费/库存/叫号/患者/会员/大屏/报表
├── NOT 消费: systemSetting.* / role / employee / department
└── 第二权限源: JudgeAuthClass.getAuth("isSuperAdmin") (1 处, L17483)
```

### DAG G: 身份体系 → 业务一级菜单

```
[身份体系]
├── corpInfo (LocalStorage, 写入登录后, 仅 UI 文案)
├── companyId (8 个 Source, ~100 处 API Request)
├── corpId (31 处, 登录 + 报损, B 级对应 companyId)
├── adminId (21 处, 仅 role 簇内部)
├── adminRoleId (37 处, role 簇)
├── roleId (4 处 State Param, ≡ adminRoleId)
├── employeeId (32 处核心, 业务 + 员工模块)
├── departmentId (2 处, 仅 department 簇)
├── corpAdminHome (46 处, role 簇)
├── adminStateList (88 处, role 簇)
├── roleHaveState (24 处, role 簇 UI)
├── fromCorpAdminHomeIdArray (3 处, role 写 API)
├── grantAuth (73 处, 跨业务模块, NOT 跨 systemSetting)
├── JudgeAuthClass (1 处, isSuperAdmin)
└── schoolId (200+ 处, 筛查机构域, ≠ companyId)
   ↓
[8 个业务一级菜单]
├── 就诊流程: A (grantAuth.hasAuthForCompany / getFrontAuth / getGrant) / 其余 F
├── 预约叫号: A (grantAuth.hasAuthForCompany) / 其余 F
├── 患者维护: A (grantAuth.getFrontAuth / getGrant) / 其余 F
├── 营销管理: A (grantAuth.getFrontAuth / getAuthList) / 其余 F
├── 筛查机构: A (grantAuth.isUnlockFun("healthScreenConfig")) / 其余 F
├── 物资管理: A (grantAuth.getGrant / hasAuthForCompany / getAuthList("goods")) / 其余 F
├── 数据报表: A (grantAuth.getAuthList("report.reportSale") / getFrontAuth) / 其余 F
├── 诊所管理 (hospital): A (grantAuth.hasAuthForCompany) / 其余 F
└── 系统设置: F / 0 桥
```

---

## 13. 26 项证据矩阵

| # | 项 | 结论 | 证据等级 | 文件/行号 | 当前采用 |
|---:|---|---|---|---|---|
| 1 | grantAuth 来源 | 外部 JS (F) | F | controller.js 0 命中赋值 | F |
| 2 | grantAuth 消费者 | 73 处 / 7 方法 / 跨业务模块 | A | controller.js 多处 | A |
| 3 | corpInfo 来源 | 登录后 /admin/getCorpTypeConfVo.json 响应 | A | L14263-L14268 | A |
| 4 | corpInfo 消费者 | 7 处 LocalStorage 读 + 13 处 companyTitle + 8 处 employeeTitle | A | L585, L871, L3529, L38225, L50843, L51941 + 字段 | A |
| 5 | companyId 来源 | 8 个 Source（State Param / Response / LocalStorage / ObjectFactory / $scope.companyInfo / 业务页派生 / getCompanyOfMine） | A | 详见第 4.1 | A |
| 6 | companyId 消费者 | 100+ 处 API Request | A | 多处 | A |
| 7 | adminId 来源 | $stateParams / Response / createAdminRole Response | A | L297, L681, L1369, L1052, L1412 | A |
| 8 | adminId 消费者 | 仅 role 簇（5 Controller）+ 4 API（getOtherAdminInfo / getAdminStateListForUpdate / updateAdminStateList / changeAdminRole / transferAdminRole / getAdminAreaLevelVo） | A | role 簇 5 Ctrl | A |
| 9 | adminRoleId 来源 | $stateParams.roleId (4 处) / createAdminRole Response (2 处) / adminRole.id Response (1 处) | A | L1473, L1542, L1597, L1666, L1052, L1412, L415 | A |
| 10 | adminRoleId 消费者 | role 簇 4 API（getAdminRoleVo / getRoleStateListForUpdate / updateRoleStateList / updateAdminRoleInfo） | A | role 簇 | A |
| 11 | roleId 来源 | 4 处 $stateParams.roleId | A | L1473, L1542, L1597, L1666 | A |
| 12 | roleId 消费者 | **直接赋值给 adminRoleId**（A 级字符级） | A | L1473, L1542, L1597, L1666 | A |
| 13 | employee 来源 | /admin/selectEmployeeVoList.json + selectEmployeeVoListOfMyCompany.json + getEmployeeVo.json + 业务页派生 | A | L1292, L15355, L8564, L8209-L8251 | A |
| 14 | employeeId 来源 | $stateParams (2 处) / Response (item.employee.id / res.object.id / res.employee.id) / 转移 (employeeIdFrom/To) | A | L8209, L8251, L34796, L49983, L1300 | A |
| 15 | employeeId 消费者 | 业务页 + role 簇 + 员工模块自身 | A | 多处 | A |
| 16 | department 来源 | /admin/getDepartmentList.json + getDepartmentInfo.json | A | L15330, L15700 | A |
| 17 | departmentId 消费者 | 仅 department 簇 4 Controller | A | L15691, L15692, L14860, L15711, L15726 | A |
| 18 | corpAdminHome 来源 | /admin/getRoleStateListForUpdate.json Response | A | L504, L823, L1483, L1607 | A |
| 19 | adminStateList 来源 | 同上 | A | 同上 | A |
| 20 | roleHaveState 来源 | API Response 字段 | A | L151, L159, L166 等 24 处 | A |
| 21 | permissionId 是否存在 | **不存在**（A 级 0 命中） | A | controller.js 0 命中 | F |
| 22 | userId 是否存在 | **不存在**（A 级 0 命中） | A | controller.js 0 命中 | F |
| 23 | 跨 Controller 桥 | grantAuth 唯一（73 处） / moveCustomer Function 共享（F 边界） | A | grantAuth 73 处 + moveCustomer L1319/L1727 | A |
| 24 | 跨一级菜单桥 | grantAuth 跨业务 / systemSetting 0 桥 | A | 多处 | A |
| 25 | 最终身份 DAG | 7 个 DAG (A-G) | A | 本文档第 12 节 | A |
| 26 | 复刻红线 | 第 15 节 | A | controller.js | A |

---

## 14. A/B/C/D/E/F 分布

- **A（直接源码）**：21
- **B（多源互证）**：1（corpId ≡ companyId 业务高度相关但未直接互证）
- **C（局部）**：0
- **D（冲突）**：3
  - L29297: corpInfo 作为 API 响应参数名 vs LocalStorage 的 corpInfo Object
  - permissionToReturn 是 coupon 字段（L12538, L12908）vs 误以为是 permission
  - roleAdminCtrl 命名"角色管理" vs 实际有 moveCustomer 员工迁移
- **E（推断）**：**0**（红线要求）
- **F（证据范围不可得）**：8
  - grantAuth 实现源
  - corpId ≡ companyId 直接互证
  - 后端 permission 表
  - corpInfo.companyTitle ≡ companyInfo.name
  - superType=10 含义
  - roleAdminCtrl 命名意图
  - moveCustomer 在 roleAdminCtrl 中的来源
  - 后端 FK / 表结构

---

## 15. 历史结论差异对照

### 15.1 S1-130 中 adminClinicCtrl 归属误判

| 项 | 历史 (S1-130) | 本轮 (S1-131) | 差异 | 当前采用 |
|---|---|---|---|---|
| adminClinicCtrl 归属 | 诊所管理一级菜单 | **hospital.updateHospital tab 5** | D 冲突 | A — 本轮发现 hospital 状态树完整结构（L15040, L15371, L15381, L15403, L15572, L15855-L15869, L15861-L15865） |
| 诊所管理一级菜单 | 1 个 adminClinicCtrl | **1 个 hospital 状态树（含 adminHospital / departmentList / employeeList / adminMap / adminClinic 5 tab）** | 状态树扩展 | A |

**【S1-131 重大修正】**：adminClinicCtrl 实际是 `hospital.updateHospital.adminClinic` 状态（hospital 树的第 5 tab），不是独立的"诊所管理"一级菜单。S1-130 中"诊所管理只有 1 个 adminClinicCtrl"是误判——实际是 hospital 一级菜单的子 tab。

### 15.2 S1-130 中 grantAuth 统计

| 项 | 历史 (S1-130) | 本轮 (S1-131) | 差异 | 当前采用 |
|---|---|---|---|---|
| grantAuth 7 方法总调用 | 73 | 73 | 一致 | A |
| 跨模块消费 | "系统设置 + 业务 0 桥" | **修正：systemSetting.* 0 消费，业务页 Controller 通过 grantAuth 间接桥** | 部分修正 | A |
| isUnlockFun 调用 | 1 (importMembers) | 5 (importMembers / healthScreenConfig / screenConditionBatch / screenInStore / addLens) | 数量修正 | A |

### 15.3 S1-128 / S1-129 employeeId 统计

| 项 | 历史 | 本轮精确分类 | 当前采用 |
|---|---|---|---|
| employeeId 出现 149+ | S1-128 | **149+ 拆解为**：2 State Param / 3 Response / 2 ObjectField / 5 转移 / 16 selectEmployeeVoList / 5 业务页 | A |

### 15.4 S1-126 / S1-127 corpId vs companyId

| 项 | 历史 (S1-126/127) | 本轮 | 差异 | 当前采用 |
|---|---|---|---|---|
| corpId 统计 | 未单独列出 | 31 处（独立 ID） | 新发现 | A |
| corpId ≡ companyId | 未提及 | **B 级业务相关，A 级未直接互证** | F 边界 | F |

### 15.5 schoolId 重大发现

| 项 | 历史 | 本轮 | 当前采用 |
|---|---|---|---|
| schoolId | 散见多处 | **200+ 处独立 ID 系统（筛查机构）** | A — schoolId ≠ companyId |

schoolId 主要在 `getSchoolInfo` / `selectInYearListBySchoolPlan` / `getSchoolClassList` 等筛查机构 API 中，与 companyId 是**完全独立的两套 ID 系统**。

### 15.6 roleAdminCtrl 命名

| 项 | 历史 (S1-130) | 本轮 (S1-131) | 差异 | 当前采用 |
|---|---|---|---|---|
| roleAdminCtrl 职责 | 角色管理 | **角色管理 + 员工会员迁移（$scope.moveCustomer / employeeIdTo）** | D 冲突 | F — 无法判断命名意图 |

### 15.7 corpAdminHome.state 含义

| 项 | 历史 (S1-130) | 本轮 (S1-131) | 差异 | 当前采用 |
|---|---|---|---|---|
| corpAdminHome.state | "state 路由名" | **强 B 级互证**：L37791 `$state.go(iten.children[0].corpAdminHome.state)` + L37795 `/admin/getChildrenLinkAndCode.json` | 强化 | A |

### 15.8 corpInfo 字段

| 项 | 历史 (S1-130) | 本轮 (S1-131) | 差异 | 当前采用 |
|---|---|---|---|---|
| corpInfo 字段 | companyTitle / employeeTitle / username | + 确认 0 命中 companyId / adminId / employeeId | 一致 | A |

### 15.9 grantAuth 跨业务模块桥

| 项 | 历史 (S1-130) | 本轮 (S1-131) | 差异 | 当前采用 |
|---|---|---|---|---|
| "诊所管理 + 系统设置 0 桥" | S1-130 主结论 | **修正**：adminClinicCtrl 0 桥（修正 S1-130） / grantAuth 才是真正跨业务桥 | 部分修正 | A |

---

## 16. 复刻红线

### 16.1 不能自行增加的字段

- `adminInfo` Object（0 命中）
- `userId`（0 命中）
- `permissionId`（0 命中）
- `orgId` / `organizationId`（0 命中）
- `currentUser` / `currentAdmin`（0 命中）
- corpInfo 中的 `companyId / id / adminId / employeeId` 字段（corpInfo 只有 3 个字段）

### 16.2 不能自行补桥的模块

- **companyId 与 corpId 不能直接等同**（A 级未互证）
- **corpInfo.companyTitle 与 companyInfo.name 不能等同**（A 级未互证）
- **corpId 与 corpInfo 不能等同**（不同对象）
- **systemSetting.* Controller 不能调用 grantAuth**（A 级 0 命中）
- **role/employee/department Controller 不能调用 grantAuth**（A 级 0 命中）
- **业务 Controller 不能直接消费 adminId / adminRoleId**（A 级 0 命中跨业务）
- **customerId 不能与 employeeId / adminId 互换**（A 级不同 ID）
- **employee ↔ department 0 直接关系**（A 级 0 命中）

### 16.3 不能假定存在的 API

- `getCompanyInfo` 不能假定从 companyId 取公司（`getHospitalInfo({ id: companyId })` 是 hospital 域 API，命名误导）
- 任何未在 controller.js 中出现的 API
- 任何 controller.js 中 0 命名的 API

### 16.4 不能等同的 ID

| ID | 不能等同 |
|---|---|
| adminId | ≠ employeeId / customerId / userId / companyId / patientId |
| employeeId | ≠ adminId / customerId / userId / companyId |
| customerId | ≠ employeeId / adminId / userId / companyId |
| companyId | ≠ clinicId / shopId / hospitalId / corpId（未直接互证）/ schoolId（筛查机构 ID） |
| corpId | ≠ companyId（未直接互证） |
| corpInfo.xxxId | corpInfo **不存在 ID 字段**（0 命中） |
| corpAdminHome.id | ≠ permissionId（permissionId 不存在） |
| roleHaveState | ≠ permissionId（boolean 字段） |
| superType=10 | 含义 F（仅 1 处 L862） |
| schoolId | ≠ companyId（200+ 处独立 ID 系统，属筛查机构） |
| adminRoleId | ≡ roleId（A 级字符级直接赋值） |
| $stateParams.roleId | ≡ adminRoleId（A 级字符级直接赋值） |

### 16.5 必须保留的 F 边界

- grantAuth 后端实现 F
- corpId ≡ companyId 直接互证 F
- corpInfo.companyTitle ≡ companyInfo.name F
- 后端 permission 表是否存在 F
- superType=10 含义 F
- roleAdminCtrl 命名意图 F
- moveCustomer 在 roleAdminCtrl 中的来源 F
- 后端 FK / 表结构 F
- $stateProvider.state() 注册 F
- URL 路由表 F
- HTML template 引用 F
- $rootScope 全局共享 F（除 appid）
- adminProjectService F

### 16.6 命名误导必须标注

- `roleAdminCtrl` 命名"角色管理" 但实际有 `moveCustomer` 员工迁移逻辑
- `adminClinicCtrl` 命名"管理诊所" 实际是 `hospital.updateHospital.adminClinic` 状态 tab 5
- `getHospitalInfo` API 实际接受 companyId（L50832 `getHospitalInfo({ id: $scope.obj.companyId })`）— **公司 ID 通过 hospital API 取**（D 命名误导）
- `corpInfo` 在 L29297 是 API 响应参数名（不是 LocalStorage 的 corpInfo Object）— **同名不同源**

---

## 17. 红线检查

| 项 | 实际 | 通过 |
|---|---|---|
| API actual | 0 | ✓ |
| Write actual | 0 | ✓ |
| Production mutation | 0 | ✓ |
| controller.js SHA256 | F40589FF0C56DE60BF4785A0B6C5B0A33519B75A30A15D988B0E1BDCF0A19433（**未变化**） | ✓ |
| deliveryList.html SHA256 | 5B79B6F0E423A90188EE420234A9449531E623528C9807800C5780BD61006476（**未变化**） | ✓ |
| machineOrderCompleted.html SHA256 | F1602AB17D56C31C95B3E869397B00B34366ABF29C06D03A6BD28ADD230BDB24（**未变化**） | ✓ |
| machineOrderList.html SHA256 | A429507C94B67725C7ACFDCB873467754EF1EDEC8300DCC6AC3914BD5AB9B26A（**未变化**） | ✓ |
| 历史 MD 165-192 | **未修改** | ✓ |
| P0 = 54 | 冻结 | ✓ |
| P1 = 8 | 冻结 | ✓ |
| 10 untracked 保留 | 全部保留 | ✓ |
| 视光之家url.txt 继续 ignored | 保留 | ✓ |
| ignored = 1 | 确认 | ✓ |
| 本轮只新增 193_*.md | 是 | ✓ |
| 临时脚本（2 个）在 C:\Users\18671\AppData\Local\Temp\ | 不进入 Git | ✓ |

---

## 18. Git

- 本轮 commit hash：（待执行 `git add -- 193_*.md` / `git commit -m "docs(193): S1-131 权限角色员工公司诊所身份体系深度审计"` / `git push origin master`）
- 本轮 LOCAL == REMOTE：待最终校验
- 本轮 tracked 预期：200 → 201
- 本轮 untracked 预期：10（保留 10 untracked，新增 193 后变 10 untracked 因为 193 进 tracked）
- 本轮 ignored 预期：1（视光之家url.txt 保留）
- 本轮 staged only：193_S1-131_*.md
