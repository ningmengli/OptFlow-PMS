# S1-165 门诊预约 + 接诊业务对象与状态机最终细化

> **本轮定位**:业务模型细化。
> - 不做 API 字段级设计
> - 不做编码
> - 不修改 225 / 226
> - 新增本文档作为业务模型**冻结基准**

---

## §0 完整性闸门

- **HEAD**:`f6d594c125c1f8b0506c787bce6d316161bb7781`(S1-163 commit 后)
- **REMOTE**:同 LOCAL ✓
- **0 号闸门**:4 文件 SHA256 校验待 commit 前复检
  - controller.js `F40589FF0C56DE60BF4785A0B6C5B0A33519B75A30A15D988B0E1BDCF0A19433`
  - deliveryList.html `5B79B6F0E423A90188EE420234A9449531E623528C9807800C5780BD61006476`
  - machineOrderCompleted.html `F1602AB17D56C31C95B3E869397B00B34366ABF29C06D03A6BD28ADD230BDB24`
  - machineOrderList.html `A429507C94B67725C7ACFDCB873467754EF1EDEC8300DCC6AC3914BD5AB9B26A`
- **tracked = 234 / untracked = 10 / ignored = 1 / staged = 0**(S1-163 末态)
- **历史 MD(190~226 共 37 份)未修改** ✓

---

## §1 对象总清单(冻结)

### 1.1 S1-165 正式对象总数

| 类别 | 数量 | 说明 |
|---|:-:|---|
| **A 复用实体**(S1-147 / S1-153 / S1-158 冻结)| **6** | Company / Customer / Patient / Employee / ConsultRoom / BigScreen |
| **B 新增实体**(OptFlow PMS 本轮设计)| **6** | Appointment / AppointmentSchedule / AppointmentSlot / TriageQueue / ReceptionQueue / Visit |
| **C 枚举内置** | **2** | AppointmentStatus(12) / ReceptionQueueStatus(6) |
| **D UI / 辅助对象**(不存数据库,只前端状态)| **0** | 无独立对象 |
| **E 明确不独立建模** | **2** | AppointmentPatient(并入 Customer+Patient) / QueueStatus 独立枚举(并入 ReceptionQueueStatus) |
| **冻结对象总数** | **14 个候选**(6 复用 + 6 实体 + 2 枚举)| 0 个独立 UI 辅助 / 0 个独立 AppointmentPatient |

**S1-165 关键校准**(对照 S1-162/S1-163 模糊口径):
- ❌ "11 个核心对象"(S1-162 §6.1 标题)→ 拆解为 8 业务 + 2 枚举 + 1 失效(AppointmentPatient) = 11(表述可接受但需注脚)
- ❌ "12 个"(S1-163 §2.1 误读)→ 不存在
- ❌ "14 个候选"(S1-163 §2.2 终态)→ 调整为 14 = 6 复用 + 6 实体 + 2 枚举(S1-165 采纳)
- ✓ **S1-165 正式口径**:**14 个候选对象 = 6 复用 + 6 新增实体 + 2 状态枚举**

### 1.2 A 复用实体(6 个)

| # | 对象 | 来源 | 复用方式 | 关键 ID |
|---:|---|---|---|---|
| A1 | **Company** | S1-158 冻结 | 作用域 / companyId | companyId |
| A2 | **Customer** | S1-147 冻结 | 预约主体来源 | customerId |
| A3 | **Patient** | S1-147 冻结 | 预约实际主体 | patientId |
| A4 | **Employee** | S1-158 冻结 | 接诊医生(doctorId = employeeId) | employeeId |
| A5 | **ConsultRoom** | S1-158 冻结 | 诊室 | consultationRoomId = consultRoomId |
| A6 | **BigScreen** | S1-153 冻结 | 叫号大屏(显示数据,不是队列) | bigScreenId |

**禁止**:
- 不重新建 Customer / Patient / Employee
- 不复用具名 ID 替换:不把 `appointId`(原 18 命中)直接当 Appointment.id
- Employee 三条关系(→ Department / Role / ConsultRoom)仍禁造,只保留 employeeId 单向引用

### 1.3 B 新增实体(6 个)

| # | 对象 | 业务 | 状态数 |
|---:|---|---|:-:|
| B1 | **Appointment** | 单次预约记录 | 12 |
| B2 | **AppointmentSchedule** | 医生+日期的排班(上层) | 1 字段(非状态机)|
| B3 | **AppointmentSlot** | Schedule 子项(具体时间片) | 1 字段(非状态机)|
| B4 | **TriageQueue** | 全局分诊池条目 | 3 |
| B5 | **ReceptionQueue** | 诊室接诊队列条目 | 6 |
| B6 | **Visit** | 实际到院就诊记录 | 5 |

### 1.4 C 枚举(2 个)

| # | 枚举 | 取值数 | 取值 |
|---:|---|:-:|---|
| C1 | **AppointmentStatus** | 12 | DRAFT / CONFIRMED / WAITING_ARRIVAL / ARRIVED / TRIAGE_WAITING / WAITING_RECEPTION / CALLED / IN_CONSULTATION / COMPLETED / CANCELLED / NO_SHOW / RESCHEDULED |
| C2 | **ReceptionQueueStatus** | 6 | ASSIGNED / WAITING / CALLED / SKIPPED / IN_CONSULTATION / DONE |

**S1-165 校准**:S1-162 §9 把 IN_TRIAGE_POOL 写为 ReceptionQueue 第 1 状态。本轮**明确拆分**:
- "在分诊池"语义由 TriageQueue 表达(IN_POOL)
- ReceptionQueue 起点 = ASSIGNED(已分诊到诊室)
- C2 枚举从 S1-162 的 7 状态 → S1-165 正式 6 状态

### 1.5 D UI / 辅助对象

**0 个**。OptFlow PMS 不为前端交互独立建表:
- 当前患者、当前队列等都是 ReceptionQueue 的查询视图
- 不存"当前叫号编号"独立表(由 ReceptionQueue.sequenceInRoom + status 派生)

### 1.6 E 明确不独立建模(2 个)

| # | 误读 | 正确做法 |
|---:|---|---|
| E1 | **AppointmentPatient**(S1-162 §6.1 #2 列出) | **不单独建**,复用 Customer + Patient(S1-147 冻结)|
| E2 | **QueueStatus**(S1-162 §6.1 #11 列出) | **不单独建**,并入 C2 ReceptionQueueStatus(6 状态) |

---

## §2 对象职责与关系

### 2.1 对象职责

| 对象 | 单一职责 | 创建时机 | 终态时机 |
|---|---|---|---|
| **Appointment** | 表达"一次预约" | appointment/create | COMPLETED / CANCELLED / NO_SHOW / RESCHEDULED |
| **AppointmentSchedule** | 表达"某医生某天的整体排班" | schedule/create | schedule/cancel(整体)|
| **AppointmentSlot** | 表达"某医生某天某时段的可预约单元" | schedule/create(随 Schedule)| slot/close |
| **TriageQueue** | 表达"全院分诊池中的一条待分诊记录" | arrival/checkin 同步创建 | 移出分诊池(REMOVED)/ ASSIGNED 转出 |
| **ReceptionQueue** | 表达"某诊室内的接诊队列条目" | triage/assign 同步创建 | DONE |
| **Visit** | 表达"一次到院就诊" | arrival/checkin 同步创建 | COMPLETED |

### 2.2 对象核心关系矩阵

| 关系 | 关系类型 | 说明 |
|---|---|---|
| Appointment → Patient | 多对一 | appointment.patientId |
| Appointment → Employee | 多对一(接诊医生)| appointment.employeeId |
| Appointment → AppointmentSchedule | 多对一 | appointment.scheduleId |
| Appointment → AppointmentSlot | 多对一 | appointment.slotId |
| Appointment → Appointment | 自引用(改约)| rescheduledFromId |
| Visit → Appointment | 多对一(可空,WalkIn)| visit.appointmentId |
| Visit → Patient | 多对一 | visit.patientId |
| Visit → Employee | 多对一(接诊医生)| visit.doctorId |
| Visit → ConsultRoom | 多对一(接诊诊室)| visit.consultationRoomId |
| TriageQueue → Visit | 一对一 | triage.visitId |
| TriageQueue → Appointment | 一对一 | triage.appointmentId |
| TriageQueue → ConsultRoom | 多对一(目标)| triage.consultationRoomId |
| TriageQueue → Employee | 多对一(目标医生)| triage.doctorId |
| ReceptionQueue → Visit | 一对一 | reception.visitId |
| ReceptionQueue → ConsultRoom | 多对一(必填)| reception.consultationRoomId |
| ReceptionQueue → Employee | 多对一(必填)| reception.doctorId |

**全部 14 条关系 = 14 个 A 级关系**,与 S1-160 冻结的 82 A Relation 兼容(本域新增部分)。

### 2.3 生命周期时序

```
1. schedule/create → AppointmentSchedule
2. schedule/create (生成子项) → AppointmentSlot × N
3. appointment/create → Appointment (DRAFT)
4. appointment/confirm → Appointment (CONFIRMED)
5. [系统定时 0:00] Appointment → WAITING_ARRIVAL
6. arrival/checkin → Appointment (ARRIVED) + Visit (CHECKED_IN) + TriageQueue (IN_POOL)
7. Appointment → TRIAGE_WAITING (同步入池)
8. triage/assign → TriageQueue (ASSIGNED) + ReceptionQueue (ASSIGNED)
9. Appointment → WAITING_RECEPTION
10. queue/start → ReceptionQueue 状态保持 ASSIGNED(医生进入诊室)
11. queue/next → ReceptionQueue (CALLED) + Appointment (CALLED)
12. queue/start-consult → ReceptionQueue (IN_CONSULTATION) + Appointment (IN_CONSULTATION) + Visit (IN_CONSULTATION)
13. queue/complete → ReceptionQueue (DONE) + Appointment (COMPLETED) + Visit (COMPLETED)
```

---

## §3 Appointment 状态机(12 状态)

### 3.1 状态总览

| # | 状态码 | 英文 | 显示名 | 业务语义 | 终态? |
|---:|---|---|---|---|:-:|
| 1 | 010 | DRAFT | 草稿 | 预约刚创建,未确认 | ✗ |
| 2 | 020 | CONFIRMED | 已预约 | 预约员/系统已确认 | ✗ |
| 3 | 030 | WAITING_ARRIVAL | 待到院 | 预约日 0:00 系统自动进入 | ✗ |
| 4 | 040 | ARRIVED | 已到院 | 患者签到成功 | ✗ |
| 5 | 050 | TRIAGE_WAITING | 待分诊 | 进入分诊池等待分诊 | ✗ |
| 6 | 060 | WAITING_RECEPTION | 待叫号 | 分诊到诊室,等待叫号 | ✗ |
| 7 | 070 | CALLED | 已叫号 | 医生已叫该患者 | ✗ |
| 8 | 080 | IN_CONSULTATION | 接诊中 | 医生开始接诊 | ✗ |
| 9 | 090 | COMPLETED | 已完成 | 接诊完成 | ✓ 终态 |
| 10 | 100 | CANCELLED | 已取消 | 任意非终态可取消 | ✓ 终态 |
| 11 | 110 | NO_SHOW | 爽约 | 预约日超时未到 | ✓ 终态 |
| 12 | 120 | RESCHEDULED | 已改约 | 已生成新预约,旧记录终结 | ✓ 终态 |

**状态码规则**:3 位数字,每 10 进位预留扩展。

### 3.2 状态详细定义

#### 3.2.1 DRAFT(草稿)

| 项目 | 描述 |
|---|---|
| **进入条件** | appointment/create 调用成功,患者 + Slot 校验通过 |
| **触发动作** | appointment/create(POST) |
| **操作角色** | 预约员 / 管理员 |
| **进入页面** | D. 新增预约(创建后跳转 E. 预约详情)|
| **退出条件** | appointment/confirm → CONFIRMED / appointment/cancel → CANCELLED / appointment/reschedule → RESCHEDULED |
| **合法下一状态** | CONFIRMED / CANCELLED / RESCHEDULED |
| **不可逆规则** | 无(草稿可任意修改)|
| **后置动作** | (1) AppointmentSlot.bookedCount +1 (2) Appointment.createdBy 写入当前用户 (3) 写 AuditLog |
| **可修改字段** | 全部(患者 / 医生 / 时间 / 备注)|

#### 3.2.2 CONFIRMED(已预约)

| 项目 | 描述 |
|---|---|
| **进入条件** | DRAFT → appointment/confirm 成功 |
| **触发动作** | appointment/confirm(POST) |
| **操作角色** | 预约员 / 管理员 / 系统(API 触发)|
| **退出条件** | 系统定时任务(schedule 0:00)→ WAITING_ARRIVAL / 取消 / 改约 |
| **合法下一状态** | WAITING_ARRIVAL / CANCELLED / RESCHEDULED |
| **不可逆规则** | 确认后 patientId / slotId 不可直接改,只能改约 |
| **后置动作** | (1) Appointment.confirmedAt = now (2) 写 AuditLog |
| **可修改字段** | remark / birthDate / age / gender(限制字段)|

#### 3.2.3 WAITING_ARRIVAL(待到院)

| 项目 | 描述 |
|---|---|
| **进入条件** | 预约日 0:00 系统定时任务扫描 appointment.slotDate == today 且 status == CONFIRMED |
| **触发动作** | 系统任务(无需 API)|
| **操作角色** | 系统自动 |
| **退出条件** | arrival/checkin → ARRIVED / 系统超时(默认预约日 23:59)→ NO_SHOW / 取消 / 改约 |
| **合法下一状态** | ARRIVED / NO_SHOW / CANCELLED / RESCHEDULED |
| **不可逆规则** | 一旦进入不可回退到 CONFIRMED;若改约需新建 Appointment |
| **后置动作** | (1) 推送提醒(短信/微信,待确认) (2) 写状态变更日志 |
| **可修改字段** | 无 |

#### 3.2.4 ARRIVED(已到院)

| 项目 | 描述 |
|---|---|
| **进入条件** | arrival/checkin 成功,签到记录匹配一条 WAITING_ARRIVAL 的 Appointment |
| **触发动作** | arrival/checkin(POST) |
| **操作角色** | 预约员 / 前台 / 自助机(实际=任意登录用户)|
| **退出条件** | 入分诊池(自动)→ TRIAGE_WAITING / 撤销签到 arrival/cancel → WAITING_ARRIVAL(回退)【待确认】 |
| **合法下一状态** | TRIAGE_WAITING / WAITING_ARRIVAL(回退)/ CANCELLED |
| **不可逆规则** | 撤销签到后患者必须重新签到(可保留,【待确认】)|
| **后置动作** | (1) 同步创建 Visit(CHECKED_IN) (2) 同步创建 TriageQueue(IN_POOL) (3) 写 AuditLog |
| **可修改字段** | checkinAt(系统时间,只读)|

**【S1-165 决定】**:ARRIVED → WAITING_ARRIVAL 撤销签到路径**默认不开启**。签到错误需联系管理员手动调整,避免业务混乱。【待确认】。

#### 3.2.5 TRIAGE_WAITING(待分诊)

| 项目 | 描述 |
|---|---|
| **进入条件** | Visit 创建后,自动入分诊池,Appointment 同步进入此状态 |
| **触发动作** | 系统自动(由 arrival/checkin 触发) |
| **操作角色** | 系统 |
| **退出条件** | triage/assign → WAITING_RECEPTION / 取消 |
| **合法下一状态** | WAITING_RECEPTION / CANCELLED |
| **不可逆规则** | 进入后 patientId / appointmentId 不可改 |
| **后置动作** | TriageQueue 已在 IN_POOL 等待分诊员处理 |
| **可修改字段** | 无 |

#### 3.2.6 WAITING_RECEPTION(待叫号)

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/assign 成功,TriageQueue → ASSIGNED,Appointment 同步 |
| **触发动作** | triage/assign(POST) |
| **操作角色** | 分诊员 / 管理员(系统自动分诊可由系统触发)|
| **退出条件** | queue/call → CALLED / 重新分诊 triage/reassign → TRIAGE_WAITING(回退) |
| **合法下一状态** | CALLED / TRIAGE_WAITING(回退) |
| **不可逆规则** | doctorId / consultationRoomId 已确定,需重新分诊才能换 |
| **后置动作** | ReceptionQueue 已创建并 ASSIGNED(可被医生 queue/start 进入诊室)|
| **可修改字段** | 无(改诊室走 triage/reassign)|

#### 3.2.7 CALLED(已叫号)

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/call 或 queue/next |
| **触发动作** | queue/call / queue/next(POST) |
| **操作角色** | 医生 |
| **退出条件** | queue/start-consult → IN_CONSULTATION / 超时(默认 5 分钟)→ WAITING_RECEPTION(回退,自动)/ queue/skip → WAITING_RECEPTION(放回队列) |
| **合法下一状态** | IN_CONSULTATION / WAITING_RECEPTION(超时或过号)|
| **不可逆规则** | 一旦开始接诊,不可回退到 WAITING_RECEPTION |
| **后置动作** | (1) ReceptionQueue.CALLED (2) 写 AuditLog (3) 触发大屏显示(bigscreen/display) |
| **可修改字段** | 无 |

#### 3.2.8 IN_CONSULTATION(接诊中)

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/start-consult |
| **触发动作** | queue/start-consult(POST) |
| **操作角色** | 医生 |
| **退出条件** | queue/complete → COMPLETED / 异常:医生临时停诊 → 强制 COMPLETED(异常场景 6) |
| **合法下一状态** | COMPLETED |
| **不可逆规则** | 不可回退到 CALLED / WAITING_RECEPTION(必须完成才能继续) |
| **后置动作** | (1) 同步 Visit → IN_CONSULTATION (2) 写 AuditLog |
| **可修改字段** | consultationStartedAt(只读) |

#### 3.2.9 COMPLETED(已完成) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/complete |
| **触发动作** | queue/complete(POST) |
| **操作角色** | 医生 |
| **退出条件** | 无(终态) |
| **合法下一状态** | 无 |
| **不可逆规则** | **永久不可逆**。已完成预约不可回退、不可修改、不可取消 |
| **后置动作** | (1) Appointment.completedAt = now (2) 同步 Visit → COMPLETED (3) 同步 ReceptionQueue → DONE (4) 写 AuditLog (5) 触发统计 |
| **可修改字段** | 无(全部只读)|

#### 3.2.10 CANCELLED(已取消) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | 任意非终态执行 appointment/cancel |
| **触发动作** | appointment/cancel(POST) |
| **操作角色** | 预约员 / 管理员 / 患者(自助取消,待确认) |
| **退出条件** | 无(终态) |
| **合法下一状态** | 无 |
| **不可逆规则** | **永久不可逆**。取消后必须重新预约,不能直接"恢复" |
| **后置动作** | (1) Appointment.cancelledAt + cancelReason 写入 (2) 释放 AppointmentSlot.bookedCount -1 (3) 同步 Visit → CANCELLED(若已签到)(4) 写 AuditLog |
| **可修改字段** | cancelReason(可后续补全)|

#### 3.2.11 NO_SHOW(爽约) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | 系统定时任务扫描 slotDate < today 且 status == WAITING_ARRIVAL |
| **触发动作** | 系统任务(无需 API,默认每日 0:00 扫描)|
| **操作角色** | 系统自动 |
| **退出条件** | 无(终态) |
| **合法下一状态** | 无 |
| **不可逆规则** | **永久不可逆**。爽约记录保留,不可撤销 |
| **后置动作** | (1) 释放 AppointmentSlot.bookedCount -1 (2) 写 AuditLog(标"system_no_show")(3) 触发统计(爽约率)|
| **可修改字段** | 无 |

#### 3.2.12 RESCHEDULED(已改约) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | appointment/reschedule 成功,新 Appointment 已 CONFIRMED |
| **触发动作** | appointment/reschedule(POST) |
| **操作角色** | 预约员 / 管理员 |
| **退出条件** | 无(终态,但保留全部历史)|
| **合法下一状态** | 无 |
| **不可逆规则** | **永久不可逆**(原 Appointment 层面)。新 Appointment 是独立记录 |
| **后置动作** | (1) Appointment.rescheduledAt = now (2) 释放原 AppointmentSlot.bookedCount -1 (3) 新 Appointment.rescheduledFromId = 原 id (4) 写 AuditLog |
| **可修改字段** | 无 |

### 3.3 Appointment 合法迁移总表

| From | To | 触发 | 角色 | API |
|---|---|---|---|---|
| (none)| DRAFT | 新建 | 预约员 | appointment/create |
| DRAFT | CONFIRMED | 确认 | 预约员 | appointment/confirm |
| CONFIRMED | WAITING_ARRIVAL | 系统定时 0:00 | 系统 | – |
| WAITING_ARRIVAL | ARRIVED | 签到 | 前台/预约员 | arrival/checkin |
| ARRIVED | TRIAGE_WAITING | 入池 | 系统 | (随 arrival/checkin 触发)|
| TRIAGE_WAITING | WAITING_RECEPTION | 分诊 | 分诊员 | triage/assign |
| WAITING_RECEPTION | CALLED | 叫号 | 医生 | queue/call / queue/next |
| CALLED | IN_CONSULTATION | 开始接诊 | 医生 | queue/start-consult |
| IN_CONSULTATION | COMPLETED | 完成接诊 | 医生 | queue/complete |
| WAITING_RECEPTION | TRIAGE_WAITING | 重新分诊 | 分诊员 | triage/reassign |
| CALLED | WAITING_RECEPTION | 超时/过号 | 系统/医生 | queue/skip / 系统任务 |
| 任意非终态 | CANCELLED | 取消 | 预约员/管理员 | appointment/cancel |
| CONFIRMED / WAITING_ARRIVAL | RESCHEDULED | 改约 | 预约员 | appointment/reschedule |
| WAITING_ARRIVAL | NO_SHOW | 预约日 23:59 仍未签到 | 系统 | – |

### 3.4 Appointment 非法迁移(禁止列表)

| 非法迁移 | 禁止原因 |
|---|---|
| COMPLETED → * | 终态,永久不可逆 |
| CANCELLED → * | 终态,需重新预约 |
| NO_SHOW → * | 终态,爽约记录必须保留 |
| RESCHEDULED → * | 终态,改约走新 Appointment |
| DRAFT → WAITING_ARRIVAL | 跳过确认,可能数据未填完 |
| DRAFT → ARRIVED | 必须先确认 |
| CONFIRMED → ARRIVED | 跳过待到院,签到必须先有 WAITING_ARRIVAL |
| CONFIRMED → WAITING_RECEPTION | 跳过签到和分诊 |
| WAITING_ARRIVAL → TRIAGE_WAITING | 必须先签到(ARRIVED)|
| ARRIVED → WAITING_RECEPTION | 必须先分诊 |
| IN_CONSULTATION → CANCELLED | 接诊中不允许取消,必须先完成 |
| CALLED → CANCELLED | 叫号后不允许直接取消,需先 SKIP 再取消 |
| WAITING_RECEPTION → CANCELLED | 【待确认】分诊后取消是否允许(默认禁止,需先 reassign 再 cancel)|

---

## §4 Visit 状态机(5 状态)

### 4.1 状态总览

| # | 状态码 | 英文 | 显示名 | 业务 | 终态? |
|---:|---|---|---|---|:-:|
| 1 | 010 | DRAFT | 草稿 | Visit 刚创建,初始态 | ✗ |
| 2 | 020 | CHECKED_IN | 已签到 | 与 Appointment.ARRIVED 同步 | ✗ |
| 3 | 030 | TRIAGED | 已分诊 | 与 Appointment.WAITING_RECEPTION 同步 | ✗ |
| 4 | 040 | IN_CONSULTATION | 接诊中 | 与 Appointment.IN_CONSULTATION 同步 | ✗ |
| 5 | 050 | COMPLETED | 已完成 | 与 Appointment.COMPLETED 同步 | ✓ 终态 |
| 6 | 060 | CANCELLED | 已取消 | 与 Appointment.CANCELLED 同步(若已签到) | ✓ 终态 |

**S1-165 校准**:S1-162 §8.1 列 5 状态不含 CANCELLED。S1-165 **增加 CANCELLED** 作为第 6 状态(因为签到后可被取消)。

### 4.2 状态详细定义

#### 4.2.1 DRAFT(草稿)

| 项目 | 描述 |
|---|---|
| **进入条件** | arrival/checkin 触发,Visit 记录创建 |
| **操作角色** | 系统 |
| **退出条件** | 入分诊池(立即)→ CHECKED_IN |
| **不可逆规则** | DRAFT 是瞬时态,通常创建后立即 CHECKED_IN |

#### 4.2.2 CHECKED_IN(已签到)

| 项目 | 描述 |
|---|---|
| **进入条件** | Visit 创建后同步进入分诊池(无 DRAFT 停留)|
| **操作角色** | 系统 |
| **退出条件** | triage/assign → TRIAGED / 取消(若 Appointment 被 cancel) |
| **不可逆规则** | 一旦 TRIAGED,不可回退到 CHECKED_IN |

#### 4.2.3 TRIAGED(已分诊)

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/assign 成功 |
| **操作角色** | 分诊员 |
| **退出条件** | queue/start-consult → IN_CONSULTATION / 重新分诊 → CHECKED_IN(回退)|
| **不可逆规则** | 进入 IN_CONSULTATION 后不可回退 |

#### 4.2.4 IN_CONSULTATION(接诊中)

| 项目 | 描述 |
|---|---|
| **进入条件** | queue/start-consult |
| **操作角色** | 医生 |
| **退出条件** | queue/complete → COMPLETED |
| **不可逆规则** | 一旦接诊,必须完成才能继续 |

#### 4.2.5 COMPLETED(已完成) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | queue/complete |
| **操作角色** | 医生 |
| **不可逆规则** | 永久不可逆 |
| **后置动作** | (1) visit.completedAt = now (2) 触发 MedicalRecord 创建(若适用)|

#### 4.2.6 CANCELLED(已取消) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | 关联 Appointment 进入 CANCELLED(且 visit 已在 CHECKED_IN / TRIAGED / IN_CONSULTATION 之一)|
| **操作角色** | 预约员 / 管理员 / 系统 |
| **不可逆规则** | 永久不可逆 |
| **后置动作** | 同步 visit.cancelledAt |

### 4.3 Visit 合法迁移总表

| From | To | 触发 | 角色 | API |
|---|---|---|---|---|
| (none)| DRAFT | 签到触发创建 | 系统 | (随 arrival/checkin) |
| DRAFT | CHECKED_IN | 入分诊池 | 系统 | – |
| CHECKED_IN | TRIAGED | 分诊 | 分诊员 | triage/assign |
| TRIAGED | IN_CONSULTATION | 开始接诊 | 医生 | queue/start-consult |
| IN_CONSULTATION | COMPLETED | 完成接诊 | 医生 | queue/complete |
| CHECKED_IN / TRIAGED / IN_CONSULTATION | CANCELLED | 关联 Appointment 取消 | 预约员 | (随 appointment/cancel) |
| TRIAGED | CHECKED_IN | 重新分诊 | 分诊员 | triage/reassign |

### 4.4 Visit 非法迁移

| 非法迁移 | 禁止原因 |
|---|---|
| COMPLETED → * | 终态 |
| CANCELLED → * | 终态 |
| DRAFT → TRIAGED | 跳过入池 |
| CHECKED_IN → IN_CONSULTATION | 跳过分诊 |
| DRAFT → COMPLETED | 不可能,瞬时态 |
| IN_CONSULTATION → TRIAGED | 不可回退 |

---

## §5 TriageQueue 状态机(3 状态)

### 5.1 状态总览

| # | 状态码 | 英文 | 显示名 | 业务 | 终态? |
|---:|---|---|---|---|:-:|
| 1 | 010 | IN_POOL | 在分诊池 | 等待分诊员处理 | ✗ |
| 2 | 020 | ASSIGNED | 已分配 | 分诊完成,已同步到 ReceptionQueue | ✗(转出)|
| 3 | 030 | REMOVED | 已移出 | 异常退回,退出分诊池 | ✓ 终态 |

**S1-165 决定**:TriageQueue 仅有 3 状态,设计为"分诊池条目"独立对象,不复用 ReceptionQueue。

### 5.2 状态详细定义

#### 5.2.1 IN_POOL(在分诊池)

| 项目 | 描述 |
|---|---|
| **进入条件** | arrival/checkin 同步创建 TriageQueue 行,默认 IN_POOL |
| **操作角色** | 系统 |
| **退出条件** | triage/auto-assign / triage/assign → ASSIGNED / triage/remove → REMOVED |
| **合法下一状态** | ASSIGNED / REMOVED |
| **不可逆规则** | 移出后不可回退(需重新签到)|

#### 5.2.2 ASSIGNED(已分配)

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/assign 成功,目标诊室 + 医生确定 |
| **操作角色** | 分诊员 / 系统(自动分诊)|
| **退出条件** | (1) 同步创建 ReceptionQueue(ASSIGNED),TriageQueue 完成使命 (2) 重新分诊 → IN_POOL(回退)|
| **不可逆规则** | 重新分诊可回退到 IN_POOL |
| **后置动作** | ReceptionQueue 同步创建 |

**S1-165 设计关键点**:TriageQueue.ASSIGNED 是"已完成分诊"语义,实际数据不立即消失,而是保留 7 天用于追溯(【待确认】保留时长)。ASSIGNED 不等于"待医生接诊",**待医生接诊 = ReceptionQueue.ASSIGNED**。

#### 5.2.3 REMOVED(已移出) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/remove(异常退回) |
| **操作角色** | 分诊员 / 管理员 |
| **不可逆规则** | 永久不可逆。需重新签到才能入池 |
| **后置动作** | (1) 写 AuditLog(标 removeReason) (2) 同步取消 Appointment?【待确认】 |

### 5.3 TriageQueue 合法迁移

| From | To | 触发 | 角色 |
|---|---|---|---|
| (none)| IN_POOL | 签到触发创建 | 系统 |
| IN_POOL | ASSIGNED | 分诊 | 分诊员 |
| IN_POOL | REMOVED | 异常移出 | 分诊员 |
| ASSIGNED | IN_POOL | 重新分诊 | 分诊员 |

### 5.4 TriageQueue 非法迁移

| 非法迁移 | 禁止原因 |
|---|---|
| REMOVED → * | 终态 |
| ASSIGNED → REMOVED | 已分诊不应异常移出,应走 reassign |
| IN_POOL → IN_POOL | 自循环,无意义 |

---

## §6 ReceptionQueue 状态机(6 状态)

### 6.1 状态总览

| # | 状态码 | 英文 | 显示名 | 业务 | 终态? |
|---:|---|---|---|---|:-:|
| 1 | 010 | ASSIGNED | 已分配 | 已分诊到诊室,医生未进入 | ✗ |
| 2 | 020 | WAITING | 等待中 | 医生已 queue/start,患者等待叫号 | ✗ |
| 3 | 030 | CALLED | 已叫号 | 医生已叫该患者 | ✗ |
| 4 | 040 | SKIPPED | 已过号 | 患者未响应,放回队列尾部 | ✗ |
| 5 | 050 | IN_CONSULTATION | 接诊中 | 医生开始接诊 | ✗ |
| 6 | 060 | DONE | 已完成 | 医生完成接诊 | ✓ 终态 |

**S1-165 校准**:S1-162 §9 写 7 状态含 IN_TRIAGE_POOL。S1-165 **正式去除** IN_TRIAGE_POOL,因为"在分诊池"语义已由 TriageQueue 表达,ReceptionQueue 起点为 ASSIGNED。

### 6.2 状态详细定义

#### 6.2.1 ASSIGNED(已分配)

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/assign 同步创建 ReceptionQueue,默认 ASSIGNED |
| **操作角色** | 分诊员(由 triage 触发) |
| **退出条件** | 医生 queue/start → WAITING / 重新分诊 → TriageQueue(回退,ReceptionQueue 删除) |
| **合法下一状态** | WAITING |
| **不可逆规则** | 诊室 + 医生已确定,改需 reassign |
| **后置动作** | 大屏可显示该诊室 ASSIGNED 数(可选项)|

#### 6.2.2 WAITING(等待中)

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/start(进入诊室)|
| **操作角色** | 医生 |
| **退出条件** | queue/call / queue/next → CALLED / 取消(关联 Appointment 取消) |
| **合法下一状态** | CALLED |
| **不可逆规则** | 叫号后不可回退 WAITING(只能 SKIPPED)|

#### 6.2.3 CALLED(已叫号)

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/call 或 queue/next |
| **操作角色** | 医生 |
| **退出条件** | queue/start-consult → IN_CONSULTATION / queue/skip → SKIPPED / 超时 → WAITING(回退,自动)|
| **合法下一状态** | IN_CONSULTATION / SKIPPED / WAITING(超时回退) |
| **不可逆规则** | 一旦 IN_CONSULTATION,不可回退 |
| **后置动作** | (1) 触发大屏显示 (2) 写 AuditLog |

#### 6.2.4 SKIPPED(已过号)

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/skip,患者未在规定时间内响应 |
| **操作角色** | 医生 / 系统(超时自动)|
| **退出条件** | queue/recall → CALLED / 取消 |
| **合法下一状态** | CALLED |
| **不可逆规则** | 一旦 recall → CALLED,可再次 SKIP |
| **后置动作** | ReceptionQueue.skippedTimes +1 |

#### 6.2.5 IN_CONSULTATION(接诊中)

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/start-consult |
| **操作角色** | 医生 |
| **退出条件** | queue/complete → DONE |
| **合法下一状态** | DONE |
| **不可逆规则** | 不可回退到 CALLED / SKIPPED / WAITING |
| **后置动作** | 同步 Appointment / Visit → IN_CONSULTATION |

#### 6.2.6 DONE(已完成) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | 医生执行 queue/complete |
| **操作角色** | 医生 |
| **不可逆规则** | 永久不可逆 |
| **后置动作** | (1) 同步 Appointment / Visit → COMPLETED (2) 释放诊室(后续患者可入)|

### 6.3 ReceptionQueue 合法迁移总表

| From | To | 触发 | 角色 | API |
|---|---|---|---|---|
| (none)| ASSIGNED | 分诊触发创建 | 分诊员 | (随 triage/assign) |
| ASSIGNED | WAITING | 医生进入诊室 | 医生 | queue/start |
| WAITING | CALLED | 叫号 | 医生 | queue/call / queue/next |
| CALLED | IN_CONSULTATION | 开始接诊 | 医生 | queue/start-consult |
| CALLED | SKIPPED | 过号 | 医生 / 系统(超时) | queue/skip |
| CALLED | WAITING | 超时自动回退(5 分钟)| 系统 | – |
| SKIPPED | CALLED | 重新叫号 | 医生 | queue/recall |
| IN_CONSULTATION | DONE | 完成接诊 | 医生 | queue/complete |
| WAITING / CALLED / SKIPPED | (删除)| 关联 Appointment 取消 | 预约员 | (随 appointment/cancel) |

### 6.4 ReceptionQueue 非法迁移

| 非法迁移 | 禁止原因 |
|---|---|
| DONE → * | 终态 |
| ASSIGNED → CALLED | 跳 WAITING(医生必须先 start) |
| WAITING → IN_CONSULTATION | 必须先 CALLED |
| SKIPPED → IN_CONSULTATION | 必须先 RECALL → CALLED → start-consult |
| IN_CONSULTATION → WAITING | 接诊中不可回退 |
| IN_CONSULTATION → SKIPPED | 接诊中不存在过号 |

### 6.5 "叫号"与"开始接诊"分离决策 ★ 关键

**S1-165 决定**:**采用 B 方案,叫号和开始接诊分离**。

| 方案 | 流程 | 优点 | 缺点 | S1-165 采纳 |
|---|---|---|---|:-:|
| **A** | WAITING → CALLED → IN_CONSULTATION(一气呵成)| 操作少 | 语义混淆,无法处理"叫号后患者未到" | ✗ |
| **B** | WAITING → CALLED → IN_CONSULTATION(显式两步)| 语义清晰,可处理过号 / 重新叫号 | 操作多 1 步 | ✓ **采纳** |

**B 方案理由**:
1. 原系统"下一位 = 自动分诊 + 叫号"造成状态语义不清(S1-162 §4.1 已识别)
2. 实际业务有"叫号后患者不在 / 未响应"场景,需独立 SKIPPED 状态
3. 医生可能想"叫号但先处理完当前患者再接诊下一位",B 方案支持
4. **明确动作分离**:queue/call(叫号)和 queue/start-consult(开始接诊)是两个独立 API

**S1-165 强制约束**:
- 不允许 CALLED 直接 → IN_CONSULTATION 跳过"开始接诊"动作
- 任何"叫号即接诊"的设计提案必须先改本节,经业务确认后才允许

---

## §7 WalkIn 业务路径

### 7.1 主路径(冻结)

```
现场患者到店
  ↓
前台选择 "现场建预约"
  ↓
appointment/create (appointmentType = WALK_IN)
  ↓
Appointment (DRAFT) → 立即 confirm
  ↓
Appointment (CONFIRMED) → 系统立即进入 WAITING_ARRIVAL(跳过定时)
  ↓
前台继续 arrival/checkin
  ↓
Appointment (ARRIVED) + Visit (CHECKED_IN) + TriageQueue (IN_POOL)
  ↓
... 进入正常分诊 / 叫号 / 接诊流程
```

**S1-165 冻结**:WalkIn 主路径走"appointment/create(WALK_IN) + arrival/checkin",**与正常预约共用一套下游流程**。

### 7.2 Fallback 路径(异常 / 紧急)

```
现场患者到店 + 前台系统异常 / 患者紧急
  ↓
后台特殊入口:visit/create-direct
  ↓
Visit (CHECKED_IN) + TriageQueue (IN_POOL)
  ↓
... 正常分诊 / 叫号 / 接诊
```

**S1-165 决定**:
- Fallback 路径**不作为主业务路径**
- 仅在 appointment/create 失败(网络 / 系统)时使用
- Fallback 路径默认**不开放**给所有用户,需要"特殊权限 + 审计"
- Fallback 触发的 Visit.appointmentId = null(WalkIn 直接路径)

**标注**:**【兼容 / 异常路径】**,需在管理后台审计。

### 7.3 WalkIn 字段标识

| 对象 | 字段 | 取值 |
|---|---|---|
| Appointment | appointmentType | WALK_IN |
| Appointment | appointmentSource | FRONT(前台)|
| Visit | visitType | WALK_IN |
| Visit | appointmentId | null(若走 Fallback)或 appointment.id(若走主路径)|

**S1-165 冻结**:
- WalkIn 患者必须可识别(通过 appointmentType 或 visitType)
- 统计模块可按 WalkIn / 预约分类
- WalkIn 不算"爽约"(NO_SHOW 状态不适用于 WalkIn)

---

## §8 角色矩阵(5 角色)

### 8.1 5 个角色定义

| # | 角色 | 业务身份 | 核心职责 |
|---:|---|---|---|
| R1 | **ADMIN**(管理员)| 系统配置 + 全量管理 | 排班配置、诊室管理、用户管理 |
| R2 | **RECEP**(预约员 / 前台)| 预约全流程 + 签到 | 创建 / 修改 / 取消 / 改约 / 签到 |
| R3 | **TRIAGE**(分诊员)| 全局分诊 | 自动 / 手动 / 重新分诊 |
| R4 | **DOCTOR**(医生 / 验光师)| 接诊 | 开始接诊 / 叫号 / 过号 / 完成 |
| R5 | **STAFF**(普通员工)| 辅助 | 仅签到(自助) |

### 8.2 状态 × 角色 操作矩阵(冻结)

#### 8.2.1 Appointment 状态 → 角色操作

| 状态 | 可执行操作 | 允许角色 |
|---|---|---|
| (none) → DRAFT | appointment/create | R1 / R2 |
| DRAFT | appointment/update / appointment/cancel / appointment/reschedule | R1 / R2 |
| DRAFT → CONFIRMED | appointment/confirm | R1 / R2 |
| CONFIRMED | appointment/cancel / appointment/reschedule | R1 / R2 |
| CONFIRMED → WAITING_ARRIVAL | (系统定时)| SYSTEM |
| WAITING_ARRIVAL | arrival/checkin | R1 / R2 / R5(自助)|
| WAITING_ARRIVAL → NO_SHOW | (系统定时)| SYSTEM |
| ARRIVED | (回退 / 取消 - 默认不开放)| R1(管理员审批)|
| TRIAGE_WAITING | (等待分诊)| – |
| WAITING_RECEPTION | triage/reassign | R3 / R1 |
| WAITING_RECEPTION → CALLED | queue/call / queue/next | R4 |
| CALLED → IN_CONSULTATION | queue/start-consult | R4 |
| CALLED → SKIPPED | queue/skip | R4 / SYSTEM(超时) |
| IN_CONSULTATION → COMPLETED | queue/complete | R4 |
| 任意非终态 → CANCELLED | appointment/cancel | R1 / R2 |
| CONFIRMED / WAITING_ARRIVAL → RESCHEDULED | appointment/reschedule | R1 / R2 |

#### 8.2.2 Visit 状态 → 角色操作

| 状态 | 可执行操作 | 允许角色 |
|---|---|---|
| (none) → DRAFT → CHECKED_IN | (随 arrival/checkin 自动)| SYSTEM |
| CHECKED_IN → TRIAGED | triage/assign | R3 / R1 |
| TRIAGED → IN_CONSULTATION | queue/start-consult | R4 |
| IN_CONSULTATION → COMPLETED | queue/complete | R4 |
| 任意非终态 → CANCELLED | (随 appointment/cancel 同步)| R1 / R2 |

#### 8.2.3 TriageQueue 状态 → 角色操作

| 状态 | 可执行操作 | 允许角色 |
|---|---|---|
| (none) → IN_POOL | (随 arrival/checkin)| SYSTEM |
| IN_POOL → ASSIGNED | triage/assign / triage/auto-assign | R3 / R1 / SYSTEM |
| IN_POOL → REMOVED | triage/remove | R3 / R1 |
| ASSIGNED → IN_POOL | triage/reassign | R3 / R1 |

#### 8.2.4 ReceptionQueue 状态 → 角色操作

| 状态 | 可执行操作 | 允许角色 |
|---|---|---|
| (none) → ASSIGNED | (随 triage/assign)| R3 |
| ASSIGNED → WAITING | queue/start | R4 |
| WAITING → CALLED | queue/call / queue/next | R4 |
| CALLED → IN_CONSULTATION | queue/start-consult | R4 |
| CALLED → SKIPPED | queue/skip | R4 / SYSTEM(超时)|
| CALLED → WAITING | (系统超时回退)| SYSTEM |
| SKIPPED → CALLED | queue/recall | R4 |
| IN_CONSULTATION → DONE | queue/complete | R4 |

### 8.3 跨角色禁止规则

| 禁止 | 原因 |
|---|---|
| 医生不能创建 / 修改 / 取消预约 | 角色边界,避免越权 |
| 分诊员不能叫号 / 接诊 | 角色边界 |
| 预约员不能分诊 | 角色边界 |
| 管理员可代任何角色,但必须有 AuditLog | 全量权限需审计 |
| 普通员工只能签到,不能看其他预约 | 最小权限 |

---

## §9 非法迁移清单(全状态机)

### 9.1 Appointment 非法迁移(15 条)

1. COMPLETED → *:终态不可逆
2. CANCELLED → *:终态不可逆
3. NO_SHOW → *:终态不可逆
4. RESCHEDULED → *:终态不可逆
5. DRAFT → WAITING_ARRIVAL:跳过确认
6. DRAFT → ARRIVED:跳过确认 + 签到
7. CONFIRMED → ARRIVED:跳 WAITING_ARRIVAL
8. CONFIRMED → WAITING_RECEPTION:跳签到 + 分诊
9. WAITING_ARRIVAL → TRIAGE_WAITING:跳签到
10. ARRIVED → WAITING_RECEPTION:跳分诊
11. IN_CONSULTATION → CANCELLED:接诊中禁止取消
12. CALLED → CANCELLED:叫号后禁止直接取消(需 SKIP 后再 cancel)
13. WAITING_RECEPTION → CANCELLED:分诊后取消【待确认】(默认禁止)
14. DRAFT → COMPLETED:无意义
15. 任意 → DRAFT:不可回退到草稿

### 9.2 Visit 非法迁移(8 条)

1. COMPLETED → *:终态
2. CANCELLED → *:终态
3. DRAFT → TRIAGED:跳入池
4. CHECKED_IN → IN_CONSULTATION:跳分诊
5. DRAFT → COMPLETED:瞬时态不可能
6. IN_CONSULTATION → TRIAGED:不可回退
7. TRIAGED → DRAFT:不可回退
8. CHECKED_IN → DRAFT:不可回退

### 9.3 TriageQueue 非法迁移(3 条)

1. REMOVED → *:终态
2. ASSIGNED → REMOVED:已分诊不应异常移出
3. IN_POOL → IN_POOL:自循环无意义

### 9.4 ReceptionQueue 非法迁移(6 条)

1. DONE → *:终态
2. ASSIGNED → CALLED:跳 WAITING
3. WAITING → IN_CONSULTATION:跳 CALLED
4. SKIPPED → IN_CONSULTATION:跳 CALLED
5. IN_CONSULTATION → WAITING:接诊中不可回退
6. IN_CONSULTATION → SKIPPED:接诊中不存在过号

### 9.5 跨对象非法同步

| 禁止 | 原因 |
|---|---|
| Appointment 改 CANCELLED 但 Visit 仍 IN_CONSULTATION | 必须同步 |
| Visit 改 IN_CONSULTATION 但 Appointment 仍 WAITING_RECEPTION | 必须同步 |
| TriageQueue 改 ASSIGNED 但 ReceptionQueue 未创建 | 必须同步 |
| ReceptionQueue 改 DONE 但 Appointment 仍 IN_CONSULTATION | 必须同步 |

**S1-165 强制约束**:Appointment / Visit / TriageQueue / ReceptionQueue 的状态变更**必须原子同步**,不允许出现 4 对象状态不一致。事务边界 = 单次 API 调用。

---

## §10 ID 原则(冻结)

### 10.1 独立 ID 清单

| ID 字段 | 类型 | 所属对象 | 来源 |
|---|---|---|---|
| **appointmentId** | uuid | Appointment | OptFlow 新生成(不复用原系统 appointId 18 命中)|
| **visitId** | uuid | Visit | OptFlow 新生成 |
| **triageQueueId** | uuid | TriageQueue | OptFlow 新生成 |
| **receptionQueueId** | uuid | ReceptionQueue | OptFlow 新生成 |
| **consultRoomId** | uuid | ConsultRoom | S1-158 复用 |
| **employeeId** | uuid | Employee | S1-158 复用(doctorId = employeeId 别名)|
| **patientId** | uuid | Patient | S1-147 复用 |
| **scheduleId** | uuid | AppointmentSchedule | OptFlow 新生成 |
| **slotId** | uuid | AppointmentSlot | OptFlow 新生成 |
| **companyId** | uuid | Company | S1-158 复用 |
| **bigScreenId** | uuid | BigScreen | S1-153 复用 |

### 10.2 互斥规则(禁止)

| 禁止 | 原因 |
|---|---|
| 用 appointmentId 代替 visitId | Appointment 与 Visit 语义不同,必须独立 |
| 用 visitId 代替 receptionQueueId | Visit 是"一次到院",ReceptionQueue 是"一条队列条目" |
| 用 appointmentId 代替 receptionQueueId | 预约 ≠ 队列 |
| 用原系统 appointId(18 命中)直接当 Appointment.id | 原系统命名误导,新设计必须独立 |
| 用原系统 appointmentId(4 命中)直接当 Appointment.id | 同上 |
| 用 consultRoomId 代替 doctorId | 诊室 ≠ 医生 |
| 用 bigScreenId 代替 consultRoomId | 大屏 ≠ 诊室 |

### 10.3 ID 生成规则

| ID | 生成方式 |
|---|---|
| appointmentId | UUID v4,由 appointment/create 生成 |
| visitId | UUID v4,由 arrival/checkin 同步生成 |
| triageQueueId | UUID v4,由 arrival/checkin 同步生成 |
| receptionQueueId | UUID v4,由 triage/assign 同步生成 |
| scheduleId | UUID v4,由 schedule/create 生成 |
| slotId | UUID v4,由 schedule/create 子项生成 |

**禁止**:
- 不用业务字段(手机号/身份证)做主键
- 不用连续递增数字(避免业务推断)
- 不用可逆编码(避免泄漏业务量)

---

## §11 核心业务流程图

### 11.1 主流程:正常预约到完成

```
[预约员 / 患者]
  ↓ appointment/create (DRAFT)
  ↓ appointment/confirm (CONFIRMED)
  ↓ [系统 0:00] WAITING_ARRIVAL
  ↓
[预约日 患者到店]
  ↓ arrival/checkin
  ↓ ARRIVED + Visit(CHECKED_IN) + TriageQueue(IN_POOL) + Appointment(TRIAGE_WAITING)
  ↓
[分诊员]
  ↓ triage/assign (doctorId, consultRoomId)
  ↓ TriageQueue(ASSIGNED) + ReceptionQueue(ASSIGNED) + Appointment(WAITING_RECEPTION)
  ↓
[医生 进入诊室]
  ↓ queue/start
  ↓ ReceptionQueue(WAITING)
  ↓ queue/next (选 sequenceInRoom 最小)
  ↓ ReceptionQueue(CALLED) + Appointment(CALLED)
  ↓
[患者 到达诊室]
  ↓ queue/start-consult
  ↓ ReceptionQueue(IN_CONSULTATION) + Appointment(IN_CONSULTATION) + Visit(IN_CONSULTATION)
  ↓
[医生 接诊中]
  ↓ ... 写病历 / 开处方 ...
  ↓ queue/complete
  ↓ ReceptionQueue(DONE) + Appointment(COMPLETED) + Visit(COMPLETED)
```

### 11.2 异常流程:过号

```
[医生 叫号]
  ↓ queue/next
  ↓ ReceptionQueue(CALLED) + Appointment(CALLED)
  ↓
[5 分钟超时 / 患者未到]
  ↓ 系统任务 / queue/skip
  ↓ ReceptionQueue(SKIPPED) + Appointment(WAITING_RECEPTION)
  ↓
[医生 重新叫号]
  ↓ queue/recall
  ↓ ReceptionQueue(CALLED)
  ↓ 或 queue/next(选下一位)
```

### 11.3 异常流程:医生临时停诊

```
[医生 接诊中]
  ↓ queue/pause (停诊)
  ↓ IN_CONSULTATION → COMPLETED(强制完成)
  ↓ ReceptionQueue(DONE) + Appointment(COMPLETED) + Visit(COMPLETED)
  ↓
[其他 CALLED 患者]
  ↓ CALLED → WAITING(放回队列)
  ↓ 等待医生恢复
```

### 11.4 异常流程:诊室临时更换

```
[患者 TRIAGED / 医生 IN_CONSULTATION]
  ↓ triage/reassign
  ↓ Visit(TRIAGED → CHECKED_IN 回退) + TriageQueue(IN_POOL) + ReceptionQueue(删除)
  ↓
[新分诊]
  ↓ triage/assign (新 doctor + 新 consultRoom)
  ↓ 正常继续
```

### 11.5 WalkIn 流程

```
[现场患者]
  ↓ appointment/create (appointmentType=WALK_IN)
  ↓ 立即 appointment/confirm
  ↓ CONFIRMED → 系统立即 WAITING_ARRIVAL
  ↓ arrival/checkin
  ↓ 进入主流程(ARRIVED → ...)
```

---

## §12 当前采用口径(S1-165 冻结)

| 维度 | S1-165 正式口径 |
|---|---|
| **业务对象总数** | **14 个候选** = 6 复用 + 6 新增实体 + 2 状态枚举 |
| **业务实体数** | **6 新增**:Appointment / AppointmentSchedule / AppointmentSlot / TriageQueue / ReceptionQueue / Visit |
| **复用实体数** | **6**:Company / Customer / Patient / Employee / ConsultRoom / BigScreen |
| **状态枚举数** | **2**:AppointmentStatus(12)/ ReceptionQueueStatus(6) |
| **状态总数** | **12 + 5 + 3 + 6 = 26 个** |
| **状态机数** | **4 个**(Appointment / Visit / TriageQueue / ReceptionQueue) |
| **AppointmentPatient** | **不单独建**,复用 Customer + Patient |
| **核心业务 API** | **35 个**(8 预约 + 7 排班 + 3 到院 + 5 分诊 + 10 叫号 + 2 大屏) |
| **核心业务页面** | **10 个**(A~J) |
| **P0 Task** | **12 个**(T-01 ~ T-12) |
| **异常场景** | **10 个** |
| **角色数** | **5 个**(管理员 / 预约员 / 分诊员 / 医生 / 普通员工) |
| **Visit 创建时机** | **签到时**(arrival/checkin) |
| **WalkIn 主路径** | **appointment/create(WALK_IN) + arrival/checkin** |
| **WalkIn Fallback** | **visit/create-direct(异常 / 紧急,需特殊权限 + 审计)** |
| **叫号 vs 接诊** | **分离**(B 方案,queue/call 和 queue/start-consult 独立)|
| **过号语义** | **CALLED → SKIPPED → CALLED 可多次** |
| **改约语义** | **新建 Appointment,旧 RESCHEDULED 终态** |
| **AppointmentPatient 单独建** | **禁止** |
| **ID 互斥** | **强制 11 个 ID 独立,不可替代** |
| **状态同步** | **Appointment / Visit / TriageQueue / ReceptionQueue 必须原子同步** |
| **复用的 OptFlow 冻结对象** | 6 个,无新增依赖 |

---

## §13 待确认问题

| # | 问题 | 影响 | S1-165 决定 | 后续 |
|---:|---|---|---|---|
| 1 | ARRIVED → WAITING_ARRIVAL 撤销签到 | Visit / TriageQueue 同步 | **默认不开启**,需管理员手动调整 | 业务运营确认 |
| 2 | WAITING_RECEPTION → CANCELLED | 状态机非法迁移 | **默认禁止**,需先 reassign 再 cancel | 业务运营确认 |
| 3 | 号源扣减机制(行锁 / 乐观锁 / Redis)| Slot 并发 | **默认行锁** | 编码阶段性能测试决定 |
| 4 | 爽约触发时机(系统定时 vs 人工)| NO_SHOW 状态 | **默认系统定时每日 0:00 扫描** | 产品确认 |
| 5 | WalkIn Fallback 权限 | visit/create-direct | **需特殊权限 + 审计** | 管理员配置 |
| 6 | 签到是否需身份证 / 医保卡 | Visit 字段 | **暂不要求** | 产品确认 |
| 7 | 临时停诊是否通知患者 | 业务流程 | **默认否** | 运营确认 |
| 8 | 改约是否通知原医生 | 业务流程 | **默认否** | 运营确认 |
| 9 | 优先级规则(VIP / 老人 / 儿童)| TriageQueue.priority | **预留字段 P1 实现** | 产品 P1 阶段 |
| 10 | 诊室同时可接诊多医生 | 业务 | **默认 1 诊室 = 1 医生** | 运营确认 |
| 11 | 叫号大屏可显示多少诊室 | 性能 | **默认 8 个** | 性能测试 |
| 12 | 预约号生成规则(序号 vs 数字)| 业务 | **默认 `HH:MM-HH:MM 序号`** | UI 设计确认 |
| 13 | TriageQueue.ASSIGNED 保留时长 | 数据保留 | **默认 7 天** | 运营确认 |
| 14 | AppointmentPatient 是否在 UI 单独展示 | UI | **复用 Patient 展示** | 前端确认 |
| 15 | ReceptionQueue.DONE 后是否可"复诊" | 业务 | **不支持**,需新建 Appointment | 产品确认 |

---

## §14 状态机总览图

### 14.1 Appointment 状态机全景

```
       [DRAFT]
          │ confirm
          ▼
     [CONFIRMED]
          │ 系统 0:00
          ▼
  [WAITING_ARRIVAL] ──────────────┐
          │ checkin                │ 超时
          ▼                        ▼
      [ARRIVED]                [NO_SHOW] ★
          │ 入池
          ▼
   [TRIAGE_WAITING]
          │ assign
          ▼
[WAITING_RECEPTION] ◄─── reassign
          │ call/next
          ▼
       [CALLED] ──── timeout ──→ (回 WAITING_RECEPTION)
          │ start-consult
          ▼
  [IN_CONSULTATION]
          │ complete
          ▼
     [COMPLETED] ★
          
  任意非终态 ──cancel──→ [CANCELLED] ★
  CONFIRMED/WAITING_ARRIVAL ──reschedule──→ [RESCHEDULED] ★
```

### 14.2 Visit 状态机全景

```
      [DRAFT] ─── (瞬时) ──→ [CHECKED_IN]
                                  │ assign
                                  ▼
                              [TRIAGED]
                          ┌───────┴───────┐
                          │ reassign       │ start-consult
                          ▼                ▼
                      [CHECKED_IN]  [IN_CONSULTATION]
                                          │ complete
                                          ▼
                                     [COMPLETED] ★
                                          
  任意非终态 ──(Appointment cancel)──→ [CANCELLED] ★
```

### 14.3 TriageQueue 状态机全景

```
  (arrival/checkin)
         │
         ▼
     [IN_POOL]
       │  │  │
       │  │  └── remove ──→ [REMOVED] ★
       │  │
       │  └── reassign(从 ASSIGNED 回)──┐
       │                                │
       │ assign                         │
       ▼                                │
    [ASSIGNED] ─────────────────────────┘
```

### 14.4 ReceptionQueue 状态机全景

```
  (triage/assign)
         │
         ▼
    [ASSIGNED]
         │ queue/start
         ▼
    [WAITING]
         │ call/next
         ▼
    [CALLED] ──────── timeout(5min) ──→ [WAITING]
         │  │
         │  └── skip ──→ [SKIPPED] ── recall ──→ [CALLED]
         │
         │ start-consult
         ▼
[IN_CONSULTATION]
         │ complete
         ▼
     [DONE] ★
```

---

## §15 冻结条件

### 15.1 最终判断

**【PASS:对象与状态机可以进入字段级设计】**

### 15.2 冻结依据

1. **对象清单定死**:14 个候选(6 复用 + 6 实体 + 2 枚举)无歧义
2. **状态机完整**:4 个状态机,共 26 个状态,每个状态有完整定义(进入 / 角色 / 退出 / 不可逆 / 后置)
3. **合法迁移闭合**:每个状态有明确合法下一状态,无悬空
4. **非法迁移显式**:32 条非法迁移及原因列全
5. **角色矩阵完整**:5 角色 × 4 状态机,所有操作可追溯到角色
6. **WalkIn 主路径冻结**:主路径 + Fallback 明确
7. **ID 原则强制**:11 个 ID 独立,7 条互斥规则
8. **同步约束强制**:4 对象状态变更必须原子同步
9. **0 项业务模型冲突**:仅 15 项【待确认】,均不阻塞字段级设计

### 15.3 不阻塞条件

S1-165 不要求关闭 15 项【待确认】,因:
- 全部属于运营 / 性能 / UI 层面
- 不影响主数据模型
- 不影响状态机逻辑
- 字段级设计可基于当前冻结推进

### 15.4 后续轮次建议

| 轮次 | 主题 | 依赖 |
|---|---|---|
| S1-166 | API 字段级设计(35 个 API 的 Request / Response / 错误码)| 227 冻结 |
| S1-167 | 数据表 DDL 草案(14 个对象 + 索引) | 227 冻结 |
| S1-168 | 前后端接口契约(OpenAPI 3.0) | S1-166 |
| S1-169 | 异常场景 × 状态机 单元测试矩阵 | 227 冻结 + S1-166 |

---

## §16 Git 验证(本轮预期)

- **新增**:`227_S1-165_*.md`(本轮唯一新增)
- **预期**:Tracked = 235 / Untracked = 10 / Ignored = 1 / LOCAL == origin/master
- **历史 MD(190~226 共 37 份)未修改** ✓
- **controller.js SHA256 未变化** ✓
- **4 文件 SHA256 全部一致** ✓
- **10 untracked 文件继续保留** ✓
- **视光之家url.txt 继续 ignored** ✓

---

## §17 总结

S1-165 完成"业务对象与状态机最终细化":

- **14 个候选对象**分类冻结(6 复用 + 6 实体 + 2 枚举)
- **4 个状态机,26 个状态,每个状态完整定义**:
  - Appointment 12 状态
  - Visit 5 状态(+1 CANCELLED)
  - TriageQueue 3 状态
  - ReceptionQueue 6 状态(去除 IN_TRIAGE_POOL)
- **4 张合法迁移表 + 4 张非法迁移表**(共 32 条非法迁移及原因)
- **5 角色 × 4 状态机 操作矩阵**
- **WalkIn 主路径 + Fallback 双路径**
- **叫号与接诊分离**(B 方案,核心架构决策)
- **11 个 ID 独立原则 + 7 条互斥规则**
- **4 对象状态原子同步约束**
- **15 项【待确认】**全部不阻塞字段级设计

**结论:对象与状态机已冻结,可进入字段级设计(S1-166)和 DDL 设计(S1-167)。**
