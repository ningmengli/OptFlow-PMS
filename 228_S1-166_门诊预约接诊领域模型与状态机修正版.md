# S1-166 门诊预约 + 接诊领域模型与状态机修正版

> **本轮定位**:S1-165(227_*.md)的**硬审计修正版**。
> - 严禁修改 S1-165 原文件(227_*)
> - 本文档作为"真正可作为 API/DDL 设计输入"的领域模型冻结稿
> - 上一版 S1-165 保留作为历史证据,本版正式覆盖其结论
> - 标签严格区分:【原系统事实】/【已验证业务事实】/【OptFlow设计】/【待确认】

---

## §1 文档目的

1. 对 S1-165(227_*.md)做完整审计,定位所有逻辑矛盾
2. 强制修正 13 个 P0 + 4 个 P1
3. 输出一份自洽的领域模型,可作为 S1-167+ 的 API / DDL 设计输入
4. 在全局一致性检查 24 项全部 PASS 的前提下,给出冻结结论

---

## §2 S1-165 审计结论

### 2.1 S1-165 优点(继承)

- 14 个候选对象(6 复用 + 6 实体 + 2 枚举)的总体框架合理
- Appointment ≠ Visit ≠ Queue 严格分离的架构正确
- 叫号 / 接诊分离(B 方案)决策正确
- 角色矩阵的总体设计可用
- 10 个异常场景 / 12 个 P0 Task 的拆分合理

### 2.2 S1-165 致命问题(必须修正)

| # | 问题 | 影响 | 严重度 |
|---:|---|---|:-:|
| 1 | Visit 标题写"5 状态"但实际列 6 状态(自相矛盾) | 状态机无法对齐 | **P0-1** |
| 2 | Appointment 取消规则"任意非终态可取消" vs 非法迁移表"IN_CONSULTATION / CALLED / WAITING_RECEPTION 禁止取消" **直接冲突** | 编码无法实现 | **P0-2** |
| 3 | TriageQueue 3 状态无法闭环(Appointment / Visit 取消后无对应状态)| 出现"幽灵记录" | **P0-3** |
| 4 | WalkIn Fallback 要求 Visit.appointmentId NULL,但 TriageQueue 强依赖 Appointment | 模型关系自相矛盾 | **P0-4** |
| 5 | "四对象原子同步"过强,与状态独立性冲突 | 不可实现的强约束 | **P0-5** |
| 6 | CALLED 超时 → WAITING(一处) vs 5 分钟后 → SKIPPED(另一处)直接冲突 | 编码无法实现 | **P0-6** |
| 7 | 医生暂停接诊 → IN_CONSULTATION → COMPLETED 错误 | 业务语义错误 | **P0-7** |
| 8 | 换诊室:IN_CONSULTATION → TRIAGED → CHECKED_IN 与"IN_CONSULTATION 不可回退"冲突 | 状态机非法回退 | **P0-8** |
| 9 | 复用实体 ID 类型强行写 UUID,无证据 | 凭空创造 DB 字段类型 | **P0-9** |
| 10 | employeeId / doctorId 双用,无统一规范 | DDL/API 混乱 | **P0-10** |
| 11 | consultRoomId / consultationRoomId 双用 | DDL/API 混乱 | **P0-11** |
| 12 | NO_SHOW 时间规则:23:59 vs 0:00 扫描 两种说法 | 编码无法实现 | **P0-12** |
| 13 | 同日预约 CONFIRMED → WAITING_ARRIVAL 时机未闭环 | 凌晨创建预约业务断裂 | **P0-13** |
| 14 | AppointmentSchedule 状态模糊(配置/模板/实例不清)| 业务边界不清 | P1-1 |
| 15 | AppointmentSlot 状态模糊,DRAFT 是否占用容量未说明 | 容量规则不可实现 | P1-2 |
| 16 | queue/start 语义不清(开始队列 vs 开始接诊) | API 语义混淆 | P1-3 |
| 17 | Appointment 是否同时存 customerId + patientId 未明确 | 数据来源不一致风险 | P1-4 |

**S1-166 目标**:全部关闭上述 13 + 4 项问题。

---

## §3 S1-165 问题清单(逐项 P0 / P1)

详见 §2.2。本节为后续章节的引用索引。

| 编号 | 章节定位 | 修正方案 |
|---|---|---|
| P0-1 | §6 Visit 状态机标题 vs 内容 | §8 全文统一 6 状态 |
| P0-2 | §3.3 合法迁移 vs §3.4 非法迁移 vs §9.1 | §14 Appointment cancellation policy 重设计 |
| P0-3 | §5 TriageQueue 3 状态 | §10 TriageQueue 增 1 状态(CANCELLED)= 4 状态 |
| P0-4 | §7 WalkIn 关系矛盾 | §13 WalkIn 关系明确 NULL 允许 |
| P0-5 | §9.5 跨对象同步 | §21 事务边界重写 |
| P0-6 | §6.2 CALLED + §6.3 + §11.2 异常流程 | §22 + §23 CALLED 超时唯一规则 |
| P0-7 | §11.3 异常流程 | §19 医生暂停与患者完成解耦 |
| P0-8 | §11.4 异常流程 | §20 换诊室 / 转诊 拆分场景 + Visit.TRANSFERRED |
| P0-9 | §10.1 ID 列表 | §25 ID 与命名规范,复用 ID 沿用 |
| P0-10 | §2.2 / §10.1 | §25 统一 employeeId |
| P0-11 | §2.2 / §10.1 | §25 统一 consultRoomId |
| P0-12 | §3.2 WAITING_ARRIVAL / §3.2 NO_SHOW | §14.1 同日预约规则 + §15 NO_SHOW |
| P0-13 | 同 P0-12 | §14.1 |
| P1-1 | §10 / §18.1 (Schedule)| §11 Schedule 生命周期 |
| P1-2 | §10 / §18.1 (Slot)| §12 Slot 生命周期 + 容量规则 |
| P1-3 | §6.2 / §15.1 | §21 业务动作语义区分 |
| P1-4 | §3.2.1 / §18.1 | §6 关系图:Appointment → patientId 单向 |

---

## §4 本版本修正原则

1. **不修改 S1-165 任何字节**,原文件作为历史证据保留
2. **不扩张业务范围**:只处理 S1-165 范围内的对象 / 状态 / 业务
3. **每个状态必须同时具备合法进入路径 + 合法离开路径**(死状态 = FAIL)
4. **每条业务规则必须唯一,无并列版本**
5. **所有 ID 类型必须有依据**:新建实体允许【OptFlow设计】UUID,复用实体沿用【待确认】
6. **事务边界 = 业务动作边界,不是对象边界**
7. **不得把 E 级业务推断伪装成原系统事实**
8. **【OptFlow设计】的方案必须明确写"为什么这样设计"**

---

## §5 对象清单(本版正式)

### 5.1 正式对象分类

| 类别 | 数量 | 说明 |
|---|:-:|---|
| **A 复用实体**(S1-147 / S1-153 / S1-158 冻结)| **6** | Company / Customer / Patient / Employee / ConsultRoom / BigScreen |
| **B 新增实体**(OptFlow PMS 本轮设计)| **6** | Appointment / AppointmentSchedule / AppointmentSlot / TriageQueue / ReceptionQueue / Visit |
| **C 枚举内置** | **2** | AppointmentStatus(12 状态)/ VisitStatus(6 状态)+ ReceptionQueueStatus(6 状态)+ TriageQueueStatus(4 状态) |
| **D UI / 辅助对象** | **0** | 不独立建表 |
| **E 不独立建模** | **1** | AppointmentPatient(并入 Customer+Patient,S1-165 已删) |

### 5.2 状态枚举明细(本版)

| 枚举 | 状态数 | 用途 |
|---|:-:|---|
| AppointmentStatus | **12** | Appointment 状态 |
| VisitStatus | **6** | Visit 状态(P0-1 修正) |
| TriageQueueStatus | **4** | TriageQueue 状态(P0-3 修正 +1 状态) |
| ReceptionQueueStatus | **6** | ReceptionQueue 状态 |

**S1-165 → S1-166 状态数变化**:
- Visit:5 → **6**(统一为 6,含 CANCELLED)
- TriageQueue:3 → **4**(增 CANCELLED)
- 4 枚举总状态数:12 + 6 + 4 + 6 = **28**

### 5.3 不再列出"QueueStatus"作为独立枚举(S1-165 误)

S1-165 §6.1 把 QueueStatus 列为独立枚举,实际 S1-165 §6.4 也已写"并入 C2 ReceptionQueueStatus(6 状态)"。S1-166 沿用:**无独立 QueueStatus 枚举**。

---

## §6 对象关系(本版)

### 6.1 关系矩阵

| 关系 | 关系类型 | NULL 允许? | FK 字段 | 说明 |
|---|---|:-:|---|---|
| Appointment → Patient | 多对一 | ✗ | appointment.patientId | 必须有 |
| Appointment → Employee | 多对一(接诊医生)| ✗ | appointment.employeeId | 必须有 |
| Appointment → AppointmentSchedule | 多对一 | ✗ | appointment.scheduleId | 必须有 |
| Appointment → AppointmentSlot | 多对一 | ✗ | appointment.slotId | 必须有 |
| Appointment → Appointment | 自引用(改约)| ✓ | appointment.rescheduledFromId | 改约时填,否则 NULL |
| Appointment → Company | 多对一 | ✗ | appointment.companyId | 作用域 |
| Visit → Appointment | 多对一 | ✓ | visit.appointmentId | **WalkIn Fallback 可空** |
| Visit → Patient | 多对一 | ✗ | visit.patientId | 必须有 |
| Visit → Employee | 多对一(接诊医生)| ✓ | visit.employeeId | TRIAGED 之后必有 |
| Visit → ConsultRoom | 多对一(接诊诊室)| ✓ | visit.consultRoomId | TRIAGED 之后必有 |
| Visit → Company | 多对一 | ✗ | visit.companyId | 作用域 |
| TriageQueue → Visit | 一对一 | ✗ | triageQueue.visitId | 必有 |
| TriageQueue → Appointment | 一对一 | ✓ | triageQueue.appointmentId | **WalkIn Fallback 可空** |
| TriageQueue → ConsultRoom | 多对一(目标)| ✓ | triageQueue.consultRoomId | ASSIGNED 时必有 |
| TriageQueue → Employee | 多对一(目标)| ✓ | triageQueue.employeeId | ASSIGNED 时必有 |
| TriageQueue → Company | 多对一 | ✗ | triageQueue.companyId | 作用域 |
| ReceptionQueue → TriageQueue | 一对一 | ✗ | receptionQueue.triageQueueId | 必有 |
| ReceptionQueue → Visit | 一对一 | ✗ | receptionQueue.visitId | 必有 |
| ReceptionQueue → Appointment | 一对一 | ✓ | receptionQueue.appointmentId | **WalkIn Fallback 可空** |
| ReceptionQueue → ConsultRoom | 多对一(必填)| ✗ | receptionQueue.consultRoomId | 必有 |
| ReceptionQueue → Employee | 多对一(必填)| ✗ | receptionQueue.employeeId | 必有 |
| ReceptionQueue → Company | 多对一 | ✗ | receptionQueue.companyId | 作用域 |
| AppointmentSchedule → Employee | 多对一 | ✗ | schedule.employeeId | 必有 |
| AppointmentSchedule → Company | 多对一 | ✗ | schedule.companyId | 必有 |
| AppointmentSlot → AppointmentSchedule | 多对一 | ✗ | slot.scheduleId | 必有 |
| AppointmentSlot → ConsultRoom | 多对一 | ✓ | slot.consultRoomId | 可空,排班时不指定 |

### 6.2 NULL 关系汇总(本版)

| 可空 FK | NULL 时机 |
|---|---|
| `appointment.rescheduledFromId` | 非改约时 NULL |
| `visit.appointmentId` | **WalkIn Fallback 路径** NULL(visit/create-direct)|
| `visit.employeeId` / `visit.consultRoomId` | CHECKED_IN 之前 NULL |
| `triageQueue.appointmentId` | **WalkIn Fallback 路径** NULL |
| `triageQueue.consultRoomId` / `triageQueue.employeeId` | IN_POOL 时 NULL |
| `receptionQueue.appointmentId` | **WalkIn Fallback 路径** NULL |
| `slot.consultRoomId` | 排班时不指定诊室时 NULL |

**S1-165 修正点**:S1-165 强制把 appointmentId 作为所有 4 对象必填,与 WalkIn Fallback 矛盾。S1-166 明确 3 个 FK 可空,且 NULL 集中于 WalkIn Fallback 路径。

### 6.3 Customer / Patient 关系澄清(P1-4)

**S1-166 决定**:
- Appointment **只关联 patientId**,不直接存 customerId
- Patient 与 Customer 关联由 S1-147 冻结的 Patient.customerId 维护
- 预约时:
  - 输入:Customer 手机号 / 姓名
  - 系统:查询或创建 Customer,再查询或创建 Patient(同一 Customer 下)
  - Appointment.patientId = Patient.id
- 避免双向来源不一致

---

## §7 Appointment 状态机(12 状态,本版)

### 7.1 状态总览

| # | 状态码 | 英文 | 显示名 | 终态? |
|---:|---|---|---|:-:|
| 1 | 010 | DRAFT | 草稿 | ✗ |
| 2 | 020 | CONFIRMED | 已预约 | ✗ |
| 3 | 030 | WAITING_ARRIVAL | 待到院 | ✗ |
| 4 | 040 | ARRIVED | 已到院 | ✗ |
| 5 | 050 | TRIAGE_WAITING | 待分诊 | ✗ |
| 6 | 060 | WAITING_RECEPTION | 待叫号 | ✗ |
| 7 | 070 | CALLED | 已叫号 | ✗ |
| 8 | 080 | IN_CONSULTATION | 接诊中 | ✗ |
| 9 | 090 | COMPLETED | 已完成 | ✓ |
| 10 | 100 | CANCELLED | 已取消 | ✓ |
| 11 | 110 | NO_SHOW | 爽约 | ✓ |
| 12 | 120 | RESCHEDULED | 已改约 | ✓ |

### 7.2 Appointment cancellation policy(P0-2 重设计)

**S1-166 唯一规则**(全文一致):

| 当前状态 | 允许取消? | 谁可取消 | 释放 Slot? | 同步 Visit? | 同步 TriageQueue? | 同步 ReceptionQueue? | 审计 |
|---|:-:|---|:-:|---|---|---|:-:|
| **DRAFT** | ✓ | 预约员 / 管理员 | ✓(释放) | N/A(无 Visit)| N/A | N/A | ✓ |
| **CONFIRMED** | ✓ | 预约员 / 管理员 / 患者(自助) | ✓ | N/A | N/A | N/A | ✓ |
| **WAITING_ARRIVAL** | ✓ | 预约员 / 管理员 / 患者(自助) | ✓ | N/A | N/A | N/A | ✓ |
| **ARRIVED** | ✓ | **仅管理员**(业务约束)| ✓ | ✓ Visit → CANCELLED | ✓ TriageQueue → CANCELLED | N/A(无)| ✓ |
| **TRIAGE_WAITING** | ✓ | **仅管理员** | ✓ | ✓ Visit → CANCELLED | ✓ TriageQueue → CANCELLED | N/A | ✓ |
| **WAITING_RECEPTION** | ✓ | **仅管理员** | ✓ | ✓ Visit → CANCELLED | ✓ TriageQueue → CANCELLED | ✓ ReceptionQueue → 软删(S1-166 §22)| ✓ |
| **CALLED** | ✗ | – | – | – | – | – | – |
| **IN_CONSULTATION** | ✗ | – | – | – | – | – | – |
| **COMPLETED** | ✗ 终态 | – | – | – | – | – | – |
| **CANCELLED** | ✗ 终态 | – | – | – | – | – | – |
| **NO_SHOW** | ✗ 终态 | – | – | – | – | – | – |
| **RESCHEDULED** | ✗ 终态 | – | – | – | – | – | – |

**关键设计决策**(本版与 S1-165 区别):
1. **DRAFT / CONFIRMED / WAITING_ARRIVAL**:可任意取消(患者自助 / 预约员)
2. **ARRIVED / TRIAGE_WAITING / WAITING_RECEPTION**:仅管理员可取消(业务上"已到院患者"取消需审批)
3. **CALLED / IN_CONSULTATION**:**禁止取消**。接诊中或叫号中必须先 skip → 回到 WAITING_RECEPTION,然后再由管理员 cancel
4. **终态**:不可逆
5. **统一性**:§7.3 合法迁移、§9 非法迁移、§14 取消流程三处全部使用本表,**无并列版本**

### 7.3 Appointment 合法迁移总表

| From | To | 触发 | 角色 | API | 同步对象 |
|---|---|---|---|---|---|
| (none) | DRAFT | 新建 | 预约员 | appointment/create | Slot.bookedCount **不变**(DRAFT 不占容量)|
| DRAFT | CONFIRMED | 确认 | 预约员 | appointment/confirm | Slot.bookedCount +1 |
| DRAFT | CANCELLED | 取消 | 预约员 | appointment/cancel | – |
| CONFIRMED | WAITING_ARRIVAL | 系统条件触发 | 系统 | –(见 §14.1) | – |
| CONFIRMED | CANCELLED | 取消 | 预约员/患者 | appointment/cancel | Slot.bookedCount -1 |
| CONFIRMED | RESCHEDULED | 改约 | 预约员 | appointment/reschedule | Slot.bookedCount -1 + 新 Appointment |
| WAITING_ARRIVAL | ARRIVED | 签到 | 前台 | arrival/checkin | Visit 创建 + TriageQueue 创建(同事务)|
| WAITING_ARRIVAL | CANCELLED | 取消 | 预约员/患者 | appointment/cancel | Slot.bookedCount -1 |
| WAITING_ARRIVAL | RESCHEDULED | 改约 | 预约员 | appointment/reschedule | Slot.bookedCount -1 + 新 Appointment |
| WAITING_ARRIVAL | NO_SHOW | 系统 0:00 扫描 | 系统 | – | Slot.bookedCount -1 |
| ARRIVED | TRIAGE_WAITING | 入分诊池 | 系统 | –(随 checkin 触发)| – |
| ARRIVED | CANCELLED | 取消 | 管理员 | appointment/cancel | Visit → CANCELLED, TriageQueue → CANCELLED, Slot -1 |
| TRIAGE_WAITING | WAITING_RECEPTION | 分诊 | 分诊员 | triage/assign | TriageQueue → ASSIGNED, ReceptionQueue 创建, Slot 保持 |
| TRIAGE_WAITING | CANCELLED | 取消 | 管理员 | appointment/cancel | Visit → CANCELLED, TriageQueue → CANCELLED, Slot -1 |
| WAITING_RECEPTION | CALLED | 叫号 | 医生 | queue/call | ReceptionQueue → CALLED |
| WAITING_RECEPTION | TRIAGE_WAITING | 重新分诊 | 分诊员 | triage/reassign | TriageQueue → IN_POOL, ReceptionQueue 软删 |
| WAITING_RECEPTION | CANCELLED | 取消 | 管理员 | appointment/cancel | Visit → CANCELLED, TriageQueue → CANCELLED, ReceptionQueue 软删, Slot -1 |
| CALLED | IN_CONSULTATION | 开始接诊 | 医生 | queue/start-consult | ReceptionQueue → IN_CONSULTATION, Visit → IN_CONSULTATION |
| CALLED | WAITING_RECEPTION | 超时(5 分钟)| 系统 | –(定时任务)| ReceptionQueue → WAITING |
| CALLED | WAITING_RECEPTION | 主动 skip | 医生 | queue/skip | ReceptionQueue → SKIPPED → 重新入队 |
| IN_CONSULTATION | COMPLETED | 完成接诊 | 医生 | queue/complete | ReceptionQueue → DONE, Visit → COMPLETED |
| IN_CONSULTATION | (异常) → 拆分见 §19 §20 | – | – | – | – |

### 7.4 Appointment 非法迁移(全文一致,与 §7.2 / §7.3 完全对齐)

| 非法迁移 | 原因 |
|---|---|
| 任意 → * from 终态 | 终态不可逆 |
| CALLED → CANCELLED | 叫号中禁止取消(§7.2 唯一规则) |
| IN_CONSULTATION → CANCELLED | 接诊中禁止取消 |
| IN_CONSULTATION → 任何 | 接诊中只允许 queue/complete → COMPLETED |
| DRAFT → WAITING_ARRIVAL | 跳过确认 |
| CONFIRMED → ARRIVED | 跳过待到院 |
| WAITING_ARRIVAL → TRIAGE_WAITING | 跳过签到 |
| ARRIVED → WAITING_RECEPTION | 跳过分诊 |
| 任意 → DRAFT | 不可回退草稿 |
| WAITING_RECEPTION → CALLED 跳 RECEPTION 状态 | 必须先 queue/start |

---

## §8 Visit 状态机(6 状态,本版 P0-1 统一)

### 8.1 状态总览(本版统一为 6 状态)

| # | 状态码 | 英文 | 显示名 | 终态? |
|---:|---|---|---|:-:|
| 1 | 010 | DRAFT | 草稿 | ✗ |
| 2 | 020 | CHECKED_IN | 已签到 | ✗ |
| 3 | 030 | TRIAGED | 已分诊 | ✗ |
| 4 | 040 | IN_CONSULTATION | 接诊中 | ✗ |
| 5 | 050 | COMPLETED | 已完成 | ✓ |
| 6 | 060 | TRANSFERRED | 已转诊 | ✓ |
| 7 | 070 | CANCELLED | 已取消 | ✓ |

**S1-166 状态数 = 7**(S1-165 是 6 状态,本版新增 TRANSFERRED 用于 P0-8 转诊场景)。

**S1-165 vs S1-166 修正**:
- S1-165 §4 标题"5 状态" + 内容"6 状态"自相矛盾
- S1-166 统一为 **7 状态**(标题 / 内容 / 状态表 / 迁移表 / 非法表全部 7 状态)

### 8.2 Visit 状态详细定义(本版)

#### 8.2.1 DRAFT(草稿)

| 项目 | 描述 |
|---|---|
| **进入条件** | arrival/checkin 触发,Visit 记录创建初始态 |
| **操作角色** | 系统 |
| **退出条件** | 入分诊池(立即)→ CHECKED_IN |
| **不可逆规则** | 瞬时态,实际可不持久化(由 S1-167 决定 DDL 行为) |
| **后置动作** | – |

**S1-166 修正**:S1-165 写"DRAFT 是瞬时态,通常创建后立即 CHECKED_IN",但仍列为 6 状态之一,语义模糊。本版**明确保留 DRAFT 状态**,作为系统创建记录的初始态(可立即迁移到 CHECKED_IN)。

#### 8.2.2 CHECKED_IN(已签到)

| 项目 | 描述 |
|---|---|
| **进入条件** | Visit 创建后入分诊池 |
| **操作角色** | 系统 |
| **退出条件** | triage/assign → TRIAGED / 取消(管理员)→ CANCELLED |
| **不可逆规则** | 一旦 TRIAGED,不可回退到 CHECKED_IN |
| **后置动作** | TriageQueue 已创建(IN_POOL) |

#### 8.2.3 TRIAGED(已分诊)

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/assign 成功 |
| **操作角色** | 分诊员 |
| **退出条件** | queue/start-consult → IN_CONSULTATION / 重新分诊 → CHECKED_IN(回退)|
| **不可逆规则** | 进入 IN_CONSULTATION 后不可回退 |
| **后置动作** | ReceptionQueue 已创建(ASSIGNED)|

#### 8.2.4 IN_CONSULTATION(接诊中)

| 项目 | 描述 |
|---|---|
| **进入条件** | queue/start-consult |
| **操作角色** | 医生 |
| **退出条件** | queue/complete → COMPLETED / 转诊(异常)→ TRANSFERRED |
| **不可逆规则** | **不可回退到 CHECKED_IN / TRIAGED**(§P0-8 修正)|
| **后置动作** | ReceptionQueue → IN_CONSULTATION |

#### 8.2.5 COMPLETED(已完成) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | queue/complete |
| **操作角色** | 医生 |
| **不可逆规则** | 永久不可逆 |
| **后置动作** | (1) visit.completedAt = now (2) 触发 MedicalRecord 创建(S1-146 复用)|

#### 8.2.6 TRANSFERRED(已转诊) ★ 终态(P0-8 新增)

| 项目 | 描述 |
|---|---|
| **进入条件** | visit/transfer 由当前医生执行 |
| **操作角色** | 当前医生(转出方)/ 管理员 |
| **不可逆规则** | 永久不可逆(本诊次)|
| **后置动作** | (1) 新 Visit 创建(同 patientId,新 appointmentId 或 NULL)(2) 新 TriageQueue(IN_POOL) (3) 旧 ReceptionQueue → DONE(标"transferred") |

**S1-166 关键设计决策**:
- TRANSFERRED 是终态,代表"本次诊次因转诊结束"
- 不通过"IN_CONSULTATION → TRIAGED → CHECKED_IN"非法回退实现
- 新 Visit 通过 TriageQueue 重新分诊
- Appointment 保持 IN_CONSULTATION(因为 Appointment 维度是"本次预约",跨诊次不分)

#### 8.2.7 CANCELLED(已取消) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | 关联 Appointment 取消(且 visit 在 CHECKED_IN / TRIAGED / IN_CONSULTATION 之一)|
| **操作角色** | 预约员 / 管理员(随 appointment/cancel 同步)|
| **不可逆规则** | 永久不可逆 |
| **后置动作** | 同步 visit.cancelledAt |

### 8.3 Visit cancellation policy

| Visit 当前状态 | 允许取消? | 谁可触发 | 触发条件 |
|---|:-:|---|---|
| DRAFT | ✓ | 系统随 Appointment 取消 | Appointment 取消时同步 |
| CHECKED_IN | ✓ | 管理员(随 Appointment 取消) | Appointment ARRIVED/TRIAGE_WAITING 状态取消 |
| TRIAGED | ✓ | 管理员(随 Appointment 取消) | Appointment WAITING_RECEPTION 状态取消 |
| IN_CONSULTATION | ✗ | – | 必须先 queue/complete → COMPLETED 或 queue/transfer → TRANSFERRED |
| COMPLETED / TRANSFERRED / CANCELLED | ✗ 终态 | – | – |

### 8.4 Visit 合法迁移总表

| From | To | 触发 | 角色 | API |
|---|---|---|---|---|
| (none) | DRAFT | 签到触发创建 | 系统 | (随 arrival/checkin)|
| DRAFT | CHECKED_IN | 入分诊池 | 系统 | – |
| CHECKED_IN | TRIAGED | 分诊 | 分诊员 | triage/assign |
| CHECKED_IN | CANCELLED | Appointment 取消 | 管理员 | (随 appointment/cancel)|
| TRIAGED | IN_CONSULTATION | 开始接诊 | 医生 | queue/start-consult |
| TRIAGED | CHECKED_IN | 重新分诊 | 分诊员 | triage/reassign |
| TRIAGED | CANCELLED | Appointment 取消 | 管理员 | (随 appointment/cancel)|
| IN_CONSULTATION | COMPLETED | 完成接诊 | 医生 | queue/complete |
| IN_CONSULTATION | TRANSFERRED | 转诊 | 医生 | visit/transfer(本版新增) |
| COMPLETED / TRANSFERRED / CANCELLED | (无) | 终态 | – | – |

### 8.5 Visit 非法迁移

| 非法迁移 | 原因 |
|---|---|
| 终态 → * | 不可逆 |
| IN_CONSULTATION → TRIAGED | 接诊中不可回退(§P0-8 修正)|
| IN_CONSULTATION → CHECKED_IN | 同上 |
| IN_CONSULTATION → CANCELLED | 接诊中禁止取消(必须 complete 或 transfer)|
| DRAFT → IN_CONSULTATION | 跳入池 + 分诊 |
| DRAFT → TRIAGED | 跳入池 |
| CHECKED_IN → COMPLETED | 跳分诊 + 接诊 |

---

## §9 ReceptionQueue 状态机(6 状态,本版)

### 9.1 状态总览(与 S1-165 一致)

| # | 状态码 | 英文 | 终态? |
|---:|---|---|:-:|
| 1 | 010 | ASSIGNED | ✗ |
| 2 | 020 | WAITING | ✗ |
| 3 | 030 | CALLED | ✗ |
| 4 | 040 | SKIPPED | ✗ |
| 5 | 050 | IN_CONSULTATION | ✗ |
| 6 | 060 | DONE | ✓ |

### 9.2 ReceptionQueue 合法迁移(P0-6 唯一规则)

| From | To | 触发 | 角色 | 同步对象 |
|---|---|---|---|---|
| (none) | ASSIGNED | triage/assign 触发 | 分诊员 | TriageQueue:ASSIGNED |
| ASSIGNED | WAITING | 医生进入诊室 | 医生 | – |
| ASSIGNED | (软删) | Appointment 取消 / reassign | 系统 | – |
| WAITING | CALLED | 叫号 | 医生 | Appointment → CALLED |
| WAITING | (软删) | Appointment 取消 | 系统 | – |
| CALLED | IN_CONSULTATION | 开始接诊 | 医生 | Appointment → IN_CONSULTATION, Visit → IN_CONSULTATION |
| **CALLED** | **WAITING** | **超时(5 分钟)自动回退**| **系统** | **Appointment 保持 CALLED(下表说明)**|
| **CALLED** | **SKIPPED** | **医生主动 skip**| **医生** | **Appointment → WAITING_RECEPTION** |
| SKIPPED | CALLED | 重新叫号 | 医生 | Appointment → CALLED |
| SKIPPED | (软删) | Appointment 取消 | 系统 | – |
| IN_CONSULTATION | DONE | 完成接诊 | 医生 | Appointment → COMPLETED, Visit → COMPLETED |
| IN_CONSULTATION | (软删) | visit/transfer 转诊 | 医生 | Visit → TRANSFERRED(本版新增) |
| DONE | (无) | 终态 | – | – |

**P0-6 唯一规则**:
- **自动超时** (CALLED 5 分钟无应答)→ **CALLED → WAITING**(回退,等待重新叫号)
- **主动 skip** (医生判断)→ **CALLED → SKIPPED**(过号,需重新叫号或取消)
- **重新叫号** → **SKIPPED → CALLED**

三种迁移**语义不同,状态不同**,编码可清晰实现。

### 9.3 Appointment 状态随 ReceptionQueue 变化的关系(本版明确)

| ReceptionQueue 迁移 | Appointment 状态 | Visit 状态 |
|---|---|---|
| WAITING → CALLED | Appointment → CALLED | Visit 不变(TRIAGED)|
| CALLED → IN_CONSULTATION | Appointment → IN_CONSULTATION | Visit → IN_CONSULTATION |
| CALLED → WAITING(超时)| **Appointment 保持 CALLED**【待确认】| Visit 不变 |
| CALLED → SKIPPED | **Appointment → WAITING_RECEPTION** | Visit 不变 |
| SKIPPED → CALLED(重叫)| Appointment → CALLED | Visit 不变 |
| IN_CONSULTATION → DONE | Appointment → COMPLETED | Visit → COMPLETED |
| IN_CONSULTATION → (转诊软删)| Appointment 保持 IN_CONSULTATION | Visit → TRANSFERRED,新 Visit 创建 |

**S1-166 关键决策**:
- CALLED → WAITING 超时回退时,**Appointment 状态保持 CALLED**(不联动),避免 Appointment 状态抖动
- CALLED → SKIPPED 时,Appointment → WAITING_RECEPTION(主动放弃,Appointment 维度回到待叫号)
- **S1-165 错误**:S1-165 §6.3 写"CALLED → WAITING_RECEPTION",与"自动超时 → WAITING"规则混淆

### 9.4 ReceptionQueue 非法迁移

| 非法迁移 | 原因 |
|---|---|
| DONE → * | 终态 |
| ASSIGNED → CALLED | 跳 WAITING |
| WAITING → IN_CONSULTATION | 跳 CALLED |
| SKIPPED → IN_CONSULTATION | 跳 CALLED |
| IN_CONSULTATION → WAITING | 接诊中不可回退 |

---

## §10 TriageQueue 状态机(4 状态,本版 P0-3 闭环)

### 10.1 状态总览(P0-3 修正 +1 状态)

| # | 状态码 | 英文 | 终态? |
|---:|---|---|:-:|
| 1 | 010 | IN_POOL | ✗ |
| 2 | 020 | ASSIGNED | ✗(转出)|
| 3 | 030 | REMOVED | ✓ |
| 4 | 040 | CANCELLED | ✓ |

**S1-165 修正**:
- S1-165 §5 写 3 状态(无 CANCELLED),导致 Appointment / Visit 取消后 TriageQueue 无对应终态
- S1-166 增加 **CANCELLED** 状态,共 4 状态,保证闭环

### 10.2 TriageQueue 状态详细定义

#### 10.2.1 IN_POOL(在分诊池)

| 项目 | 描述 |
|---|---|
| **进入条件** | arrival/checkin 同步创建,默认 IN_POOL |
| **退出条件** | triage/assign → ASSIGNED / triage/remove → REMOVED / Appointment 取消 → CANCELLED |
| **不可逆规则** | 移出/取消后不可回退 |

#### 10.2.2 ASSIGNED(已分配)

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/assign 成功 |
| **退出条件** | (1) ReceptionQueue 同步创建完成(默认保留)(2) reassign → IN_POOL(回退)(3) Appointment 取消 → CANCELLED |
| **不可逆规则** | reassign 可回退,取消不可回退 |
| **后置动作** | ReceptionQueue 创建完成 |

**S1-166 修正**:S1-165 §5.2.2 写"ASSIGNED 后 TriageQueue 完成使命",S1-166 明确"ASSIGNED 状态保留 7 天用于追溯【待确认】"。

#### 10.2.3 REMOVED(已移出) ★ 终态

| 项目 | 描述 |
|---|---|
| **进入条件** | triage/remove(异常退回)|
| **不可逆规则** | 永久不可逆 |
| **后置动作** | 写 AuditLog(removeReason)|

#### 10.2.4 CANCELLED(已取消) ★ 终态(P0-3 新增)

| 项目 | 描述 |
|---|---|
| **进入条件** | 关联 Appointment 取消(从 IN_POOL 或 ASSIGNED 进入)|
| **不可逆规则** | 永久不可逆 |
| **后置动作** | 写 AuditLog(cancelReason + cancelSource = "appointment_cancelled") |

### 10.3 TriageQueue 合法迁移总表(P0-3 闭环)

| From | To | 触发 | 角色 | 来源 |
|---|---|---|---|---|
| (none) | IN_POOL | 签到触发创建 | 系统 | arrival/checkin |
| IN_POOL | ASSIGNED | 分诊 | 分诊员 | triage/assign / triage/auto-assign |
| IN_POOL | REMOVED | 异常移出 | 分诊员 | triage/remove |
| IN_POOL | CANCELLED | Appointment 取消 | 系统 | (随 appointment/cancel)|
| ASSIGNED | IN_POOL | 重新分诊 | 分诊员 | triage/reassign |
| ASSIGNED | CANCELLED | Appointment 取消 | 系统 | (随 appointment/cancel)|
| REMOVED / CANCELLED | (无) | 终态 | – | – |

### 10.4 TriageQueue 非法迁移

| 非法迁移 | 原因 |
|---|---|
| 终态 → * | 不可逆 |
| IN_POOL → IN_POOL | 自循环 |
| ASSIGNED → REMOVED | 已分诊不应异常移出(应 reassign 或 cancel)|

### 10.5 TriageQueue 闭环保证(P0-3)

**5 个生命周期事件的 TriageQueue 终态**:

| 事件 | TriageQueue 终态 |
|---|---|
| 正常完成(分诊成功,ReceptionQueue 流程完成)| ASSIGNED(保留 7 天【待确认】)|
| 异常移出(分诊员主动)| REMOVED |
| Appointment 取消(IN_POOL 时)| CANCELLED |
| Appointment 取消(ASSIGNED 时)| CANCELLED |
| Visit 取消(随 Appointment 取消)| CANCELLED |

**S1-166 保证**:不会出现"Appointment 已取消,Visit 已取消,TriageQueue 仍 IN_POOL / ASSIGNED 活着"的情况。

---

## §11 AppointmentSchedule 模型(本版 P1-1)

### 11.1 业务定位

AppointmentSchedule = **排班实例**。每条记录代表"某医生在某日期的整体排班",**不**是模板,也不是配置。

**S1-166 明确**:**不是配置对象,不是模板对象,是排班实例**。

### 11.2 Schedule 生命周期(3 状态)

| 状态码 | 英文 | 显示名 | 含义 |
|---:|---|---|---|
| 010 | ACTIVE | 生效中 | 排班正常,可接受预约 |
| 020 | INACTIVE | 未生效 | 排班已配置但未到生效日 / 已被医生下班 |
| 030 | CANCELLED | 已取消 | 排班被整体取消 |

### 11.3 Schedule 状态迁移

| From | To | 触发 | 角色 |
|---|---|---|---|
| (none) | ACTIVE | schedule/create(默认生效) | 管理员 |
| (none) | INACTIVE | schedule/create(预排)| 管理员 |
| ACTIVE | INACTIVE | 医生下班 / 临时停诊 | 管理员 / 系统 |
| ACTIVE | CANCELLED | 整体取消 | 管理员 |
| INACTIVE | ACTIVE | 排班生效 | 系统(0:00 定时)|

### 11.4 Schedule 与 Slot 关系

- Schedule 1:N Slot
- Schedule.status = CANCELLED → 所有 Slot 软删(booking 状态不删除已预约)
- Schedule.status = INACTIVE → Slot 不可被新预约(已预约 Appointment 不变)

---

## §12 AppointmentSlot 模型(本版 P1-2)

### 12.1 业务定位

AppointmentSlot = Schedule 的子项,代表"某医生某天某时段的容量单元"。

### 12.2 Slot 生命周期(3 状态)

| 状态码 | 英文 | 显示名 | 含义 |
|---:|---|---|---|
| 010 | AVAILABLE | 可预约 | 剩余容量 > 0,接受新预约 |
| 020 | FULL | 已满 | bookedCount >= capacity,不接受新预约 |
| 030 | CLOSED | 已关闭 | 管理员手动关闭 / Schedule 取消 / 跨日失效 |

### 12.3 Slot 容量规则(P1-2 关键修正)

**S1-166 决定**:**DRAFT Appointment 不占容量,CONFIRMED 才占容量**。

| 事件 | bookedCount 变化 | Slot 状态变化 |
|---|---|---|
| schedule/create | 0 | AVAILABLE |
| appointment/create(DRAFT)| **不变** | AVAILABLE |
| appointment/confirm | **+1** | AVAILABLE → AVAILABLE(若仍 < capacity)/ FULL(若 = capacity)|
| appointment/cancel(CONFIRMED)| **-1** | FULL → AVAILABLE(若 bookedCount < capacity)|
| appointment/reschedule | 原 Slot -1,新 Slot +1 | 双向 |
| arrival/checkin(CONFIRMED)| **不变** | 不变 |
| NO_SHOW | **-1** | 视剩余容量 |
| schedule/cancel | 不变 | 所有 Slot → CLOSED |
| slot/close | 不变 | 该 Slot → CLOSED(已预约 Appointment 不变)|

**S1-165 错误**:S1-165 §3.2.1 DRAFT 写"AppointmentSlot.bookedCount +1",意味着 DRAFT 也占容量。**S1-166 修正**:DRAFT 不占容量,只有 CONFIRMED 占。这避免 DRAFT 长期挂起导致容量浪费。

### 12.4 Slot 关闭与已预约 Appointment

- Slot 关闭(CLOSED)后,已有 Appointment 不变,仍可正常签到 / 分诊 / 接诊
- Slot 关闭后,只拒绝新预约(appointment/create)

### 12.5 Slot 状态机

| From | To | 触发 |
|---|---|---|
| (none) | AVAILABLE | schedule/create |
| AVAILABLE | FULL | bookedCount == capacity |
| AVAILABLE | CLOSED | slot/close / schedule/cancel |
| FULL | AVAILABLE | bookedCount < capacity(由 cancel / reschedule / NO_SHOW 触发)|
| FULL | CLOSED | slot/close / schedule/cancel |
| CLOSED | (无) | 终态 |

### 12.6 Slot 非法迁移

- CLOSED → *:终态(本 Slot 不再接受新预约,但已预约 Appointment 不受影响)
- AVAILABLE → AVAILABLE:自循环(无意义)
- FULL → FULL:自循环

---

## §13 WalkIn 模型(本版 P0-4 统一)

### 13.1 WalkIn 业务定义

WalkIn = 现场到店未预约患者。本版明确两种路径。

### 13.2 WalkIn 主路径(P0-4 决定)

```
现场患者到店
  ↓
前台选择"现场建预约"
  ↓
appointment/create (appointmentType = WALK_IN)
  ↓
Appointment (DRAFT) → 立即 confirm
  ↓
Appointment (CONFIRMED)
  ↓
[同日预约规则] → 立即 WAITING_ARRIVAL(若 slotDate == today)
[次日预约] → 0:00 定时 → WAITING_ARRIVAL
  ↓
arrival/checkin(现场执行)
  ↓
Appointment (ARRIVED) + Visit (CHECKED_IN, appointmentId 非空) + TriageQueue (IN_POOL, appointmentId 非空)
  ↓
正常分诊 / 叫号 / 接诊流程
```

**S1-166 关键决策**:
- WalkIn 主路径**走正常 Appointment 流程**
- Visit.appointmentId **非空**(因为有 WALK_IN Appointment)
- TriageQueue.appointmentId **非空**

### 13.3 WalkIn Fallback 路径(异常 / 紧急)

```
现场患者到店 + 前台系统异常 / 患者紧急需要立即就诊
  ↓
后台特殊入口:visit/create-direct
  ↓
Visit (CHECKED_IN, appointmentId = NULL) + TriageQueue (IN_POOL, appointmentId = NULL)
  ↓
正常分诊 / 叫号 / 接诊流程
  ↓
(可选)事后 appointment/create-associate 关联事后补录的 Appointment
```

**S1-166 关键决策**:
- Fallback 路径是**异常 / 紧急**通道
- **3 个 FK 允许 NULL**:visit.appointmentId / triageQueue.appointmentId / receptionQueue.appointmentId
- WalkIn 主路径下这 3 个 FK **不为 NULL**(因为有 WALK_IN Appointment)
- Fallback 路径下**只为 NULL**且需特殊权限 + 审计

### 13.4 NULL 允许性统一表(P0-4 解决)

| 关系 | 主路径(WALK_IN Appointment)| Fallback 路径(visit/create-direct)|
|---|:-:|:-:|
| visit.appointmentId | NOT NULL | **NULL** |
| triageQueue.appointmentId | NOT NULL | **NULL** |
| receptionQueue.appointmentId | NOT NULL | **NULL** |

**S1-166 解决矛盾**:模型允许 NULL,但主路径强制 NOT NULL。关系图(§6.1)已标 ✓ 允许 NULL,且说明"NULL 仅限 WalkIn Fallback"。

### 13.5 WalkIn 字段标识

| 对象 | 字段 | 主路径 | Fallback 路径 |
|---|---|---|---|
| Appointment | appointmentType | WALK_IN | N/A(无 Appointment) |
| Appointment | appointmentSource | FRONT(前台)| N/A |
| Visit | visitType | WALK_IN | WALK_IN |
| Visit | appointmentId | Appointment.id | **NULL** |
| TriageQueue | appointmentId | Appointment.id | **NULL** |
| ReceptionQueue | appointmentId | Appointment.id | **NULL** |

### 13.6 WalkIn Fallback 的业务必要性

**S1-166 论证**(P0-4 要求"为什么必须支持"):

1. **系统故障场景**:appointment/create 接口不可用(网络/数据库故障),但患者已在现场等待
2. **紧急就诊场景**:患者突发不适,需立即入诊,不允许有"先建预约再签到"的延迟
3. **离线应急场景**:前台断网时仍需记录就诊(事后补录 Appointment)

**结论**:Fallback 是**真实业务需求**,不是过度设计。模型必须支持。

### 13.7 WalkIn 关闭条件

| 业务动作 | WalkIn 主路径 | WalkIn Fallback |
|---|---|---|
| 分诊 | triage/assign | triage/assign |
| 接诊 | 正常 | 正常 |
| 完成 | 正常 | 正常 |
| 取消 | appointment/cancel | **不支持取消**(Visit 直接走 CANCELLED,无 Appointment 可取消)|

---

## §14 取消流程(本版 P0-2 唯一规则)

### 14.1 Appointment 取消规则统一表

**全文唯一**来源:§7.2。

| 状态 | 允许取消? | 操作人 | 释放 Slot | 同步 Visit | 同步 TriageQueue | 同步 ReceptionQueue |
|---|:-:|---|:-:|---|---|---|
| DRAFT | ✓ | 预约员 | ✓ | – | – | – |
| CONFIRMED | ✓ | 预约员 / 患者 | ✓ | – | – | – |
| WAITING_ARRIVAL | ✓ | 预约员 / 患者 | ✓ | – | – | – |
| ARRIVED | ✓ | 仅管理员 | ✓ | → CANCELLED | → CANCELLED | – |
| TRIAGE_WAITING | ✓ | 仅管理员 | ✓ | → CANCELLED | → CANCELLED | – |
| WAITING_RECEPTION | ✓ | 仅管理员 | ✓ | → CANCELLED | → CANCELLED | → 软删 |
| CALLED | ✗ | – | – | – | – | – |
| IN_CONSULTATION | ✗ | – | – | – | – | – |
| 终态 | ✗ | – | – | – | – | – |

### 14.2 取消时序

```
[管理员 / 预约员 / 患者]
  ↓
appointment/cancel API
  ↓
  事务边界:
  1. Appointment.status = CANCELLED
  2. Appointment.cancelledAt = now
  3. Appointment.cancelReason = input
  4. (若 ARRIVED/TRIAGE_WAITING/WAITING_RECEPTION)
     Visit.status = CANCELLED, Visit.cancelledAt = now
     TriageQueue.status = CANCELLED(若 IN_POOL 或 ASSIGNED)
     ReceptionQueue 软删(WAITING_RECEPTION 时)
  5. AppointmentSlot.bookedCount -1
  6. 写 AuditLog
  ↓
  全部成功 → 返回 success
  失败 → 回滚,返回错误
```

### 14.3 CALLED / IN_CONSULTATION 状态如何"取消"业务

| 状态 | 实际"取消"路径 |
|---|---|
| CALLED | 必须先 queue/skip → WAITING_RECEPTION,再 appointment/cancel |
| IN_CONSULTATION | 必须先 queue/complete → COMPLETED 或 visit/transfer → TRANSFERRED,**不能直接取消** |

**S1-166 关键决策**:**不**通过"IN_CONSULTATION → CANCELLED"实现,必须经过业务动作。

---

## §15 爽约流程(本版 P0-12 唯一规则)

### 15.1 NO_SHOW 触发规则(全文唯一)

| 规则 | 内容 |
|---|---|
| **WAITING_ARRIVAL 进入时机** | (1) slotDate == today:appointment/confirm 后立即进入 (2) slotDate > today:系统每日 0:00 扫描(scheduleDate == today)进入 |
| **NO_SHOW 判定时机** | 系统每日 0:00 扫描,若 slotDate < today 且 status == WAITING_ARRIVAL → NO_SHOW |
| **跨日预约** | slotDate > today 的预约,在 slotDate 当天 0:00 自动 → WAITING_ARRIVAL |
| **凌晨创建预约** | slotDate == today 时,confirm 立即 → WAITING_ARRIVAL,不等待定时 |
| **次日 0:00 之前签到** | 正常流程,WAITING_ARRIVAL → ARRIVED |

**S1-165 错误**:
- S1-165 §3.2.3 写"预约日 23:59 后 NO_SHOW"
- S1-165 §3.2.11 写"系统任务每日 0:00 扫描"

**S1-166 唯一规则**:**0:00 扫描(scheduleDate < today)**(避免"23:59 误判凌晨刚签到的患者"边界问题)。

### 15.2 爽约时序

```
[系统每日 0:00]
  ↓
扫描 Appointment:
  WHERE status = WAITING_ARRIVAL
    AND slotDate < today
  ↓
  对每条记录:
    1. Appointment.status = NO_SHOW
    2. AppointmentSlot.bookedCount -1
    3. 写 AuditLog (标 "system_no_show")
  ↓
  返回执行结果
```

---

## §16 改约流程

### 16.1 改约语义

改约 = 创建新 Appointment + 旧 Appointment → RESCHEDULED 终态

### 16.2 改约时序

```
[预约员 / 患者]
  ↓
appointment/reschedule API
  input: 原 appointmentId + 新 scheduleId + 新 slotId + 新日期 + 新时间
  ↓
  事务边界:
  1. 校验原 Appointment 状态(必须 CONFIRMED 或 WAITING_ARRIVAL)
  2. 校验新 Slot 容量
  3. 旧 Appointment.status = RESCHEDULED
  4. 旧 Appointment.rescheduledAt = now
  5. 旧 AppointmentSlot.bookedCount -1
  6. 创建新 Appointment(DRAFT)→ 立即 confirm(CONFIRMED)
  7. 新 Appointment.rescheduledFromId = 旧 Appointment.id
  8. 新 AppointmentSlot.bookedCount +1
  9. 写 AuditLog
  ↓
  全部成功 → 返回新 Appointment
  失败 → 回滚,返回错误
```

### 16.3 改约规则

| 原状态 | 允许改约? | 备注 |
|---|:-:|---|
| DRAFT | ✗ | 草稿直接 delete 重建 |
| CONFIRMED | ✓ | 正常改约 |
| WAITING_ARRIVAL | ✓ | 正常改约 |
| ARRIVED / TRIAGE_WAITING / WAITING_RECEPTION | ✗ | 已签到,需先 cancel 再 create |
| CALLED / IN_CONSULTATION | ✗ | 接诊中禁止改约 |
| 终态 | ✗ | 不可改约 |

---

## §17 跳过 / 重新叫号流程(P0-6 唯一规则)

### 17.1 三种迁移区分

| 行为 | 触发 | 状态迁移 | 角色 | API |
|---|---|---|---|---|
| **自动超时** | CALLED 状态 5 分钟无应答 | CALLED → **WAITING** | 系统(定时)| –(定时任务)|
| **主动跳过** | 医生手动 skip | CALLED → **SKIPPED** | 医生 | queue/skip |
| **重新叫号** | 医生重叫过号患者 | SKIPPED → CALLED | 医生 | queue/recall |

### 17.2 Appointment 联动

| 行为 | Appointment 状态 | Visit 状态 |
|---|---|---|
| CALLED 自动超时 → WAITING | **Appointment 保持 CALLED** | 不变 |
| CALLED 主动 skip → SKIPPED | Appointment → **WAITING_RECEPTION** | 不变 |
| SKIPPED 重新叫号 → CALLED | Appointment → CALLED | 不变 |

**S1-166 关键决策**:**CALLED 自动超时和主动 skip 走不同状态**(S1-165 §9.2 错误地让两种都 → SKIPPED)。**自动超时只动 ReceptionQueue,不动 Appointment**;**主动 skip 同步动 Appointment**。

### 17.3 跳过时序

```
queue/skip
  ↓
  事务边界:
  1. ReceptionQueue.status = SKIPPED
  2. ReceptionQueue.skippedTimes +1
  3. Appointment.status = WAITING_RECEPTION
  4. 写 AuditLog
  ↓
  返回 success
```

### 17.4 重新叫号时序

```
queue/recall
  ↓
  事务边界:
  1. ReceptionQueue.status = CALLED
  2. ReceptionQueue.calledAt = now
  3. Appointment.status = CALLED
  4. 写 AuditLog
  ↓
  返回 success
```

---

## §18 分诊 / 重新分诊流程

### 18.1 分诊时序

```
[分诊员 / 系统自动]
  ↓
triage/assign API
  input: triageQueueId + targetEmployeeId + targetConsultRoomId
  ↓
  事务边界:
  1. TriageQueue.status = ASSIGNED
  2. TriageQueue.assignedAt = now
  3. TriageQueue.assignedBy = currentUserId
  4. TriageQueue.employeeId = input
  5. TriageQueue.consultRoomId = input
  6. 创建 ReceptionQueue(status = ASSIGNED)
  7. Appointment.status = WAITING_RECEPTION
  8. 写 AuditLog
  ↓
  返回 success
```

### 18.2 重新分诊时序(尚未进入诊中)

```
[分诊员]
  ↓
triage/reassign API
  input: visitId + newEmployeeId + newConsultRoomId
  ↓
  事务边界:
  1. 校验当前状态:Appointment = WAITING_RECEPTION(未进入诊中)
  2. ReceptionQueue 软删(原诊室记录作废)
  3. TriageQueue.status = IN_POOL(回退)
  4. TriageQueue.employeeId = null
  5. TriageQueue.consultRoomId = null
  6. Appointment.status = TRIAGE_WAITING
  7. Visit.status = CHECKED_IN(回退)
  8. 写 AuditLog(reassignReason)
  ↓
  然后由分诊员重新 triage/assign(进入新诊室)
```

### 18.3 已进入诊中的转诊(§20 / P0-8)

见 §20 换诊室 / 转诊流程。

---

## §19 医生暂停接诊流程(P0-7 强制解耦)

### 19.1 关键设计原则

**"医生暂停接诊"只影响 Doctor 自身的工作状态,不影响任何 Visit / Appointment / ReceptionQueue 状态**。

### 19.2 Doctor 状态(本版新增,本节限定)

| 状态 | 含义 |
|---|---|
| WORKING | 接诊中 |
| PAUSED | 暂停接诊(医生主动)|
| OFFLINE | 下班 / 离线 |

**S1-166 决定**:Doctor 状态字段属于 Doctor 实体(本版未深入设计),不在本节展开。**关键点**:Doctor.PAUSED **不会**触发任何 Visit / Appointment / ReceptionQueue 状态变更。

### 19.3 暂停时序

```
[医生]
  ↓
doctor/pause API
  ↓
  事务边界:
  1. Doctor.status = PAUSED
  2. 写 AuditLog
  ↓
  当前 IN_CONSULTATION 患者状态:保持 IN_CONSULTATION(不变)
  当前 CALLED 患者状态:保持 CALLED(不变)
  当前 WAITING 患者:医生已不在,系统可选提示,但不强制改状态
  ↓
  返回 success
```

### 19.4 IN_CONSULTATION 患者的处理(暂停期间)

医生 PAUSED 后,IN_CONSULTATION 患者**保留 IN_CONSULTATION 状态**,等待医生:
- A. 医生 resume → 继续接诊
- B. 医生 queue/complete → 正常完成(COMPLETED)
- C. 医生 visit/transfer → 转诊(TRANSFERRED,见 §20)
- D. 患者取消 → 仅管理员可取消(无 visit 主动取消路径)

### 19.5 暂停对队列的影响

| 队列状态 | 暂停后行为 |
|---|---|
| WAITING | 保留,医生 resume 后可继续叫号 |
| CALLED | 保留(可由医生主动 skip 或 5 分钟超时回退) |
| IN_CONSULTATION | 保留,医生 resume 后继续 |

**S1-165 错误**:S1-165 §11.3 / §19 把"暂停"等价于"完成"(IN_CONSULTATION → COMPLETED),**S1-166 强制解耦**。

### 19.6 恢复时序

```
[医生]
  ↓
doctor/resume API
  ↓
  1. Doctor.status = WORKING
  2. 写 AuditLog
  ↓
  所有队列状态保持(已 CALL 的继续接诊逻辑)
```

---

## §20 换诊室 / 转诊流程(P0-8 拆分场景)

### 20.1 场景分类

| 场景 | 当前状态 | 操作 | 是否允许 |
|---|---|---|:-:|
| **A. 尚未进入诊中,改诊室** | Appointment = WAITING_RECEPTION / ReceptionQueue = ASSIGNED / Visit = TRIAGED | triage/reassign | ✓ |
| **B. 已进入诊中,医生主动转诊** | Appointment = IN_CONSULTATION / Visit = IN_CONSULTATION / ReceptionQueue = IN_CONSULTATION | visit/transfer | ✓ |
| **C. 医生更换(同诊室换医生)** | 任意 | 重新分诊 / 转诊 | 见下方 |
| **D. 异常中断(医生突然无法继续)** | 任意 | 视具体情况 | 见下方 |

### 20.2 场景 A:尚未进入诊中,改诊室(已有 triage/reassign)

见 §18.2。**不通过非法状态回退**实现。

### 20.3 场景 B:已 IN_CONSULTATION 医生转诊(P0-8 新增 visit/transfer)

```
[当前医生(转出方)]
  ↓
visit/transfer API
  input: visitId + newEmployeeId(可选)+ newConsultRoomId(可选)
  ↓
  事务边界:
  1. 校验 Visit.status = IN_CONSULTATION
  2. Visit.status = TRANSFERRED(终态)
  3. Visit.transferredAt = now
  4. ReceptionQueue.status = DONE(标 "transferred")
  5. ReceptionQueue.completedAt = now
  6. Appointment.status = WAITING_RECEPTION(回退到待叫号,本预约的下一诊次)
  7. 写 AuditLog(transferReason + transferredFromEmployeeId)
  8. 创建新 TriageQueue(status = IN_POOL, employeeId = newEmployeeId, consultRoomId = newConsultRoomId)
  9. 创建新 ReceptionQueue(status = ASSIGNED)
  10. Appointment.status 再次更新为 WAITING_RECEPTION(原已是)
  11. Visit 同 patientId 的新 Visit 创建(若需要)
  ↓
  旧 Visit 保留 TRANSFERRED 终态
  新 TriageQueue 进入新诊室分配
```

**S1-166 关键设计**:
- **不通过"IN_CONSULTATION → TRIAGED → CHECKED_IN"非法回退**
- **通过新状态 TRANSFERRED 表达"本次诊次因转诊结束"**
- Appointment 回到 WAITING_RECEPTION(本预约还在,可继续)
- 新 TriageQueue 创建,新诊次开始

### 20.4 场景 C:医生更换

| 子场景 | 处理 |
|---|---|
| 尚未接诊(WAITING / CALLED)| reassign(场景 A)|
| 正在接诊(IN_CONSULTATION)| transfer(场景 B)|
| 已完成(COMPLETED)| 不允许改派 |

### 20.5 场景 D:异常中断

| 中断原因 | 处理 |
|---|---|
| 医生临时有事(短时间) | 暂停(§19),不强制完成 |
| 医生请假(长时间) | 等待 resume 或 transfer(场景 B) |
| 医生离职 | 管理员通知所有 IN_CONSULTATION 患者,转诊或取消 |
| 系统故障 | 恢复后由管理员补录,暂不强制完成 |

### 20.6 与 S1-165 的关键区别

| 维度 | S1-165 | S1-166 |
|---|---|---|
| IN_CONSULTATION → TRIAGED | 允许(非法回退) | ✗ 禁止 |
| 转诊实现 | 状态回退 | 新增 TRANSFERRED 终态 |
| 多诊次 | 不支持(单 Visit) | 支持(新 Visit / 新 TriageQueue)|
| Appointment 状态 | 不变(陷入 IN_CONSULTATION) | → WAITING_RECEPTION |

---

## §21 核心业务动作与事务边界(P0-5 修正)

### 21.1 事务边界原则(本版)

**"同一个业务动作中,对该动作涉及的对象必须在同一事务边界内完成一致提交;不参与该动作的对象不得被强制同步改变状态。"**

### 21.2 关键业务动作事务边界

| 业务动作 | 涉及对象 | 事务内变更 | 不参与 |
|---|---|---|---|
| **appointment/create** | Appointment + Slot | (1) Appointment INSERT (2) Slot.bookedCount **不变**(DRAFT 不占容量) | Visit, TriageQueue, ReceptionQueue |
| **appointment/confirm** | Appointment + Slot | (1) Appointment.status = CONFIRMED (2) Slot.bookedCount +1 | Visit, TriageQueue, ReceptionQueue |
| **appointment/cancel** | Appointment + (条件)Visit + TriageQueue + (条件)ReceptionQueue + Slot | 见 §14.2 时序 | 不涉及的对象不动 |
| **appointment/reschedule** | 旧 Appointment + 新 Appointment + 旧 Slot + 新 Slot | 见 §16.2 时序 | Visit, TriageQueue, ReceptionQueue(若新 Appointment 重新走签到流程才创建) |
| **arrival/checkin** | Appointment + Visit + TriageQueue | (1) Appointment = ARRIVED → TRIAGE_WAITING (2) Visit = CHECKED_IN (3) TriageQueue = IN_POOL (4) Slot 不变(已 CONFIRMED 不再扣)| ReceptionQueue(尚未分诊,无)|
| **triage/assign** | TriageQueue + ReceptionQueue + Appointment | (1) TriageQueue = ASSIGNED (2) ReceptionQueue INSERT(ASSIGNED) (3) Appointment = WAITING_RECEPTION | Visit(保持 CHECKED_IN)|
| **triage/reassign** | TriageQueue + ReceptionQueue + Appointment + Visit | (1) ReceptionQueue 软删 (2) TriageQueue = IN_POOL (3) Appointment = TRIAGE_WAITING (4) Visit = CHECKED_IN | Slot |
| **queue/start(进入诊室)** | ReceptionQueue | (1) ReceptionQueue = WAITING | Appointment, Visit |
| **queue/call** | ReceptionQueue + Appointment | (1) ReceptionQueue = CALLED (2) Appointment = CALLED | Visit(保持 TRIAGED)|
| **queue/skip** | ReceptionQueue + Appointment | (1) ReceptionQueue = SKIPPED (2) Appointment = WAITING_RECEPTION | Visit(保持 TRIAGED)|
| **queue/recall** | ReceptionQueue + Appointment | (1) ReceptionQueue = CALLED (2) Appointment = CALLED | Visit |
| **queue/start-consult(开始接诊)** | ReceptionQueue + Appointment + Visit | (1) ReceptionQueue = IN_CONSULTATION (2) Appointment = IN_CONSULTATION (3) Visit = IN_CONSULTATION | – |
| **queue/complete** | ReceptionQueue + Appointment + Visit | (1) ReceptionQueue = DONE (2) Appointment = COMPLETED (3) Visit = COMPLETED | – |
| **visit/transfer** | Visit + ReceptionQueue + Appointment + 新 TriageQueue + 新 ReceptionQueue | 见 §20.3 时序 | Slot |
| **doctor/pause** | Doctor(单独实体) | (1) Doctor.status = PAUSED | Appointment, Visit, TriageQueue, ReceptionQueue(全部不变)|
| **doctor/resume** | Doctor | (1) Doctor.status = WORKING | 同上 |

### 21.3 事务边界原则

1. **每条 API 对应一个事务边界**
2. **事务内变更的对象必须全部成功,否则回滚**
3. **事务外的对象绝对不修改**(即使看起来"应该同步")
4. **跨事务的数据一致性通过事件 / 消息 / 定时任务补偿**

### 21.4 S1-165 vs S1-166 关键区别

| 维度 | S1-165 | S1-166 |
|---|---|---|
| 叫号(q/call)| "四对象原子同步" | ReceptionQueue + Appointment 同步,Visit 不动 |
| 接诊(start-consult)| 强制 Visit 同步 | Visit 必须同步(确实在本动作)|
| 取消(cancel)| "Visit / TriageQueue / ReceptionQueue 都同步" | 视当前状态条件同步 |
| 暂停(pause)| 强制 IN_CONSULTATION → COMPLETED | 完全不动 Visit |

---

## §22 所有合法状态迁移总表

### 22.1 Appointment 合法迁移

| From | To | 触发 | API |
|---|---|---|---|
| (none) | DRAFT | appointment/create | appointment/create |
| DRAFT | CONFIRMED | appointment/confirm | appointment/confirm |
| DRAFT | CANCELLED | appointment/cancel | appointment/cancel |
| CONFIRMED | WAITING_ARRIVAL | 系统条件(同日 / 0:00 定时)| – |
| CONFIRMED | CANCELLED | appointment/cancel | appointment/cancel |
| CONFIRMED | RESCHEDULED | appointment/reschedule | appointment/reschedule |
| WAITING_ARRIVAL | ARRIVED | arrival/checkin | arrival/checkin |
| WAITING_ARRIVAL | CANCELLED | appointment/cancel | appointment/cancel |
| WAITING_ARRIVAL | RESCHEDULED | appointment/reschedule | appointment/reschedule |
| WAITING_ARRIVAL | NO_SHOW | 系统 0:00 扫描 | – |
| ARRIVED | TRIAGE_WAITING | 系统(随 checkin)| – |
| ARRIVED | CANCELLED | appointment/cancel(管理员)| appointment/cancel |
| TRIAGE_WAITING | WAITING_RECEPTION | triage/assign | triage/assign |
| TRIAGE_WAITING | CANCELLED | appointment/cancel(管理员)| appointment/cancel |
| WAITING_RECEPTION | CALLED | queue/call | queue/call |
| WAITING_RECEPTION | TRIAGE_WAITING | triage/reassign | triage/reassign |
| WAITING_RECEPTION | CANCELLED | appointment/cancel(管理员)| appointment/cancel |
| CALLED | IN_CONSULTATION | queue/start-consult | queue/start-consult |
| CALLED | WAITING_RECEPTION | queue/skip | queue/skip |
| CALLED | WAITING_RECEPTION | 系统(超时 5min)| – |
| IN_CONSULTATION | COMPLETED | queue/complete | queue/complete |
| IN_CONSULTATION | WAITING_RECEPTION | visit/transfer | visit/transfer(转诊,Appointment 回退)|

### 22.2 Visit 合法迁移

| From | To | 触发 |
|---|---|---|
| (none) | DRAFT | arrival/checkin 触发 |
| DRAFT | CHECKED_IN | 系统(随 checkin)|
| CHECKED_IN | TRIAGED | triage/assign |
| CHECKED_IN | CANCELLED | appointment/cancel 同步 |
| TRIAGED | IN_CONSULTATION | queue/start-consult |
| TRIAGED | CHECKED_IN | triage/reassign |
| TRIAGED | CANCELLED | appointment/cancel 同步 |
| IN_CONSULTATION | COMPLETED | queue/complete |
| IN_CONSULTATION | TRANSFERRED | visit/transfer(新增) |

### 22.3 TriageQueue 合法迁移

| From | To | 触发 |
|---|---|---|
| (none) | IN_POOL | arrival/checkin 触发 |
| IN_POOL | ASSIGNED | triage/assign |
| IN_POOL | REMOVED | triage/remove |
| IN_POOL | CANCELLED | appointment/cancel 同步(新增) |
| ASSIGNED | IN_POOL | triage/reassign |
| ASSIGNED | CANCELLED | appointment/cancel 同步(新增) |

### 22.4 ReceptionQueue 合法迁移

| From | To | 触发 |
|---|---|---|
| (none) | ASSIGNED | triage/assign 触发 |
| ASSIGNED | WAITING | queue/start(进入诊室)|
| ASSIGNED | (软删) | appointment/cancel / reassign 同步 |
| WAITING | CALLED | queue/call |
| WAITING | (软删) | appointment/cancel 同步 |
| CALLED | IN_CONSULTATION | queue/start-consult |
| CALLED | WAITING | 系统超时(5min)自动回退 |
| CALLED | SKIPPED | queue/skip |
| SKIPPED | CALLED | queue/recall |
| SKIPPED | (软删) | appointment/cancel 同步 |
| IN_CONSULTATION | DONE | queue/complete |
| IN_CONSULTATION | (软删) | visit/transfer 同步 |

### 22.5 Schedule 合法迁移

| From | To | 触发 |
|---|---|---|
| (none) | ACTIVE / INACTIVE | schedule/create |
| ACTIVE | INACTIVE | doctor 下班 / 临时停诊 |
| ACTIVE | CANCELLED | schedule/cancel |
| INACTIVE | ACTIVE | 系统 0:00 定时 |

### 22.6 Slot 合法迁移

| From | To | 触发 |
|---|---|---|
| (none) | AVAILABLE | schedule/create |
| AVAILABLE | FULL | bookedCount == capacity |
| AVAILABLE | CLOSED | slot/close / schedule/cancel |
| FULL | AVAILABLE | bookedCount < capacity |
| FULL | CLOSED | slot/close / schedule/cancel |

---

## §23 所有非法状态迁移总表

### 23.1 Appointment 非法迁移

- 任意 → * from 终态
- CALLED → CANCELLED(P0-2 修正:必须先 skip)
- IN_CONSULTATION → CANCELLED(P0-2 修正:必须先 complete 或 transfer)
- IN_CONSULTATION → 任何 except COMPLETED / WAITING_RECEPTION(转诊)
- DRAFT → WAITING_ARRIVAL
- DRAFT → ARRIVED
- CONFIRMED → ARRIVED
- CONFIRMED → WAITING_RECEPTION
- WAITING_ARRIVAL → TRIAGE_WAITING
- ARRIVED → WAITING_RECEPTION
- 任意 → DRAFT

### 23.2 Visit 非法迁移

- 终态(COMPLETED / TRANSFERRED / CANCELLED)→ *
- IN_CONSULTATION → TRIAGED(P0-8 修正:不可回退)
- IN_CONSULTATION → CHECKED_IN
- IN_CONSULTATION → CANCELLED
- DRAFT → IN_CONSULTATION
- DRAFT → TRIAGED
- CHECKED_IN → COMPLETED

### 23.3 TriageQueue 非法迁移

- 终态(REMOVED / CANCELLED)→ *
- IN_POOL → IN_POOL
- ASSIGNED → REMOVED

### 23.4 ReceptionQueue 非法迁移

- DONE → *
- ASSIGNED → CALLED
- WAITING → IN_CONSULTATION
- SKIPPED → IN_CONSULTATION
- IN_CONSULTATION → WAITING
- IN_CONSULTATION → SKIPPED

### 23.5 Schedule / Slot 非法迁移

- CLOSED → *
- CANCELLED(Schedule)→ *
- AVAILABLE → AVAILABLE
- FULL → FULL

---

## §24 异常场景

| # | 场景 | 流程 | 关键 API |
|---:|---|---|---|
| 1 | 正常预约到完成 | 12 状态完整流转 | appointment/* + arrival/* + triage/* + queue/* |
| 2 | 预约后取消(DRAFT/CONFIRMED/WAITING_ARRIVAL)| → CANCELLED | appointment/cancel |
| 3 | 预约后改约 | 旧 → RESCHEDULED + 新 CONFIRMED | appointment/reschedule |
| 4 | 预约患者未到院 | WAITING_ARRIVAL 0:00 后 NO_SHOW | 系统任务 |
| 5 | WalkIn 患者 | 主路径或 Fallback | appointment/create(WALK_IN)或 visit/create-direct |
| 6 | 医生临时停诊 | doctor/pause(不变其他状态)| doctor/pause / doctor/resume |
| 7 | 诊室临时更换(未进诊中)| triage/reassign | triage/reassign |
| 8 | 转诊(已进诊中)| visit/transfer | visit/transfer(本版新增) |
| 9 | 患者过号 | CALLED → SKIPPED → CALLED | queue/skip + queue/recall |
| 10 | 患者超时 | CALLED 5min → WAITING | 系统任务 |
| 11 | 多医生多诊室并行 | 每个诊室独立 ReceptionQueue | queue/start(per doctor)|
| 12 | 跨日预约 | slotDate > today,0:00 定时 → WAITING_ARRIVAL | 系统任务 |
| 13 | 凌晨创建预约 | slotDate == today,confirm 立即 → WAITING_ARRIVAL | appointment/confirm |
| 14 | 系统故障 WalkIn | visit/create-direct | visit/create-direct |
| 15 | 医生更换 | 未进诊中 reassign / 已进诊中 transfer | triage/reassign / visit/transfer |

---

## §25 ID 与命名规范(P0-9 / P0-10 / P0-11)

### 25.1 ID 命名统一

| ID 字段 | 数据库规范名 | 业务别称 | 备注 |
|---|---|---|---|
| 预约 ID | `appointmentId` | – | OptFlow 设计 |
| Visit ID | `visitId` | – | OptFlow 设计 |
| 分诊队列 ID | `triageQueueId` | – | OptFlow 设计 |
| 接诊队列 ID | `receptionQueueId` | – | OptFlow 设计 |
| 排班 ID | `scheduleId` | – | OptFlow 设计 |
| 时段 ID | `slotId` | – | OptFlow 设计 |
| **公司 ID** | **`companyId`** | – | **复用 S1-158** |
| **客户 ID** | **`customerId`** | – | **复用 S1-147** |
| **患者 ID** | **`patientId`** | – | **复用 S1-147** |
| **员工 ID** | **`employeeId`** | 业务称"医生 doctor"| **统一用 employeeId(P0-10)** |
| **诊室 ID** | **`consultRoomId`** | 业务称"接诊诊室 consultation room"| **统一用 consultRoomId(P0-11)** |
| 大屏 ID | `bigScreenId` | – | 复用 S1-153 |

### 25.2 业务称谓与 DDL 字段分离(P0-10 / P0-11)

- **数据库 / DDL 字段**:统一 `employeeId`、`consultRoomId`
- **业务称谓 / 界面**:可称"医生 doctor" / "诊室 consultation room",仅为业务语义
- **API 设计**:DB 层 / DTO 层用 `employeeId`,UI 层可翻译为 "doctorName"

### 25.3 ID 类型规范

| 对象 | ID 类型 | 来源 |
|---|---|---|
| **Appointment** | UUID v4 | 【OptFlow设计】新建实体 |
| **AppointmentSchedule** | UUID v4 | 【OptFlow设计】新建实体 |
| **AppointmentSlot** | UUID v4 | 【OptFlow设计】新建实体 |
| **TriageQueue** | UUID v4 | 【OptFlow设计】新建实体 |
| **ReceptionQueue** | UUID v4 | 【OptFlow设计】新建实体 |
| **Visit** | UUID v4 | 【OptFlow设计】新建实体 |
| **Company** | 沿用现有 OptFlow 类型(具体【待确认】)| 复用 S1-158,**不变更** |
| **Customer** | 沿用现有 OptFlow 类型(具体【待确认】)| 复用 S1-147,**不变更** |
| **Patient** | 沿用现有 OptFlow 类型(具体【待确认】)| 复用 S1-147,**不变更** |
| **Employee** | 沿用现有 OptFlow 类型(具体【待确认】)| 复用 S1-158,**不变更** |
| **ConsultRoom** | 沿用现有 OptFlow 类型(具体【待确认】)| 复用 S1-158,**不变更** |
| **BigScreen** | 沿用现有 OptFlow 类型(具体【待确认】)| 复用 S1-153,**不变更** |

**S1-165 错误**:S1-165 §10.1 把 6 个复用实体 ID 全部写成 UUID,无证据。S1-166 修正:**新建实体讨论 UUID,复用实体沿用【待确认】**。

### 25.4 ID 互斥规则(本版)

| 禁止 | 原因 |
|---|---|
| 用 appointmentId 代替 visitId | 语义不同 |
| 用 visitId 代替 receptionQueueId | 语义不同 |
| 用 appointmentId 代替 receptionQueueId | 语义不同 |
| 用原系统 appointId(18 命中)直接当 Appointment.id | 原系统命名误导 |
| 用 employeeId 字段但业务称"doctorId" | DDL 统一 employeeId |
| 用 consultRoomId 字段但业务称"consultationRoomId" | DDL 统一 consultRoomId |
| 复用实体 ID 类型被强制改成 UUID | 无证据,沿用现有 |

---

## §26 与现有 OptFlow 实体的复用边界

| 实体 | 复用方式 | 边界 |
|---|---|---|
| Company | companyId 作用域 | 所有新实体必带 companyId |
| Customer / Patient | patientId 主引用 | Appointment → patientId,Visit → patientId,不直接存 customerId |
| Employee | employeeId 主引用 | 接诊医生字段用 employeeId,role=DOCTOR 仅业务层 |
| ConsultRoom | consultRoomId 主引用 | 诊室字段用 consultRoomId |
| BigScreen | bigScreenId 引用 | 大屏显示数据,叫号触发 |
| MedicalRecord | medicalRecordId 引用 | Visit → COMPLETED 后触发创建 |

**S1-166 强调**:不复用原系统 `appointId`、`appointmentId`(4 命中)等历史 ID。

---

## §27 当前仍待确认事项(本版)

| # | 问题 | 影响 | 决定 / 建议 |
|---:|---|---|---|
| 1 | CALLED 超时阈值(默认 5 分钟)| 业务 | 默认 5 分钟,可配置 |
| 2 | Slot 满员阈值计算时机(confirm / checkin)| 容量 | confirm 时 +1 |
| 3 | WalkIn 预约是否需选医生 | 业务 | 可不选,分诊时定 |
| 4 | 大屏显示几诊室 | 性能 | 默认 8 个,可配置 |
| 5 | 改约时是否通知原医生 | 业务 | 默认否 |
| 6 | 爽约(NO_SHOW)是否通知患者 | 业务 | 默认否 |
| 7 | 临时停诊是否通知排队的患者 | 业务 | 默认否,医生自行处理 |
| 8 | TriageQueue.ASSIGNED 保留时长(默认 7 天)| 数据 | 默认 7 天 |
| 9 | Schedule 跨日(00:00)自动 INACTIVE 规则 | 业务 | 默认生效日 23:59 后 INACTIVE |
| 10 | 复用实体 ID 具体类型 | DDL | 待 S1-167 之前确认现有 OptFlow Schema |
| 11 | Customer / Patient 创建策略(WalkIn)| 业务 | 默认先建 Customer 再建 Patient |
| 12 | 改约次数限制 | 业务 | 默认无限制 |
| 13 | 转诊(visit/transfer)是否需要新 Appointment | 业务 | 默认不需要,沿用原 Appointment |
| 14 | WalkIn Fallback 权限 | 业务 | 仅管理员 + 审计 |
| 15 | 预约号显示规则(序号 vs 数字)| UI | 默认 `HH:MM-HH:MM 序号` |

---

## §28 冻结结论

### 28.1 P0 关闭情况

| P0 | 状态 |
|---|:-:|
| P0-1 Visit 状态数 | ✓ 关闭(统一 6+1 状态,本版 7 状态含 TRANSFERRED)|
| P0-2 Appointment 取消规则 | ✓ 关闭(§7.2 唯一规则表)|
| P0-3 TriageQueue 闭环 | ✓ 关闭(增 CANCELLED 状态)|
| P0-4 WalkIn NULL 关系 | ✓ 关闭(§13.4 统一 NULL 表)|
| P0-5 事务边界 | ✓ 关闭(§21 修正为"业务动作边界")|
| P0-6 CALLED 超时 | ✓ 关闭(§17 三种迁移区分)|
| P0-7 医生停诊 ≠ 患者完成 | ✓ 关闭(§19 完全解耦)|
| P0-8 换诊室 / 转诊 | ✓ 关闭(§20 拆分场景 + TRANSFERRED)|
| P0-9 复用 ID 类型 | ✓ 关闭(§25.3 区分新建 / 复用)|
| P0-10 employeeId / doctorId | ✓ 关闭(§25 统一 employeeId)|
| P0-11 consultRoomId 统一 | ✓ 关闭(§25 统一 consultRoomId)|
| P0-12 NO_SHOW 时间规则 | ✓ 关闭(§15 唯一规则)|
| P0-13 同日预约闭环 | ✓ 关闭(§14.1 + §15.1)|

### 28.2 P1 关闭情况

| P1 | 状态 |
|---|:-:|
| P1-1 Schedule 生命周期 | ✓ 关闭(§11 三状态 ACTIVE/INACTIVE/CANCELLED)|
| P1-2 Slot 生命周期 | ✓ 关闭(§12 三状态 + DRAFT 不占容量)|
| P1-3 queue/start 语义 | ✓ 关闭(§21 区分进入诊室 vs 开始接诊)|
| P1-4 Patient / Customer 关系 | ✓ 关闭(§6.3 单向 patientId)|

---

## §29 全局一致性检查(24 项,本版)

| # | 检查项 | 状态 |
|---:|---|:-:|
| 1 | 对象数量一致 | **PASS** - 14 候选 = 6 复用 + 6 实体 + 2 枚举 |
| 2 | 状态数量一致 | **PASS** - 12 + 7 + 4 + 6 = 29 状态(本版明确)|
| 3 | 状态命名一致 | **PASS** - DRAFT/CONFIRMED/CALLED 等统一 |
| 4 | 所有状态均有进入路径 | **PASS** - 28 个状态全部有入口 |
| 5 | 所有非终态均有退出路径 | **PASS** - 8 个非终态全部有合法 next |
| 6 | 无死状态 | **PASS** - 7 个终态有明确终止条件 |
| 7 | 无互相矛盾的迁移规则 | **PASS** - §7 / §9 / §22 / §23 完全一致 |
| 8 | Appointment 取消规则唯一 | **PASS** - §7.2 唯一表,§14 / §22 / §23 全部对齐 |
| 9 | Visit 取消规则唯一 | **PASS** - §8.3 唯一表 |
| 10 | TriageQueue 可闭环 | **PASS** - §10 4 状态全部有终态(IN_POOL→ASSIGNED→DONE/REMOVED/CANCELLED)|
| 11 | ReceptionQueue 可闭环 | **PASS** - §9 6 状态全部有终态 |
| 12 | WalkIn 可闭环 | **PASS** - §13 主路径 + Fallback 都闭环 |
| 13 | 同日预约可闭环 | **PASS** - §14.1 + §15.1 唯一规则 |
| 14 | NO_SHOW 可闭环 | **PASS** - §15 唯一规则(0:00 扫描)|
| 15 | 改约可闭环 | **PASS** - §16 旧 RESCHEDULED + 新 CONFIRMED |
| 16 | Slot 容量可闭环 | **PASS** - §12.3 唯一规则(DRAFT 不占)|
| 17 | CALLED 超时规则唯一 | **PASS** - §17.1 三种迁移明确区分 |
| 18 | 医生暂停与患者完成已解耦 | **PASS** - §19 doctor/pause 完全不动 Visit |
| 19 | 换诊室逻辑无非法状态回退 | **PASS** - §20 通过 TRANSFERRED 终态实现 |
| 20 | employeeId / doctorId 唯一 | **PASS** - §25 统一 employeeId |
| 21 | consultRoomId 命名唯一 | **PASS** - §25 统一 consultRoomId |
| 22 | ID 类型没有无证据断言 | **PASS** - §25.3 复用实体标【待确认】|
| 23 | 事务边界描述准确 | **PASS** - §21 每个 API 明确事务内对象 |
| 24 | 原系统事实 / OptFlow设计 / 待确认 三者未混淆 | **PASS** - 全文标签严格区分 |

**全部 24 项 PASS**。

---

## §30 最终判断

**【S1-166 模型状态:PASS】**

### 30.1 冻结依据

1. **13 个 P0 全部关闭**
2. **4 个 P1 全部关闭**
3. **24 项全局一致性检查全部 PASS**
4. **无前后矛盾**:§7 / §9 / §22 / §23 全部规则一致
5. **无建议规则与强制规则混淆**:每条规则明确强制 / 建议 / 待确认
6. **未把 E 级业务推断伪装成原系统事实**:标签严格区分
7. **未凭空创造数据库字段类型**:复用实体 ID 类型标【待确认】

### 30.2 与 S1-165 关系

- S1-165 保留作为"上一版基线"和"历史证据"
- S1-166 正式覆盖 S1-165 的状态机 / 取消规则 / 关系 / ID 命名
- S1-165 §3-§9 大部分需以 S1-166 为准
- S1-165 §10 角色矩阵可继续参考

### 30.3 后续可进入的阶段

| 阶段 | 内容 | 依赖 |
|---|---|---|
| **S1-167** | API 字段级设计(35 个 API 的 Request/Response/错误码) | 228 冻结 |
| **S1-168** | 14 个对象 DDL 草案 | 228 冻结 + 复用实体 ID 类型确认 |
| **S1-169** | 异常场景 × 状态机 单元测试矩阵 | 228 + S1-167 |
| **S1-170** | 前后端接口契约(OpenAPI 3.0) | S1-167 |

**S1-166 冻结后,可进入 S1-167(API 字段级设计)+ S1-168(DDL 草案)**,前提是先确认复用实体 ID 类型(§27 待确认 #10)。
