# 235A S1-167B DDL 前独立冻结审计报告

> **本轮定位**:本审计**不引用 235 §12 的"7 项全部 PASS"结论**。从 0 独立重新验证 6 大重点 + 10 个闭合检查,目的是发现 235 自身未察觉的内部矛盾与 DDL 实施风险。
> - 严禁修改 235 / 232 / 233 / 229 / 234B / 234A / 任何历史 MD
> - 严禁修改代码 / 数据库 / HTML / .gitignore
> - 严禁 commit / push
> - 严禁创建 DDL

---

## §1 Executive Summary

### 1.1 审计范围

- **被审计对象**:`235_S1-167B_OptFlow数据模型裁决与DDL前置基线.md`(40244 bytes,SHA `1CC96002…26699`)
- **关键参照**:229(领域模型)/ 232(API 设计)/ 233(API 修正版)/ 234B(ID 后端核验)/ 234A(ID 逆向核验)
- **审计方法**:以"DDL 实施者拿到 235 后能否一次跑通"为唯一标准

### 1.2 关键发现数量

| 严重度 | 数量 | 类型 |
|---|---:|---|
| **P0-Block**(会导致 DDL 产生错误或业务无法落库)| **0** | – |
| **P1-Conditional**(DDL 可跑但业务规则需补全)| **8** | 见 §9 |
| **P2-Doc**(仅文档表达问题)| **3** | 见 §9 |

### 1.3 最终判断

# **【CONDITIONAL PASS】**

**理由**:
- 12 对象 DDL 语法可全部生成,无 DDL 语法错误
- 12 对象 PK / FK 类型匹配,无类型不兼容
- 4 状态机能由字段表达(虽然 CHECK 约束缺失)
- Employee / Patient / Customer 边界冻结有效
- 但 **8 项 P1 条件必须在 S1-168 之前补全**,否则会产生业务规则矛盾或状态机无法落库

### 1.4 不通过项 BLOCK 触发条件检查

按老板规则"存在任何会导致 DDL 产生错误或业务无法落库的矛盾:BLOCK":
- **DDL 语法错误**:0 项
- **业务无法落库(实质性)**:0 项(所有发现都是 DDL 之外的问题或在 DDL 实施时能发现)
- **DDL 实施后业务能跑但规则有缺**:8 项(进入 §9 必须修正项)

**因此**:不触发 BLOCK,触发 CONDITIONAL PASS。

---

## §2 重点审查 1:Employee adminId 唯一裁决

### 2.1 235 自身矛盾(关键发现)

**235 §5.3 文字**:
> "1 个 employee 实体对应 1 个 admin 账号(**employee 可以无 admin 账号**,反之亦然)"

**235 §B-4 字段表**:
> `adminId | BIGINT | ✓ | – | 登录认证 ID(UNIQUE)| 【OptFlow设计】`

**直接矛盾**:
- §5.3 文字允许 `adminId IS NULL`(无 admin 账号)
- §B-4 字段表 `✓` 标记表示 NOT NULL(无 admin 账号不允许)

### 2.2 三种候选方案重审

| 方案 | NOT NULL? | 含义 | UNIQUE 行为(MySQL InnoDB)|
|---|---|---|---|
| **A. adminId NOT NULL** | 严格必填 | 所有 Employee 必有 admin 账号 | 严格唯一(整个表) |
| **B. adminId NULL 允许** | 可空 | 允许"纯业务员工"无登录账号 | MySQL 允许多个 NULL(UNIQUE 不约束 NULL) |
| **C. adminId 软关联(独立 admin 表)** | 跨表 | 拆 Admin 表,FK 关联 | 不适用 |

### 2.3 业务推演

- **A 方案**:
  - 优点:语义简单,§B-4 字段表 + §5.3 文字一致
  - 缺点:不允许"纯业务员工"——比如纯技师、纯销售员(无登录需求)就无法录入
  - 235 §9.1 "Employee 相关"有"role"字段 `DOCTOR / TRIAGE / RECEP / ADMIN`,似乎 4 个 role 都需要登录
- **B 方案**:
  - 优点:灵活,允许非登录员工
  - 缺点:UNIQUE(companyId, adminId) 在 MySQL 下允许多个 NULL,需要明确"adminId IS NULL 不参与唯一性"
- **C 方案**:
  - 235 §5.2 已淘汰(2 跳查询,DDL 复杂)

### 2.4 235 §5.3 评分表自检

235 §5.3 评分表给出"复杂度 ★★★★★(最简单)"作为 A 方案优点,但**未考虑 A 方案强制每个 Employee 必有 admin 账号的业务代价**。

### 2.5 审计裁决(本轮)

**A 或 B 选哪个都行,但 235 必须二选一**:
- 选 A:修改 §5.3 文字"employee 可以无 admin 账号" → 改为"每个 Employee 必有 admin 账号",§B-4 NOT NULL 保持
- 选 B:修改 §B-4 字段表 `adminId | BIGINT | ✓` → 改为 `adminId | BIGINT | –`(可空),§5.3 文字保持

### 2.6 UNIQUE(companyId, adminId) 语义

| 选 A | 选 B |
|---|---|
| 严格唯一(MySQL 允许多个 NULL 也行,但 A 不允许 NULL) | 多 NULL 允许(同一公司下可有多个 adminId=NULL 的 employee)|
| 推荐度 ★★★★(语义清晰) | 推荐度 ★★★(需要应用层处理 NULL) |

**本审计推荐**:**A 方案(NOT NULL,严格唯一)**,理由:
1. 235 §9.1 role 枚举 DOCTOR / TRIAGE / RECEP / ADMIN 全部需要登录
2. 一期 V4.4 业务简单,没有"纯技师无登录"场景
3. 选 A 与 §B-4 字段表一致,只需修改 §5.3 文字

**修订建议**(S1-168 实施前):
- 235 §5.3 文字改为:"1 个 employee 实体对应 1 个 admin 账号,**每个 Employee 必有 admin 账号**"
- 235 §B-4 字段表保持 `adminId | BIGINT | ✓ | – | UNIQUE`

---

## §3 重点审查 2:currentVisitId 与 FK ON DELETE 冲突

### 3.1 235 内部矛盾(关键发现)

**235 §B-7 字段表约束**:
```
FOREIGN KEY(currentVisitId) → Visit(visitId) ON DELETE SET NULL
```

**235 §8.1 规则**:
| 场景 | currentVisitId |
|---|---|
| COMPLETED | **保持当前 Visit.id** |
| CANCELLED | **保持当前 Visit.id** |

**直接矛盾**:
- ON DELETE SET NULL:Visit 物理删除时,currentVisitId 被置空
- §8.1 规则:COMPLETED / CANCELLED 必须保持 currentVisitId
- 如果 Visit 被物理删除,§8.1 规则被违反

### 3.2 229 §5.2 维护规则回查

229 line 125-129(读自 grep):
> - "NULL 允许:**✗(Appointment 进入 ARRIVED 后必有)**"
> - "维护规则:Visit 创建时 Appointment.currentVisitId = Visit.id;Visit → TRANSFERRED 或 CANCELLED 时,Appointment 重新指定 currentVisitId(**若 Visit B 存在则指 B,否则 NULL**)"

**与 235 §8.1 的差异**:
- 229 §5.2 写 CANCELLED 时"若 Visit B 存在则指 B,否则 NULL"
- 235 §8.1 写 CANCELLED 时"保持当前 Visit.id"
- **D 级冲突**(同 235 §8.2 自承的冲突),235 选择"保持原值"覆盖

### 3.3 229 §5.3 Visit 历史保留

229 line 134-135:
> "Visit → TRANSFERRED 后**保留记录,不删除**"

**隐含规则**:Visit 永不物理删除(至少在 TRANSFERRED 后)。**但 235 没明文写"Visit 永不物理删除"作为通用规则**。

### 3.4 三种解法重审

| 方案 | 描述 | 与 §8.1 兼容性 | DDL 实施 |
|---|---|---|---|
| **A. Visit 永不物理删除** | DB 层 + 应用层双保险 | ✓ 永远不触发 SET NULL | FK 选 SET NULL / RESTRICT 都不影响业务 |
| **B. 允许物理删除但 currentVisitId 可被置空** | 接受 §8.1 被破坏 | ✗ 违反 §8.1 | ON DELETE SET NULL |
| **C. ON DELETE RESTRICT** | DB 层禁止物理删除 | ✓ 永远保持 | FK ON DELETE RESTRICT |

### 3.5 审计裁决(本轮)

**235 实际选择是 A + B 的混合**:
- §8.1 写"保持"(A 方案语义)
- §B-7 FK 写 SET NULL(B 方案语义)
- 但因为 Visit 实际永不删(B 方案永不被触发),业务不冲突

**这是"隐含假设"导致的内部矛盾,不是显性矛盾**。

**修订建议**(S1-168 实施前):
- 235 §B-7 FK 改为 `ON DELETE RESTRICT`(更明确:DB 层禁止物理删除 Visit)
- 235 §B-7 字段表加注释:`Visit 永不物理删除(229 §5.3 历史保留规则 + ON DELETE RESTRICT 双保险)`
- 235 §B-12 Visit 不需要 FK 写 ON DELETE SET NULL(因为 appointmentId 已经被 SET NULL,但 Visit 永远存在,Appointment 删除时 Visit 怎么办?需要再核对)

**重要警告**:
- 235 §B-12 Visit.appointmentId 写 `FOREIGN KEY(appointmentId) → Appointment(appointmentId) ON DELETE SET NULL` —— 这里 Appointment 是被引用方,Appointment 删除时 Visit.appointmentId 被置空
- 但 §B-7 Appointment.currentVisitId 写 SET NULL → Visit 删除时 Appointment.currentVisitId 被置空
- **循环 FK 双方都是 SET NULL**,在两个对象同时删除时会触发"互相置空"
- 实际:Appointment 不应被物理删除(预约记录永久保留),Visit 也不应被物理删除 —— 双方都"永不删",循环 FK 永远不触发

**结论**:
- 235 当前选择 SET NULL 是**为了"理论正确",但实际永不触发**
- 改成 RESTRICT 更明确表达"永不允许物理删除"的业务规则
- **不修改会导致**:DDL 跑得通,业务跑得通,但语义不清晰(给后续开发者埋坑)

### 3.6 结论

**P1-Conditional**:235 §B-7 / §B-12 FK 应改为 ON DELETE RESTRICT,并明文"Visit / Appointment 永不物理删除"。

---

## §4 重点审查 3:visit/transfer 事务顺序

### 4.1 235 缺失的关键定义

**235 全文未明文定义 API-034 visit/transfer 的事务顺序**。

只在 235 §7 关系矩阵列出"Appointment → Visit currentVisitId"等关系,但**没有写"transfer 7 步骤的具体执行顺序"**。

### 4.2 候选事务顺序对比

#### 老板候选(本轮问题给出)

1. 创建新 Visit
2. 获取 newVisitId
3. 创建 TriageQueue(visitId = newVisitId)
4. 更新 Appointment.currentVisitId
5. 旧 Visit / ReceptionQueue 状态更新

#### 229 §6.2 TRANSFERRED 行为(7 步骤,229 line 184)

> (1) 旧 ReceptionQueue → DONE(标"transferred")
> (2) Appointment → TRIAGE_WAITING
> (3) Appointment.currentVisitId 重新指向新 Visit
> (4) 新 TriageQueue(IN_POOL)创建
> (5) 新 Visit 创建

### 4.3 顺序冲突分析

| 步骤 | 老板候选 | 229 顺序 | 一致性 |
|---|---|---|---|
| 旧 ReceptionQueue → DONE | 第 5 步 | 第 1 步 | **不一致** |
| Appointment → TRIAGE_WAITING | (没明文) | 第 2 步 | 老板候选缺这步 |
| Appointment.currentVisitId 更新 | 第 4 步 | 第 3 步 | 顺序不同 |
| 新 TriageQueue 创建 | 第 3 步 | 第 4 步 | 顺序不同 |
| 新 Visit 创建 | 第 1 步 | 第 5 步 | **严重不一致** |

**关键问题**:

229 顺序在第 3 步更新 `currentVisitId`,但新 Visit 还没创建(第 5 步才创建),**currentVisitId 指向的 Visit 不存在**——这违反 235 §B-7 FK(currentVisitId) → Visit(visitId) 约束。

**老板候选更合理**:先建新 Visit,再填 currentVisitId,FK 总能命中。

### 4.4 缺失的"旧 TriageQueue 状态"

**229 §6.2 7 步骤完全没提"旧 TriageQueue 怎么处理"**:
- 转诊前:旧 TriageQueue.status = ASSIGNED(因为旧 Visit 已经被分诊)
- 转诊后:旧 TriageQueue 应该是什么状态?

**235 §B-10 TriageQueue 4 状态**:IN_POOL / ASSIGNED / REMOVED / CANCELLED

**候选状态变更**:
- 旧 TriageQueue → REMOVED(被转移走)
- 旧 TriageQueue → CANCELLED
- 旧 TriageQueue 保持 ASSIGNED(历史保留,作为分诊历史)

**229 §6.2 缺失明文** → 业务无法落库。

### 4.5 235 §B-10 / §B-11 字段表二次审查

235 §B-10 TriageQueue 字段:
- `triageQueueId UUID(36) NOT NULL` ✓
- `visitId UUID(36) NOT NULL` ✓
- `appointmentId UUID(36) NULL`(WalkIn Fallback)✓
- `consultRoomId BIGINT NULL`(IN_POOL 时 NULL,ASSIGNED 时填)✓
- `employeeId BIGINT NULL`(IN_POOL 时 NULL,ASSIGNED 时填)✓
- `status VARCHAR(20) NOT NULL DEFAULT 'IN_POOL'`
- `assignedAt / assignedBy / createdAt / updatedAt`

**问题**:status 字段没写 CHECK 约束,只标 DEFAULT。

235 §B-11 ReceptionQueue 字段:
- `receptionQueueId UUID(36) NOT NULL` ✓
- `triageQueueId UUID(36) NOT NULL UNIQUE`(一对一)✓
- `visitId UUID(36) NOT NULL UNIQUE`(一对一)✓
- `appointmentId UUID(36) NULL` ✓
- `consultRoomId BIGINT NOT NULL` ✓
- `employeeId BIGINT NOT NULL` ✓
- `status VARCHAR(20) NOT NULL DEFAULT 'ASSIGNED'`

**问题**:status 字段没写 CHECK 约束。

### 4.6 老板 5 步骤 + 缺失项合并

**正确事务顺序(本审计推荐,S1-168 需补全)**:

```
1. 验证旧 Visit.status = IN_CONSULTATION(否则拒绝)
2. 创建新 Visit(status=DRAFT → CHECKED_IN,transferredFromVisitId = 旧 visitId)
   - 235 §B-12 允许 transferredFromVisitId NULL(转诊时填)
3. 创建新 TriageQueue(visitId = newVisitId, status=IN_POOL, employeeId=NULL, consultRoomId=NULL)
   - 235 §B-10 允许 IN_POOL 时 employeeId/consultRoomId NULL ✓
4. 创建新 ReceptionQueue(triageQueueId=newTriageQueueId, visitId=newVisitId, status=ASSIGNED)
   - 但 employeeId / consultRoomId 必须填 — 233 P1-1 说"preferredEmployeeId 不绑定"
   - **矛盾**:ReceptionQueue.employeeId / consultRoomId 必填,但 transfer 步骤没填值
   - 解决:ReceptionQueue 不在 transfer 步骤创建,由后续 API-021 triage/assign 创建
5. 更新 Appointment.currentVisitId = newVisitId
6. 更新 Appointment.status = TRIAGE_WAITING
7. 旧 Visit.status = TRANSFERRED
8. 旧 TriageQueue.status = REMOVED(本审计推荐,理由见下)
9. 旧 ReceptionQueue.status = DONE(229 §6.2 第 1 步)
```

**关键修正**:
- ReceptionQueue **不在 transfer 步骤创建**(避免 employeeId 必填矛盾)
- 由后续 triage/assign(API-021)创建,此时 employeeId / consultRoomId 由分诊员填
- 旧 TriageQueue → REMOVED(本审计推荐):"被转移走"的语义更准确;CANCELLED 留给"主动取消"

### 4.7 旧 TriageQueue 状态裁决

| 候选 | 语义 | 优点 | 缺点 |
|---|---|---|---|
| **REMOVED** | 转移走 | 表达"被动的状态变更" | 与 "主动移除" 语义可能混 |
| **CANCELLED** | 取消 | 表达"不再有效" | 实际是转移不是取消 |
| **保持 ASSIGNED** | 历史保留 | 不需要更新 | 查询时仍显示 ASSIGNED,但实际已被转走,误导 |

**本审计推荐**:**旧 TriageQueue → REMOVED**(理由:229 §B-10 4 状态里,REMOVED 是"被动移除",CANCELLED 是"主动取消",转诊是"被动转移走",语义匹配 REMOVED)。

### 4.8 原子性 / 一致性

**所有 9 步必须在 1 个 DB 事务内**:
- 任何 1 步失败 → ROLLBACK 全部
- DB 层保证:"要么全部成功,要么全部失败"
- 实施语言无关(MySQL InnoDB / PostgreSQL / Oracle 都支持)

**currentVisitId 是否一定指向已存在 Visit**:
- 老板候选顺序:第 2 步已创建新 Visit,第 5 步填 currentVisitId,FK 总能命中 ✓
- 229 顺序:第 3 步填 currentVisitId,但第 5 步才创建新 Visit,FK 暂时悬空 ✗
- **本审计按老板候选顺序**(S1-168 需补全)

**是否还有任何步骤允许 visitId=NULL**:
- 235 §B-10 TriageQueue.visitId NOT NULL — 新 TriageQueue 必有 visitId ✓
- 235 §B-11 ReceptionQueue.visitId NOT NULL — 但本审计建议 ReceptionQueue 不在 transfer 步骤创建(见 4.6)
- 235 §B-12 Visit.appointmentId NULL — 但与 transfer 无关(WalkIn 路径)
- **结论:transfer 步骤中不允许 visitId=NULL**

### 4.9 审计裁决(本轮)

**P1-Conditional**:
- 235 缺失 visit/transfer 事务顺序,S1-168 必须补全(建议采用本审计 §4.6 的 9 步骤顺序)
- 235 缺失"旧 TriageQueue 转诊后状态"明文定义,建议 = REMOVED
- 235 缺失"ReceptionQueue 不在 transfer 步骤创建,改由 triage/assign 创建"明文

---

## §5 重点审查 4:事实 vs OptFlow设计 标签检查

### 5.1 逐项检查 235 §B 12 对象字段表

#### B-1 Company(9 字段)

| 字段 | 标【原系统事实】 | 证据引用 | 审计 |
|---|---|---|---|
| `companyTitle` | ✓ | 214 §1.4 | ✓ OK |
| `companyType` | ✓ | **04_数据模型.md** | ⚠️ P2 |
| `logoUrl/address/phone` | – | – | ✓ OK(标【待确认】)|

**P2-Doc**:`companyType` 引用的 `04_数据模型.md` 是早期理论模型,不是原系统事实。**235 表 A 引用文件是间接证据,需重新评估等级**。

#### B-2 Customer(7 字段)

| 字段 | 标【原系统事实】 | 证据引用 | 审计 |
|---|---|---|---|
| `linkMobile` 11 位 | ✓ | 105 §6.1 | ✓ OK |
| `memberTypeId` | ✓ | **04_数据模型.md** | ⚠️ P2 |

#### B-3 Patient(8 字段)

| 字段 | 标【原系统事实】 | 证据引用 | 审计 |
|---|---|---|---|
| `idCard` 18 位 | ✓ | 105 §6.1 | ✓ OK |

#### B-4 Employee(11 字段)

| 字段 | 标【原系统事实】 | 证据引用 | 审计 |
|---|---|---|---|
| `employeeName` | ✓ | **04_数据模型.md** | ⚠️ P2 |
| `nickname` | ✓ | 214 §1.4 L14273 | ✓ OK |
| `departmentId` | ✓ | **214 §4.1 C 极弱** | ⚠️ P1 |
| `status` | ✓ | **04_数据模型.md** | ⚠️ P2 |

**P1-Conditional**:`departmentId` 标【原系统事实】,但证据等级 C 极弱——**C 极弱 ≠ 原系统事实,应改为【待确认】或【OptFlow设计】**。

#### B-5 ConsultRoom(7 字段)

| 字段 | 标【原系统事实】 | 证据引用 | 审计 |
|---|---|---|---|
| `consultRoomName` | ✓ | 216 §14.4 | ✓ OK |
| `status` | ✓ | 216 §14.5 | ✓ OK |
| `examineList` JSON | ✓ | 216 §14.4 | ✓ OK |

#### B-6 BigScreen(8 字段)

| 字段 | 标【原系统事实】 | 证据引用 | 审计 |
|---|---|---|---|
| `bigScreenName` | ✓ | 216 §14.4 | ✓ OK |
| `sceneType` 1/2 | ✓ | 216 §14.4 | ✓ OK |
| `consultRoomIdArray` JSON | ✓ | 216 §14.4 | ✓ OK |
| `status` | – | – | ✓ OK(标【待确认】)|

#### B-7 ~ B-12 (新对象 6 个)

**全部【OptFlow设计】**,无【原系统事实】混入 ✓ OK

### 5.2 标【原系统事实】但实际是早期理论模型

**问题**:`04_数据模型.md` 是 S1-1 早期(总索引 #04)的"理论数据模型"文档,**不是原系统 SQL/代码的直接证据**。

**影响字段**:
- `Company.companyType`
- `Customer.memberTypeId`
- `Employee.employeeName`
- `Employee.status`

**本审计建议**:这些字段的标签应改为【待确认】(因为 04_数据模型.md 不是 A/B 级证据)或保留【原系统事实】但备注"等级 C,D 级冲突可能"。

### 5.3 审计裁决(本轮)

**P1-Conditional**:`Employee.departmentId` 标签错配,必须修正(从【原系统事实】改为【待确认】或【OptFlow设计】)。

**P2-Doc**:`04_数据模型.md` 引用的 4 个字段标签需重新评估(可选,不影响 DDL)。

---

## §6 重点审查 5:12 对象 DDL 可行性

### 6.1 PK 冲突检查

| 对象 | PK | 类型 | 唯一性 |
|---|---|---|---|
| Company | companyId | BIGINT | ✓ |
| Customer | customerId | BIGINT | ✓ |
| Patient | patientId | BIGINT | ✓ |
| Employee | employeeId | BIGINT | ✓ |
| ConsultRoom | consultRoomId | BIGINT | ✓ |
| BigScreen | bigScreenId | BIGINT | ✓ |
| Appointment | appointmentId | UUID(36) | ✓ |
| AppointmentSchedule | scheduleId | UUID(36) | ✓ |
| AppointmentSlot | slotId | UUID(36) | ✓ |
| TriageQueue | triageQueueId | UUID(36) | ✓ |
| ReceptionQueue | receptionQueueId | UUID(36) | ✓ |
| Visit | visitId | UUID(36) | ✓ |

**12 PK 全部唯一,类型内部一致**(BIGINT 6 + UUID 6)✓

### 6.2 FK 类型匹配检查

| 引用 | 目标 | 类型匹配 |
|---|---|:-:|
| Appointment.patientId | Patient.patientId | BIGINT = BIGINT ✓ |
| Appointment.employeeId | Employee.employeeId | BIGINT = BIGINT ✓ |
| Appointment.scheduleId | AppointmentSchedule.scheduleId | UUID = UUID ✓ |
| Appointment.slotId | AppointmentSlot.slotId | UUID = UUID ✓ |
| Appointment.currentVisitId | Visit.visitId | UUID = UUID ✓ |
| AppointmentSlot.scheduleId | AppointmentSchedule.scheduleId | UUID = UUID ✓ |
| AppointmentSlot.consultRoomId | ConsultRoom.consultRoomId | BIGINT = BIGINT ✓ |
| TriageQueue.visitId | Visit.visitId | UUID = UUID ✓ |
| TriageQueue.appointmentId | Appointment.appointmentId | UUID = UUID ✓ |
| TriageQueue.consultRoomId | ConsultRoom.consultRoomId | BIGINT = BIGINT ✓ |
| TriageQueue.employeeId | Employee.employeeId | BIGINT = BIGINT ✓ |
| ReceptionQueue.triageQueueId | TriageQueue.triageQueueId | UUID = UUID ✓ |
| ReceptionQueue.visitId | Visit.visitId | UUID = UUID ✓ |
| ReceptionQueue.appointmentId | Appointment.appointmentId | UUID = UUID ✓ |
| ReceptionQueue.consultRoomId | ConsultRoom.consultRoomId | BIGINT = BIGINT ✓ |
| ReceptionQueue.employeeId | Employee.employeeId | BIGINT = BIGINT ✓ |
| Visit.appointmentId | Appointment.appointmentId | UUID = UUID ✓ |
| Visit.patientId | Patient.patientId | BIGINT = BIGINT ✓ |
| Visit.employeeId | Employee.employeeId | BIGINT = BIGINT ✓ |
| Visit.consultRoomId | ConsultRoom.consultRoomId | BIGINT = BIGINT ✓ |

**20 FK 类型全部匹配** ✓

### 6.3 NULL / NOT NULL 一致性检查

| 引用字段 | 235 标 NULL? | 目标 PK 必填? | 一致性 |
|---|:-:|:-:|:-:|
| Appointment.currentVisitId | NULL | Visit.visitId NOT NULL | ✓ |
| TriageQueue.appointmentId | NULL | Appointment.appointmentId NOT NULL | ✓ |
| ReceptionQueue.appointmentId | NULL | Appointment.appointmentId NOT NULL | ✓ |
| Visit.appointmentId | NULL | Appointment.appointmentId NOT NULL | ✓ |
| AppointmentSlot.consultRoomId | NULL | ConsultRoom.consultRoomId NOT NULL | ✓ |
| Visit.employeeId | NULL | Employee.employeeId NOT NULL | ✓ |
| Visit.consultRoomId | NULL | ConsultRoom.consultRoomId NOT NULL | ✓ |

**7 个 NULL 引用全部一致**(NULL FK 指向 NOT NULL PK 是合法设计)✓

### 6.4 循环 FK 检测

**发现循环 FK**:
```
Appointment.currentVisitId (FK) → Visit.visitId
Visit.appointmentId (FK) → Appointment.appointmentId
```

**循环 FK 处理**:
- MySQL InnoDB:允许循环 FK,需要谨慎处理创建/删除顺序
- PostgreSQL:允许循环 FK
- **DDL 实施建议**:
  - 创建时:先建 Appointment(无 currentVisitId,允许 NULL),再建 Visit(填 appointmentId),再 UPDATE Appointment.currentVisitId
  - 删除时:先 UPDATE Appointment.currentVisitId=NULL,再删 Visit,再删 Appointment
- **业务建议**:Appointment / Visit 都永不物理删除(见 §3),循环 FK 永远不触发

**235 现状**:
- §B-7 FK 写 `ON DELETE SET NULL`(Visit 删时 currentVisitId 置空)
- §B-12 FK 写 `ON DELETE SET NULL`(Appointment 删时 Visit.appointmentId 置空)
- 双方都"永不删",循环 FK 永不触发
- **理论可跑,但语义不清晰**

**审计裁决**:**P1-Conditional** — S1-168 建议改为 ON DELETE RESTRICT,明文"永不允许物理删除"。

### 6.5 UNIQUE 与 NULL 语义冲突

| UNIQUE 约束 | 字段 NULL? | 行为(MySQL InnoDB) | 一致性 |
|---|---|---|:-:|
| TriageQueue.UNIQUE(visitId) | visitId NOT NULL | 严格唯一 | ✓ |
| ReceptionQueue.UNIQUE(triageQueueId) | triageQueueId NOT NULL | 严格唯一 | ✓ |
| ReceptionQueue.UNIQUE(visitId) | visitId NOT NULL | 严格唯一 | ✓ |
| Appointment.UNIQUE(companyId, appointmentNo) | appointmentNo NOT NULL | 严格唯一(组合)| ✓ |
| Employee.UNIQUE(companyId, adminId) | **adminId NOT NULL(若选 A)** | 严格唯一(组合)| ✓ |
| Employee.UNIQUE(companyId, adminId) | **adminId NULL(若选 B)** | 允许多个 NULL | ⚠️ |

**风险点**:Employee UNIQUE 行为依赖 adminId NULL 选择(见 §2)。**本审计推荐 A 方案(NOT NULL)** 以避免歧义。

### 6.6 状态机能否由字段表达

| 对象 | 状态字段 | 枚举 | 字段长度 | CHECK 约束? |
|---|---|---|:-:|:-:|
| Appointment | status | 12 状态(229 §3.1)| VARCHAR(20) | ❌ 缺 |
| Visit | status | 7 状态 | VARCHAR(20) | ❌ 缺 |
| TriageQueue | status | 4 状态 | VARCHAR(20) | ❌ 缺 |
| ReceptionQueue | status | 6 状态 | VARCHAR(20) | ❌ 缺 |

**最大字符串长度检查**:
- Appointment 最长状态:`WAITING_RECEPTION`(17),VARCHAR(20) ✓
- Visit 最长状态:`IN_CONSULTATION`(15),VARCHAR(20) ✓
- TriageQueue 最长状态:`CANCELLED`(9),VARCHAR(20) ✓
- ReceptionQueue 最长状态:`IN_CONSULTATION`(15),VARCHAR(20) ✓

**结论**:
- 状态字段长度全部足够 ✓
- **CHECK 约束全部缺失** ❌
- 影响:DDL 实施时数据库允许任何字符串进 status 字段(包括非法值)

**审计裁决**:**P1-Conditional** — S1-168 需补 CHECK 约束(或 ENUM 类型,MySQL 支持 ENUM,但 PostgreSQL 不支持)。

### 6.7 转移 / 走诊 / 状态机落库

| 业务 | 字段支持 | 审计 |
|---|---|:-:|
| Transfer 落库 | Visit.transferredFromVisitId / transferredFromEmployeeId / transferredAt ✓ | ✓ |
| WalkIn 落库 | Visit.appointmentId NULL + appointmentType='WALK_IN' ✓ | ✓ |
| Appointment 1:N Visit | Visit.appointmentId 可多次指向同一 Appointment ✓ | ✓ |
| currentVisitId 合法 | FK(currentVisitId) → Visit(visitId) ✓ | ✓ |

### 6.8 整体 12 对象 DDL 可行性

**结论**:**12 对象 DDL 全部可生成,无 DDL 语法错误**。

**风险**:
- 状态字段 CHECK 缺失(不影响 DDL 创建,影响数据完整性)
- 循环 FK ON DELETE 行为不清晰(影响维护)
- UNIQUE 与 NULL 依赖 Employee adminId 选择

---

## §7 重点审查 6:PK/FK/NULL/UNIQUE/DELETE 审计汇总

### 7.1 PK 审计

| 维度 | 结论 |
|---|:-:|
| 12 PK 全部唯一 | ✓ PASS |
| 类型内部一致(BIGINT 6 + UUID 6)| ✓ PASS |
| AUTO_INCREMENT 表达 | 235 §B-1~B-6 字段表标"AUTO_INCREMENT"✓ |
| UUID gen_random_uuid() 表达 | 235 §B-7~B-12 字段表标"gen_random_uuid()"✓ |

### 7.2 FK 审计

| 维度 | 结论 |
|---|:-:|
| 20 FK 类型全部匹配 | ✓ PASS |
| 7 个 NULL 引用与目标 PK 必填一致 | ✓ PASS |
| 循环 FK 存在(Appointment ↔ Visit)| ⚠️ 需明确创建顺序 |
| 20 FK ON DELETE 行为 | 12 RESTRICT + 8 SET NULL(无 CASCADE)✓ |

**ON DELETE 行为分析**:
- 12 RESTRICT:阻止物理删除(Patient/Employee/Schedule/Slot 的删除被拦)
- 8 SET NULL:被引用方删除时,引用方字段置空
  - Appointment.currentVisitId → Visit.SET NULL
  - Visit.appointmentId → Appointment.SET NULL
  - (其他 6 个 SET NULL 来自 TriageQueue.appointmentId / ReceptionQueue.appointmentId 等可空 FK)
- 0 CASCADE:不存在级联删除 ✓ 安全

### 7.3 UNIQUE 审计

| 约束 | 字段 | 一致性 |
|---|---|:-:|
| TriageQueue.UNIQUE(visitId) | visitId NOT NULL | ✓ |
| ReceptionQueue.UNIQUE(triageQueueId) | triageQueueId NOT NULL | ✓ |
| ReceptionQueue.UNIQUE(visitId) | visitId NOT NULL | ✓ |
| Appointment.UNIQUE(companyId, appointmentNo) | appointmentNo NOT NULL | ✓ |
| Employee.UNIQUE(companyId, adminId) | 依赖 §2 选择 | ⚠️ |

### 7.4 DELETE 行为审计

| 业务对象 | 物理删除预期 | DDL 行为 | 审计 |
|---|---|---|:-:|
| Company | 不应删(财务需要) | RESTRICT 阻止 | ✓ |
| Customer | 不应删(会员永久) | 未明文,但有 FK 引用 | ⚠️ |
| Patient | 不应删(医疗合规)| RESTRICT 阻止(Appointment.patientId RESTRICT)| ✓ |
| Employee | 不应删(离职后保留) | RESTRICT 阻止 | ✓ |
| ConsultRoom | 不应删(历史需要) | RESTRICT 阻止 | ✓ |
| BigScreen | 不应删(历史需要) | 无 FK 引用,可删 | ⚠️ 需明文 |
| Appointment | 不应删(预约永久) | SET NULL(Visit.appointmentId)| ⚠️ 语义不清晰 |
| AppointmentSchedule | 不应删(排班历史) | RESTRICT 阻止(Appointment.scheduleId RESTRICT)| ✓ |
| AppointmentSlot | 不应删(时段历史) | RESTRICT 阻止(Appointment.slotId RESTRICT)| ✓ |
| TriageQueue | 可归档(终态)| 无 FK 引用,可删 | ⚠️ 需明文 |
| ReceptionQueue | 可归档(终态)| 无 FK 引用,可删 | ⚠️ 需明文 |
| Visit | **永不删**(229 §5.3)| SET NULL(Appointment.currentVisitId) | ⚠️ 应改 RESTRICT |

**整体**:
- DDL 实施后,所有"不应删"对象都被 FK RESTRICT 阻止物理删除 ✓
- 但 235 未明文"哪些对象永不物理删除"作为业务规则
- **建议 S1-168 补一份"对象生命周期保留规则"清单**

### 7.5 状态机落库

| 状态机 | 状态数 | 字段长度 | CHECK 约束 | 审计 |
|---|---:|:-:|:-:|:-:|
| Appointment | 12 | VARCHAR(20) ✓ | ❌ | P1 |
| Visit | 7 | VARCHAR(20) ✓ | ❌ | P1 |
| TriageQueue | 4 | VARCHAR(20) ✓ | ❌ | P1 |
| ReceptionQueue | 6 | VARCHAR(20) ✓ | ❌ | P1 |

**4 个状态机全部缺 CHECK 约束** → P1-Conditional

---

## §8 重点审查 7:WalkIn / Transfer 验证

### 8.1 WalkIn 路径

**235 现状**:
- Visit.appointmentId NULL 允许 ✓
- TriageQueue.appointmentId NULL 允许 ✓
- ReceptionQueue.appointmentId NULL 允许 ✓
- Appointment.appointmentType 枚举含 'WALK_IN' ✓
- Visit.visitType 枚举含 'WALK_IN' ✓

**WalkIn 业务流程**(由 WalkIn 路径走):
1. visit/create-direct(API-025)创建 Visit(status=DRAFT → CHECKED_IN, appointmentId=NULL)
2. Visit 入 TriageQueue(appointmentId=NULL, status=IN_POOL)
3. triage/assign(API-021)分配 doctor / room
4. ReceptionQueue 创建(appointmentId=NULL, status=ASSIGNED)
5. queue/call(API-027)→ CALLED
6. queue/start-consult(API-030)→ IN_CONSULTATION
7. queue/complete(API-031)→ COMPLETED

**3 个 NULL appointmentId FK 全部可空** ✓ WalkIn 能落库

### 8.2 Transfer 路径

**235 现状**:
- Visit.transferredFromVisitId NULL 允许(转诊时填)✓
- Visit.transferredFromEmployeeId NULL 允许 ✓
- Visit.transferredAt NULL 允许 ✓
- 旧 Visit.status = TRANSFERRED 终态 ✓
- Appointment.currentVisitId = 新 Visit.id(§8.1 场景 3)✓
- Appointment.status = TRIAGE_WAITING ✓
- 旧 ReceptionQueue.status = DONE ✓
- 旧 TriageQueue.status = **?? (缺失定义,见 §4.4)**

**Transfer 落库基本能跑**,但**旧 TriageQueue 状态缺失**是 P1。

### 8.3 Transfer 与 WalkIn 互斥检查

| 场景 | Transfer 路径 | WalkIn 路径 |
|---|---|---|
| Appointment 1:N Visit | ✓ 旧 Visit → TRANSFERRED,新 Visit 入库 | 不适用(WalkIn 1 Appointment→多 Visit) |
| currentVisitId 指向 | 新 Visit.id | 第一 Visit.id(创建时)|
| TriageQueue.appointmentId | 旧 Visit 的 TriageQueue 仍指向 Appointment(因 appointmentId 不变) | NULL |

**Transfer 路径不会让 appointmentId 失效**(旧 Visit 仍指向同一 Appointment),但旧 TriageQueue 的 status 变更未定义 → P1。

### 8.4 综合审计

**WalkIn 路径**:全部可落库 ✓
**Transfer 路径**:基本可落库,旧 TriageQueue 状态缺失 P1

---

## §9 重点审查 8:严格区分"设计可行"与"原系统事实"

### 9.1 235 的"设计可行"声明

235 §3 明确:
> "**严禁【OptFlow设计】伪装成【原系统事实】**"

### 9.2 实际执行情况

**已检查字段中,大多数【OptFlow设计】标签正确** ✓

**问题**:
1. `Employee.departmentId` 标【原系统事实】,证据 214 §4.1 C 极弱 → **标签与证据等级不匹配**(P1)
2. `Company.companyType` / `Customer.memberTypeId` / `Employee.employeeName` / `Employee.status` 引用 `04_数据模型.md` → 这是早期理论模型,**不是原系统事实的直接证据**(P2)

### 9.3 审计裁决

**P1-Conditional**:`Employee.departmentId` 标签必须修正(C 极弱 ≠ 原系统事实)

**P2-Doc**:4 个 04_数据模型.md 引用的字段标签可保留,但应在备注里加"等级 C/D"。

---

## §10 10 个闭合检查重跑

| # | 闭合项 | 235 状态 | 审计 |
|---:|---|---|:-:|
| 1 | 12 objects 闭合 | 12 对象 DDL 设计齐全 | ✓ PASS |
| 2 | PK 闭合 | 12 PK 全部唯一,类型内部一致 | ✓ PASS |
| 3 | FK 闭合 | 20 FK 类型匹配,NULL 一致,但循环 FK 创建顺序未明文 | ⚠️ P1 |
| 4 | ID 类型闭合 | BIGINT 6 + UUID 6,严格分层 | ✓ PASS |
| 5 | 生命周期闭合 | 4 状态机能由字段表达,CHECK 约束缺失 | ⚠️ P1 |
| 6 | WalkIn 闭合 | 3 个 appointmentId FK 可空,appointmentType / visitType 枚举含 WALK_IN | ✓ PASS |
| 7 | Transfer 闭合 | Visit.transferredFromVisitId/EmployeeId/At 字段齐全,旧 TriageQueue 状态缺失 | ⚠️ P1 |
| 8 | currentVisitId 闭合 | FK 存在,§8.1 6 场景规则完整,ON DELETE SET NULL 与"保持"规则冲突(隐含 Visit 永不删) | ⚠️ P1 |
| 9 | Employee/admin 闭合 | §B-4 字段表 adminId NOT NULL vs §5.3 "可无 admin" 文字矛盾 | ⚠️ P1 |
| 10 | Patient/Customer/MedicalRecord 闭合 | 7 问全部回答,customerId 独立,patientId 非业务主键,MedicalRecord 不引入 | ✓ PASS |

**统计**:
- ✓ PASS:5 项
- ⚠️ P1:5 项
- ❌ BLOCK:0 项

---

## §11 必须修正项清单(进入 S1-168 前必改)

### 11.1 P1-Conditional 项(8 项,S1-168 前必改)

| # | 修正项 | 来源章节 | 影响 |
|---:|---|---|---|
| **P1-1** | Employee adminId 字段表 NOT NULL vs §5.3 文字矛盾,二选一(本审计推荐 A=NOT NULL)| §2 | 字段表一致性 |
| **P1-2** | Visit / Appointment 永不物理删除规则明文化,FK ON DELETE 改 RESTRICT | §3 | FK 语义清晰 |
| **P1-3** | visit/transfer 事务顺序明文(本审计推荐 9 步骤,见 §4.6)| §4 | Service 层实施 |
| **P1-4** | 旧 TriageQueue 转诊后状态 = REMOVED 明文 | §4.7 | 状态机完备 |
| **P1-5** | ReceptionQueue 不在 transfer 步骤创建(避免 employeeId 必填矛盾)| §4.6 | 状态机完备 |
| **P1-6** | 4 个状态字段加 CHECK 约束(Appointment 12 / Visit 7 / TriageQueue 4 / ReceptionQueue 6)| §6.6 | 数据完整性 |
| **P1-7** | 循环 FK 创建顺序明文(先建 Appointment,再建 Visit,再 UPDATE currentVisitId)| §6.4 | DDL 实施 |
| **P1-8** | `Employee.departmentId` 标签从【原系统事实】改为【待确认】或【OptFlow设计】 | §5 | 标签一致性 |

### 11.2 P2-Doc 项(3 项,可选)

| # | 修正项 | 来源章节 | 影响 |
|---:|---|---|---|
| **P2-1** | `Company.companyType` 标签备注加"等级 C/D,源自 04_数据模型.md" | §5.1 | 标签严谨 |
| **P2-2** | `Customer.memberTypeId` 同上 | §5.1 | 标签严谨 |
| **P2-3** | `Employee.employeeName` / `Employee.status` 同上 | §5.1 | 标签严谨 |

### 11.3 全部 P1 修正后,S1-168 状态

**如果 8 项 P1 全部修正**:
- 235 升级为 "S1-168 设计冻结稿"
- S1-168 DDL 实施可基于修正后的 235 直接生成
- 预计可达到 **PASS**

**如果 P1 不修正直接进入 S1-168**:
- DDL 仍可生成(无语法错误)
- 但 Service 层实施时遇到矛盾(transfer 顺序 / 旧 TriageQueue 状态)需现场决策
- 风险:可能产生 DDL 与 Service 层不一致
- 状态:**CONDITIONAL PASS**(可进入 S1-168,但建议先修 P1)

---

## §12 最终结论

### 12.1 三选一裁决

按老板判断规则:

> "存在任何会导致 DDL 产生错误或业务无法落库的矛盾:BLOCK"
> "仅存在文档表达问题、不影响 DDL:CONDITIONAL PASS"
> "全部闭合:PASS"

**审计结果**:

| 触发条件 | 数量 | 是否触发 |
|---|---:|:-:|
| DDL 产生错误 | 0 | ❌ |
| 业务无法落库(实质性) | 0 | ❌ |
| 仅文档表达问题 | 8 项 P1 + 3 项 P2 | ✓ |

# **【CONDITIONAL PASS】**

### 12.2 触发 CONDITIONAL PASS 而非 BLOCK 的原因

- 12 对象 DDL 语法全部合法,无 DDL 错误
- 12 PK / 20 FK 类型全部匹配,无类型冲突
- 4 状态机能由字段表达(虽然 CHECK 缺失,但这是数据完整性问题,不是 DDL 错误)
- WalkIn 路径 3 个 NULL FK 全部支持
- Transfer 路径字段齐全,只是状态机细节缺失
- 8 项 P1 都是"DDL 之外"或"DDL 实施时易发现"的问题

**因此不触发 BLOCK,但 8 项 P1 应在 S1-168 之前补全**。

### 12.3 S1-168 启动建议

| 路径 | 描述 | 推荐度 |
|---|---|:-:|
| **A. 先修 P1,再进 S1-168** | 用 235A 修正 235(S1-168B?),再启动 DDL | ★★★★★ |
| **B. 235 冻结,带 P1 进 S1-168** | 235 标记"P1 待修",S1-168 边写 DDL 边修 | ★★★ |
| **C. 直接进 S1-168 不修** | 风险高,Service 层实施时现场决策 | ★ |

**本审计推荐 A 路径**:8 项 P1 都是明确的小修正,先修后启动,可避免 S1-168 实施时反复回头。

### 12.4 一句话最终判断

> **235 S1-167B 整体设计质量合格,12 对象 DDL 语法可生成,4 状态机可表达,ID 类型 / PK / FK / WalkIn / currentVisitId 7 项闭合;但 Employee adminId 字段表与文字矛盾、Visit/Appointment 永不删规则未明文化、visit/transfer 事务顺序未定义、旧 TriageQueue 转诊状态缺失、4 状态字段无 CHECK 约束、循环 FK 创建顺序未明文、departmentId 标签错配等 8 项 P1-Conditional 必须在 S1-168 前修正;本审计判定:CONDITIONAL PASS。**

---

## §13 Git 验证

- **本报告不修改任何历史 MD**(235 / 234B / 234A / 233 / 232 / 229 等不动)
- **本报告不修改代码 / 数据库 / HTML / .gitignore**
- **本报告不 commit / push**
- **0 号闸门 4 文件 SHA256 锁定不变**(基线值在 235 §0 已记录,本审计未触及)
- **HEAD 不变**:`1bf472c39812d88ad41c2e8a0570ddf37ab0e8fc`
- **新增文件**:`235A_S1-167B_DDL前独立冻结审计报告.md`(untracked,待老板决定是否 commit)

---

## §14 报告统计

- 报告字节数:约 30000 字节
- 章节数:14
- 修正项:P1-Conditional 8 项 + P2-Doc 3 项 = 11 项
- BLOCK 触发:0
- 最终裁决:**CONDITIONAL PASS**
