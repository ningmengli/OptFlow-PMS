# 235B S1-167B 最终修订裁决与 S1-168 启动前置

> **本轮定位**:**最终覆盖裁决层**。不是重写 235 / 235A,而是**叠加在 235 + 235A 之上,作为 S1-168 唯一可执行的规则源**。
> - 严禁修改 235 / 235A / 234B / 234A / 233 / 232 / 229 / 任何历史 MD
> - 严禁修改代码 / HTML / 数据库 / .gitignore
> - 严禁 commit / push / 创建 DDL
> - S1-168 启动后必须以 **235 + 235A + 235B** 三者合并为唯一有效规则
> - 三者冲突时:**235B 优先于 235A,235A 优先于 235**

---

## §0 完整性闸门

- **HEAD**:`1bf472c39812d88ad41c2e8a0570ddf37ab0e8fc`(S1-167A)
- **REMOTE == LOCAL**:`1bf472c39812d88ad41c2e8a0570ddf37ab0e8fc` ✓
- **0 号闸门 4 文件 SHA256 全部 PASS** ✓
- **本轮不写代码 / 不提交 Git** ✓
- **历史 MD(190~235A 共 48 份)未修改** ✓
- **新文件**:`235B_S1-167B_最终修订裁决与S1-168启动前置.md`(本文件)

---

## §1 235B 定位

| 维度 | 235 | 235A | 235B(本版)|
|---|---|---|---|
| 性质 | 原始裁决稿 | 独立审计报告 | **最终覆盖裁决层** |
| 角色 | 起始基线 | 问题发现 | **问题关闭 + 新规则冻结** |
| 与 S1-168 关系 | 起草 | 审计 | **执行依据** |
| 优先级(冲突时) | 最低 | 中 | **最高** |

**重要原则**:
- 235B 关闭 235A 列出的所有 P1 项,**不修改 235A 报告内容**
- 235B 冻结 235 留下的所有"待补"位置
- 235B 不重写 235 / 235A,只引用并覆盖
- S1-168 必须基于 **235 + 235A + 235B** 三者,按优先级应用

---

## §2 关闭 P1-1:Employee adminId NOT NULL 裁决

### 2.1 覆盖规则

**235B 覆盖 235 §5.3 与 §B-4**:
- 235 §5.3 文字"employee 可以无 admin 账号"**被覆盖**
- 235 §B-4 字段表 `adminId | BIGINT | ✓` 保持**作为最终裁决**

### 2.2 最终规则(本版冻结)

| 维度 | 最终裁决 |
|---|---|
| `Employee.adminId` 是否 NOT NULL | **✓ NOT NULL(强制必填)** |
| `Employee.adminId` 是否 UNIQUE | **✓ UNIQUE(companyId, adminId)组合唯一** |
| "每个 Employee 必有 admin 账号" | **✓ 是,严格强制** |
| 一期 V4.4 业务是否需要"纯无登录员工" | **不需要,4 个 role(Doctor/Triage/Recep/Admin)都需登录** |
| 234B 留下的"双 ID 冲突" | **本版彻底关闭,采用方案 A** |

### 2.3 实施影响

- DDL: `adminId BIGINT NOT NULL, UNIQUE KEY uk_company_admin (companyId, adminId)`
- Service 层:创建 Employee 时必须传 adminId,不能 NULL
- 一期 V4.4 不预留"无登录员工"扩展(后续可改)

### 2.4 P1-1 状态

# **P1-1 【CLOSED】**

---

## §3 关闭 P1-2:currentVisitId 与物理删除规则

### 3.1 覆盖规则

**235B 覆盖 235 §B-7 / §B-12 FK 行为 + §8.1 6 场景**:
- 235 §B-7 `FOREIGN KEY(currentVisitId) → Visit(visitId) ON DELETE SET NULL` **被覆盖** → 改为 `ON DELETE RESTRICT`
- 235 §B-12 `FOREIGN KEY(appointmentId) → Appointment(appointmentId) ON DELETE SET NULL` **被覆盖** → 改为 `ON DELETE RESTRICT`
- 235 §8.1 "COMPLETED / CANCELLED 保持 currentVisitId" **作为最终行为规则保留**

### 3.2 最终规则(本版冻结)

| 维度 | 最终裁决 |
|---|---|
| **Appointment 是否允许物理删除** | **✗ 永不物理删除(终态保留)** |
| **Visit 是否允许物理删除** | **✗ 永不物理删除(终态保留)** |
| **Appointment.currentVisitId → Visit FK 行为** | **ON DELETE RESTRICT** |
| **Visit.appointmentId → Appointment FK 行为** | **ON DELETE RESTRICT** |
| **COMPLETED 状态 currentVisitId 行为** | **✓ 保持当前 Visit.id(DB 层保证永不被置空)** |
| **CANCELLED 状态 currentVisitId 行为** | **✓ 保持当前 Visit.id(DB 层保证永不被置空)** |
| **229 §5.2 旧规则"重置为 NULL"** | **被覆盖,以 235 + 235B 为准** |

### 3.3 实施影响

- DDL: 所有指向 Visit / Appointment 的 FK 全部使用 `ON DELETE RESTRICT`
- Service 层:任何"删除 Visit"的代码必须改为"软删"(status 置为 CANCELLED,或归档)
- 物理删除被 DB 层直接拒绝,误操作不会污染 currentVisitId

### 3.4 闭环检查

- **Appointment 取消后**:Appointment 本身不删,status=CANCELLED,currentVisitId 保持 → §8.1 规则满足 ✓
- **Visit 取消后**:Visit 本身不删,status=CANCELLED,Appointment.currentVisitId 保持 → §8.1 规则满足 ✓
- **转诊后**:旧 Visit.status=TRANSFERRED,Appointment.currentVisitId=newVisit.id → §8.1 场景 3 满足 ✓
- **Visit 永远存在**:currentVisitId FK 永远不会触发 SET NULL(因为根本没有 DELETE 发生)→ §8.1 COMPLETED/CANCELLED 永远保持 ✓

### 3.5 P1-2 状态

# **P1-2 【CLOSED】**

---

## §4 关闭 P1-3 / P1-4 / P1-5:API-034 visit/transfer 事务顺序

### 4.1 覆盖规则

**235B 覆盖 229 §6.2 TRANSFERRED 7 步骤(因为 229 步骤有 FK 悬空问题,见 235A §4.3)**:
- 229 §6.2 7 步骤**被覆盖**(文档本身不修改,作为历史证据保留)
- 235B 冻结 **9 步事务顺序**作为最终执行规则

### 4.2 最终规则:API-034 visit/transfer 9 步事务(本版冻结)

**全部 9 步必须在 1 个 DB 事务内执行,任一步失败全部 ROLLBACK**。

| # | 步骤 | 操作 | 对象状态 | 触发 API |
|---:|---|---|---|---|
| 1 | 验证前置 | 检查旧 Visit.status = IN_CONSULTATION,否则拒绝 | – | API-034 |
| 2 | 创建新 Visit | `INSERT INTO visit(..., status='DRAFT', transferredFromVisitId=oldVisitId, transferredFromEmployeeId=oldEmployeeId)` | 新 Visit = DRAFT | API-034 |
| 3 | 创建新 TriageQueue | `INSERT INTO triage_queue(visitId=newVisitId, appointmentId=oldTriageQueue.appointmentId, status='IN_POOL', employeeId=NULL, consultRoomId=NULL)` | 新 TriageQueue = IN_POOL(employee/room NULL)| API-034 |
| 4 | 更新 Appointment.currentVisitId | `UPDATE appointment SET currentVisitId=newVisitId WHERE appointmentId=oldAppointmentId` | Appointment.currentVisitId = newVisit.id | API-034 |
| 5 | 更新 Appointment.status | `UPDATE appointment SET status='TRIAGE_WAITING' WHERE appointmentId=oldAppointmentId` | Appointment = TRIAGE_WAITING | API-034 |
| 6 | 旧 Visit 终态 | `UPDATE visit SET status='TRANSFERRED', transferredAt=NOW() WHERE visitId=oldVisitId` | 旧 Visit = TRANSFERRED | API-034 |
| 7 | 旧 TriageQueue 终态 | `UPDATE triage_queue SET status='REMOVED' WHERE triageQueueId=oldTriageQueueId` | 旧 TriageQueue = REMOVED | API-034 |
| 8 | 旧 ReceptionQueue 终态 | `UPDATE reception_queue SET status='DONE', cancelReason='TRANSFERRED' WHERE receptionQueueId=oldReceptionQueueId` | 旧 ReceptionQueue = DONE(标"transferred")| API-034 |
| 9 | 提交 | `COMMIT` | – | API-034 |

### 4.3 不在 API-034 创建的对象

| 对象 | 原因 | 创建时点 |
|---|---|---|
| **新 ReceptionQueue** | employeeId / consultRoomId 必填,但 transfer 步骤没有确定值 | 由 API-021 `triage/assign` 在分诊员分配 doctor/room 时创建 |
| **新 Appointment** | Transfer 是 Visit 级行为,Appointment 不变(只是 currentVisitId 变) | 不需要 |
| **新 Schedule / Slot** | Transfer 不涉及排班变化 | 不需要 |

### 4.4 关键决策点

| 决策点 | 最终选择 | 理由 |
|---|---|---|
| 旧 TriageQueue 终态 | **REMOVED** | 语义:被动转移走(CANCELLED 留给主动取消)|
| 旧 ReceptionQueue 终态 | **DONE**(cancelReason='TRANSFERRED')| 229 §6.2 第 1 步,DONE + 备注原因 |
| 新 TriageQueue.employeeId | **NULL** | 233 P1-1 修正,preferredEmployeeId 仅作建议 |
| 新 TriageQueue.consultRoomId | **NULL** | 同上 |
| currentVisitId 更新时机 | **第 4 步(在新 Visit 创建后)**| 避免 FK 悬空(229 旧顺序的 bug)|
| Appointment.status 更新 | **TRIAGE_WAITING** | 229 §6.2 第 2 步,Appointment 重新进入分诊池 |

### 4.5 闭环检查

- ✓ 所有 9 步在 1 事务内,失败全部 ROLLBACK
- ✓ currentVisitId 永远指向已存在 Visit(因新 Visit 第 2 步已建)
- ✓ 不允许 visitId=NULL(新 TriageQueue.visitId = newVisitId NOT NULL)
- ✓ 旧 TriageQueue 状态更新(REMOVED),不会误导后续查询
- ✓ ReceptionQueue 不在 transfer 创建,避免 employeeId 必填矛盾

### 4.6 P1-3 / P1-4 / P1-5 状态

# **P1-3 / P1-4 / P1-5 全部【CLOSED】**

---

## §5 关闭 BigScreen / ConsultRoom 关系裁决(P1-7 范围扩展)

### 5.1 235 内部矛盾

**235 §7 关系矩阵**:
> "**BigScreen → ConsultRoom** = 多对一 ✓ 【OptFlow设计】 216 S1-153 §14.4"

**235 §B-6 BigScreen 字段表**:
> `consultRoomIdArray | JSON | – | – | 绑定诊室列表`

**直接矛盾**:
- 关系矩阵写 N:1(标准 FK)
- 字段表是 JSON 数组(JSON 逻辑引用)
- 关系名相同,实现不同

### 5.2 覆盖规则

**235B 覆盖 235 §7 关系矩阵中 BigScreen → ConsultRoom 行**:

| 维度 | 最终裁决 |
|---|---|
| 关系实现 | **JSON 逻辑引用集合(consultRoomIdArray JSON 数组)** |
| 是否为 DB 标准 FK | **✗ 否(是 JSON 应用层引用)** |
| 关系矩阵表达 | 改写为"**逻辑 N:M 引用(无标准 FK)**" |
| DDL 是否创建外键约束 | **✗ 不创建(由应用层维护 JSON 内容)** |
| 数据一致性 | 由应用层 Service 保证(JSON 内的 BIGINT 必须指向已存在的 consultRoomId)|

### 5.3 实施影响

- DDL: `consultRoomIdArray JSON` 字段,不创建 FK 约束
- 关系矩阵表达: **BigScreen ↔ ConsultRoom = JSON 逻辑 N:M(应用层维护)**
- Service 层:更新 consultRoomIdArray 时,需验证每个 BIGINT 指向已存在的 ConsultRoom
- 数据完整性:无 DB 级 FK 约束(由应用层负责)

### 5.4 与其他 JSON 字段的对比

| 对象 | 字段 | 关系 | DB FK? | 应用层维护? |
|---|---|---|:-:|:-:|
| BigScreen | `consultRoomIdArray` | BigScreen ↔ ConsultRoom N:M | ✗ | ✓ |
| ConsultRoom | `examineList` | ConsultRoom ↔ ExamineType N:M | ✗ | ✓ |

**统一原则**:**所有 JSON 数组字段都是应用层维护的逻辑引用,不是 DB FK**。

### 5.5 关系矩阵修订建议(S1-168 应用,235 不修改)

| Source | Target | 类型 | 是否 DB FK |
|---|---|---|:-:|
| BigScreen → ConsultRoom | JSON 逻辑 N:M | ✗ |
| ConsultRoom → ExamineType(若引入)| JSON 逻辑 N:M | ✗ |

### 5.6 P1-7 状态(范围扩展)

# **BigScreen/ConsultRoom 关系裁决【CLOSED】**

---

## §6 关闭 P1-7(原):循环 FK 创建顺序 + 正式冻结 FK 闭合规则

### 6.1 235B 覆盖 235A P1-7

**235A P1-7 识别的循环 FK**:
```
Appointment.currentVisitId (FK) → Visit.visitId
Visit.appointmentId (FK) → Appointment.appointmentId
```

**235B 关闭方案**:

| 步骤 | 操作 | 顺序要求 |
|---|---|---|
| 1 | 创建 DDL | `CREATE TABLE appointment` 先建(无 currentVisitId 引用) |
| 2 | 创建 DDL | `CREATE TABLE visit` 后建(填 appointmentId) |
| 3 | 数据写入 | `INSERT appointment` (currentVisitId=NULL) |
| 4 | 数据写入 | `INSERT visit` (填 appointmentId) |
| 5 | 数据回填 | `UPDATE appointment SET currentVisitId=visit.id` |
| 6 | DDL 约束生效 | 两条 FK ON DELETE RESTRICT(见 §3) |

**235B 重要原则**:
- 物理删除被永久禁止(见 §3)
- 循环 FK 永远不触发(RESTRICT 阻止删除)
- 创建顺序明文,作为 S1-168 DDL 实施规范

### 6.2 正式冻结 FK 闭合规则

**235B 区分两类关系**:

| 类型 | 定义 | 实施要求 | 235 关系矩阵位置 |
|---|---|---|---|
| **A. 真 DB FK** | 在 DDL 中用 `FOREIGN KEY` 表达 | 必须在 §B 表约束中逐条落地 | §7 关系矩阵标 ✓ |
| **B. 逻辑关系 / JSON 引用** | 字段是 JSON 数组,或仅业务引用 | 关系矩阵必须明确标"逻辑关系",DDL 不创建 FK | §7 关系矩阵标"JSON"或"逻辑" |

### 6.3 235 §7 关系矩阵逐条审定

| Source | Target | 235 §7 标 | 235 §B 表达 | 最终类型 | DB FK 落地 |
|---|---|---|---|---|:-:|
| Appointment → Patient | 多对一 | ✓ | FK(patientId)→Patient | A 真 FK | ✓ |
| Appointment → Employee | 多对一 | ✓ | FK(employeeId)→Employee | A 真 FK | ✓ |
| Appointment → Schedule | 多对一 | ✓ | FK(scheduleId)→Schedule | A 真 FK | ✓ |
| Appointment → Slot | 多对一 | ✓ | FK(slotId)→Slot | A 真 FK | ✓ |
| Appointment → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| Appointment → Visit(currentVisitId)| N:1 标记 | ✓ | FK(currentVisitId)→Visit RESTRICT | A 真 FK | ✓ |
| Visit → Appointment | 多对一(可空)| ✓ | FK(appointmentId)→Appointment RESTRICT | A 真 FK | ✓ |
| Visit → Patient | 多对一 | ✓ | FK(patientId)→Patient | A 真 FK | ✓ |
| Visit → Employee | 多对一 | ✓ | FK(employeeId)→Employee | A 真 FK | ✓ |
| Visit → ConsultRoom | 多对一 | ✓ | FK(consultRoomId)→ConsultRoom | A 真 FK | ✓ |
| Visit → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| TriageQueue → Visit | 一对一 | ✓ | FK(visitId)→Visit UNIQUE | A 真 FK | ✓ |
| TriageQueue → Appointment(可空)| 一对一(可空)| ✓ | FK(appointmentId)→Appointment | A 真 FK | ✓ |
| TriageQueue → ConsultRoom | 多对一 | ✓ | FK(consultRoomId)→ConsultRoom | A 真 FK | ✓ |
| TriageQueue → Employee | 多对一 | ✓ | FK(employeeId)→Employee | A 真 FK | ✓ |
| TriageQueue → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| ReceptionQueue → TriageQueue | 一对一 | ✓ | FK(triageQueueId)→TriageQueue UNIQUE | A 真 FK | ✓ |
| ReceptionQueue → Visit | 一对一 | ✓ | FK(visitId)→Visit UNIQUE | A 真 FK | ✓ |
| ReceptionQueue → Appointment(可空)| 一对一(可空)| ✓ | FK(appointmentId)→Appointment | A 真 FK | ✓ |
| ReceptionQueue → ConsultRoom | 多对一(必填)| ✓ | FK(consultRoomId)→ConsultRoom | A 真 FK | ✓ |
| ReceptionQueue → Employee | 多对一(必填)| ✓ | FK(employeeId)→Employee | A 真 FK | ✓ |
| ReceptionQueue → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| AppointmentSchedule → Employee | 多对一 | ✓ | FK(employeeId)→Employee | A 真 FK | ✓ |
| AppointmentSchedule → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| AppointmentSlot → Schedule | 多对一 | ✓ | FK(scheduleId)→Schedule | A 真 FK | ✓ |
| AppointmentSlot → ConsultRoom(可空)| 多对一(可空)| ✓ | FK(consultRoomId)→ConsultRoom | A 真 FK | ✓ |
| Employee → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| ConsultRoom → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| Customer → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |
| Patient → Customer | N:1 | ✓ | FK(customerId)→Customer | A 真 FK | ✓ |
| **BigScreen → ConsultRoom** | **多对一(JSON 数组)** | **✓(标 N:1)** | **JSON consultRoomIdArray** | **B 逻辑 JSON** | **✗ 不创建 FK** |
| BigScreen → Company | 多对一 | ✓ | FK(companyId)→Company | A 真 FK | ✓ |

**统计**:
- A 真 FK:**30 条**(全部需在 DDL 创建约束)
- B 逻辑 JSON:**1 条**(BigScreen → ConsultRoom,应用层维护)
- 循环 FK:**1 对**(Appointment ↔ Visit,见 §3 + §6.1,已闭环)

### 6.4 P1-7 状态

# **P1-7(循环 FK)【CLOSED】**
# **BigScreen JSON 关系【CLOSED】(§5)**
# **FK 闭合规则【FROZEN】(§6.3 表)**

---

## §7 冻结数据库方言(关键前置)

### 7.1 235 当前方言问题

235 §B 表混合使用:
- `AUTO_INCREMENT`(MySQL 语法)
- `gen_random_uuid()`(PostgreSQL 14+ 原生函数,MySQL 8 不支持)
- `CHAR(36)` UUID 存储(跨方言通用)
- `DATETIME`(MySQL / SQL Server / PostgreSQL 都支持,精度不同)
- `JSON`(MySQL 5.7+ / PostgreSQL / SQL Server 都支持)
- `CHECK 约束`(MySQL 8.0.16+ 才强制执行,之前不强制)

**结论**:**235 §B 不能直接在任意 DB 上执行**,必须先冻结方言。

### 7.2 235B 推荐默认方言

**OptFlow V4.4 推荐默认**:**MySQL 8.0+(8.0.16 之后,支持 CHECK 强制执行)**

| 维度 | 选择 | 理由 |
|---|---|---|
| **目标数据库** | **MySQL 8.0+(InnoDB 引擎)** | Spring Boot 默认 / Docker 部署成熟 / JSON 原生支持 / CHECK 强制执行 |
| 字符集 | `utf8mb4` | 支持 emoji / 4 字节字符 |
| 排序规则 | `utf8mb4_0900_ai_ci` | MySQL 8 默认,大小写不敏感 |
| 事务引擎 | `InnoDB` | 唯一支持事务 + FK |
| ID 生成 | `AUTO_INCREMENT`(BIGINT)/ `UUID()` 函数 + `CHAR(36)`(UUID) | MySQL 8 原生 UUID() 函数 |
| 时间类型 | `DATETIME(3)`(毫秒精度) | 业务需要毫秒级时间戳 |
| JSON 字段 | `JSON` 类型(不是 TEXT)| 原生 JSON,支持 JSON_EXTRACT 等函数 |
| CHECK 约束 | `CHECK` 子句 | MySQL 8.0.16+ 强制执行 |
| FK 约束 | `FOREIGN KEY` + `ON DELETE RESTRICT`(默认) | 见 §3 + §6 |

### 7.3 235 §B 字段到 MySQL 8 方言的映射

| 235 §B 表达 | MySQL 8 实际语法 | 说明 |
|---|---|---|
| `BIGINT AUTO_INCREMENT` | `BIGINT NOT NULL AUTO_INCREMENT` | 直接对应 |
| `UUID (CHAR(36)) gen_random_uuid()` | `CHAR(36) NOT NULL DEFAULT (UUID())` | MySQL 8 UUID() 函数 |
| `VARCHAR(N)` | `VARCHAR(N)` | 直接对应 |
| `INT` | `INT` | 直接对应 |
| `TINYINT` | `TINYINT` | 直接对应 |
| `DATETIME NOW()` | `DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)` | 毫秒精度 |
| `JSON` | `JSON` | 直接对应 |
| `CHECK` | `CHECK (...)` | MySQL 8.0.16+ |

### 7.4 一致性原则

**S1-168 DDL 必须在同一 MySQL 8 方言下生成**:
- 所有 ID 生成一致(BIGINT 用 AUTO_INCREMENT,UUID 用 CHAR(36)+UUID())
- 所有时间类型一致(DATETIME(3) + CURRENT_TIMESTAMP(3))
- 所有 JSON 字段一致(JSON 类型,非 TEXT)
- 所有 CHECK 约束有效(MySQL 8.0.16+)
- 所有 FK 约束一致(ON DELETE RESTRICT 为默认,见 §3)

### 7.5 数据库方言状态

# **推荐 MySQL 8.0+(InnoDB)【待老板确认】**

**【重要警告】**:
> **在数据库方言尚未确认前,不得声称"DDL 可直接执行"。**
> **S1-168 DDL 必须基于本节 §7.2 推荐的 MySQL 8.0+,经老板确认后启动。**

### 7.6 P1-Dialect(新)状态

# **P1-Dialect 【BLOCK until confirmed】** — 需老板确认 MySQL 8.0+

---

## §8 状态 CHECK:DB 完整性增强项(P1-6)

### 8.1 235B 重新归类

**235A P1-6** 把 4 个状态字段 CHECK 缺失列为 P1-Conditional。

**235B 重新归类**:
- **不作为模型冻结 BLOCK**
- **作为 DB 完整性增强项**
- **S1-168 正式 DDL 必须实现**

### 8.2 4 个状态字段 CHECK 定义

```sql
-- Appointment.status (12 状态)
ALTER TABLE appointment ADD CONSTRAINT chk_appointment_status
  CHECK (status IN (
    'DRAFT','CONFIRMED','WAITING_ARRIVAL','ARRIVED',
    'TRIAGE_WAITING','WAITING_RECEPTION','CALLED',
    'IN_CONSULTATION','COMPLETED','CANCELLED','NO_SHOW','RESCHEDULED'
  ));

-- Visit.status (7 状态)
ALTER TABLE visit ADD CONSTRAINT chk_visit_status
  CHECK (status IN (
    'DRAFT','CHECKED_IN','TRIAGED','IN_CONSULTATION',
    'COMPLETED','TRANSFERRED','CANCELLED'
  ));

-- TriageQueue.status (4 状态)
ALTER TABLE triage_queue ADD CONSTRAINT chk_triage_status
  CHECK (status IN (
    'IN_POOL','ASSIGNED','REMOVED','CANCELLED'
  ));

-- ReceptionQueue.status (6 状态)
ALTER TABLE reception_queue ADD CONSTRAINT chk_reception_status
  CHECK (status IN (
    'ASSIGNED','WAITING','CALLED','SKIPPED',
    'IN_CONSULTATION','DONE'
  ));
```

### 8.3 实施影响

- 必须在 MySQL 8.0.16+ 上执行(8.0.16 之前 CHECK 不强制)
- 状态字段写入非法值会被 DB 拒绝
- 数据完整性 100% 由 DB 层保证

### 8.4 P1-6 状态

# **P1-6【DOWNGRADED to DB 增强项,需 S1-168 DDL 实现】**

---

## §9 标签修正:P1-8 + P2-1 ~ P2-3

### 9.1 P1-8:Employee.departmentId 标签修正

**235B 覆盖 235 §B-4 字段表标签**:

| 字段 | 235 §B-4 标签 | 235B 最终标签 | 理由 |
|---|---|---|---|
| `Employee.departmentId` | 【原系统事实】(214 §4.1 C 极弱)| **【待确认】** | 214 §4.1 仅为 C 极弱证据,不能作为原系统事实 |

**P1-8 状态**:**【CLOSED】**(已从【原系统事实】改为【待确认】)

### 9.2 P2-1 ~ P2-3:4 个字段加证据等级备注

**235B 在 235 §B 表中加备注**(S1-168 应用,235 不修改):

| 字段 | 235 标 | 235B 备注追加 |
|---|---|---|
| `Company.companyType` | 【原系统事实】(04_数据模型.md) | **备注:源自 04_数据模型.md,为早期理论模型,等级 C,非 A/B 级原系统直接证据** |
| `Customer.memberTypeId` | 【原系统事实】(04_数据模型.md) | **备注:同上** |
| `Employee.employeeName` | 【原系统事实】(04_数据模型.md) | **备注:同上** |
| `Employee.status` | 【原系统事实】(04_数据模型.md) | **备注:同上** |

**P2-1 ~ P2-3 状态**:**【FROZEN】**(已加备注,S1-168 应用时需把备注写入 DDL COMMENT)

---

## §10 235A P1 状态汇总(8 项全部 CLOSED)

| P1 # | 描述 | 235B 关闭位置 | 状态 |
|---:|---|---|:-:|
| P1-1 | Employee adminId 矛盾 | §2 | **CLOSED** |
| P1-2 | currentVisitId ON DELETE 矛盾 | §3 | **CLOSED** |
| P1-3 | visit/transfer 事务顺序 | §4 | **CLOSED** |
| P1-4 | 旧 TriageQueue 转诊后状态 | §4(步骤 7) | **CLOSED**(REMOVED) |
| P1-5 | ReceptionQueue 不在 transfer 创建 | §4(§4.3)| **CLOSED** |
| P1-6 | 4 状态字段无 CHECK | §8 | **CLOSED(DB 增强项)** |
| P1-7 | 循环 FK + BigScreen JSON | §5 + §6 | **CLOSED** |
| P1-8 | departmentId 标签错配 | §9.1 | **CLOSED** |

**8 项 P1 全部【CLOSED】** ✓

**新增 BLOCK 项**:
- **P1-Dialect(数据库方言)【待老板确认】** — 必须明确 MySQL 8.0+ 才能开始 S1-168 DDL

---

## §11 S1-168 启动判断

### 11.1 启动条件

| 条件 | 状态 |
|---|:-:|
| 235A 8 项 P1 全部 CLOSED | ✓ |
| 数据库方言明确 | **⚠️ 待老板确认 MySQL 8.0+** |
| 关系矩阵 / FK 逐条闭合(30 真 FK + 1 JSON 逻辑)| ✓ |
| BigScreen JSON 关系裁决 | ✓ |
| 标签修正完成 | ✓ |
| 历史 MD 未修改 | ✓ |
| 0 号闸门 4 文件 SHA256 锁定 | ✓ |

### 11.2 三种状态

| 状态 | 触发条件 | 当前? |
|---|---|:-:|
| **PASS** | P1 全部 CLOSED + 数据库方言确认 | **差最后一步** |
| **CONDITIONAL PASS** | P1 全部 CLOSED,但数据库方言待确认 | **✓ 当前状态** |
| **BLOCK** | P1 未 CLOSED | ✗ |

### 11.3 最终判断

# **【CONDITIONAL PASS】**

**条件解除方法**:
- 老板确认目标数据库 = MySQL 8.0+(InnoDB,utf8mb4,8.0.16+)
- 老板在 235B §7.2 表上明确"采用"或选择其他方言
- 一旦确认,235B 升级为 **PASS**,S1-168 DDL 可立即开始

### 11.4 S1-168 DDL 是否允许开始

| 答案 | 条件 |
|---|---|
| **条件性允许** | 老板先确认数据库方言(§7),然后 S1-168 DDL 立即可开始 |
| **不允许** | 数据库方言未确认 |

---

## §12 尚未确认事项清单(进入 S1-168 前必须明确)

### 12.1 数据库方言(必须老板裁决)

| 候选 | 235B 推荐 |
|---|:-:|
| **MySQL 8.0+(InnoDB, utf8mb4)** | **✓ 默认推荐** |
| PostgreSQL 14+ | 备选(更严格 CHECK / 部分 JSON 语法差异) |
| SQL Server | 备选(DATETIME 语法不同 / JSON 路径不同) |
| 国产数据库(达梦 / 人大金仓)| 备选(需验证语法兼容性) |

**裁决项**:**目标数据库 = ?**

### 12.2 业务规则选项(可由 S1-168 实施时决定,非 BLOCK)

| 事项 | 默认 | 备选 |
|---|---|---|
| 是否引入 MedicalRecord 实体 | 否(235 §6.4 决定) | 一期后引入 |
| customerId / patientId 是否能合并 | 否(235 §6.3 决定)| 永远分离 |
| 一期 V4.4 是否包含多公司隔离 | 是(companyId scope) | 一期单公司 |
| 时区处理 | 全部用 DATETIME + 服务端时区 | 客户端时区 |

### 12.3 235B 后续维护

- 235B 一旦被老板确认,可作为 S1-168 唯一执行依据
- S1-168 DDL 必须 100% 符合 235 + 235A + 235B 合并规则
- 任何新发现的矛盾,**优先追加 235B**(不修改 235 / 235A)

---

## §13 一句话最终判断

> **235B 是最终覆盖裁决层。8 项 235A P1 全部 CLOSED:Employee.adminId NOT NULL + UNIQUE 关闭 P1-1;Appointment/Visit 永不删 + FK ON DELETE RESTRICT 关闭 P1-2;API-034 9 步事务顺序(含旧 TriageQueue=REMOVED + ReceptionQueue 不在 transfer 创建)关闭 P1-3/4/5;BigScreen/ConsultRoom 保留 JSON 逻辑引用 + 30 真 FK 逐条闭合关闭 P1-7;4 状态字段 CHECK 降级为 S1-168 DDL 增强项关闭 P1-6;departmentId 改为【待确认】+ 4 个 04_数据模型.md 字段加证据等级备注关闭 P1-8 + P2-1~3。当前状态【CONDITIONAL PASS】,解除条件:老板确认目标数据库 = MySQL 8.0+(InnoDB,utf8mb4,8.0.16+)。一旦确认,S1-168 DDL 可立即开始。**

---

## §14 Git 验证

- **HEAD 不变**:`1bf472c39812d88ad41c2e8a0570ddf37ab0e8fc`
- **本轮不提交 Git**(按老板指示)
- **0 号闸门 4 文件 SHA256 锁定不变** ✓
- **历史 MD(190~235A 共 48 份)未修改** ✓
- **新文件**:`235B_S1-167B_最终修订裁决与S1-168启动前置.md`(untracked,待老板决定是否 commit)
- **本文件不允许修改任何已有 MD**

---

## §15 报告统计

- 报告字节数:约 26000 字节
- 章节数:15
- 关闭 P1 项:8 项全部 CLOSED
- 新增 BLOCK 项:1 项(P1-Dialect,待老板确认)
- P2 项:3 项全部 FROZEN(加备注)
- 最终判断:**CONDITIONAL PASS**(差老板最后一步方言确认)
