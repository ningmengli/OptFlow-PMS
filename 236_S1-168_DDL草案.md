# 236 S1-168 DDL 草案

> **本轮定位**:S1-168 正式 DDL 草案生成。**数据库方言已由 235B 老板确认 = MySQL 8.0.16+(InnoDB, utf8mb4, utf8mb4_0900_ai_ci)**。本草案严格依据 **235B > 235A > 235** 三层规则。
> - 严禁修改 235 / 235A / 235B / 任何历史 MD
> - 严禁修改代码 / HTML / 数据库 / .gitignore
> - 严禁运行 DDL / 严禁连接生产数据库执行任何 SQL
> - 严禁 commit / push
> - 本文件**只**作为 DDL 草案,不实际执行

---

## §0 完整性闸门

- **HEAD**:`1bf472c39812d88ad41c2e8a0570ddf37ab0e8fc`(S1-167A,本轮不动)
- **REMOTE == LOCAL**:`1bf472c39812d88ad41c2e8a0570ddf37ab0e8fc` ✓
- **0 号闸门 4 文件 SHA256 全部 PASS** ✓
- **历史 MD(190~235B 共 49 份)未修改** ✓
- **新文件**:`236_S1-168_DDL草案.md`(本文件,本轮唯一产出)
- **本文件不实际执行任何 DDL**

---

## §1 235B 升级状态 + 设计依据

### 1.1 235B 状态

| 维度 | 235B 状态 |
|---|---|
| 8 项 P1 | 全部 CLOSED |
| 数据库方言 | **老板已确认 MySQL 8.0.16+** |
| 综合状态 | **PASS**(本轮正式进入 DDL 实施阶段)|

### 1.2 三层规则优先级

> **235B > 235A > 235**

**冲突处理**:
- 235B 已明确覆盖 235 §B-7 / §B-12 FK 行为(ON DELETE SET NULL → RESTRICT)
- 235B 已明确覆盖 235 §5.3 Employee adminId 文字("可无 admin" → "必有 admin")
- 235A 仅作为独立审计证据,不作为规则源
- 235 作为原始设计稿,被 235B 覆盖的部分以 235B 为准

### 1.3 不在本轮范围

- ❌ 不执行 DDL(只在 Markdown 中呈现)
- ❌ 不连接任何数据库(MySQL 客户端、Docker、远程 DB)
- ❌ 不修改现有任何文件
- ❌ 不增加 235 / 235A / 235B 之外的字段(无 `tenantId` / `deleted` / `version` / `createdByUser` 等)

---

## §2 MySQL 8.0.16+ 方言规范(本草案统一)

| 维度 | 规范 |
|---|---|
| 存储引擎 | `ENGINE=InnoDB` |
| 字符集 | `DEFAULT CHARSET=utf8mb4` |
| 排序规则 | `COLLATE=utf8mb4_0900_ai_ci` |
| 整数主键 | `BIGINT NOT NULL AUTO_INCREMENT` |
| UUID 主键 | `CHAR(36) NOT NULL DEFAULT (UUID())` |
| 时间类型 | `DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)` |
| 更新时间 | `ON UPDATE CURRENT_TIMESTAMP(3)`(仅 updatedAt) |
| JSON 字段 | `JSON NULL`(原生 JSON 类型) |
| CHECK 约束 | `CHECK (... IN (...))`(MySQL 8.0.16+ 强制执行) |
| FK 行为 | `ON DELETE RESTRICT`(默认,除 235B 明文规定外) |
| 禁止 | `ON DELETE CASCADE` / `gen_random_uuid()`(PostgreSQL 语法)/ `TEXT` 替代 JSON |

---

## §3 12 对象 DDL 草案

### 3.1 创建顺序(基于 FK 依赖 + 循环 FK 双阶段方案)

> 235B §6.1 已要求循环 FK(Appointment ↔ Visit)必须分阶段创建。本草案采用 **Phase 1 (CREATE TABLE) + Phase 2 (ALTER TABLE ADD CONSTRAINT)** 双阶段方案。

**Phase 1:CREATE TABLE 阶段**

| # | 表 | 依赖 | 说明 |
|---:|---|---|---|
| 1 | `company` | – | 根表,无 FK |
| 2 | `customer` | company | |
| 3 | `patient` | company, customer | |
| 4 | `employee` | company | |
| 5 | `consult_room` | company | |
| 6 | `big_screen` | company(JSON 引用 consult_room,**不创建 FK**)| |
| 7 | `appointment_schedule` | company, employee | |
| 8 | `appointment_slot` | appointment_schedule, company, consult_room | |
| 9 | `appointment` | company, patient, employee, appointment_schedule, appointment_slot | **currentVisitId 不创建 FK 约束** |
| 10 | `visit` | company, patient, employee, consult_room, appointment | appointmentId FK 创建 |
| 11 | `triage_queue` | company, visit, appointment, consult_room, employee | |
| 12 | `reception_queue` | company, triage_queue, visit, appointment, consult_room, employee | |

**Phase 2:ALTER TABLE 阶段**

| # | 操作 | 说明 |
|---:|---|---|
| 13 | `ALTER TABLE appointment ADD CONSTRAINT fk_appointment_current_visit FOREIGN KEY(currentVisitId) REFERENCES visit(visitId) ON DELETE RESTRICT` | 闭合循环 FK |

### 3.2 DDL 草案(完整 12 表 + 1 ALTER)

```sql
-- =====================================================================
-- S1-168 OptFlow PMS 门诊预约接诊域 DDL 草案
-- 数据库方言:MySQL 8.0.16+ / InnoDB / utf8mb4 / utf8mb4_0900_ai_ci
-- 设计依据:235 + 235A + 235B(优先级 235B > 235A > 235)
-- 创建时间:2026-09-11
-- 注意:本草案仅在 Markdown 中呈现,未实际执行
-- =====================================================================

-- 设置会话字符集(执行时由客户端保证,此处仅注释)
-- SET NAMES utf8mb4;

-- =====================================================================
-- Phase 1 / Step 1: company(根表)
-- =====================================================================
CREATE TABLE company (
  companyId       BIGINT       NOT NULL AUTO_INCREMENT,
  companyTitle    VARCHAR(100) NOT NULL,
  companyName     VARCHAR(100) NULL,
  companyType     INT          NULL,
  logoUrl         VARCHAR(255) NULL,
  address         VARCHAR(255) NULL,
  phone           VARCHAR(20)  NULL,
  createdAt       DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt       DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (companyId)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '公司(根表,所有对象的 companyId scope)';

-- =====================================================================
-- Phase 1 / Step 2: customer(依赖 company)
-- =====================================================================
CREATE TABLE customer (
  customerId      BIGINT       NOT NULL AUTO_INCREMENT,
  companyId       BIGINT       NOT NULL,
  customerName    VARCHAR(50)  NOT NULL,
  linkMobile      VARCHAR(11)  NOT NULL,
  idCard          VARCHAR(18)  NULL,
  memberTypeId    BIGINT       NULL,
  createdAt       DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt       DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (customerId),
  CONSTRAINT fk_customer_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  INDEX idx_customer_company (companyId)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '会员(patient_owner,1:N Patient)';

-- =====================================================================
-- Phase 1 / Step 3: patient(依赖 company, customer)
-- =====================================================================
CREATE TABLE patient (
  patientId        BIGINT       NOT NULL AUTO_INCREMENT,
  companyId        BIGINT       NOT NULL,
  customerId       BIGINT       NOT NULL,
  patientName      VARCHAR(50)  NOT NULL,
  patientGender    TINYINT      NULL,
  patientBirthday  DATE         NULL,
  idCard           VARCHAR(18)  NULL,
  createdAt        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (patientId),
  CONSTRAINT fk_patient_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  CONSTRAINT fk_patient_customer FOREIGN KEY (customerId) REFERENCES customer(customerId) ON DELETE RESTRICT,
  INDEX idx_patient_company (companyId),
  INDEX idx_patient_customer (customerId)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '患者(N:1 Customer,严禁 patientId 与 customerId 合并)';

-- =====================================================================
-- Phase 1 / Step 4: employee(依赖 company)
-- 235B §2: adminId NOT NULL + UNIQUE(companyId, adminId)
-- 235B §9.1: departmentId 标签【待确认】(允许字段存在)
-- =====================================================================
CREATE TABLE employee (
  employeeId       BIGINT       NOT NULL AUTO_INCREMENT,
  companyId        BIGINT       NOT NULL,
  adminId          BIGINT       NOT NULL COMMENT '每个 Employee 必有 admin 账号(235B §2)',
  employeeName     VARCHAR(50)  NOT NULL,
  nickname         VARCHAR(50)  NULL,
  mobile           VARCHAR(11)  NULL,
  role             VARCHAR(20)  NOT NULL,
  departmentId     BIGINT       NULL COMMENT '235B §9.1 标签【待确认】',
  status           TINYINT      NOT NULL DEFAULT 1,
  createdAt        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (employeeId),
  CONSTRAINT fk_employee_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  UNIQUE KEY uk_employee_company_admin (companyId, adminId),
  INDEX idx_employee_company_role (companyId, role)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '员工(单对象 + employeeId/adminId 双字段,235B §2 方案 A)';

-- =====================================================================
-- Phase 1 / Step 5: consult_room(依赖 company)
-- =====================================================================
CREATE TABLE consult_room (
  consultRoomId    BIGINT       NOT NULL AUTO_INCREMENT,
  companyId        BIGINT       NOT NULL,
  consultRoomName  VARCHAR(50)  NOT NULL,
  status           TINYINT      NOT NULL DEFAULT 1,
  examineList      JSON         NULL COMMENT '检查能力(数组),应用层维护 235B §5.4',
  createdAt        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (consultRoomId),
  CONSTRAINT fk_consultroom_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  INDEX idx_consultroom_company (companyId)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '诊室(JSON examineList 是逻辑引用,非 FK)';

-- =====================================================================
-- Phase 1 / Step 6: big_screen(依赖 company;consultRoomIdArray 是 JSON 逻辑,非 FK)
-- =====================================================================
CREATE TABLE big_screen (
  bigScreenId          BIGINT       NOT NULL AUTO_INCREMENT,
  companyId            BIGINT       NOT NULL,
  bigScreenName        VARCHAR(100) NOT NULL,
  sceneType            TINYINT      NOT NULL COMMENT '1=接诊 / 2=检查',
  consultRoomIdArray   JSON         NULL COMMENT 'JSON 逻辑 N:M 引用 consult_room,235B §5',
  status               TINYINT      NOT NULL DEFAULT 1,
  createdAt            DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt            DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (bigScreenId),
  CONSTRAINT fk_bigscreen_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  INDEX idx_bigscreen_company (companyId)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '大屏(consultRoomIdArray 是 JSON 逻辑引用,严禁创建 FK)';

-- =====================================================================
-- Phase 1 / Step 7: appointment_schedule(依赖 company, employee)
-- 状态:3 个枚举(235 §B-8:ACTIVE / INACTIVE / CANCELLED)
-- =====================================================================
CREATE TABLE appointment_schedule (
  scheduleId     CHAR(36)     NOT NULL DEFAULT (UUID()),
  companyId      BIGINT       NOT NULL,
  employeeId     BIGINT       NOT NULL,
  scheduleDate   DATE         NOT NULL,
  status         VARCHAR(20)  NOT NULL DEFAULT 'ACTIVE',
  remark         VARCHAR(500) NULL,
  createdAt      DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt      DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (scheduleId),
  CONSTRAINT fk_schedule_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  CONSTRAINT fk_schedule_employee FOREIGN KEY (employeeId) REFERENCES employee(employeeId) ON DELETE RESTRICT,
  INDEX idx_schedule_company (companyId),
  INDEX idx_schedule_employee (employeeId),
  CONSTRAINT chk_schedule_status CHECK (status IN ('ACTIVE','INACTIVE','CANCELLED'))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '排班(3 状态,229 §11 冻结)';

-- =====================================================================
-- Phase 1 / Step 8: appointment_slot(依赖 schedule, company, consult_room)
-- 状态:3 个枚举(235 §B-9:AVAILABLE / FULL / CLOSED)
-- 235 §B-9 写 CHECK(bookedCount <= capacity)
-- =====================================================================
CREATE TABLE appointment_slot (
  slotId         CHAR(36)     NOT NULL DEFAULT (UUID()),
  scheduleId     CHAR(36)     NOT NULL,
  companyId      BIGINT       NOT NULL,
  consultRoomId  BIGINT       NULL,
  startTime      TIME         NOT NULL,
  endTime        TIME         NOT NULL,
  capacity       INT          NOT NULL DEFAULT 0,
  bookedCount    INT          NOT NULL DEFAULT 0 COMMENT 'DRAFT 不占容量,229 §12.3',
  status         VARCHAR(20)  NOT NULL DEFAULT 'AVAILABLE',
  serviceType    VARCHAR(50)  NULL,
  createdAt      DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt      DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (slotId),
  CONSTRAINT fk_slot_schedule FOREIGN KEY (scheduleId) REFERENCES appointment_schedule(scheduleId) ON DELETE RESTRICT,
  CONSTRAINT fk_slot_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  CONSTRAINT fk_slot_consultroom FOREIGN KEY (consultRoomId) REFERENCES consult_room(consultRoomId) ON DELETE RESTRICT,
  INDEX idx_slot_schedule_status (scheduleId, status),
  CONSTRAINT chk_slot_status CHECK (status IN ('AVAILABLE','FULL','CLOSED')),
  CONSTRAINT chk_slot_booked_capacity CHECK (bookedCount <= capacity)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '时段(3 状态,DRAFT 不占容量)';

-- =====================================================================
-- Phase 1 / Step 9: appointment(依赖 company, patient, employee, schedule, slot)
-- 状态:12 个枚举(229 §3.1)
-- currentVisitId 不创建 FK 约束(Phase 2 ALTER 补)
-- =====================================================================
CREATE TABLE appointment (
  appointmentId      CHAR(36)     NOT NULL DEFAULT (UUID()),
  companyId          BIGINT       NOT NULL,
  patientId          BIGINT       NOT NULL,
  employeeId         BIGINT       NOT NULL,
  scheduleId         CHAR(36)     NOT NULL,
  slotId             CHAR(36)     NOT NULL,
  appointmentNo      VARCHAR(20)  NOT NULL,
  appointmentType    VARCHAR(20)  NOT NULL DEFAULT 'NORMAL',
  appointmentSource  VARCHAR(20)  NOT NULL DEFAULT 'WEB',
  status             VARCHAR(20)  NOT NULL DEFAULT 'DRAFT',
  slotDate           DATE         NOT NULL,
  slotStartTime      TIME         NOT NULL,
  slotEndTime        TIME         NOT NULL,
  currentVisitId     CHAR(36)     NULL COMMENT 'FK 在 Phase 2 ADD CONSTRAINT 补',
  rescheduledFromId  CHAR(36)     NULL,
  remark             VARCHAR(500) NULL,
  createdBy          BIGINT       NOT NULL,
  createdAt          DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  confirmedAt        DATETIME(3)  NULL,
  completedAt        DATETIME(3)  NULL,
  cancelledAt        DATETIME(3)  NULL,
  cancelReason       VARCHAR(200) NULL,
  updatedAt          DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (appointmentId),
  CONSTRAINT fk_appointment_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  CONSTRAINT fk_appointment_patient FOREIGN KEY (patientId) REFERENCES patient(patientId) ON DELETE RESTRICT,
  CONSTRAINT fk_appointment_employee FOREIGN KEY (employeeId) REFERENCES employee(employeeId) ON DELETE RESTRICT,
  CONSTRAINT fk_appointment_schedule FOREIGN KEY (scheduleId) REFERENCES appointment_schedule(scheduleId) ON DELETE RESTRICT,
  CONSTRAINT fk_appointment_slot FOREIGN KEY (slotId) REFERENCES appointment_slot(slotId) ON DELETE RESTRICT,
  UNIQUE KEY uk_appointment_company_no (companyId, appointmentNo),
  INDEX idx_appointment_slot_employee_status (slotDate, employeeId, status),
  INDEX idx_appointment_patient_status (patientId, status),
  CONSTRAINT chk_appointment_status CHECK (status IN (
    'DRAFT','CONFIRMED','WAITING_ARRIVAL','ARRIVED',
    'TRIAGE_WAITING','WAITING_RECEPTION','CALLED',
    'IN_CONSULTATION','COMPLETED','CANCELLED','NO_SHOW','RESCHEDULED'
  )),
  CONSTRAINT chk_appointment_type CHECK (appointmentType IN (
    'NORMAL','WALK_IN','FOLLOW_UP','RESCHEDULED'
  )),
  CONSTRAINT chk_appointment_source CHECK (appointmentSource IN (
    'WEB','PHONE','WECHAT','FRONT'
  ))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '预约(12 状态 + currentVisitId 在 Phase 2 闭合循环 FK)';

-- =====================================================================
-- Phase 1 / Step 10: visit(依赖 company, patient, employee, consult_room, appointment)
-- 状态:7 个枚举(229 §6.1)
-- WalkIn Fallback 路径:appointmentId NULL 允许
-- =====================================================================
CREATE TABLE visit (
  visitId                     CHAR(36)     NOT NULL DEFAULT (UUID()),
  companyId                   BIGINT       NOT NULL,
  appointmentId               CHAR(36)     NULL COMMENT 'WalkIn Fallback 可空',
  patientId                   BIGINT       NOT NULL,
  employeeId                  BIGINT       NULL COMMENT 'TRIAGED 之后必有',
  consultRoomId               BIGINT       NULL COMMENT 'TRIAGED 之后必有',
  visitType                   VARCHAR(20)  NOT NULL DEFAULT 'NORMAL',
  status                      VARCHAR(20)  NOT NULL DEFAULT 'DRAFT',
  transferredFromVisitId      CHAR(36)     NULL,
  transferredFromEmployeeId   BIGINT       NULL,
  cancelledAt                 DATETIME(3)  NULL,
  cancelReason                VARCHAR(200) NULL,
  completedAt                 DATETIME(3)  NULL,
  transferredAt               DATETIME(3)  NULL,
  createdAt                   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt                   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (visitId),
  CONSTRAINT fk_visit_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  CONSTRAINT fk_visit_appointment FOREIGN KEY (appointmentId) REFERENCES appointment(appointmentId) ON DELETE RESTRICT,
  CONSTRAINT fk_visit_patient FOREIGN KEY (patientId) REFERENCES patient(patientId) ON DELETE RESTRICT,
  CONSTRAINT fk_visit_employee FOREIGN KEY (employeeId) REFERENCES employee(employeeId) ON DELETE RESTRICT,
  CONSTRAINT fk_visit_consultroom FOREIGN KEY (consultRoomId) REFERENCES consult_room(consultRoomId) ON DELETE RESTRICT,
  INDEX idx_visit_appointment (appointmentId),
  INDEX idx_visit_patient_status (patientId, status),
  INDEX idx_visit_company_status_created (companyId, status, createdAt),
  CONSTRAINT chk_visit_status CHECK (status IN (
    'DRAFT','CHECKED_IN','TRIAGED','IN_CONSULTATION',
    'COMPLETED','TRANSFERRED','CANCELLED'
  )),
  CONSTRAINT chk_visit_type CHECK (visitType IN ('NORMAL','WALK_IN'))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '就诊(7 状态,WalkIn 路径 appointmentId NULL 允许,235B §3 不允许物理删除)';

-- =====================================================================
-- Phase 1 / Step 11: triage_queue(依赖 company, visit, appointment, consult_room, employee)
-- 状态:4 个枚举(229 §6.1)
-- visitId NOT NULL + UNIQUE(visitId)
-- IN_POOL 时 consultRoomId / employeeId NULL,ASSIGNED 时填
-- =====================================================================
CREATE TABLE triage_queue (
  triageQueueId   CHAR(36)     NOT NULL DEFAULT (UUID()),
  companyId       BIGINT       NOT NULL,
  visitId         CHAR(36)     NOT NULL,
  appointmentId   CHAR(36)     NULL COMMENT 'WalkIn Fallback 可空',
  consultRoomId   BIGINT       NULL COMMENT 'IN_POOL NULL,ASSIGNED 填',
  employeeId      BIGINT       NULL COMMENT 'IN_POOL NULL,ASSIGNED 填',
  status          VARCHAR(20)  NOT NULL DEFAULT 'IN_POOL',
  assignedAt      DATETIME(3)  NULL,
  assignedBy      BIGINT       NULL,
  createdAt       DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt       DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (triageQueueId),
  CONSTRAINT fk_triage_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  CONSTRAINT fk_triage_visit FOREIGN KEY (visitId) REFERENCES visit(visitId) ON DELETE RESTRICT,
  CONSTRAINT fk_triage_appointment FOREIGN KEY (appointmentId) REFERENCES appointment(appointmentId) ON DELETE RESTRICT,
  CONSTRAINT fk_triage_consultroom FOREIGN KEY (consultRoomId) REFERENCES consult_room(consultRoomId) ON DELETE RESTRICT,
  CONSTRAINT fk_triage_employee FOREIGN KEY (employeeId) REFERENCES employee(employeeId) ON DELETE RESTRICT,
  UNIQUE KEY uk_triage_visit (visitId),
  INDEX idx_triage_status_created (status, createdAt),
  CONSTRAINT chk_triage_status CHECK (status IN (
    'IN_POOL','ASSIGNED','REMOVED','CANCELLED'
  ))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '分诊池(4 状态,visitId UNIQUE 一对一,Transfer 旧 → REMOVED)';

-- =====================================================================
-- Phase 1 / Step 12: reception_queue(依赖 company, triage, visit, appointment, consult_room, employee)
-- 状态:6 个枚举(229 §6.1)
-- 235B §4.3:API-034 不创建 ReceptionQueue,由 API-021 创建
-- =====================================================================
CREATE TABLE reception_queue (
  receptionQueueId  CHAR(36)     NOT NULL DEFAULT (UUID()),
  companyId         BIGINT       NOT NULL,
  triageQueueId     CHAR(36)     NOT NULL,
  visitId           CHAR(36)     NOT NULL,
  appointmentId     CHAR(36)     NULL COMMENT 'WalkIn Fallback 可空',
  consultRoomId     BIGINT       NOT NULL,
  employeeId        BIGINT       NOT NULL,
  status            VARCHAR(20)  NOT NULL DEFAULT 'ASSIGNED',
  sequenceInRoom    INT          NOT NULL DEFAULT 0,
  calledAt          DATETIME(3)  NULL,
  startedAt         DATETIME(3)  NULL,
  completedAt       DATETIME(3)  NULL,
  skippedTimes      INT          NOT NULL DEFAULT 0,
  cancelReason      VARCHAR(200) NULL,
  createdAt         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updatedAt         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (receptionQueueId),
  CONSTRAINT fk_reception_company FOREIGN KEY (companyId) REFERENCES company(companyId) ON DELETE RESTRICT,
  CONSTRAINT fk_reception_triage FOREIGN KEY (triageQueueId) REFERENCES triage_queue(triageQueueId) ON DELETE RESTRICT,
  CONSTRAINT fk_reception_visit FOREIGN KEY (visitId) REFERENCES visit(visitId) ON DELETE RESTRICT,
  CONSTRAINT fk_reception_appointment FOREIGN KEY (appointmentId) REFERENCES appointment(appointmentId) ON DELETE RESTRICT,
  CONSTRAINT fk_reception_consultroom FOREIGN KEY (consultRoomId) REFERENCES consult_room(consultRoomId) ON DELETE RESTRICT,
  CONSTRAINT fk_reception_employee FOREIGN KEY (employeeId) REFERENCES employee(employeeId) ON DELETE RESTRICT,
  UNIQUE KEY uk_reception_triage (triageQueueId),
  UNIQUE KEY uk_reception_visit (visitId),
  INDEX idx_reception_room_status_seq (consultRoomId, status, sequenceInRoom),
  CONSTRAINT chk_reception_status CHECK (status IN (
    'ASSIGNED','WAITING','CALLED','SKIPPED',
    'IN_CONSULTATION','DONE'
  ))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
COMMENT '接诊队列(6 状态,Transfer 旧 → DONE cancelReason=TRANSFERRED)';

-- =====================================================================
-- Phase 2 / Step 13: 闭合循环 FK
-- Appointment.currentVisitId → Visit.visitId
-- 235B §3 ON DELETE RESTRICT(Appointment / Visit 永不物理删除)
-- =====================================================================
ALTER TABLE appointment
  ADD CONSTRAINT fk_appointment_current_visit
  FOREIGN KEY (currentVisitId) REFERENCES visit(visitId) ON DELETE RESTRICT;

-- =====================================================================
-- DDL 草案结束
-- 总计:12 表 + 1 ALTER + 33 真 FK + 5 UNIQUE + 13 INDEX + 6 CHECK
-- =====================================================================
```

### 3.3 FK 数量复核(逐表统计)

| # | 表 | 真 FK 数量 | 真 FK 列表 |
|---:|---|---:|---|
| 1 | company | 0 | – |
| 2 | customer | 1 | fk_customer_company |
| 3 | patient | 2 | fk_patient_company, fk_patient_customer |
| 4 | employee | 1 | fk_employee_company |
| 5 | consult_room | 1 | fk_consultroom_company |
| 6 | big_screen | 1 | fk_bigscreen_company(consultRoomIdArray 是 JSON 逻辑,不计)|
| 7 | appointment_schedule | 2 | fk_schedule_company, fk_schedule_employee |
| 8 | appointment_slot | 3 | fk_slot_schedule, fk_slot_company, fk_slot_consultroom |
| 9 | appointment | 5 + 1(Phase 2)| fk_appointment_company, fk_appointment_patient, fk_appointment_employee, fk_appointment_schedule, fk_appointment_slot, fk_appointment_current_visit |
| 10 | visit | 5 | fk_visit_company, fk_visit_appointment, fk_visit_patient, fk_visit_employee, fk_visit_consultroom |
| 11 | triage_queue | 5 | fk_triage_company, fk_triage_visit, fk_triage_appointment, fk_triage_consultroom, fk_triage_employee |
| 12 | reception_queue | 6 | fk_reception_company, fk_reception_triage, fk_reception_visit, fk_reception_appointment, fk_reception_consultroom, fk_reception_employee |
| **总** | – | **33 真 FK** | – |

**说明**:
- 235B §6.4 写"30 条",实际本草案落地 **33 条**真 FK
- 差异来源:235B 数法可能漏算 BigScreen→Company / AppointmentSlot 的 Company FK 等 3 条
- **以 33 条为准**(本草案 235B 关系矩阵逐条落地)
- BigScreen → ConsultRoom 走 JSON 逻辑,不计真 FK

---

## §4 30/33 真 FK 闭合矩阵

> 按 235B §6.3 关系矩阵逐条落地。下表 33 行 = 12 对象间全部真 FK。

| # | Source | Field | Target | Target Field | ON DELETE | 是否已落地 |
|---:|---|---|---|---|---|:-:|
| 1 | customer | companyId | company | companyId | RESTRICT | ✓ |
| 2 | patient | companyId | company | companyId | RESTRICT | ✓ |
| 3 | patient | customerId | customer | customerId | RESTRICT | ✓ |
| 4 | employee | companyId | company | companyId | RESTRICT | ✓ |
| 5 | consult_room | companyId | company | companyId | RESTRICT | ✓ |
| 6 | big_screen | companyId | company | companyId | RESTRICT | ✓ |
| – | **big_screen** | **consultRoomIdArray** | **consult_room** | **–** | **–** | **JSON 逻辑(不创建 FK)** |
| 7 | appointment_schedule | companyId | company | companyId | RESTRICT | ✓ |
| 8 | appointment_schedule | employeeId | employee | employeeId | RESTRICT | ✓ |
| 9 | appointment_slot | scheduleId | appointment_schedule | scheduleId | RESTRICT | ✓ |
| 10 | appointment_slot | companyId | company | companyId | RESTRICT | ✓ |
| 11 | appointment_slot | consultRoomId | consult_room | consultRoomId | RESTRICT | ✓ |
| 12 | appointment | companyId | company | companyId | RESTRICT | ✓ |
| 13 | appointment | patientId | patient | patientId | RESTRICT | ✓ |
| 14 | appointment | employeeId | employee | employeeId | RESTRICT | ✓ |
| 15 | appointment | scheduleId | appointment_schedule | scheduleId | RESTRICT | ✓ |
| 16 | appointment | slotId | appointment_slot | slotId | RESTRICT | ✓ |
| 17 | **appointment** | **currentVisitId** | **visit** | **visitId** | **RESTRICT** | **✓(Phase 2)** |
| 18 | visit | companyId | company | companyId | RESTRICT | ✓ |
| 19 | visit | appointmentId | appointment | appointmentId | RESTRICT | ✓ |
| 20 | visit | patientId | patient | patientId | RESTRICT | ✓ |
| 21 | visit | employeeId | employee | employeeId | RESTRICT | ✓ |
| 22 | visit | consultRoomId | consult_room | consultRoomId | RESTRICT | ✓ |
| 23 | triage_queue | companyId | company | companyId | RESTRICT | ✓ |
| 24 | triage_queue | visitId | visit | visitId | RESTRICT | ✓ |
| 25 | triage_queue | appointmentId | appointment | appointmentId | RESTRICT | ✓ |
| 26 | triage_queue | consultRoomId | consult_room | consultRoomId | RESTRICT | ✓ |
| 27 | triage_queue | employeeId | employee | employeeId | RESTRICT | ✓ |
| 28 | reception_queue | companyId | company | companyId | RESTRICT | ✓ |
| 29 | reception_queue | triageQueueId | triage_queue | triageQueueId | RESTRICT | ✓ |
| 30 | reception_queue | visitId | visit | visitId | RESTRICT | ✓ |
| 31 | reception_queue | appointmentId | appointment | appointmentId | RESTRICT | ✓ |
| 32 | reception_queue | consultRoomId | consult_room | consultRoomId | RESTRICT | ✓ |
| 33 | reception_queue | employeeId | employee | employeeId | RESTRICT | ✓ |

**统计**:
- ✓ 真 FK 落地:33 条 / 33 条
- ✗ 漏落:0
- JSON 逻辑(BigScreen→ConsultRoom):1 条(已按 235B §5 裁决,严禁创建 FK)

---

## §5 索引闭合矩阵

| # | 表 | 索引名 | 类型 | 列 | 来源 | 状态 |
|---:|---|---|---|---|---|:-:|
| 1 | customer | idx_customer_company | INDEX | (companyId) | 【OptFlow设计】 235 §B-2 | ✓ |
| 2 | patient | idx_patient_company | INDEX | (companyId) | 【OptFlow设计】 235 §B-3 | ✓ |
| 3 | patient | idx_patient_customer | INDEX | (customerId) | 【OptFlow设计】 235 §B-3 | ✓ |
| 4 | employee | **uk_employee_company_admin** | **UNIQUE** | **(companyId, adminId)** | **【OptFlow设计】 235B §2** | ✓ |
| 5 | employee | idx_employee_company_role | INDEX | (companyId, role) | 【OptFlow设计】 235 §B-4 | ✓ |
| 6 | consult_room | idx_consultroom_company | INDEX | (companyId) | 【OptFlow设计】 235 §B-5 | ✓ |
| 7 | big_screen | idx_bigscreen_company | INDEX | (companyId) | 【OptFlow设计】 235 §B-6 | ✓ |
| 8 | appointment_schedule | idx_schedule_company | INDEX | (companyId) | 【OptFlow设计】 235 §B-8 | ✓ |
| 9 | appointment_schedule | idx_schedule_employee | INDEX | (employeeId) | 【OptFlow设计】 235 §B-8 | ✓ |
| 10 | appointment_slot | idx_slot_schedule_status | INDEX | (scheduleId, status) | 【OptFlow设计】 235 §B-9 | ✓ |
| 11 | appointment | **uk_appointment_company_no** | **UNIQUE** | **(companyId, appointmentNo)** | **【OptFlow设计】 235 §B-7** | ✓ |
| 12 | appointment | idx_appointment_slot_employee_status | INDEX | (slotDate, employeeId, status) | 【OptFlow设计】 235 §B-7 | ✓ |
| 13 | appointment | idx_appointment_patient_status | INDEX | (patientId, status) | 【OptFlow设计】 235 §B-7 | ✓ |
| 14 | visit | idx_visit_appointment | INDEX | (appointmentId) | 【OptFlow设计】 235 §B-12 | ✓ |
| 15 | visit | idx_visit_patient_status | INDEX | (patientId, status) | 【OptFlow设计】 235 §B-12 | ✓ |
| 16 | visit | idx_visit_company_status_created | INDEX | (companyId, status, createdAt) | 【OptFlow设计】 235 §B-12 | ✓ |
| 17 | triage_queue | **uk_triage_visit** | **UNIQUE** | **(visitId)** | **【OptFlow设计】 235 §B-10** | ✓ |
| 18 | triage_queue | idx_triage_status_created | INDEX | (status, createdAt) | 【OptFlow设计】 235 §B-10 | ✓ |
| 19 | reception_queue | **uk_reception_triage** | **UNIQUE** | **(triageQueueId)** | **【OptFlow设计】 235 §B-11** | ✓ |
| 20 | reception_queue | **uk_reception_visit** | **UNIQUE** | **(visitId)** | **【OptFlow设计】 235 §B-11** | ✓ |
| 21 | reception_queue | idx_reception_room_status_seq | INDEX | (consultRoomId, status, sequenceInRoom) | 【OptFlow设计】 235 §B-11 | ✓ |

**统计**:
- 唯一约束(UNIQUE):5 条
  - uk_employee_company_admin(235B §2)
  - uk_appointment_company_no(235 §B-7)
  - uk_triage_visit(235 §B-10)
  - uk_reception_triage(235 §B-11)
  - uk_reception_visit(235 §B-11)
- 普通索引(INDEX):16 条
- 总计:21 条索引约束

---

## §6 CHECK 约束闭合矩阵

| # | 表 | 约束名 | 表达式 | 状态 |
|---:|---|---|---|:-:|
| 1 | appointment_schedule | chk_schedule_status | status IN ('ACTIVE','INACTIVE','CANCELLED') | ✓ |
| 2 | appointment_slot | chk_slot_status | status IN ('AVAILABLE','FULL','CLOSED') | ✓ |
| 3 | appointment_slot | chk_slot_booked_capacity | bookedCount <= capacity | ✓ |
| 4 | appointment | chk_appointment_status | 12 状态(229 §3.1)| ✓ |
| 5 | appointment | chk_appointment_type | ('NORMAL','WALK_IN','FOLLOW_UP','RESCHEDULED') | ✓ |
| 6 | appointment | chk_appointment_source | ('WEB','PHONE','WECHAT','FRONT') | ✓ |
| 7 | visit | chk_visit_status | 7 状态(229 §6.1)| ✓ |
| 8 | visit | chk_visit_type | ('NORMAL','WALK_IN') | ✓ |
| 9 | triage_queue | chk_triage_status | 4 状态 | ✓ |
| 10 | reception_queue | chk_reception_status | 6 状态 | ✓ |

**统计**:
- 4 状态机 CHECK 全部实现(Appointment 12 / Visit 7 / TriageQueue 4 / ReceptionQueue 6)
- 2 状态机辅助 CHECK(Schedule 3 / Slot 3)
- 其他 4 字段 CHECK(AppointmentType / AppointmentSource / VisitType / Slot booked_capacity)
- 总计:10 条 CHECK 约束(MySQL 8.0.16+ 强制执行)

---

## §7 NOT NULL / NULL 一致性核对

> 与 235 + 235A + 235B 严格一致,逐表核对。

| 表 | NOT NULL 字段 | NULL 字段 |
|---|---|---|
| company | companyId, companyTitle, createdAt, updatedAt | companyName, companyType, logoUrl, address, phone |
| customer | customerId, companyId, customerName, linkMobile, createdAt, updatedAt | idCard, memberTypeId |
| patient | patientId, companyId, customerId, patientName, createdAt, updatedAt | patientGender, patientBirthday, idCard |
| employee | employeeId, companyId, **adminId(235B §2)**, employeeName, role, status, createdAt, updatedAt | nickname, mobile, departmentId(235B §9.1 标签【待确认】)|
| consult_room | consultRoomId, companyId, consultRoomName, status, createdAt, updatedAt | examineList |
| big_screen | bigScreenId, companyId, bigScreenName, sceneType, status, createdAt, updatedAt | consultRoomIdArray(JSON)|
| appointment_schedule | scheduleId, companyId, employeeId, scheduleDate, status, createdAt, updatedAt | remark |
| appointment_slot | slotId, scheduleId, companyId, startTime, endTime, capacity, bookedCount, status, createdAt, updatedAt | consultRoomId, serviceType |
| appointment | appointmentId, companyId, patientId, employeeId, scheduleId, slotId, appointmentNo, appointmentType, appointmentSource, status, slotDate, slotStartTime, slotEndTime, createdBy, createdAt, updatedAt | currentVisitId, rescheduledFromId, remark, confirmedAt, completedAt, cancelledAt, cancelReason |
| visit | visitId, companyId, patientId, visitType, status, createdAt, updatedAt | appointmentId(WalkIn 可空), employeeId, consultRoomId, transferredFromVisitId, transferredFromEmployeeId, cancelledAt, cancelReason, completedAt, transferredAt |
| triage_queue | triageQueueId, companyId, visitId, status, createdAt, updatedAt | appointmentId, consultRoomId(IN_POOL NULL), employeeId(IN_POOL NULL), assignedAt, assignedBy |
| reception_queue | receptionQueueId, companyId, triageQueueId, visitId, consultRoomId, employeeId, status, sequenceInRoom, skippedTimes, createdAt, updatedAt | appointmentId, calledAt, startedAt, completedAt, cancelReason |

**核对结论**:
- ✓ 所有 NOT NULL 与 235 + 235B 一致
- ✓ 所有 NULL(可空)与 235 + 235B 一致
- ✓ Employee.adminId NOT NULL(235B §2)
- ✓ Visit.appointmentId NULL(WalkIn Fallback)
- ✓ TriageQueue.appointmentId / consultRoomId / employeeId NULL(WalkIn + IN_POOL)

---

## §8 8 项特别审计(最易出错 8 项)

| # | 审计项 | 状态 | 落地证据 |
|---:|---|:-:|---|
| 1 | **Employee.adminId NOT NULL** | ✓ | employee 表 DDL:`adminId BIGINT NOT NULL COMMENT '每个 Employee 必有 admin 账号(235B §2)'` + `UNIQUE KEY uk_employee_company_admin (companyId, adminId)` |
| 2 | **currentVisitId ON DELETE RESTRICT** | ✓ | appointment 表 DDL 中 currentVisitId 字段无 FK(Phase 1 注释),Phase 2 ALTER:`ADD CONSTRAINT fk_appointment_current_visit FOREIGN KEY(currentVisitId) REFERENCES visit(visitId) ON DELETE RESTRICT` |
| 3 | **Visit.appointmentId ON DELETE RESTRICT** | ✓ | visit 表 DDL:`CONSTRAINT fk_visit_appointment FOREIGN KEY (appointmentId) REFERENCES appointment(appointmentId) ON DELETE RESTRICT` |
| 4 | **API-034 不创建 ReceptionQueue** | ✓ | §10 §11 第 1/12 步不包含 `INSERT INTO reception_queue`,只在 transfer 完成后由 API-021 创建(235B §4.3 冻结)|
| 5 | **Transfer 创建新 TriageQueue 时 visitId 必须已有新 Visit** | ✓ | §10 §11 第 2 步先 `INSERT INTO visit`(返回 newVisitId),第 3 步 `INSERT INTO triage_queue(visitId=newVisitId, status='IN_POOL', employeeId=NULL, consultRoomId=NULL)` |
| 6 | **BigScreen JSON 不创建 FK** | ✓ | big_screen 表 DDL 中 consultRoomIdArray 字段:`JSON NULL COMMENT 'JSON 逻辑 N:M 引用 consult_room,235B §5'`,**无 FK 约束** |
| 7 | **Appointment ↔ Visit 循环 FK 创建顺序** | ✓ | Phase 1:先建 appointment(无 currentVisitId FK),再建 visit(含 appointmentId FK);Phase 2:ALTER ADD CONSTRAINT 闭合循环 |
| 8 | **PostgreSQL gen_random_uuid() 不得残留** | ✓ | 全部使用 MySQL 8 语法:`CHAR(36) NOT NULL DEFAULT (UUID())`,**无 gen_random_uuid** |

---

## §9 API-034 visit/transfer 9 步事务(本草案 SQL 模板)

> 235B §4 冻结。本节提供 DDL 实施后 Service 层可用的 SQL 模板(仅作为参考,Service 层不在本 DDL 范围)。

```sql
-- API-034 visit/transfer 9 步事务(235B §4 冻结)
-- 必须在 1 个 DB 事务内,任一失败 ROLLBACK
-- 前置: 旧 visitId, 转诊原因 transferReason

START TRANSACTION;

-- 步骤 1:验证旧 Visit = IN_CONSULTATION
SELECT status FROM visit WHERE visitId = ? FOR UPDATE;
-- 若 status != 'IN_CONSULTATION' → ROLLBACK + 错误

-- 步骤 2:创建新 Visit(DRAFT, 转诊来源)
INSERT INTO visit (
  companyId, appointmentId, patientId, employeeId, consultRoomId,
  visitType, status, transferredFromVisitId, transferredFromEmployeeId, transferredAt
) VALUES (
  ?, ?, ?, ?, ?,
  'NORMAL', 'DRAFT', ?, ?, NOW(3)
);
-- 获得 newVisitId

-- 步骤 3:创建新 TriageQueue(IN_POOL, employeeId/consultRoomId NULL)
INSERT INTO triage_queue (
  companyId, visitId, appointmentId, consultRoomId, employeeId, status
) VALUES (
  ?, ?, ?, NULL, NULL, 'IN_POOL'
);

-- 步骤 4:更新 Appointment.currentVisitId
UPDATE appointment SET currentVisitId = ? WHERE appointmentId = ?;

-- 步骤 5:更新 Appointment.status
UPDATE appointment SET status = 'TRIAGE_WAITING' WHERE appointmentId = ?;

-- 步骤 6:旧 Visit 终态
UPDATE visit SET status = 'TRANSFERRED', transferredAt = NOW(3) WHERE visitId = ?;

-- 步骤 7:旧 TriageQueue 终态
UPDATE triage_queue SET status = 'REMOVED' WHERE visitId = ? AND status != 'REMOVED';

-- 步骤 8:旧 ReceptionQueue 终态
UPDATE reception_queue SET status = 'DONE', cancelReason = 'TRANSFERRED'
WHERE visitId = ? AND status != 'DONE';

-- 步骤 9:COMMIT
COMMIT;
```

**注意**:
- 不在 API-034 创建新 ReceptionQueue(235B §4.3)
- 新 ReceptionQueue 由 API-021 triage/assign 在分诊员分配时创建
- 转诊原因 transferReason 必须在 Service 层记录(可写入 visit.cancelReason 或单独字段,235 未定义,留给 Service 层)

---

## §10 10 项 DDL 自检

| # | 自检项 | 状态 | 证据 |
|---:|---|:-:|---|
| 1 | 12 个表是否全部存在 | ✓ | company / customer / patient / employee / consult_room / big_screen / appointment_schedule / appointment_slot / appointment / visit / triage_queue / reception_queue |
| 2 | 12 个主键是否全部存在 | ✓ | 6 BIGINT AUTO_INCREMENT + 6 CHAR(36) DEFAULT (UUID()),见 §3.2 |
| 3 | 30/33 真 FK 是否全部存在 | ✓ | 33 条真 FK 全部落地(§4 矩阵) |
| 4 | JSON 逻辑关系是否没有错误创建 FK | ✓ | big_screen.consultRoomIdArray 是 JSON,无 FK 约束(§5 + §8 特别审计 6)|
| 5 | 所有 FK 类型是否匹配 | ✓ | BIGINT ↔ BIGINT / CHAR(36) ↔ CHAR(36),无类型不匹配 |
| 6 | 所有 NOT NULL / NULL 是否与 235B 一致 | ✓ | §7 一致性核对全 PASS |
| 7 | 所有 UNIQUE 是否闭合 | ✓ | 5 条 UNIQUE 全部落地(§5 索引矩阵)|
| 8 | 所有 CHECK 是否闭合 | ✓ | 10 条 CHECK(MySQL 8.0.16+ 强制执行)|
| 9 | 所有索引是否闭合 | ✓ | 21 条索引约束(5 UNIQUE + 16 INDEX)|
| 10 | MySQL 8.0.16+ 语法是否统一 | ✓ | UUID() / CHAR(36) / DATETIME(3) / JSON / CHECK 全部 MySQL 8 语法,无 PostgreSQL 残留 |

**自检结论**:10/10 PASS

---

## §11 剩余问题清单

### 11.1 已解决

| 问题 | 解决方案 |
|---|---|
| Employee adminId NOT NULL vs §5.3 文字矛盾 | 235B §2 已覆盖,DDL 采 NOT NULL |
| currentVisitId ON DELETE 矛盾 | 235B §3 已覆盖,DDL 改 RESTRICT + 双 Phase |
| visit/transfer 事务顺序未定义 | 235B §4 已冻结 9 步 |
| 旧 TriageQueue 状态缺失 | 235B §4 步骤 7 = REMOVED |
| ReceptionQueue 在 transfer 创建矛盾 | 235B §4.3 不创建 |
| BigScreen JSON vs FK 矛盾 | 235B §5 已裁决 JSON 逻辑 |
| 4 状态字段无 CHECK | 235B §8 DDL 已全部加 CHECK |
| 循环 FK 创建顺序 | 235B §6.1 双 Phase 方案 |
| departmentId 标签错配 | 235B §9.1 改【待确认】 |
| 04_数据模型.md 引用字段 | 235B §9.2 加备注 |
| 数据库方言未确认 | 235B 老板已确认 MySQL 8.0.16+ |

### 11.2 仍需后续阶段解决(本 DDL 草案不涉及)

| 事项 | 后续阶段 |
|---|---|
| Service 层代码实现(API-001 ~ API-040) | S1-169 / S1-170 |
| Java 实体类生成 | S1-169 |
| 种子数据 / 默认角色 | S1-172 |
| 实际 DDL 在测试库执行 + 数据验证 | S1-168 独立 DDL 审计之后 |
| 性能压测(索引是否够用)| 性能测试阶段 |
| 备份 / 恢复策略 | 运维阶段 |

### 11.3 未发现 BLOCK 项

---

## §12 最终状态

# **S1-168 DDL 草案状态:PASS**

**判定依据**(全部满足 10 项 PASS 条件):
- ✓ MySQL 8.0.16+ 方言统一
- ✓ 12 表闭合
- ✓ 12 PK 闭合
- ✓ 33 真 FK 闭合(实际超过 235B §6.4 的 30 条,因本草案按关系矩阵逐条落地)
- ✓ BigScreen JSON 关系正确(无 FK 约束)
- ✓ NOT NULL / NULL 闭合
- ✓ UNIQUE 闭合(5 条)
- ✓ CHECK 闭合(10 条,MySQL 8.0.16+ 强制)
- ✓ 索引闭合(21 条,含 5 UNIQUE + 16 INDEX)
- ✓ 8 项特别审计全 PASS
- ✓ 235B 全部 8 项 P1 CLOSED 落实到 DDL

**注意**:
- 本 PASS 仅指 DDL **草案**层面(SQL 语法 / 约束 / 索引 / FK 闭合)
- **不**代表:实际数据库执行已成功 / 性能已验证 / 种子数据已导入
- 后续 S1-168 独立 DDL 审计 会对本草案做二次审计

---

## §13 一句话最终判断

> **S1-168 DDL 草案基于 235 + 235A + 235B(优先级 235B > 235A > 235)生成,采用老板确认的 MySQL 8.0.16+ / InnoDB / utf8mb4 / utf8mb4_0900_ai_ci 方言,12 对象表 + 33 真 FK + 5 UNIQUE + 21 索引 + 10 CHECK 全部闭合,BigScreen JSON 逻辑引用严格按 235B §5 不创建 FK,Appointment ↔ Visit 循环 FK 通过 Phase 1 (CREATE) + Phase 2 (ALTER) 双阶段方案闭合,API-034 9 步事务(含旧 TriageQueue=REMOVED、ReceptionQueue 不创建)严格按 235B §4 冻结。10 项 DDL 自检全 PASS,8 项特别审计全 PASS,本草案状态【PASS】。**

---

## §14 Git 验证

- **HEAD 不变**:`1bf472c39812d88ad41c2e8a0570ddf37ab0e8fc`
- **本轮不 commit / push** ✓
- **0 号闸门 4 文件 SHA256 锁定不变** ✓
- **历史 MD(190~235B 共 49 份)未修改** ✓
- **新文件**:`236_S1-168_DDL草案.md`(untracked,待老板决定是否 commit / 是否进入独立 DDL 审计阶段)
- **DDL 仅在 Markdown 中,未实际执行任何数据库** ✓
- **本文件不覆盖任何已有文件** ✓
