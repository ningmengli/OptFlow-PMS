# 240 S1-169 Q4 跨公司隔离架构正式裁决

> **本轮定位**:S1-169 阶段 Q4 跨公司隔离架构**正式裁决**。基于 239A 真实 V4.3 后端证据(`E:\C\minimax\VisionCare PMS\backend\`),把 Q4 从【待确认】冻结为【OptFlow设计】。
> - 严禁修改 235 / 235A / 235B / 236 / 236A / 236B / 237 / 237A / 238 / 238A / 239 / 239A / 任何历史 MD
> - 严禁修改 VisionCare PMS 任何源码
> - 严禁修改 controller.js / HTML / .gitignore
> - 严禁运行 DDL / 严禁连接生产数据库
> - 严禁 commit / push
> - 本文件**只**做架构裁决,不实际写 Java
> - 标签:【原系统事实】/【已验证业务事实】/【OptFlow设计】/【待确认】

---

## §0 完整性闸门

- **HEAD**:`d662def0865c0845aadc60ef0e25ff8ecb797d4d`(S1-169 238+238A commit)
- **REMOTE == LOCAL**:`d662def` ✓
- **0 号闸门 4 文件 SHA256 全部 PASS** ✓
- **历史 MD(190~239 共 57 份)未修改** ✓
- **本轮不写代码 / 不 commit / 不 push** ✓

---

## §1 任务定义

### 1.1 现状回顾

| 阶段 | 结论 |
|---|---|
| 238(实施前置) | Q4 = 【待确认】,5 个候选 A/B/C/D/E/F 列出 |
| 238A(待确认问题清单) | Q4 = P0 必答,跨公司隔离策略需 S1-169 实施前明确 |
| 239(证据调查) | 本仓库无后端,需追踪外部 |
| 239A(后端追踪) | **CASE 1:发现 V4.3 真后端** Spring Boot 3.2.5 + MyBatis-Plus 3.5.5 + Sa-Token 1.37.0,已实现 TenantLineInnerInterceptor(但 TenantInterceptor 未注册 WebMvc) |

### 1.2 本轮目标

把 Q4 从【待确认】正式冻结为【OptFlow设计】,产出可执行的 240 文档,**成为 S1-169 / S1-170 实施的最终依据**。

### 1.3 严格禁止

- ✗ 不得写"composite FK 永久禁止"——只写"当前阶段不新增 composite FK"
- ✗ 不得照搬 V4.3 命名(`shop_id` / `staff` / `patient_visit`)——V4.4 用 235B / 236B 命名
- ✗ 不得机械复制 V4.3 25 张 IGNORE_TABLES——V4.4 重新裁决
- ✗ 不得把 `E:\C\minimax\VisionCare PMS\backend\` 整个目录直接复制成 V4.4
- ✗ 不得把 V4.3 `is_deleted` 软删除字段直接加入 V4.4 S1-169 Entity
- ✗ 不得把 V4.3 `medical_record` 重新引入 V4.4 域模型

---

## §2 Q4 最终裁决(【OptFlow设计】)

### 2.1 最终方案

**【OptFlow设计】** Q4 跨公司隔离架构 = **MyBatis-Plus TenantLineInnerInterceptor + Service 层跨对象 company 一致性校验**

### 2.2 职责划分(明确边界)

| 组件 | 职责 | 不负责 |
|---|---|---|
| **MyBatis-Plus TenantLineInnerInterceptor** | **查询/更新/删除层**的 company scope 自动过滤(类似"WHERE companyId = ?") | 跨对象引用的一致性校验;Context 来源验证 |
| **Service 层** | **跨实体引用**的 company 一致性验证(如 "Appointment.patientId 必须属于当前 company") | MyBatis 层 SQL 自动过滤(由 TenantLineInnerInterceptor 负责)|
| **CompanyContext(ThreadLocal)** | 存当前请求的 companyId | 来源验证(由 Sa-Token / Interceptor 负责)|

### 2.3 关键定义(写进 Service / Repository / SQL 模板)

```
TenantLineInnerInterceptor 负责查询/更新层的 company scope 自动过滤;
Service 层负责跨实体引用的 company 一致性验证。
```

### 2.4 标签

# **【OptFlow设计】**(不再是【待确认】)

---

## §3 严格措辞 — composite FK

### 3.1 ❌ 错误措辞(禁止)

- ✗ "composite FK 永久禁止"
- ✗ "V4.4 已决定永远不引入 composite FK"
- ✗ "composite FK 已被否定"

### 3.2 ✅ 正确措辞(必须)

> "**当前阶段不新增 composite FK;跨公司一致性采用 MyBatis-Plus scope + Service 校验。**"

### 3.3 未来变更路径

如果以后需要引入 composite FK,**必须**重新正式裁决,流程:
1. 新建裁决文档(类似 240 模式)
2. 不得在 S1-169 / S1-170 实施阶段"擅自引入"
3. 不得修改 240(只新建 240A / 241 等)

---

## §4 company context 严格性(关键)

### 4.1 V4.3 现状(参考,但 V4.4 不照搬)

| V4.3 实现 | 代码位置 | 行为 |
|---|---|---|
| TenantContextHolder 缺失时 fallback = 1L | `common/context/TenantContextHolder.java` | 单门店默认 |
| TenantInterceptor 缺失时静默 | `common/interceptor/TenantInterceptor.java` 第 37-40 行 | `TenantContextHolder.setShopId(1L)` |

### 4.2 V4.4 严格规则(冻结)

**【OptFlow设计】** V4.4 **严禁静默 fallback 到 1L**:

| 场景 | V4.3 行为 | V4.4 行为(冻结)|
|---|---|---|
| company context 缺失 | fallback 1L(静默)| **认证 / 请求上下文错误 → 请求失败** |
| 默认 companyId = 1 | ✓ 允许 | **✗ 禁止** |
| Sa-Token 未登录 | 跳过 shopId 设置 | **请求失败**(公开接口除外)|
| 跨店请求无权限 | 静默 | **请求失败 + 401/403** |

### 4.3 必须实现

- CompanyContext 缺失 → 抛 `CompanyContextMissingException` → 401
- 公开接口(登录、注册等)不需 company context → 在白名单中
- 默认 companyId **禁止** = 1(必须从用户登录态获取)

---

## §5 company context 来源(多层)

### 5.1 数据流

```
登录用户
   ↓
Sa-Token 登录态(用户 → 关联公司列表)
   ↓
当前 company(单店 / 多店切换)
   ↓
CompanyContext(ThreadLocal<Long> companyId)
   ↓
MyBatis-Plus TenantLineInnerInterceptor
   ↓
SQL 自动带 WHERE companyId = ?
```

### 5.2 多门店切换(必须权限验证)

**【OptFlow设计】** 多门店切换路径:

```java
// 客户端提交 X-Company-Id: 123
String targetCompanyId = request.getHeader("X-Company-Id");

// 必须验证当前登录用户是否拥有该 company 权限
Long userId = StpUtil.getLoginIdAsLong();
boolean hasAccess = userCompanyService.hasAccess(userId, targetCompanyId);
if (!hasAccess) {
    throw new CompanyAccessDeniedException("用户无权访问该门店");
}

// 通过验证后,才设置 CompanyContext
CompanyContext.setCompanyId(Long.parseLong(targetCompanyId));
```

### 5.3 严格禁止

- ✗ 不能仅因为客户端提交 `X-Company-Id` 就直接信任
- ✗ 客户端不能绕过权限验证直接切换 company
- ✗ 默认 companyId = 1(必须从登录态或显式切换获取)

### 5.4 当前请求生命周期

```
请求进入
  ↓
Sa-Token Interceptor(认证)
  ↓
CompanyContext Interceptor(注入 companyId)
  ↓
Controller → Service → Repository → MyBatis → DB
  ↓
响应返回
  ↓
afterCompletion 清理 CompanyContext
```

---

## §6 Service 层跨对象 company 一致性校验

### 6.1 必须校验的对象

至少覆盖以下 9 个对象的所有跨对象引用:

| 对象 | 关键跨对象引用 |
|---|---|
| **Appointment** | patientId, employeeId, scheduleId, slotId |
| **Patient** | customerId |
| **Employee** | (自身,无需跨对象)|
| **ConsultRoom** | (自身)|
| **AppointmentSchedule** | employeeId |
| **AppointmentSlot** | scheduleId, consultRoomId |
| **Visit** | appointmentId, patientId, employeeId, consultRoomId |
| **TriageQueue** | visitId, appointmentId, consultRoomId, employeeId |
| **ReceptionQueue** | triageQueueId, visitId, appointmentId, consultRoomId, employeeId |

### 6.2 校验规则

```java
// 伪代码示例
void assertSameCompany(Company current, Object... references) {
    for (Object ref : references) {
        Long refCompanyId = extractCompanyId(ref);
        if (!current.getCompanyId().equals(refCompanyId)) {
            throw new CompanyMismatchException(
                "实体 " + ref + " 属于 company " + refCompanyId +
                ", 与当前 company " + current.getCompanyId() + " 不一致"
            );
        }
    }
}
```

### 6.3 重点场景

**【OptFlow设计】** **Appointment 创建/更新时必须校验**:

```java
// 关键校验(不能只验证 "ID 存在")
void validateAppointmentReference(Appointment a) {
    Long currentCompanyId = CompanyContext.getCompanyId();
    
    // 1. 验证 patient 属于当前 company
    Patient p = patientRepository.findById(a.getPatientId())
        .orElseThrow(() -> new NotFoundException("Patient 不存在"));
    if (!p.getCompanyId().equals(currentCompanyId)) {
        throw new CompanyMismatchException("Patient 不属于当前 company");
    }
    
    // 2. 验证 employee 属于当前 company
    Employee e = employeeRepository.findById(a.getEmployeeId())
        .orElseThrow(() -> new NotFoundException("Employee 不存在"));
    if (!e.getCompanyId().equals(currentCompanyId)) {
        throw new CompanyMismatchException("Employee 不属于当前 company");
    }
    
    // 3. 验证 schedule 属于当前 company
    AppointmentSchedule s = scheduleRepository.findById(a.getScheduleId())
        .orElseThrow(() -> new NotFoundException("Schedule 不存在"));
    if (!s.getCompanyId().equals(currentCompanyId)) {
        throw new CompanyMismatchException("Schedule 不属于当前 company");
    }
    
    // 4. 验证 slot 属于当前 company
    AppointmentSlot sl = slotRepository.findById(a.getSlotId())
        .orElseThrow(() -> new NotFoundException("Slot 不存在"));
    if (!sl.getCompanyId().equals(currentCompanyId)) {
        throw new CompanyMismatchException("Slot 不属于当前 company");
    }
}
```

### 6.4 严格禁止

- ✗ **不能只验证 "ID 存在"**(必须验证 company 一致性)
- ✗ 不能用乐观锁(版本号)替代 company 校验
- ✗ 不能信任前端传入的 companyId 作为最终依据

---

## §7 V4.4 命名裁决(【OptFlow设计】)

### 7.1 V4.4 命名规则(冻结)

| V4.4 命名(采用 235B / 236B)| V4.3 实际命名(不沿用)|
|---|---|
| `company` | `shop` |
| `companyId` | `shop_id` |
| `employee` | `staff` |
| `visit` | `patient_visit` |
| `consult_room` | `queue_room` |
| `big_screen` | `queue_call_device` |
| `appointment` | (V4.3 没独立 appointment 表,业务在 queue_schedule / patient_visit)|
| `appointment_schedule` | `schedule_template` |
| `appointment_slot` | (无独立表)|
| `triage_queue` | (无独立表)|
| `reception_queue` | `queue_call_log` / `queue_room_doctor` |

### 7.2 命名裁决原则

- **V4.3 命名是迁移/映射来源,不是 V4.4 最终命名**
- V4.4 采用 235B / 236B 命名
- V4.3 → V4.4 迁移时,需要写 `迁移映射表`(shop → company, shop_id → companyId 等)
- 235B / 236B 是 V4.4 设计的最终依据(优先级 235B > 235A > 235)

### 7.3 必须维护的迁移映射表

| V4.3 字段 | V4.4 字段 |
|---|---|
| `shop_id` | `companyId` |
| `shop.title` | `company.companyTitle` |
| `staff.id` | `employee.employeeId` |
| `patient_visit.id` | `visit.visitId` |
| `queue_room.id` | `consult_room.consultRoomId` |
| `queue_call_device.id` | `big_screen.bigScreenId` |
| `schedule_template.id` | `appointment_schedule.scheduleId` |
| (无) | `appointment.appointmentId` |
| (无) | `appointment_slot.slotId` |
| (无) | `triage_queue.triageQueueId` |
| (无) | `reception_queue.receptionQueueId` |

---

## §8 is_deleted 软删除裁决

### 8.1 V4.3 现状

- V4.3 实际已有 `is_deleted` 字段(【原系统事实】)
- V4.3 MetaObjectHandler INSERT 时自动填 `is_deleted=0`
- V4.3 IGNORE_TABLES 不包含 `is_deleted` 字段

### 8.2 V4.4 裁决(【OptFlow设计】)

**V4.4 当前不自动增加 `is_deleted` 字段到 S1-169 域模型**。

### 8.3 理由

- S1-168 当前基线(236B)未冻结 `is_deleted` 字段
- 235B §B-1 ~ §B-12 字段表未包含 `is_deleted`
- 238A §1.1 明确禁止"新增 deleted / version / auditVersion 等字段"
- V4.3 已有 `is_deleted` 属于**原系统事实**,不直接迁移到 V4.4

### 8.4 正确措辞

> "V4.3 已存在 is_deleted 属于**原系统事实**;V4.4 是否需要软删除机制,需要在对应域**单独裁决**。"

### 8.5 不得行为

- ✗ 不得把 V4.3 `is_deleted` 字段直接加入 S1-169 域模型
- ✗ 不得在 MetaObjectHandler 自动填 `isDeleted`
- ✗ 不得修改 235B / 236B / 236A / 236B 加 `isDeleted` 字段

### 8.6 未来软删除机制

如果 V4.4 后续需要软删除(域独立),必须新建 240A / 241 等裁决文档,流程与 §3.3 一致。

---

## §9 MedicalRecord 裁决

### 9.1 V4.3 现状

- V4.3 实际**已存在** `medical_record` / `glasses_prescription` / `medical_visit_log` 表(【原系统事实】)
- 235B §6.4 已裁决:**V4.4 不引入 MedicalRecord 实体**

### 9.2 V4.4 裁决(【OptFlow设计】)

**V4.4 当前预约接诊域不引入 MedicalRecord 实体**。

维持 235B §6 冻结:
- Appointment **直接关联 Patient**(`Appointment.patientId`)
- 不引入 `Appointment → MedicalRecord → Patient` 链路
- MedicalRecord 作为业务编号概念存在(类似 `medicalCode` 模式),不作为系统实体

### 9.3 不得行为

- ✗ 不得在 V4.4 S1-169 Entity 中加入 MedicalRecord / GlassesPrescription / MedicalVisitLog 实体
- ✗ 不得在 Appointment 中加 `medicalRecordId` 字段
- ✗ 不得重新引入 V4.3 medical_record 域

### 9.4 V4.3 MedicalRecord 域处理

V4.3 现有 medical_record 域**保留在 V4.3** 但不迁移到 V4.4。V4.4 是**预约接诊域**新设计,医疗档案域属于其他域,**单独裁决**。

---

## §10 V4.3 backend 复用原则

### 10.1 不要整个目录直接复制

V4.3 backend **不要整体复制**为 V4.4 起点。理由:
- V4.3 是单门店实际产品,V4.4 是 SaaS 多门店重构
- V4.3 命名风格与 235B 不一致
- V4.3 业务表(medical_record / queue_room)与 235B 域模型不一致

### 10.2 复用策略:基础设施复用 + 业务域重构

#### 10.2.1 可以复用(V4.3 已实施,风险低)

| 组件 | V4.3 实现 | V4.4 复用? |
|---|---|:-:|
| Spring Boot 启动配置 | pom.xml | ✓ |
| Java 17 | pom.xml | ✓ |
| MyBatis-Plus 3.5.5 | pom.xml | ✓ |
| Sa-Token 1.37.0 | pom.xml | ✓ |
| Redis 集成 | RedisConfig | ✓ |
| 通用异常处理 | common/exception | ✓ |
| 统一响应 | common/result | ✓ |
| 认证框架 | SaTokenConfig | ✓ |
| 日志配置 | (Spring Boot 默认) | ✓ |
| 基础配置文件 | application.yml | ✓(需调整) |

#### 10.2.2 不复用(必须按 235B + 236B 重新实现)

| 组件 | 原因 |
|---|---|
| **预约 / 接诊域 12 对象** | V4.3 命名与 235B 不一致 |
| **company context 实现** | V4.3 TenantInterceptor 未注册,V4.4 必须真正生效 |
| **IGNORE_TABLES 25 张** | V4.3 表与 V4.4 表不同,需重新裁决 |
| **MetaObjectHandler 适配** | V4.3 用 `shop_id` / `is_deleted`,V4.4 用 `companyId` 不加 isDeleted |
| **V4.3 medical_record 域** | V4.4 不引入 MedicalRecord |
| **V4.3 字段命名风格** | V4.4 用 235B 命名 |

### 10.3 V4.4 业务域 = 235B 设计的 12 对象

**【OptFlow设计】** V4.4 业务域**必须** 按 235B + 236B + 233 重新实现:

```
company
customer
patient
employee
consult_room
big_screen
appointment_schedule
appointment_slot
appointment
visit
triage_queue
reception_queue
```

---

## §11 V4.3 TenantInterceptor 处理

### 11.1 V4.3 已知缺陷

**【原系统事实】** V4.3 TenantInterceptor 类**存在但未注册**到 WebMvcConfig:
- 实际行为:`@Component` 注解使 Spring 管理 bean,但**不**会拦截 HTTP 请求
- 结果:CompanyContext 永远 fallback 1L
- 实际运行:单门店跑 OK(默认 1L)

### 11.2 V4.4 必须修复

**【OptFlow设计】** V4.4 **必须真正把 company context 建立到请求生命周期中**:

1. 显式注册到 WebMvcConfig(`registry.addInterceptor(companyContextInterceptor)`)
2. `preHandle` 从 Sa-Token / 客户端 header 取 companyId(必须验证权限,见 §5)
3. `afterCompletion` **必须清理 context**(避免内存泄漏 / 跨请求污染)
4. context 缺失 → 抛异常(不能 fallback 1L,见 §4)

### 11.3 TenantContextHolder 升级

V4.3 TenantContextHolder 只支持 `Long shopId`:
```java
private static final ThreadLocal<Long> SHOP_ID = new ThreadLocal<>();
```

V4.4 CompanyContext 必须支持**完整上下文**(不只是 companyId):
```java
public class CompanyContext {
    private static final ThreadLocal<Long> COMPANY_ID = new ThreadLocal<>();
    private static final ThreadLocal<Long> USER_ID = new ThreadLocal<>();
    private static final ThreadLocal<List<Long>> USER_ACCESSIBLE_COMPANIES = new ThreadLocal<>();
    // ...
}
```

至少包含:
- `companyId`(当前 company)
- `userId`(当前用户)
- `userAccessibleCompanies`(用户有权访问的 company 列表,用于多店切换权限验证)

### 11.4 不得行为

- ✗ 不得照搬 V4.3 TenantInterceptor 不注册 WebMvcConfig 的"缺陷"
- ✗ 不得省略 afterCompletion 清理
- ✗ 不得 fallback 1L

---

## §12 MyBatis-Plus TenantLineInnerInterceptor V4.4 适配

### 12.1 沿用(【OptFlow设计】)

V4.4 沿用 V4.3 的 **TenantLineInnerInterceptor**(MyBatis-Plus 内置),**功能相同**:
- `getTenantId()` 从 CompanyContext 读 companyId(不能 fallback 1L)
- `getTenantIdColumn()` 返回 `companyId`(不是 `shop_id`)
- `ignoreTable(String tableName)` 重新裁决 IGNORE_TABLES(V4.4 表)

### 12.2 tenantId column 命名(关键)

**【OptFlow设计】** V4.4 tenantId column = **`companyId`**(V4.4 命名)

**不**沿用 V4.3 的 `shop_id`。

```java
@Bean
public MybatisPlusInterceptor mybatisPlusInterceptor() {
    MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();

    TenantLineInnerInterceptor tenantInterceptor = new TenantLineInnerInterceptor(new TenantLineHandler() {
        @Override
        public Expression getTenantId() {
            Long companyId = CompanyContext.getCompanyId();
            if (companyId == null) {
                throw new CompanyContextMissingException("CompanyContext 缺失");  // 不 fallback!
            }
            return new LongValue(companyId);
        }

        @Override
        public String getTenantIdColumn() {
            return "companyId";  // 不是 shop_id
        }

        @Override
        public boolean ignoreTable(String tableName) {
            return IGNORE_TABLES_V44.contains(tableName.toLowerCase());
        }
    });
    interceptor.addInnerInterceptor(tenantInterceptor);
    // ... 分页 / 乐观锁 / 防全表更新
    return interceptor;
}
```

### 12.3 关键差异 vs V4.3

| 维度 | V4.3 | V4.4 |
|---|---|---|
| tenantId column | `shop_id` | `companyId` |
| 缺失时行为 | fallback 1L | **抛异常** |
| IGNORE_TABLES | 25 张 V4.3 表 | **重新裁决(见 §13)** |

---

## §13 IGNORE_TABLES 重新裁决

### 13.1 严格禁止

- ✗ 不得直接复制 V4.3 的 25 张 IGNORE_TABLES
- ✗ 不得机械复制 V4.3 系统表名

### 13.2 V4.4 IGNORE_TABLES 裁决原则

V4.4 的 `IGNORE_TABLES` 应包含**系统级表**(无 companyId 字段或不需要按 company 过滤):

| V4.4 表 | 加入 IGNORE_TABLES? | 理由 |
|---|:-:|---|
| `flyway_schema_history` | ✓ | 系统表 |
| `sys_*` 表 | ✓ | 系统级表 |
| `medical_record` 等(若引入)| ✓(若引入)| 历史保留 |
| `company` | ✓(若 companyId 是主键) | company 是 scope 自身 |

### 13.3 V4.4 IGNORE_TABLES 必须在 S1-169 / S1-170 实施时**重新裁决**

本 240 文档**不**给出最终 IGNORE_TABLES 列表(因为 V4.4 表名未完全冻结到 MyBatis 层)。

**S1-169 / S1-170 实施时必须**:
1. 列出 V4.4 全部表(基于 236B 12 表 + V4.4 重构可能新增的表)
2. 标注每张表是否需要 company filter
3. 形成 `IGNORE_TABLES_V44` 集合
4. 在 MybatisPlusConfig.java 实际配置

---

## §14 MetaObjectHandler 重新适配

### 14.1 沿用思路(【OptFlow设计】)

可以复用 V4.3 MetaObjectHandler 的**自动填充思想**:
- INSERT 时填 `createdAt` / `updatedAt`
- 自动注入 `companyId`(从 CompanyContext 读)

### 14.2 必须重新适配

V4.4 MetaObjectHandler 必须:
- `companyId` 来源:从 CompanyContext 读(不是 `shopId`)
- 缺失 companyId → 抛异常(**不**fallback)
- 自动填 `createdAt` / `updatedAt`(用 `LocalDateTime.now()`)

### 14.3 不得行为

- ✗ **不得**自动新增 `isDeleted` 字段(见 §8)
- ✗ 不得照搬 V4.3 字段命名

```java
@Bean
public MetaObjectHandler metaObjectHandler() {
    return new MetaObjectHandler() {
        @Override
        public void insertFill(MetaObject metaObject) {
            this.strictInsertFill(metaObject, "createdAt", LocalDateTime.class, LocalDateTime.now());
            this.strictInsertFill(metaObject, "updatedAt", LocalDateTime.class, LocalDateTime.now());
            // V4.4: 自动注入 companyId(必须存在,不能 fallback)
            if (metaObject.hasGetter("companyId") && metaObject.getValue("companyId") == null) {
                Long companyId = CompanyContext.getCompanyId();
                if (companyId == null) {
                    throw new CompanyContextMissingException("CompanyContext 缺失,无法自动注入 companyId");
                }
                this.strictInsertFill(metaObject, "companyId", Long.class, companyId);
            }
            if (metaObject.hasGetter("status") && metaObject.getValue("status") == null) {
                this.strictInsertFill(metaObject, "status", Integer.class, 1);
            }
        }

        @Override
        public void updateFill(MetaObject metaObject) {
            this.strictUpdateFill(metaObject, "updatedAt", LocalDateTime.class, LocalDateTime.now());
        }
    };
}
```

---

## §15 10 项安全不变量(【OptFlow设计】,V4.4 必须保证)

| # | 不变量 | 责任组件 | 失败行为 |
|---:|---|---|---|
| 1 | 请求必须有合法 company context | CompanyContext Interceptor | 401 / 请求失败 |
| 2 | 当前用户必须有该 company 权限 | UserCompanyService | 403 / 请求失败 |
| 3 | 查询自动带 company scope | TenantLineInnerInterceptor | DB 层保证(忽略 → 0 行)|
| 4 | 更新自动带 company scope | TenantLineInnerInterceptor | DB 层保证(忽略 → 0 行) |
| 5 | 删除自动带 company scope | TenantLineInnerInterceptor | DB 层保证(忽略 → 0 行) |
| 6 | 跨对象引用必须做 company 一致性校验 | Service 层 | 抛 CompanyMismatchException |
| 7 | **不允许默认 companyId = 1** | CompanyContext + Interceptor | 抛异常(不 fallback) |
| 8 | 不允许客户端 companyId 直接绕过权限 | Interceptor(必须验证用户权限) | 403 / 请求失败 |
| 9 | 不允许 Service 使用未校验的跨 company ID | Service 层 assertSameCompany | 抛 CompanyMismatchException |
| 10 | company context 请求结束必须清理 | CompanyContext Interceptor afterCompletion | 清理 ThreadLocal(避免内存泄漏 / 跨请求污染) |

### 15.1 补充不变量(隐含)

- TenantContextHolder(CompanyContext) 缺失 → 抛 CompanyContextMissingException(**不**fallback 1L)
- 多门店切换必须验证用户是否有目标 company 权限
- X-Company-Id header 不能直接信任,必须经用户权限校验
- 公开接口(登录、注册等)不进 company context

---

## §16 Q4 状态变更(从【待确认】→【OptFlow设计】)

### 16.1 状态变更

**Q4 在 240 之前**:**【待确认】**(238A / 239A / 239)

**Q4 在 240 之后**:**【OptFlow设计】**(已冻结)

### 16.2 后续阶段引用规则

- S1-169 / S1-170 实施时,Q4 = 【OptFlow设计】(以 240 为准)
- 不得在 S1-169 / S1-170 中"重新讨论 Q4"
- 除非发现"直接证据证明当前方案与真实工程无法实现"

### 16.3 重新裁决触发条件(仅限以下 3 种)

1. 老板明确要求重新裁决
2. 实施过程中发现**直接证据**证明 TenantLineInnerInterceptor + Service 校验不可行
3. V4.4 出现新的业务需求(超出本 240 假设的范围)

**重新裁决流程**:新建 240A / 241 等文档,不修改 240。

---

## §17 V4.4 S1-169 启动条件最终检查

| 启动条件 | 状态 |
|---|:-:|
| 12 对象 Entity 字段映射完整 | ✓(238 §4) |
| 12 PK 类型明确 | ✓(236B §3) |
| 31 FK 在 Java Entity 的处理明确 | ✓(236B §6) |
| Service 责任边界明确 | ✓(238 §7) |
| API 40 个映射到 Controller 明确 | ✓(238 §8) |
| 9 步 Transfer 事务明确 | ✓(238 §12 + 235B §4) |
| 11 项测试矩阵明确 | ✓(238 §19) |
| Q4 跨公司隔离裁决 | **✓【OptFlow设计】(本 240 冻结)** |
| Q5 21/22 数字差异 | ✓(239A §5.4 锁定 21 条) |
| 状态机实现方式(Q1) | 【可现场决定】 |
| API-021 事务边界(Q2) | 【可现场决定,推荐 1 事务】 |
| BigScreen JSON ORM(Q3) | 【可现场决定,推荐 String】 |

# **S1-169 启动条件全部满足**

---

## §18 一句话最终判断

> **240 S1-169 Q4 跨公司隔离架构正式裁决:基于 239A 真实 V4.3 后端证据(Spring Boot 3.2.5 + MyBatis-Plus 3.5.5 + Sa-Token 1.37.0 + Java 17),Q4 冻结为【OptFlow设计】 = MyBatis-Plus TenantLineInnerInterceptor(自动 company 过滤,column=`companyId`)+ Service 层跨对象 company 一致性校验;V4.4 严格禁止 fallback companyId=1L(必须从登录态或显式切换获取,缺失 → 抛 CompanyContextMissingException);V4.4 不沿用 V4.3 `shop_id` / `staff` / `patient_visit` 命名,改用 235B 命名 `companyId` / `employee` / `visit` / `consult_room`;V4.4 不自动新增 `is_deleted` 软删除字段,不引入 `MedicalRecord` 实体;V4.4 IGNORE_TABLES 重新裁决(不机械复制 V4.3 25 张);V4.4 TenantInterceptor 必须显式注册到 WebMvcConfig 并 afterCompletion 清理 context(不照搬 V4.3 缺陷);V4.3 backend 基础设施可复用(Spring Boot / MyBatis-Plus / Sa-Token / Redis),业务域 12 对象必须按 235B + 236B + 233 重新实现;10 项安全不变量冻结;S1-169 启动条件全部满足,可正式进入 Entity → Repository → Service → Controller → API → Test 的 Java 实施阶段。**

---

## §19 Git 验证

- **HEAD 不变**:`d662def`(S1-169 238+238A commit)
- **本轮不 commit / push** ✓
- **0 号闸门 4 文件 SHA256 锁定不变** ✓
- **历史 MD(190~239 共 57 份)未修改** ✓
- **VisionCare PMS 任何文件未修改** ✓
- **新文件**:`240_S1-169_Q4跨公司隔离架构正式裁决.md`(本文件,untracked)
- **本文件不实际写 Java / 不执行 DDL / 不连接数据库** ✓

---

## §20 报告统计

- 报告字节数:约 22000 字节
- 章节数:20
- Q4 状态:【待确认】→【OptFlow设计】(已冻结)
- 严格禁止误读清单:5 项
- 10 项安全不变量:全部冻结
- V4.3 复用范围:基础设施 + V4.4 业务域重构
- V4.4 命名规则:235B 命名(不沿用 V4.3)
- S1-169 启动条件:**全部满足**
