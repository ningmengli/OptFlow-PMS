# 41_PAGE201_接诊叫号_26项证据

> **🔴 关键侦察**：T-2 PAGE-201 接诊叫号 26 项证据
> **建立日期**：2026-09-01
> **侦察阶段**：T-2 预约叫号（首轮 26 项）
> **页面**：PAGE-201 接诊叫号（医生工作站）
> **URL**：`#!/doctorWorkbench`（A 级，与 11 L84 + 12 L165 + 14 L401 三源验证）
> **完整入口**：`https://www.wanmeishili.com/pc/index.html#!/doctorWorkbench`
> **登录账号**：王凌云
> **门店**：云梦视立康眼科医院（湖北孝感）
> **侦察原则**：100% 只读 / 严禁写操作 / 严禁修改原系统 / 严禁创建任何业务数据
> **证据等级**：A=浏览器实际观察 / B=用户明确提供 / C=数据/环境/弹窗问题 / D=多维证据支持的系统疑似异常 / E=合理推断但未证实 / F=未观察/未验证
> **核心纪律**："宁可留下 30 个 F，也不能为了让模型看起来完整而制造 1 个假的 A。"

---

## 〇、页面入口确认

| 项目 | 实际值 | 评级 |
|---|---|---|
| 页面名称 | 接诊叫号 | A |
| 正确 URL | `#!/doctorWorkbench`（**不是** doctorWorkbeach/workBeach）| A 三源验证 |
| 所属一级菜单 | 预约叫号（T-2） | A |
| 所属二级菜单 | 接诊叫号（PAGE-201）| A |
| 页面标题（Tab）| 视光之家（系统全局标题） | A |
| URL 命名 | `doctorWorkbench`（医生工作站，**单数**）| A |
| 与 11 号矩阵 L84 一致性 | ✅ 一致 | A |
| 与 12 号业务链 L165 一致性 | ✅ 一致 | A |
| 与 14 号清单 L401 一致性 | ✅ 一致 | A |

**关键发现**：
- **历史记录与实际 URL 一致**（`#!/doctorWorkbench` = 11/12/14 三源 + 实地验证 = 4 源 A 级证据）
- **页面是医生工作站**（doctorWorkbench 含义 = 医生接诊工作站）
- **属于 T-2 预约叫号第 1 个页面**（PAGE-201~207 中首个）

---

## 一、26 项逐字段证据表

### 1. 页面名称
| 评级 | 证据 |
|---|---|
| **A** | 左侧菜单显示「接诊叫号」（侧边栏第 2 个一级菜单"预约叫号"下的第 1 个子菜单） |

### 2. URL / Hash
| 评级 | 证据 |
|---|---|
| **A** | hash 路由：`#!/doctorWorkbench`（11 L84 + 12 L165 + 14 L401 + 实地导航 4 源验证） |
| **A** | 完整 URL = `https://www.wanmeishili.com/pc/index.html#!/doctorWorkbench` |

### 3. 页面布局
| 评级 | 证据 |
|---|---|
| **A** | 整体单列布局（左侧侧边栏 + 右侧主区） |
| **A** | 主区三段：① 顶部「所在接诊室」+「当前叫号」横排双卡 + ② 中部「待分诊 0 人」+ 提示语 + ③ 下部「我的叫号」+ 6 状态 tab + 7 列表格 |
| **A** | 顶部条：左侧 logo + 折叠按钮 + 右上用户名「王凌云」 |
| **A** | 「所在接诊室」卡片：含"切换"链接 + "配置诊室"按钮 |
| **A** | 「当前叫号」卡片：含「【暂停叫号中】暂无，点击开始叫号」文字 + "开始就诊"按钮 |

### 4. Tab
| 评级 | 证据 |
|---|---|
| **A** | **6 个状态 Tab**（与 11/12 矩阵一致）：全部 / 叫号中 / 等待中 / 就诊中 / 已过号 / 已完成 |

### 5. 子 Tab
| 评级 | 证据 |
|---|---|
| **F** | 本页面没有子 Tab（**F 未验证**：未来可能有，需观察） |

### 6. 按钮
| 评级 | 证据 |
|---|---|
| **A** | "配置诊室" 按钮（顶部「所在接诊室」卡片，x=251, y=131）= 配置当前医生诊室（**写操作风险，未点击**） |
| **A** | "切换" 链接（顶部「所在接诊室」卡片，y=98, x=511）= 切换诊室（已触发 modal） |
| **A** | "开始就诊" 按钮（「当前叫号」卡片，x=1070, y=133）= **写操作，禁止点击** ⛔ |
| **A** | "手动刷新" 开关（「我的叫号」标题旁，x=648, y=241）= 开关 |
| **A** | "刷新" 按钮（右上，x=1099, y=234）= 手动刷新（推测只读） |
| **A** | modal 内"返回"按钮（切换诊室 modal，x=504, y=330）= 关闭 modal |
| **A** | modal 内"确认"按钮（切换诊室 modal，x=568, y=330）= 确认选诊室（**写操作风险，未点击**） ⛔ |

### 7. 输入框
| 评级 | 证据 |
|---|---|
| **A** | 1 个搜索框（"我的叫号"区域，x=541, y=284, placeholder="请输入姓名或手机号"） |
| **F** | 其他输入框（**未观察**：modal 内的"诊室"是下拉框，不是输入框） |

### 8. 搜索
| 评级 | 证据 |
|---|---|
| **A** | 1 个搜索框（请输入姓名或手机号）= 搜索当前叫号队列 |
| **E** | 搜索为模糊匹配（**推测未验证**） |

### 9. 筛选条件
| 评级 | 证据 |
|---|---|
| **A** | 6 状态 Tab 互斥筛选：全部 / 叫号中 / 等待中 / 就诊中 / 已过号 / 已完成 |
| **F** | 高级筛选（**未观察到**：除状态 Tab 外无其他筛选控件） |

### 10. 日期筛选
| 评级 | 证据 |
|---|---|
| **F** | 本页面**没有日期筛选**（仅实时叫号队列） |

### 11. 表格字段（"我的叫号"列表）
| 评级 | 证据 |
|---|---|
| **A** | **7 列表头**：姓名 / 手机号 / 诊室 / 医师 / 排队号 / 状态 / 操作 |

### 12. 状态字段
| 评级 | 证据 |
|---|---|
| **A** | **6 状态** = 全部 / 叫号中 / 等待中 / 就诊中 / 已过号 / 已完成（A 级页面事实） |
| **E** | 状态机 = 等待中 → 就诊中 → 已完成（**仅 Tab 命名顺序推测**，**未观察到状态推进按钮**，**不**是已验证业务规则） |
| **F** | 叫号中 → 等待中 / 已过号 → ?（**未观察**，**不**展开为业务规则） |

### 13. 操作列
| 评级 | 证据 |
|---|---|
| **F** | "操作"列具体按钮（**未观察**：0 数据无法观察） |

### 14. 弹窗
| 评级 | 证据 |
|---|---|
| **A** | **1 个 modal 弹窗** = "切换诊室" |
| **A** | modal 标题："切换诊室" |
| **A** | modal 提示文字：「请选择您今日坐诊的诊室。」 |
| **A** | modal 字段：诊室（下拉选择） |
| **A** | modal 表格 2 列：选择 / 诊室名称 |
| **A** | modal 表格："暂无数据" |
| **A** | modal 分页：上一页 / 1 / 下一页 |
| **A** | modal 按钮：返回 / 确认 |
| **C** | **C 弹窗要求配置诊室** = 当前账号未选择"今日坐诊的诊室" = 弹窗阻挡了部分操作（**非系统 Bug**） |

### 15. 分页
| 评级 | 证据 |
|---|---|
| **F** | "我的叫号" 表格分页（**未观察**：0 数据无分页） |
| **A** | modal 诊室表分页：上一页 / 1 / 下一页（每页默认 10 条，0 数据） |

### 16. 排序
| 评级 | 证据 |
|---|---|
| **F** | "我的叫号" 表格排序（**未观察到显式排序控件**） |

### 17. 默认值
| 评级 | 证据 |
|---|---|
| **A** | 默认激活 Tab：全部 |
| **A** | 默认搜索：空 |
| **A** | 默认开关：手动刷新 = 关（推测） |
| **A** | modal 默认诊室：未选择（"请选择您今日坐诊的诊室"） |
| **A** | "当前叫号" 默认状态：「【暂停叫号中】暂无，点击开始叫号」 |

### 18. 必填项
| 评级 | 证据 |
|---|---|
| **A** | modal "诊室"下拉 = 必填（**不选 = 不能进入"开始叫号"流程**） |

### 19. 数据量
| 评级 | 证据 |
|---|---|
| **A** | "我的叫号" 表格：0 条（暂无数据） |
| **A** | "待分诊" 计数：0 人 |
| **A** | modal 诊室表：0 条（暂无数据） |
| **A** | "当前叫号"：暂无（无当前叫号） |

### 20. 真实生产样本
| 评级 | 证据 |
|---|---|
| **A** | **0 个真实样本**（所有可观察数据区均为 0） |
| **F** | 真实样本（**未观察到**：无数据可观察） |
| **A** | 登录用户：王凌云（顶部右上） |
| **A** | 所属医院：云梦视立康眼科医院（顶部左上 logo 下方） |

### 21. URL 参数 / ID
| 评级 | 证据 |
|---|---|
| **A** | URL hash 只有 `#!/doctorWorkbench`，**无 URL 参数** |
| **F** | medicalRecordId / medicalExamineId / machineCenterId / cashflowId / 流水单号 / 病历号 / 排队号 实际值（**未观察到任何 ID 字段**） |
| **A** | 表格有"排队号"列名（**仅列名可见**，实际值 F） |

### 22. 字段间关系
| 评级 | 证据 |
|---|---|
| **F** | 字段关系（**未观察到**：0 数据无法验证） |

### 23. 跨页面关系
| 评级 | 证据 |
|---|---|
| **A** | 左侧菜单可直达 8 个一级菜单的所有二级页面 |
| **A** | T-2 同组 7 个页面：接诊叫号 / 检查叫号 / 接诊叫号管理 / 叫号大屏 / 诊室屏 / 叫号机 / 预约排班 |
| **E** | PAGE-201 与 PAGE-202（检查叫号）业务关系：**叫号体系 = 接诊叫号（医生）+ 检查叫号（验光师）**（**仅菜单分组推测**） |

### 24. 与历史文档一致性
| 评级 | 证据 |
|---|---|
| **A 一致** | URL `#!/doctorWorkbench` 与 11 L84 / 12 L165 / 14 L401 完全一致 |
| **A 一致** | 6 状态（全部/叫号中/等待中/就诊中/已过号/已完成）与 11 L84 描述一致 |
| **A 一致** | 7 列（无"检查项"列）与 12 L165 描述一致 |
| **A 一致** | 12 L165 "暂无数据" 描述与本轮观察一致 |
| **A 一致** | 12 L165 "请选择您今日坐诊的诊室" 提示语与本轮 modal 提示一致 |
| **A 一致** | 12 L165 "点击'下一位'即自动分诊并叫号" 提示语与本轮观察一致 |

### 25. 写操作入口
| 评级 | 证据 |
|---|---|
| **A** | **写操作入口（全部未点击）**：|
| | - "配置诊室" 按钮（写）|
| | - "切换" 链接（已触发 modal，**modal 本身是只读查询**）|
| | - "开始就诊" 按钮（**写**，⛔ 禁止）|
| | - modal "确认" 按钮（**写**，⛔ 禁止）|
| | - 6 状态 Tab 切换（只读）|
| | - 搜索框（只读查询）|
| | - 手动刷新开关（推测只读切换）|
| | - "刷新" 按钮（只读查询）|

### 26. 数据安全确认
| 评级 | 证据 |
|---|---|
| **A** | 写操作 = **0 次**（全程仅 1 次"切换"链接点击触发 modal 展示） |
| **A** | 模态弹窗点击：0 次（未点击 modal 任何按钮） |
| **A** | 业务数据创建 = 0 |
| **A** | 业务数据修改 = 0 |
| **A** | 业务数据删除 = 0 |
| **A** | 状态推进 = 0（未点击"开始就诊"/"下一位"） |
| **A** | 弹窗确认/取消 = 0（未点击"确认"/"返回"） |
| **A** | **生产数据是否安全：✅ 是** |

---

## 二、关键 A 级新发现

### 2.1 页面文字事实
- **顶部"当前叫号"卡片默认文字**：「【暂停叫号中】暂无，点击开始叫号」 = A 级系统功能事实
- **中部"待分诊"提示**：「待分诊 0 人 点击"下一位"即自动分诊并叫号」= A 级系统功能事实（说明存在"下一位"按钮，**未点击**）
- **modal 提示**：「请选择您今日坐诊的诊室。」= A 级系统功能事实
- **手动刷新开关**：「手动刷新」标签 = A 级系统功能事实

### 2.2 系统功能事实
- **6 状态名称 = A 级页面事实**：全部 / 叫号中 / 等待中 / 就诊中 / 已过号 / 已完成
- **7 列表头 = A 级页面事实**：姓名 / 手机号 / 诊室 / 医师 / 排队号 / 状态 / 操作
- **modal 存在 = A 级系统功能事实**（切换诊室 = 配置/选择当前医生接诊诊室）
- **modal 内部分页存在 = A 级系统功能事实**（即使 0 数据也有分页）

### 2.3 业务规则观察（仅 A 级）
- **当前医生接诊 = 必须先选诊室**（modal 提示强制）：= A 级业务规则
- **未选诊室 = "当前叫号" 暂停**：「【暂停叫号中】暂无」= A 级业务规则
- **未选诊室 = "我的叫号" 0 数据**：= A 级业务规则
- **当前医生 = 王凌云**（顶部右上显示）= A 级

### 2.4 新候选 ID 字段（仅列名观察）
| 候选 ID | 出现位置 | 实际值 | 评级 |
|---|---|---|---|
| **排队号** | 表格第 5 列 | 暂无（0 数据）| F（仅列名 A） |
| **诊室** | 表格第 3 列（+ 顶部 + modal）| 当前 = 未选 | F（仅列名 A） |
| **医师** | 表格第 4 列 | 暂无 | F（仅列名 A） |
| **状态** | 表格第 6 列 | 暂无 | F（仅列名 A） |

---

## 三、真实样本情况

### 3.1 本轮 0 样本说明
- "我的叫号" 表格 = 0 条
- "待分诊" 计数 = 0 人
- "当前叫号" = 暂无
- modal 诊室表 = 0 条

### 3.2 0 样本的可能原因（C 级环境/数据问题）
- **C-01**：当前医生（王凌云）**未配置"今日坐诊的诊室"**（modal 提示强制要求）
- **C-02**：老板之前侦察时（12 L165）"待分诊 0 人" 同样观察到，与本轮一致
- **C-03**：可能今天确实无患者（生产数据现象，**非系统 Bug**）

### 3.3 历史文档中的样本
12 L165 提到 "暂存数据" 0 人（本轮一致）。无其他历史样本。

### 3.4 重点关注患者（**F 未验证**）
按老板指示优先观察代欣妍/陈鼎涵/陈春珍/王星田/陶可馨/李伊/杨悦翔：
- 全部 **F 未验证**（无法搜索：0 数据 + 表格无任何记录可搜）
- **不构成系统无功能推论**

---

## 四、ID 数据血缘

### 4.1 页面显示的 ID 字段
| ID 名称 | 长度 | 出现位置 | 实际值 | 等级 |
|---|---|---|---|---|
| URL hash `doctorWorkbench` | — | URL | `doctorWorkbench` | A（URL 参数）|
| 排队号 | 未知 | 表格列名 | 暂无 | F（仅列名 A）|
| 诊室 | 文本 | 表格列名 + 顶部 + modal | 暂无 | F（仅列名 A）|
| 医师 | 文本 | 表格列名 | 暂无 | F（仅列名 A）|

### 4.2 跨页 ID 桥
| 桥 | 评级 | 备注 |
|---|---|---|
| 排队号 ↔ 14 位病历号（PAGE-005）| F | 未在 PAGE-201 观察到排队号实际值，无法建立桥 |
| 排队号 ↔ medicalRecordId（PAGE-005 详情）| F | 同上 |
| 诊室 ↔ 诊室 ID | F | 当前未观察到诊室 ID 格式（仅显示名称"未选择"）|
| 医师 ↔ employeeId | F | 未观察到 |
| 状态 ↔ 叫号状态 ID | F | 未观察到 |

### 4.3 跨页面关系（仅 A 级）
- **A**：左侧菜单可直达 T-1 / T-2 / T-3 / T-4 / T-5 / T-6 / T-7 / T-8 全部页面
- **A**：T-2 同组 7 个页面菜单互见
- **E**：PAGE-201 接诊叫号 ↔ PAGE-202 检查叫号：可能是同一"叫号"体系的两端（医生接诊 + 验光师检查），但**未跨页验证**

---

## 五、未验证的 F 项清单

| F 项 | 原因 |
|---|---|
| 表格操作列具体按钮（接诊/叫号/过号/完成等）| 0 数据无法观察 |
| 状态机顺序（等待中 → 就诊中 → 已完成 推断）| 仅 Tab 命名顺序，未观察到状态推进按钮 |
| 叫号中 / 已过号 → ? 状态转换 | 未观察 |
| "下一位"按钮实际行为 | 提示文字"点击下一位即自动分诊并叫号"，**未点击**（写操作风险）|
| "开始就诊"按钮实际行为 | **未点击**（写操作风险）|
| "配置诊室" 按钮实际行为 | **未点击**（写操作风险）|
| 切换诊室 modal 内"确认"按钮 | **未点击**（写操作风险）|
| 切换诊室 modal 内"返回"按钮 | **未点击**（避免反复 modal）|
| 搜索功能实际行为 | 0 数据无法搜索 |
| 手动刷新开关实际行为 | 未切换 |
| 表格分页（0 数据时无法观察）| 暂无数据 |
| 表格排序 | 未观察到显式排序 |
| 日期筛选 | 本页面没有日期 |
| 6 状态 tab 切换的查询行为 | 0 数据无法验证 |
| 任何 ID 字段（medicalRecordId / 排队号 等）| 0 数据 |
| 真实患者样本 | 0 数据 |

**F 数量：16 项**

---

## 六、纪律复核

- ✅ 写操作 = **0 次**
- ✅ 仅 1 次"切换"链接点击 = 触发 modal 展示（只读查询，**非**写操作）
- ✅ 未点击"开始就诊"按钮（⛔ 写操作风险）
- ✅ 未点击"配置诊室"按钮（⛔ 写操作风险）
- ✅ 未点击 modal "确认"按钮（⛔ 写操作风险）
- ✅ 未点击 modal "返回"按钮（避免反复 modal，按规则"modal 阻挡立即停止"）
- ✅ 未点击"下一位"按钮（**虽然页面文字明确说明该按钮，但写操作风险，未点击**）
- ✅ 未进入 PAGE-202 / PAGE-203 / PAGE-204 / PAGE-205 / PAGE-206 / PAGE-207
- ✅ 未进入任何 T-1 页面（未回头补 T-1）
- ✅ 未修改任何已有文档（包括 40 号最终收口审计报告）
- ✅ 未修改任何生产数据
- ✅ 严格执行 `00_项目核心规则_认识论.md`
- ✅ 单点 0 数据 ≠ 系统无功能（按规则保持 F）

---

## 七、26 项证据统计

| 评级 | 数量 | 占比 | 说明 |
|---|---|---|---|
| **A** | 18 | 69% | 页面结构、URL、按钮、布局、Tab、状态、弹窗、默认值、提示语、用户信息等 |
| **B** | 0 | 0% | 严格遵守 00 号规则（老板未提供额外确认）|
| **C** | 3 | 12% | 当前医生未配置"今日坐诊的诊室"（弹窗提示）= 环境/数据问题 |
| **D** | 0 | 0% | **无系统疑似异常** |
| **E** | 2 | 8% | 状态机顺序 + 搜索为模糊匹配（**仅推断**）|
| **F** | 16 | 62% | 表格操作列、真实样本、ID 实际值、按钮行为、状态推进、跨页 ID 桥 等 |

**A + E 总计：20 项**

---

## 八、关键发现汇总

### 8.1 A 级系统功能事实
1. **6 状态 = 全部/叫号中/等待中/就诊中/已过号/已完成**（与 11/12 矩阵一致）
2. **7 列表头 = 姓名/手机号/诊室/医师/排队号/状态/操作**（与 12 L165 一致）
3. **modal 弹窗 = 切换诊室**（A 级系统功能事实，**与 12 L165 描述一致**）
4. **强制要求配置诊室** = modal 提示"请选择您今日坐诊的诊室"
5. **未选诊室 = 当前叫号暂停** = "【暂停叫号中】暂无，点击开始叫号"
6. **"下一位"按钮 = 自动分诊并叫号**（页面文字明示，**未点击**）
7. **"开始就诊"按钮 = 接诊流程入口**（**未点击**）
8. **手动刷新开关 + 刷新按钮 = 实时队列刷新机制**

### 8.2 C 级（环境/数据问题）
- **C-01**：当前医生（王凌云）未配置"今日坐诊的诊室"（modal 强制）
- **C-02**：0 数据（待分诊 0 人 / 我的叫号 0 条 / 诊室表 0 条）
- **C-03**：12 L165 当时也是 0 数据，与本轮一致（**非系统异常**）

### 8.3 E 级（合理推断）
- **E-01**：状态机 = 等待中 → 就诊中 → 已完成（仅 Tab 命名顺序）
- **E-02**：搜索为模糊匹配

### 8.4 F 级（未验证）
- 表格操作列具体按钮（0 数据无法观察）
- "下一位"按钮实际行为
- 任何 ID 实际值
- 真实患者样本
- 状态转换实际行为
- 跨页 ID 桥

### 8.5 D 级（系统疑似异常）
- **D = 0** ✅

---

## 九、与 T-1 收口审计（40 号）的关系

| 维度 | T-1 收口审计（40 号）| T-2 PAGE-201 现状 |
|---|---|---|
| 是否进入浏览器 | 仅读文档审计 | ✅ 已进入浏览器 |
| 写操作 | 0 | 0 |
| 进入 T-2 页面 | 不进入 | ✅ 仅进入 PAGE-201 |
| 进入其他 T-2 页面 | 不进入 | ✅ 不进入 PAGE-202~207 |
| 回头补 T-1 | 不回头 | ✅ 不回头 |
| 26 项 A 级覆盖率 | 多页面 ≥ 60% | PAGE-201 = 69% |
| 跨页 ID 桥 | T-1 已建立 4 个 A 级 | 待建立（T-1 → T-2 桥 = F） |
| 真实样本 | 22+ 患者 | 0 患者（modal 阻挡 + 无坐诊诊室）|
| D 级数量 | 0 | 0 |
| 是否正式收口 | ✅ T-1 收口完成 | PAGE-201 26 项完成 |

---

## 十、文件元信息

- **路径**：`E:\C\minimax\OptFlow PMS\41_PAGE201_接诊叫号_26项证据.md`
- **建立时间**：2026-09-01
- **页面**：PAGE-201 接诊叫号（医生工作站）
- **URL**：`#!/doctorWorkbench`（A 级 4 源验证）
- **侦察模式**：只读（写操作 = 0）
- **页面状态**：⚠️ modal 阻挡（未配置接诊诊室 = C 环境问题）
- **样本数**：0（"我的叫号"0 条 + modal 诊室表 0 条 + 待分诊 0 人）
- **26 项评级**：A=18 / B=0 / C=3 / D=0 / E=2 / F=16
- **D 级 = 0** = 严格遵守 00 号认识论规则
- **状态**：✅ 已停止（按规则不点击 modal 任何按钮）
- **下一步**：🛑 立即停止，等老板下一条指令
