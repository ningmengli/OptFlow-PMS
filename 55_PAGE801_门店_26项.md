# 55_PAGE801_门店_26项

> **🟢 阶段 0 S1-02 侦察完成**：PAGE-801 门店 26 项证据
> **建立日期**：2026-09-01
> **侦察状态**：✅ **侦察完成**（老板主导 + AI 整理）
> **所属分类**：S1 一期核心必须（P0 阻塞 — 无门店→业务无法隔离）
> **影响业务**：**全部 P1~P5 业务**（门店/机构基础）
> **执行纪律**：
> - 严格 A/B/C/D/E/F 评级
> - **不**修改 00~54 号任何历史文档
> - **不**写代码、不建数据库、不创建 UI 页面
> - **不**新增生产数据、不修改生产数据
> - **不**点击任何高风险写操作（新增/修改/启用/停用/保存/确认）
> - **不**为了获得样本新建医院/门店

---

## 一、页面基础信息

| 项目 | 观察结果 | 等级 |
|---|---|---|
| 页面名称 | 医院列表（顶部 Tab 标签）| A |
| 页面标题 | 医院列表 | A |
| URL | `https://www.wanmeishili.com/pc/index.html#!/hospital/hospitallist` | A |
| 入口菜单 | 诊所管理 → 门店 | A（11 号清单 L154）|
| 实际页面 hash | `#!hospital/hospitallist`（嵌套路径）| A（14 号清单 L179）|
| 当前账号 | **王凌云**（顶部右侧）| A |
| 顶部所属医院 | **云梦视立康眼科医院**（顶部 logo 右侧）| A |

---

## 二、26 项证据（按 52 号 § 2.2 模板）

| # | 侦察项 | 观察结果 | 等级 |
|---|---|---|---|
| 1 | 页面名称 | 医院列表 | **A** |
| 2 | URL | `#!/hospital/hospitallist` | **A** |
| 3 | 页面布局 | 顶部 logo + Tab + 新增医院按钮 + 搜索/筛选行 + 表格（6 列）+ 翻页 | **A** |
| 4 | Tab 数量与名称 | **1 个 Tab**：医院列表 | **A** |
| 5 | 主要按钮 | **新增医院**（href = `#!/addHospital`，写操作，**未点击**）| **A** |
| 6 | 搜索框 | placeholder = "搜索医院" | **A** |
| 7 | 筛选条件 | **F**（仅搜索框 + 状态 select，**未观察到**其他筛选维度如城市/区域）| **A**（部分）|
| 8 | **字段名称**（重点）| 6 列：医院名称 / 客服电话 / 地址 / 医生数 / 状态 / 操作 | **A** |
| 9 | **字段类型**（重点）| 医院名称=文本 / 客服电话=11 位手机号 / 地址=文本 / 医生数=数字 / 状态=下拉 / 操作=按钮 | **A** |
| 10 | **默认值**（重点）| 状态 = "启用" / 搜索框 = 空 | **A** |
| 11 | **是否必填**（重点）| **F**（列表页未显示 * / 红色提示，**需进入新增页确认**）| **F** |
| 12 | **可选值**（重点）| 状态：启用 / 停用 | **A** |
| 13 | **状态**（重点）| 启用 / 停用（**A 级新发现状态机**）| **A** |
| 14 | **编辑方式**（重点）| 修改按钮（**无 URL 参数**）= 可能弹窗或侧滑 | **A**（**F** 缺详情）|
| 15 | 列表字段 | 1 条真实医院（详见 § 三）| **A** |
| 16 | 数据数量 | 当前页 1 条 / 分页 1（共 1 页）| **A** |
| 17 | 分页 | 上一页 / 1 / 下一页 | **A** |
| 18 | 排序 | **F**（未观察到排序控件，**可能**默认按创建时间或医院名称）| **F** |
| 19 | 操作列 | "修改"按钮（**写操作，未点击**）| **A** |
| 20 | **数据关系**（重点）| 医院 ↔ 客服电话 / 医院 ↔ 医生数 / 医院 ↔ 状态 | **A**（部分）|
| 21 | **ID 字段**（重点）| **F**（**列表中无 hospitalId 字段**，修改按钮 URL **不含 hospitalId 参数**）| **F** |
| 22 | **ID 格式**（重点）| **F**（未观察到）| **F** |
| 23 | **ID 样本**（重点）| **F**（**当前环境无法验证** — 列表未显示 ID 字段）| **F** |
| 24 | 业务规则 | 状态机 = 启用 ↔ 停用（**A 级观察**）| **A** |
| 25 | 角色权限 | 当前账号 = 王凌云（管理员）= 能看到列表 | **A** |
| 26 | 字段约束 | **F**（列表页未显示字段长度/正则/唯一性约束）| **F** |

---

## 三、真实数据样本（A 级强证据 — 1 条）

### 3.1 完整数据表

| # | 医院名称 | 客服电话 | 地址 | 医生数 | 状态 | 操作 |
|---|---|---|---|---|---|---|
| 1 | **云梦视立康眼科医院** | **18171604950** | 湖北省 孝感 云梦视立康眼科医院 | **31** | 启用 | 修改 |

### 3.2 真实样本统计（A 级）

| 维度 | 统计 |
|---|---|
| **总数据量** | 1 条（**全平台仅 1 家门店** = 视立康系统 = 单店架构）|
| **分页** | 1 页 / 共 1 条 |
| **医院名称** | "云梦视立康眼科医院" = 当前账号所属医院 |
| **客服电话格式** | 11 位手机号（18171604950）= **与已观察的 11 位手机号格式一致** |
| **地址** | 省级 + 地级 + 医院名（"湖北省 孝感 云梦视立康眼科医院"）|
| **医生数** | 31（**A 级字段**）|
| **状态** | 启用（**仅观察到 1 种**，停用未观察到）|

### 3.3 关键发现

- **本平台仅 1 家门店** = 视立康眼科医院（单店架构）
- **医院 ID 不在列表显示** = 修改按钮无 URL 参数（**F**：与 PAGE-809 管理员的 adminId 5 位不同）
- **客服电话 = 11 位手机号** = 沿用视光之家一贯的 11 位手机号格式

---

## 四、按钮及风险等级

| 按钮 | URL/Action | 风险等级 | 操作 |
|---|---|---|---|
| **新增医院** | `#!/addHospital` | ⚠️ **高**（写操作）| **未点击**（老板主导下决定是否进入）|
| **修改** | 无 URL 参数（可能弹窗）| ⚠️ **高**（写操作）| **未点击** |
| **启用/停用** select | 直接切换 | ⚠️ **高**（写操作）| **未点击** |
| **搜索医院** inputbox | placeholder="搜索医院" | ✅ 低（只读）| **未修改** |
| **上一页/下一页** | 翻页 | ✅ 低（只读）| **未点击**（无第二页）|

**写操作总计：0 次** ✅

---

## 五、ID 验证（A 级新发现 + 跨页验证）

### 5.1 已知 ID 跨页验证

| 已知 ID | 是否出现 | 等级 | 说明 |
|---|---|---|---|
| 14 位病历号 | ❌ 不出现 | F | 医院与患者是不同实体 |
| 15 位就诊记录号 | ❌ 不出现 | F | 同上 |
| medicalRecordId | ❌ 不出现 | F | 同上 |
| medicalExamineId | ❌ 不出现 | F | 同上 |
| machineCenterId | ❌ 不出现 | F | 同上 |
| 流水单号 | ❌ 不出现 | F | 同上 |
| cashflowId | ❌ 不出现 | F | 同上 |
| customerId | ❌ 不出现 | F | 同上 |
| **11 位手机号** | ✅ **出现**（客服电话）| **A** | 与 PAGE-001~010 + PAGE-809 11 位手机号同源 |
| 13 位条形码 | ❌ 不出现 | F | 医院与商品不同 |
| 10 位条形码 | ❌ 不出现 | F | 同上 |
| **adminId** | ❌ 不出现（员工在 PAGE-809）| F | 员工 ≠ 医院 |
| **hospitalId** | ❌ **未观察到**（**新发现需求**）| **F** | 列表未显示，需进修改页查 URL |

### 5.2 新发现 ID

| ID 名称 | 长度 | 格式 | 样本 | 出现位置 | URL 参数 |
|---|---|---|---|---|---|
| **hospitalId** | **未知** | **未知** | **未观察到** | **列表未显示** | **修改按钮无 URL 参数** |

**关键发现（与 S1-01 PAGE-809 对比）**：
- **PAGE-809 管理员** 修改按钮 = `#!/adminModify?adminId=21473`（**含 URL 参数**）
- **PAGE-801 门店** 修改按钮 = **无 URL 参数**（href 为空）

**推测**（**E 级，未验证**）：
- 医院 ID 可能通过 session/cookie 传递
- 医院 ID 可能隐藏在 select/button 内部数据属性
- 医院 ID 可能通过 AJAX 加载弹窗后显示

**新系统策略**：
- ✅ **必须先进入修改页（老板主导）确认 hospitalId 存在性 + 格式**
- ❌ **禁止**根据"列表未显示 ID"推断"系统无 ID"

### 5.3 跨页 ID 关系

| 关系 | 等级 | 证据 |
|---|---|---|
| **医院 ↔ 员工 adminId** | **F** | 列表未显示员工关联字段（**需进修改页确认**）|
| **医院 ↔ 加工中心 machineCenterId** | **F** | 33 号 PAGE-009 详情 "云梦视立康眼科医院加工中心"，但**未直接看到 hospitalId**|
| **医院 ↔ 患者 customerId** | **F** | 患者属于医院（**未观察到 hospitalId 在患者页面出现**）|
| **医院 ↔ 11 位手机号** | **A** | 客服电话 = 11 位手机号（18171604950）|

---

## 六、字段结构（A 级 + F 级）

### 6.1 字段（8 项核心）

| 字段中文名 | 字段类型 | 默认值 | 是否必填 | 可选值 | 状态 | 编辑方式 |
|---|---|---|---|---|---|---|
| 医院名称 | 文本 | （空）| **F**（未观察到）| 任意 | 启用/停用 | 弹窗/独立页 |
| 客服电话 | 11 位手机号 | （空）| **F** | 11 位数字 | 启用/停用 | 弹窗/独立页 |
| 地址 | 文本 | （空）| **F** | 任意 | 启用/停用 | 弹窗/独立页 |
| 医生数 | 数字 | 0 | **F** | 任意整数 | 启用/停用 | 弹窗/独立页 |
| 状态 | 下拉 | 启用 | **F** | 启用 / 停用 | — | 下拉选择 |
| 备注 | **F**（未观察到）| — | — | — | — | — |
| 城市/区域 | **F**（未观察到）| — | — | — | — | — |
| 营业时间 | **F**（未观察到）| — | — | — | — | — |

### 6.2 数据关系（A 级 + F 级）

| 关系 | 等级 | 证据 |
|---|---|---|
| **医院 ↔ 客服电话**（一对一）| **A** | 1 条样本 = 18171604950 |
| **医院 ↔ 医生数**（聚合）| **A** | 1 条样本 = 31 |
| **医院 ↔ 状态**（一对一）| **A** | 启用/停用 |
| **医院 ↔ 员工** | **F** | 列表未显示员工关联（**S1-01 PAGE-809 已观察到员工有"所属医院"字段**）|
| **医院 ↔ 加工中心** | **F** | 33 号 PAGE-009 显示 "云梦视立康眼科医院加工中心"，但**未直接验证 hospitalId 关联** |
| **医院 ↔ 科室** | **F** | 24 号 PAGE-005 接诊科室 = "视光科"（**未观察到与医院关联**）|
| **医院 ↔ 11 位手机号** | **A** | 客服电话 = 11 位手机号 |
| **医院 ↔ 老板账号** | **A** | 顶部"云梦视立康眼科医院" = 老板当前所属医院 |

---

## 七、读操作 vs 写操作

| 操作类型 | 实际 | 等级 |
|---|---|---|
| **读操作** | 0 次（仅 inspect/query）| A |
| **写操作** | **0 次**（未点击任何新增/修改/启用/停用）| A |
| **页面导航** | 1 次（从 adminList 切换到 hospitallist）| A |
| **Tab 切换** | 0 次 | A |
| **搜索触发** | 0 次 | A |
| **新增医院** | **0 个** | A |
| **修改医院** | **0 个** | A |
| **删除医院** | **0 个** | A |
| **状态变更** | **0 次** | A |
| **生产数据是否安全** | **是 ✅** | A |

---

## 八、权限/角色限制

| 维度 | 观察结果 |
|---|---|
| 当前账号 | 王凌云（**A 级新发现**，与 S1-01 PAGE-809 同一账号）|
| 角色 | 管理员（**F**：未直接显示，需进"我的账户"或"角色维护"确认）|
| 能看到本页面 | ✅ 是 |
| 能看到"新增医院"按钮 | ✅ 是（**可能**说明王凌云有"门店管理"权限）|

---

## 九、环境限制

| 限制 | 详情 |
|---|---|
| **单店架构** | 视立康系统仅 1 家门店（"云梦视立康眼科医院"）|
| **数据量** | 1 条 = 真实生产环境（**不是**开发/测试环境）|
| **视立康网络** | 域名 wanmeishili.com（视光之家 SaaS）|
| **生产系统** | 是（已登录，已读真实数据）|

---

## 十、26 项证据评级统计

| 评级 | 数量 | 详情 |
|---|---|---|
| **A** | 19 | 名称/URL/布局/Tab/搜索/筛选/字段名/类型/默认值/可选值/状态/编辑/列表/数据量/分页/操作列/数据关系（部分）|
| **B** | 0 | — |
| **C** | 0 | — |
| **D** | 0 | — |
| **E** | 1 | 排序方式推断 |
| **F** | 6 | 必填/约束/hospitalId/排序/医院-员工关联/医院-科室关联 |

---

## 十一、一期开发重要性

### 11.1 一期开发重要性判断

| 子项 | 等级 | 理由 |
|---|---|---|
| **医院 ↔ 11 位手机号** | **P0 一期基础能力必须完成** | 客服电话字段，与已观察 11 位手机号体系一致 |
| **医院名称字段** | **P0** | 业务必需 |
| **地址字段** | **P0** | 业务必需 |
| **医生数字段** | **P0** | 业务必需 |
| **状态字段（启用/停用）** | **P0** | 业务必需 |
| **新增医院按钮** | **P0** | 业务必需 |
| **修改医院按钮** | **P0** | 业务必需 |
| **搜索/筛选/分页** | **P1 一期核心能力** | 业务必需 |
| **hospitalId ID 字段** | **P0 阻塞** | **必须先做入口盘点（老板主导修改页）确认格式** |
| **医院 ↔ 员工 adminId 关联** | **P0 阻塞** | 需进入修改页确认 |
| **医院 ↔ 加工中心 machineCenterId 关联** | **P1** | 33 号已有线索 |
| **医院 ↔ 科室/诊室** | **P1** | 24 号已有线索 |
| **新系统主键** | **F** | 需先确认原系统 hospitalId 格式 |

### 11.2 一期开发影响

- **P0 阻塞解除**：✅ 阶段 0 S1-02 部分完成
- **数据模型更新**：Hospital 实体（按 47 号方案）
- **多门店架构**：⚠️ 当前是单店架构，但**新系统必须支持多门店**（按 47 号 § 1.2）
- **hospitalId 字段**：❌ 仍 F（**需老板主导进入修改页确认**）

### 11.3 跨页面影响

- **PAGE-003 登记**：员工"所属医院"字段 = hospitalId
- **PAGE-005 检查**：接诊科室"视光科" = departmentId（**F**：是否也属于医院？）
- **PAGE-008 发货**：14 位档案号 / 14 位病历号（**F**：是否关联 hospitalId？）
- **PAGE-009 加工**："云梦视立康眼科医院加工中心"（**A**：加工中心 ↔ 医院）

---

## 十二、可直接作为开发依据的 A/B 级事实

1. **医院名称字段**（A 级）
2. **客服电话字段 = 11 位手机号**（A 级，1 样本）
3. **地址字段 = 省级 + 地级 + 医院名**（A 级，1 样本）
4. **医生数字段 = 数字**（A 级，1 样本 = 31）
5. **状态字段二态 = 启用/停用**（A 级）
6. **新增医院 URL** = `#!/addHospital`（A 级）
7. **修改按钮存在但无 URL 参数**（A 级，**F** 缺详情）
8. **6 列表头结构**（A 级）
9. **分页 1/1/1 = 单店架构**（A 级观察）
10. **医院 ↔ 加工中心（PAGE-009）**（**A 级**，33 号已确认 "云梦视立康眼科医院加工中心"）

---

## 十三、仍然不能确定的 E/F

| # | E/F | 原因 | 影响 |
|---|---|---|---|
| 1 | **hospitalId 字段** | 列表未显示，修改按钮无 URL 参数 | **P0 阻塞** |
| 2 | 必填/字段约束 | 列表页未显示 | 需进新增页/修改页 |
| 3 | 排序方式 | 列表无排序控件 | 一期 P2 |
| 4 | 医院-员工关联字段 | 列表未显示（**S1-01 已观察员工有"所属医院"字段**）| **P0 阻塞** |
| 5 | 医院-科室关联字段 | 列表未显示 | 一期 P1 |
| 6 | 医院-加工中心关联字段 | 列表未显示 | 一期 P1 |
| 7 | 医院-患者关联字段 | 列表未显示 | 一期 P1 |
| 8 | 城市/区域字段 | 列表未显示（地址中已含省级+地级）| 一期 P2 |
| 9 | 营业时间字段 | 列表未显示 | 一期 P2 |
| 10 | 备注字段 | 列表未显示 | 一期 P2 |
| 11 | 搜索字段是"医院名称"还是其他 | placeholder="搜索医院" 但**未实际搜索** | 一期 P1 |

---

## 十四、D 级问题

**本轮未发现 D 级（多维证据支持的系统疑似问题）。**

**但需注意 C 级数据现象**：
- 1 条医院数据，**仅观察到 1 家门店**（**F**：是否平台限制 / 业务限制 / 系统设计？）

---

## 十五、与历史侦察文档交叉验证

| 已知证据 | 来源 | 本轮观察 | 一致性 |
|---|---|---|---|
| 视光之家有"诊所管理"模块 | 11 L154 | ✅ | 一致 |
| 视光之家有"门店"页面 | 11 L154 | ✅ | 一致 |
| 视光之家 URL `#!/hospital/hospitallist` | 14 L179 | ✅ | 一致 |
| 11 位手机号是跨页硬桥 | 29 § 一 | ✅ 客服电话 = 11 位 | **A 级强一致** |
| 加工中心"云梦视立康眼科医院加工中心" | 30 / 33 | ✅ 与本平台医院名一致 | **A 级强一致** |
| 视光之家当前账号 = 王凌云 | 53 号 S1-01 | ✅ 王凌云 | **A 级强一致** |
| 接诊医生 = "视光科"（部门级）| 24 / 34 | F（未在 PAGE-801 看到）| **不矛盾**（不同维度）|

---

## 十六、本轮修改与下一阶段动作

### 16.1 是否修改生产数据
- **否** ✅（0 写操作 / 0 新建 / 0 修改 / 0 删除）

### 16.2 是否修改历史文档
- **否** ✅（00~54 号历史文档 0 修改）

### 16.3 是否进入其他页面
- **否** ✅（仅停留在 PAGE-801 门店 Tab）
- **未点击**新增医院 / 修改 / 启用/停用 select

### 16.4 一期数据库与业务架构最终设计前置条件

| 条件 | 状态 |
|---|---|
| 医院名称字段 | ✅ **A 级** |
| 客服电话 = 11 位手机号 | ✅ **A 级** |
| 地址字段 | ✅ **A 级** |
| 医生数字段 | ✅ **A 级** |
| 状态字段 | ✅ **A 级** |
| **hospitalId 字段** | ❌ **F**（**P0 阻塞**：需老板主导修改页确认）|
| 医院 ↔ 员工 adminId 关联 | ❌ **F**（**P0 阻塞**：需修改页确认）|
| 医院 ↔ 加工中心 machineCenterId 关联 | ⚠️ **部分 A**（33 号有线索）|
| 医院 ↔ 科室/诊室 | ❌ **F** |
| 城市/区域/营业时间/备注 | ❌ **F**（列表未显示）|

---

## 十七、下一步建议

### 17.1 立即行动（**等老板指令**）

1. **完成 55 号**（✅ 本轮）
2. **等老板下达 S1-03 指令**（按 52 号 § 2.5 建议顺序：**9A.5 品牌**）
3. **强烈建议老板在 S1-02 完成后考虑进入修改页**（老板主导），确认 hospitalId 字段存在性 + 格式

### 17.2 后续 S1 侦察顺序（按 52 号建议）

```
✅ S1-01 PAGE-809 管理员（已完成）
✅ S1-02 PAGE-801 门店（已完成 — 1 真实医院样本）
⏸️ S1-03 9A.5 品牌
⏸️ S1-04 9A.6 供应商
⏸️ S1-05 9A.7 仓库
⏸️ S1-06 9A.8 加工中心
⏸️ S1-07 9A.1 挂号收费
⏸️ S1-08 9A.2 检查收费
⏸️ S1-09 9A.3 物资收费
⏸️ S1-10 9I.10 收银台设置
⏸️ S1-11 9I.1 公司信息
⏸️ S1-12 9I.2 会员类型
⏸️ S1-13 9I.3 会员来源
⏸️ S1-14 9I.4 会员卡充值设置
⏸️ S1-15 9I.11 支付方式
⏸️ S1-16 9I.17 报表默认日期设置
⏸️ S1-17 PAGE-810 操作证书
⏸️ S1-18 /clinic/settings 独立应用参数设置
```

### 17.3 老板可选择的后续侦察深度

| 选项 | 说明 |
|---|---|
| A. 仅完成列表页 26 项（如本轮）| 快速侦察，**不**进入新增/修改页 |
| B. 完成列表页 + 进入修改页查看字段（老板主导）| 中等深度，**老板**点击修改查看字段 |
| C. 完成列表页 + 进入新增页查看字段（老板主导）| 完整深度，**老板**点击添加查看字段 |

**本轮 S1-02 采用 A 选项（仅列表页）**。**强烈建议** S1-02 完成后由老板决定是否进入修改页（因 hospitalId 是 P0 阻塞）。

---

## 十八、文件元信息

- **路径**：`E:\C\minimax\OptFlow PMS\55_PAGE801_门店_26项.md`
- **建立日期**：2026-09-01
- **侦察执行**：老板主导（手动进入页面）+ AI 整理（用 browser 工具只读 inspect/query）
- **写操作**：0
- **生产数据安全**：是
- **A/B/C/D/E/F 统计**：A=19 / B=0 / C=0 / D=0 / E=1 / F=6
- **真实样本数**：1 条医院
- **新发现**：客服电话 = 11 位手机号（18171604950）/ 医生数 = 31 / 单店架构
- **未发现**：hospitalId 字段（**P0 阻塞**）
- **状态**：✅ **本轮完成，立即停止，等老板下达 S1-03 指令**
