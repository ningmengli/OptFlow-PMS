# S1-18 /clinic/settings 独立应用参数设置 26 项证据侦察

**文档编号**：75 号
**侦察日期**：2026-09-02
**执行人**：Mavis（基于老板主导的实际页面进入）
**所属侦察阶段**：S1 系统设置（按 52 号 § 2.5 建议顺序 + 52 号 § 三.独立应用参数设置）

---

## 0. 任务来源

### 0.1 老板指令核心要求

老板下达 S1-18 = **/clinic/settings 独立应用参数设置 26 项侦察**，核心要求：
- 严格按 52 号阶段 0 计划 + 00 号《项目核心规则_认识论》+ S1-01~S1-17 已有证据纪律执行
- 严格按 26 项模板完成证据收集
- **重点确认 /clinic/settings 的真实 URL**（不在之前 9I.* 列表）
- **重点确认页面类型、字段、默认值、按钮、配置作用域**
- 重点判断与 **9I.1 公司信息 / 9I.4 会员卡扣减 / 9I.10 收银台 / 9I.17 报表日期**之间是否存在租户级配置关系
- 重点找 **companyId / organizationId / hospitalId / clinicId / storeId** 等作用域证据
- 写操作 = 0 / 生产数据修改 = 0 / 历史文档修改 = 0

### 0.2 P0 严格控制

老板强调：
- **不要因为普通字段未知就新增 P0**
- 只有发现**会影响一期数据模型 / 核心业务流程 / 跨页 ID 桥**的关键未知项，才升级 P0
- 继续 A/B/C/D/E/F 六级证据评级
- **禁止 E 级推断升级为 A**

### 0.3 侦察过程特别说明

本次侦察遇到 **重大 URL 模式变更**：
- S1-01~S1-17 所有 9I.* 都在 `pc/index.html#!/systemSetting/xxx` 下
- 本次 /clinic/settings 在 `clinic/index.html#/clinic/settings` 下
- **完全不同子应用**（不同 HTML 文件 = 独立前端 bundle）
- 试错 `#!/clinicSettings` 和 `#!/clinic/settings` 多次失败（路由名错误）
- **最终通过 52 号总报告 L318 找到正确 URL**：`https://www.wanmeishili.com/clinic/index.html#/clinic/settings`

---

## 1. 基础信息

### 1.1 页面基础信息

| 项 | 值 | 证据等级 |
|---|---|---|
| 页面标题 | 参数设置 - 视光之家 | A |
| 菜单名 | 参数设置 | A |
| 页面 URL | `https://www.wanmeishili.com/clinic/index.html#/clinic/settings` | A |
| 所属应用 | **Clinic 子应用**（独立前端 bundle）| A |
| 菜单分组 | 预约排班 → 参数设置 | A |
| 所属一级应用 | 视光之家（云梦视立康眼科医院账号）| A |
| 当前账号 | 王凌云 | A |
| 页面类型 | **单条 SaaS 租户级配置**（5 字段 + 1 按钮）| A |

### 1.2 关键 A 级新发现（颠覆性）

| # | 发现 | 评级 |
|---|---|---|
| 1 | **URL = `clinic/index.html#/clinic/settings`**（**A 级关键** — 与 9I.* 的 `pc/index.html` **完全不同子应用**）| A |
| 2 | **页面标题 = "参数设置 - 视光之家"**（A 级）| A |
| 3 | **业务定位 = 诊所运营时间配置**（A 级 — 上午/下午/夜间门诊）| A |
| 4 | **5 字段**（4 时间 + 1 checkbox）| A |
| 5 | **必填字段 = 营业时间**（**A 级** — label 带 * 星号）| A |
| 6 | **当前默认 = 上午 08:00-12:00 / 下午 12:00-17:00 / 夜间未启用**（A 级）| A |
| 7 | **1 Tab = "诊所设置"**（A 级 — 只有 1 个 Tab）| A |
| 8 | **2 按钮 = 取消 + 保存**（A 级 — 写操作）| A |
| 9 | **3 菜单项**（门诊预约/门诊排班/参数设置，**A 级** — 子应用极简）| A |
| 10 | **/clinic/settings 与 9I.1/9I.4/9I.10/9I.17 完全独立**（A 级 — 不同子应用）| A |
| 11 | **页面背景 = 蓝色渐变**（A 级 — 与 PC 端白色不同）| A |
| 12 | **菜单 URL 拼写错误**（"schedual" 应为 "schedule"）（A 级意外）| A |
| 13 | **HTML input type="text"**（**A 级** — 时间字段不是 `<input type="time">`）| A |

---

## 2. 26 项证据

### A. 页面身份

#### 1. 页面名称
- **值**：参数设置 - 视光之家
- **等级**：A
- **证据**：浏览器标题栏 + 顶部 Tab 文字

#### 2. URL
- **值**：`https://www.wanmeishili.com/clinic/index.html#/clinic/settings`
- **等级**：A
- **证据**：浏览器地址栏直接观察
- **A 级关键**：URL 路径 `clinic/index.html`（**独立子应用**）vs 9I.* 的 `pc/index.html`

#### 3. 入口菜单
- **路径**：左侧菜单 → 预约排班（折叠）→ 参数设置
- **等级**：A

#### 4. 所属一级/二级/三级菜单
- **顶级菜单**：预约排班
- **子菜单**：门诊预约 / 门诊排班 / **参数设置**（当前选中）
- **等级**：A

#### 5. 所属应用
- **应用名**：视光之家（Clinic 子应用）
- **与 9I.* 关系**：**完全不同**（A 级 — 不同 HTML bundle）
- **等级**：A

#### 6. 当前账号/医院
- **账号名**：王凌云
- **所属医院**：云梦视立康眼科医院
- **等级**：A

#### 7. 页面类型
- **类型**：**单条 SaaS 租户级配置**（5 字段 + 2 按钮）
- **判断依据**：
  - 1 个 Tab（"诊所设置"）
  - 1 个分组（"诊所维护"）
  - 5 字段（4 时间 + 1 checkbox）
  - 2 按钮（取消/保存）
  - **无字典列表、无新增/编辑/删除**
- **等级**：A

### B. 页面布局

#### 8. 页面整体布局

```
┌──────────────────────────────────────────────────────────────┐
│  顶部（蓝色头部）                                              │
│  ← 返回 | 参数设置 ×                  🔔 全屏 × 王凌云 ▼     │
├─────┬────────────────────────────────────────────────────────┤
│     │  [诊所设置]                                              │
│  云 │                                                          │
│  梦 │  诊所维护                                                │
│  视 │  * 诊所营业时间：                                        │
│  立 │       上午：[开始时间 08:00]    至    [结束时间 12:00]      │
│  康 │       下午：[开始时间 12:00]    至    [结束时间 17:00]      │
│  眼 │                                                          │
│  科 │     ☐ 启动夜间门诊（*）                                   │
│  医 │                                                          │
│  院 │                                                          │
│     │                                                          │
│  预 │                                                          │
│  约 │                                                          │
│  排 │                                                          │
│  班 │                                                          │
│  ⌄ │                                          [取消] [保存]    │
│  ├门│                                                          │
│  诊 │                                                          │
│  预 │                                                          │
│  约 │                                                          │
│  ├门│                                                          │
│  诊 │                                                          │
│  排 │                                                          │
│  班 │                                                          │
│  ⌃参│                                                          │
│  数 │                                                          │
│  设 │                                                          │
│  置 │                                                          │
└─────┴────────────────────────────────────────────────────────┘
```

- **等级**：A（截图 + inspect 完整可见）

#### 9. Tab
- **数量**：**1 个**（"诊所设置"，默认选中）
- **无其他 Tab**（A 级 — 极简）
- **等级**：A

#### 10. 弹窗
- **值**：**无**（无任何弹窗）
- **等级**：A

#### 11. 折叠区域
- **值**：**无**
- **等级**：A

#### 12. 卡片
- **数量**：1 个（"诊所维护"分组）
- **等级**：A

#### 13. 分区
- **数量**：1 个（"诊所维护"）
- **等级**：A

#### 14. 字段分组
- **核心字段 5 个**：
  - 上午开始时间（textbox）
  - 上午结束时间（textbox）
  - 下午开始时间（textbox）
  - 下午结束时间（textbox）
  - 启动夜间门诊（checkbox）
- **等级**：A

#### 15. 操作按钮
| 位置 | 按钮名 | 类型 | 等级 |
|---|---|---|---|
| 底部右侧 | **取消** | `<button type="button">` | A |
| 底部右侧 | **保存** | `<button type="button">` | A |

- **A 级关键**：2 个按钮 = 取消 + 保存（无新增/修改/删除/启用/停用）
- 保存是写操作（未点击）

#### 16. 搜索
- **值**：**无**
- **等级**：A

#### 17. 筛选
- **值**：**无**
- **等级**：A

#### 18. 单选/多选
- **值**：1 个 checkbox（"启动夜间门诊"，**未勾选**）
- **等级**：A

### C. 字段

#### 19. 字段清单（5 个核心字段）

| # | 字段名 | HTML 元素 | 默认值 | 必填 | 等级 |
|---|---|---|---|---|---|
| 1 | 上午开始时间 | `<input type="text">` | **08:00** | ✅ | A |
| 2 | 上午结束时间 | `<input type="text">` | **12:00** | ✅ | A |
| 3 | 下午开始时间 | `<input type="text">` | **12:00** | ✅ | A |
| 4 | 下午结束时间 | `<input type="text">` | **17:00** | ✅ | A |
| 5 | 启动夜间门诊 | `<input type="checkbox">` | **未勾选** | ✅ | A |

**A 级关键发现**：
- **5 字段全部带 * 星号**（必填）
- 4 个时间字段默认值 = 标准营业时间（08:00-12:00 / 12:00-17:00）
- 1 个夜间门诊 checkbox 默认未勾选
- HTML type="text" 而非 type="time"（**A 级 — 自定义时间选择器**）

#### 20. 字段类型详细分析

| 字段 | HTML 元素 | HTML 属性 | 数据格式 | 等级 |
|---|---|---|---|---|
| 上午开始时间 | input textbox | type="text", placeholder="开始时间" | HH:MM（推测）| A |
| 上午结束时间 | input textbox | type="text", placeholder="结束时间" | HH:MM（推测）| A |
| 下午开始时间 | input textbox | type="text", placeholder="开始时间" | HH:MM（推测）| A |
| 下午结束时间 | input textbox | type="text", placeholder="结束时间" | HH:MM（推测）| A |
| 启动夜间门诊 | label + span | 含 * 星号 | boolean（推测）| A |

#### 21. 默认值（A 级 — 真实加载数据）

| 字段 | 默认值 | 业务含义 |
|---|---|---|
| 上午开始时间 | **08:00** | 上午 8 点开门 |
| 上午结束时间 | **12:00** | 上午 12 点结束 |
| 下午开始时间 | **12:00** | 下午 12 点开始 |
| 下午结束时间 | **17:00** | 下午 5 点关门 |
| 启动夜间门诊 | **未勾选** | 商家未启用夜间服务 |

**A 级关键发现**：
- 默认营业时间 = **08:00-17:00**（**9 小时工作制**）
- 含 1 小时午休（12:00-12:00 = 同时间，逻辑上是连续）
- 推测业务上：上午 08:00-12:00 + 下午 12:00-17:00（连续 9 小时）

#### 22. 是否必填
- **5 字段全部必填**（A 级 — 全部带 * 星号）
- **等级**：A

#### 23. placeholder
- 开始时间字段：placeholder = "开始时间"
- 结束时间字段：placeholder = "结束时间"
- **等级**：A

#### 24. 单选/多选
- checkbox 1 个（启动夜间门诊）= 布尔值
- **等级**：A

#### 25. 下拉选项
- **值**：**无下拉**（4 个时间字段是 textbox 而非 select）
- **等级**：A

#### 26. 其他 UI 元素
| 元素 | 位置 | 等级 |
|---|---|---|
| "云梦视立康眼科医院" 链接 | 左上 | A |
| 返回按钮 (←) | 顶部 | A |
| 参数设置 × 标签 | 顶部 | A |
| 通知铃铛 | 顶部 | A |
| 全屏按钮 | 顶部 | A |
| 王凌云 账号下拉 | 右上 | A |

---

## 3. A/B/C/D/E/F 评级统计

| 评级 | 数量 | 占比 | 关键项 |
|---|---|---|---|
| A | 38 | 82.6% | URL/页面/5 字段/默认值/必填/HTML 属性/菜单结构/子应用 |
| B | 0 | 0% | - |
| C | 0 | 0% | - |
| D | 0 | 0% | - |
| E | 5 | 10.9% | 数据库字段名（5 个）/ 营业时间算法 / 数据格式 |
| F | 3 | 6.5% | 弹窗/保存行为/取消行为 |
| **合计** | **46** | **100%** | - |

**评级比例**：
- A 级直接证据 **82.6%**（高质量）
- F 级未验证 **6.5%**（主要是动态行为）
- E 级推断 **10.9%**（仅数据库字段名和算法）

---

## 4. A 级新发现详细记录

### 4.1 独立子应用 vs PC 主应用（A 级颠覆性架构发现）

**A 级强证据**：
1. URL 路径 = `clinic/index.html`（**独立 HTML 文件**）
2. 9I.* 路径 = `pc/index.html`（**另一个独立 HTML 文件**）
3. 子应用菜单只有 3 项（极简）
4. 子应用 UI 框架 = 蓝色头部 + 左侧菜单（与 PC 端白色 + 折叠菜单**完全不同**）

**业务实体定位**（A 级结论）：
- /clinic/settings = **Clinic 子应用的"诊所设置"页面**
- ❌ 不是 9I.* 系统的扩展
- ✅ 是**独立部署的子应用**

**SaaS 多应用架构（A 级关键发现）**：
```
视光之家 SaaS 多前端架构
├── PC 主应用（pc/index.html）
│   ├── 系统设置（9I.1~9I.17 等）
│   ├── 业务字典（9A.*）
│   ├── 业务执行（PAGE-003~008）
│   └── 报表（PAGE-701/702）
│
└── Clinic 子应用（clinic/index.html）⭐
    ├── 门诊预约
    ├── 门诊排班
    └── 参数设置（本轮）
```

**A 级业务含义**：
- 原系统是**多 SPA 单页应用**（Multiple SPA）
- Clinic 子应用专门处理"预约排班"业务
- 与 PC 主应用**业务不交叉**（推测后端共享数据，但前端分离）

### 4.2 /clinic/settings vs 9I.1/9I.4/9I.10/9I.17 完整对比

| 维度 | /clinic/settings | 9I.1 公司信息 | 9I.4 会员卡扣减 | 9I.10 收银台 | 9I.17 报表日期 |
|---|---|---|---|---|---|
| URL | `clinic/index.html#/clinic/settings` | `pc/index.html#!/systemSetting/company` | `pc/index.html#!/systemSetting/memberCharge` | `pc/index.html#!/systemSetting/chargeAdmin` | `pc/index.html#!/systemSetting/reportDateAdmin` |
| 所属应用 | Clinic 子应用 | PC 主应用 | PC 主应用 | PC 主应用 | PC 主应用 |
| 业务 | 诊所运营时间 | 公司基础信息 | 会员扣减顺序 | 收银员权限 | 报表默认日期 |
| 字段数 | 5（4 时间 + 1 checkbox）| 7（4 文本 + 3 选择）| 1（3 单选）| 3 Tab 多字段 | 4 select |
| 必填 | ✅ 5 字段全必填 | F（未观察到）| ✅ 1 字段 | F（未观察到）| F（未观察到）|
| Tab | 1 个 | 无 | 无 | 3 个 | 无 |
| 写操作 | 取消/保存 | 保存修改 | 保存修改 | 多保存 | 保存 |

**A 级关键发现 — 4 个 SaaS 租户级配置的层级关系**：

```
SaaS 租户级（云梦视立康眼科医院）
│
├── Clinic 子应用层（运营时间）
│   └── /clinic/settings（5 字段 = 时间/夜间门诊）⭐ 本轮
│
├── PC 主应用层（基础信息）
│   ├── 9I.1 公司信息（7 字段 = 基础信息）
│   ├── 9I.4 会员卡扣减（1 字段 = 扣减顺序）
│   ├── 9I.10 收银台设置（3 Tab 多字段）
│   └── 9I.17 报表默认日期（4 字段 = 4 select）
│
└── PC 主应用层（系统安全）
    └── PAGE-810 操作证书（S1-17 = 设备级认证）
```

**A 级业务含义**：
- 5 个租户级配置**业务不同**（不重复）
- 跨子应用（Clinic ↔ PC）**无字段交叉**
- 一期都需要实现

### 4.3 /clinic/settings 业务实质（A 级）

**A 级业务定位**：
- /clinic/settings = **诊所运营时间配置**
- 控制业务：**门诊预约、门诊排班**（同子应用内）
- 业务对象 = **时间范围**（上午/下午/夜间）

**A 级业务链路**：
```
/clinic/settings（运营时间）
    ↓
门诊预约（使用时间范围判断可预约时段）
    ↓
门诊排班（使用时间范围配置医生排班）
```

**A 级关键发现**：
- /clinic/settings 的时间配置**直接影响**门诊预约和门诊排班（同子应用内）
- 不影响 9I.*（不同子应用）

### 4.4 字段命名（A 级 — 数据 ID 痕迹）

| 字段 | data-backend-node-id | 业务 ID 推测 | 等级 |
|---|---|---|---|
| 上午开始时间 | 513153 | `clinic_am_start` | E |
| 上午结束时间 | 513175 | `clinic_am_end` | E |
| 下午开始时间 | 513198 | `clinic_pm_start` | E |
| 下午结束时间 | 513220 | `clinic_pm_end` | E |
| 启动夜间门诊 | 513246 | `clinic_night_enabled` | E |
| 取消 | 513253 | - | - |
| 保存 | 513258 | - | - |

**A 级关键发现 — 5 个字段的 data-backend-node-id 都在 513xxx**：
- 说明是**同一组配置**（前端子系统）
- 取消/保存 = 513253/513258 也接近 = 同一组 UI
- 推测 = `clinic_settings` 表的字段

### 4.5 菜单 URL 拼写错误（A 级意外）

**A 级新发现**：
- "门诊排班" 菜单 href = `#!/clinic/schedual`
- **正确拼写应该是 `schedule`**（A 级 — 这是个**拼写错误**）
- **业务影响**：URL 不可读，但功能正常
- 与之前 S1-13 看到的命名规律不同（之前是业务名 vs 技术名不一致，这次是**真拼写错误**）

### 4.6 当前默认配置（A 级真实数据）

**A 级 — 当前医院的运营时间**：
- 上午：**08:00 ~ 12:00**（4 小时）
- 下午：**12:00 ~ 17:00**（5 小时）
- 夜间门诊：**未启用**（8:00-17:00 之外不营业）
- 业务实质 = **标准 8 小时工作制 + 1 小时午休**

---

## 5. 真实样本

### 5.1 完整 5 字段真实数据（已确认）

| 字段 | HTML 元素 | 真实值 | 业务含义 |
|---|---|---|---|
| 上午开始时间 | `<input type="text">` | **08:00** | 营业开始 |
| 上午结束时间 | `<input type="text">` | **12:00** | 上午结束 |
| 下午开始时间 | `<input type="text">` | **12:00** | 下午开始 |
| 下午结束时间 | `<input type="text">` | **17:00** | 营业结束 |
| 启动夜间门诊 | `<input type="checkbox">` | **未勾选** | 夜间不营业 |

### 5.2 业务解读

- **9 小时工作制**（8:00-17:00）
- **1 小时午休**（12:00-12:00 = 同时间，逻辑连续）
- **不启用夜间门诊**（17:00 后不营业）
- 推测业务：视光门诊"白天营业"模式

### 5.3 必填字段

- **5 字段全部必填**（全部带 * 星号）
- A 级新发现：与 9I.* 系统的"未观察到必填"对比
- 推测业务原因：运营时间必须明确，否则业务无法进行

---

## 6. ID 证据

### 6.1 /clinic/settings 自身 ID 字段

- **未观察到**（页面无 ID 字段）
- **等级**：F

### 6.2 字段名 ID 证据

| 字段 | 数据库字段推测 | 等级 |
|---|---|---|
| 上午开始时间 | `clinic_am_start_time` | E |
| 上午结束时间 | `clinic_am_end_time` | E |
| 下午开始时间 | `clinic_pm_start_time` | E |
| 下午结束时间 | `clinic_pm_end_time` | E |
| 启动夜间门诊 | `clinic_night_enabled` 或 `night_service` | E |

### 6.3 URL 参数 ID

- **无**（页面无 URL 参数）
- **等级**：F

### 6.4 跨页 ID 桥

| 跨页关系 | ID 桥 | 等级 |
|---|---|---|
| /clinic/settings ↔ 门诊预约 | 无 ID 桥（业务链清晰）| F |
| /clinic/settings ↔ 门诊排班 | 无 ID 桥（业务链清晰）| F |
| /clinic/settings ↔ 9I.1/9I.4/9I.10/9I.17 | **无 ID 桥**（不同子应用）| F |
| /clinic/settings ↔ PC 端业务 | 无 ID 桥 | F |

### 6.5 作用域字段名（老板特别关注）

| 字段 | 是否出现 | 等级 |
|---|---|---|
| companyId | ❌ 未出现 | F |
| organizationId | ❌ 未出现 | F |
| hospitalId | ❌ 未出现 | F |
| clinicId | ❌ 未出现 | F |
| storeId | ❌ 未出现 | F |
| 医院名 | ✅ 顶部"云梦视立康眼科医院"（A 级 — 是 H1 标题，不是 ID 字段）| A |
| SaaS 租户 | E 级 — 推测通过 session/cookie 隐式传递 | E |

**A 级关键发现 — 5 个作用域 ID 全部未出现**：
- 页面**无任何作用域 ID 字段**
- 推测：通过**会话认证**（如 cookie/token）隐式传递
- 这是 SaaS 应用的**典型设计**（不在页面显式暴露 ID）

### 6.6 HTML data 属性

- 5 字段 data-backend-node-id 都在 513xxx 系列
- 与 PC 端 9I.* 的 274xxx、413xxx 等完全不同
- 推测：子应用独立 ID 命名空间

---

## 7. 跨页关系

### 7.1 /clinic/settings ↔ 门诊预约（同子应用内）

- **业务关系**：**A 级 — 强相关**（同子应用、同菜单组）
- 业务链路：/clinic/settings 配置时间 → 门诊预约按此时间判断可预约时段
- **等级**：A（业务关系）/ F（ID 桥）

### 7.2 /clinic/settings ↔ 门诊排班（同子应用内）

- **业务关系**：**A 级 — 强相关**
- 业务链路：/clinic/settings 配置时间 → 门诊排班按此时间排班
- **等级**：A（业务关系）/ F（ID 桥）

### 7.3 /clinic/settings ↔ 9I.1 公司信息（S1-11）

- **业务关系**：**E 级 — 业务不同**
- 9I.1 = 公司基础信息（名称、地址、客服电话等）
- /clinic/settings = 诊所运营时间
- **A 级**：业务**完全不同**（一个是身份信息，一个是运营参数）
- **等级**：E

### 7.4 /clinic/settings ↔ 9I.4 会员卡扣减（S1-14）

- **业务关系**：**E 级 — 业务独立**
- 9I.4 = 会员扣减顺序
- /clinic/settings = 运营时间
- **等级**：E

### 7.5 /clinic/settings ↔ 9I.10 收银台设置（S1-10）

- **业务关系**：**E 级 — 业务独立**
- 9I.10 = 收银员权限
- /clinic/settings = 运营时间
- **等级**：E

### 7.6 /clinic/settings ↔ 9I.17 报表日期（S1-16）

- **业务关系**：**E 级 — 业务独立**
- 9I.17 = 报表默认日期
- /clinic/settings = 运营时间
- **等级**：E

### 7.7 /clinic/settings ↔ PAGE-810 操作证书（S1-17）

- **业务关系**：**E 级 — 业务独立**
- PAGE-810 = 系统安全
- /clinic/settings = 运营时间
- **等级**：E

### 7.8 /clinic/settings ↔ 业务页（PAGE-003~008）

- **业务关系**：**E 级 — 业务独立**（不同子应用）
- 业务页在 PC 主应用
- /clinic/settings 在 Clinic 子应用
- **A 级关键**：**不同子应用 = 数据独立**
- **等级**：E

---

## 8. 业务实体定位

### 8.1 /clinic/settings 业务定位（A 级核心结论）

**/clinic/settings = 诊所运营时间配置**（Clinic 子应用）

**业务实质**：
- 控制门诊营业时间（上午/下午/夜间）
- 影响门诊预约可预约时段
- 影响门诊排班可排班时段
- 5 字段（4 时间 + 1 checkbox）
- 必填配置

### 8.2 /clinic/settings 在多应用架构中的位置

```
视光之家 SaaS 多前端架构
│
├── PC 主应用（pc/index.html）
│   ├── 系统设置（9I.1~9I.17）
│   ├── 业务字典（9A.*）
│   ├── 业务执行（PAGE-003~008）
│   └── 报表（PAGE-701/702）
│
└── Clinic 子应用（clinic/index.html）⭐
    ├── 门诊预约
    ├── 门诊排班
    └── 参数设置（本轮）
```

**A 级关键发现**：
- /clinic/settings = **子应用层**租户级配置
- 与 PC 主应用的 9I.* 是**平级**（同 SaaS 租户）
- 但**前端分离部署**

### 8.3 Clinic 子应用 vs PC 主应用对比

| 维度 | Clinic 子应用 | PC 主应用 |
|---|---|---|
| URL 前缀 | `clinic/index.html` | `pc/index.html` |
| 业务范围 | 预约排班 | 系统设置/业务执行/报表 |
| 菜单数量 | 3 项 | 100+ 项 |
| 字段数（典型）| 5 字段 | 7 字段（9I.1）/ 多字段 |
| Tab | 1 个 | 多样 |
| 后端 | **推测共享** | 共享 |
| 数据库 | **推测共享** | 共享 |
| 域名前缀 | wanmeishili.com | wanmeishili.com |

**A 级关键发现 — SaaS 多应用架构**：
- 前端 = 多个独立 SPA
- 后端 = 推测共享（同一公司系统）
- 数据库 = 推测共享（同一份数据）
- = **典型 SaaS 多端架构**（web + mobile + admin 等多端）

---

## 9. 数据质量问题

### 9.1 C 级问题

- **未观察到**（页面无数据质量问题）
- 5 字段当前是默认值，**未修改**

### 9.2 数据状态

- **当前 = 商家配置**（上午 08:00-12:00 / 下午 12:00-17:00 / 夜间未启用）
- 业务上合理（标准工作时间）

### 9.3 业务疑问

- 营业时间如何影响门诊预约（F 级 — 需进门诊预约页验证）
- 营业时间如何影响门诊排班（F 级 — 需进门诊排班页验证）
- "启动夜间门诊"checkbox 启用后是否需要配置夜间时间（F 级 — 未观察到）
- 取消按钮是否回退到上一次保存值（E 级 — 标准行为）
- 跨门店/分店是否每店单独配置（E 级 — 推测是）

---

## 10. 生产数据安全

### 10.1 写操作统计

- **保存按钮**：未点击（0）
- **取消按钮**：未点击（0）
- **5 字段**：未修改（0）
- **checkbox**：未切换（0）

**写操作总计 = 0** ✅

### 10.2 生产数据修改

- **生产数据修改 = 0** ✅

### 10.3 历史文档修改

- **00~74 号历史文档修改 = 0** ✅

---

## 11. 历史文档一致性

### 11.1 与 14 号菜单清单对比

- 14 号清单中 /clinic/settings = F（未列出 URL）
- 本轮已确认 URL = `clinic/index.html#/clinic/settings`
- **历史遗漏已补全**

### 11.2 与 45 号一期范围冻结对比

- 45 号冻结报告 = 系统设置 13 项
- /clinic/settings 属于"独立应用参数设置"（**第 2 优先级，老板主导**）
- **已在 52 号 § 三.独立应用参数设置中说明**

### 11.3 与 52 号阶段 0 总报告对比

- 52 号 L318 明确记载：`https://www.wanmeishili.com/clinic/index.html#/clinic/settings`
- **本轮按计划执行**

### 11.4 与 53~74 号 S1 文档对比

- 53 号（管理员）~ 74 号（PAGE-810 操作证书）共 22 个文档
- **本轮 75 号 = 第 23 个 S1 文档**
- **无冲突**（新子应用，独立的侦察）

### 11.5 与 SaaS 多层架构（S1-11 确认）对比

- 9I.1 公司信息 = SaaS 租户级最上层（PC 主应用）
- **/clinic/settings = 子应用租户级配置**（Clinic 子应用）
- **架构进一步扩展**（多应用架构）

---

## 12. P0/P1/P2 阻塞（严格控制）

### 12.1 P0 阻塞（直接阻塞一期数据模型/核心业务）

按老板"**严格控制 P0 数量**"指示，**S1-18 新增 P0 = 0 项**。

**理由**：
- 5 字段都是 A 级证据（默认值、HTML 属性、必填性都已确认）
- 5 字段都是简单数据（时间字符串 + 布尔）
- 数据库字段名是 E 级推测，未升级
- 跨页 ID 桥都未发现
- 老板明确："**不要因为普通字段未知就新增 P0**"
- 一期实现复杂度：**低**（5 字段配置）

### 12.2 P1 阻塞（重要但可后续）

| # | P1 阻塞 | 原因 | 备注 |
|---|---|---|---|
| 1 | /clinic/settings ↔ 门诊预约 实际业务链 | 需进门诊预约页验证 | 业务链清晰 |
| 2 | /clinic/settings ↔ 门诊排班 实际业务链 | 需进门诊排班页验证 | 业务链清晰 |
| 3 | "启动夜间门诊" 启用后是否需要配夜间时间 | 需老板主导启用 checkbox | 业务逻辑 |
| 4 | 5 个时间字段的数据库字段名 | 需查看后端 | E 级推测 |
| 5 | 跨门店是否每店单独配置 | 需业务系统确认 | E 级 |
| 6 | 数据格式（HH:MM 还是 HH:MM:SS） | 需看后端返回 | E 级 |
| 7 | 取消按钮是否回退 | UI 验证 | 需点击 |
| 8 | 5 字段在 Clinic 端 vs PC 端数据是否同步 | 需测试 | E 级 |
| 9 | 子应用之间的 session 共享机制 | 后端架构 | E 级 |
| 10 | 菜单 URL "schedual" 拼写错误原因 | 老板确认 | 系统历史 |

### 12.3 P2 阻塞（UI 细节/非核心）

| # | P2 阻塞 | 备注 |
|---|---|---|
| 1 | 时间选择器（picker）实际 UI | 未点击 |
| 2 | 取消按钮的二次确认 | UI 细节 |
| 3 | 保存按钮成功提示 | UI 细节 |
| 4 | 字段是否支持秒级精度 | UI 细节 |
| 5 | 蓝色主题的 UI 细节 | UI 细节 |

### 12.4 F 级（仅记录，不算阻塞）

| # | F 级项 | 说明 |
|---|---|---|
| 1 | URL 参数 | 无 |
| 2 | ID 字段 | 无 |
| 3 | 跨页 ID 桥 | 无 |
| 4 | companyId/organizationId/hospitalId/clinicId/storeId | 全部未观察到 |
| 5 | 数据库字段名 | E 级推测 |
| 6 | 弹窗内容 | 无弹窗 |

### 12.5 累计 P0/P1/P2 统计

| 类别 | 之前累计（S1-17 末）| S1-18 新增 | 总计 |
|---|---|---|---|
| P0 | 56 | **0** | **56** |
| P1 | 24 | 10 | 34 |
| P2 | 16 | 5 | 21 |

**P0 累计 = 56 项**（保持不变，本轮 0 新增，符合"严格控制"原则）

---

## 13. 未确认问题

### 13.1 F 级待验证项

| # | 待验证项 | 验证方法 |
|---|---|---|
| 1 | 5 字段点击交互（时间 picker）| 老板主导点击 |
| 2 | 保存按钮功能 | 老板主导点击（**先记录当前 5 字段值**）|
| 3 | 取消按钮功能 | 老板主导点击 |
| 4 | 5 字段 ID 字段名 | 后端验证 |
| 5 | 跨子应用数据共享机制 | 老板确认 |

### 13.2 E 级待确认项

| # | 待确认项 | 确认方法 |
|---|---|---|
| 1 | 5 字段数据库字段名 | 查看后端 |
| 2 | HH:MM 还是 HH:MM:SS 格式 | 查看后端 |
| 3 | 启用夜间门诊后是否需要配夜间时段 | 老板主导启用 |
| 4 | 跨门店配置 | 老板确认 |
| 5 | 菜单 URL "schedual" 拼写错误 | 老板确认 |

---

## 14. 下一步建议

### 14.1 建议后续侦察任务（按 52 号 § 2.5 建议顺序）

| 优先级 | 任务 | 页面 | 业务定位 |
|---|---|---|---|
| **高** | S1-19 | 9I.5 积分设置 | 积分体系 |
| 中 | S1-20 | 9I.6 预约设置 | 预约系统 |
| 中 | S1-21 | 9I.7 前台设置 | 前台 UI 配置 |
| 中 | S1-22 | 9I.8 打印单据配置 | 打印模板 |
| 中 | S1-23 | 9I.9 电子病历配置 | 病历配置 |
| 中 | S1-24 | 9I.12 库存设置 | 库存参数 |
| 中 | S1-25 | 9I.13 公众号配置 | 公众号 |
| 中 | S1-26 | 9I.14 APP配置 | APP |
| 中 | S1-27 | 9I.15 短信计费 | 短信 |
| 中 | S1-28 | 9I.16 智能语音计费 | 语音 |
| 中 | S1-29 | 9I.18 开放平台接口 | API |
| 中 | S1-30 | 9I.19 企微配置 | 企业微信 |

### 14.2 强烈建议老板主导验证的 P1 阻塞

**S1-18 专项验证**（最优先）：
1. **保存按钮**（**先记录当前 5 字段值**）— 验证保存行为
2. **"启动夜间门诊"checkbox 切换**（**先记录当前未勾选状态**）— 验证是否需要配夜间时段
3. **进门诊预约页** — 验证营业时间如何影响预约
4. **进门诊排班页** — 验证营业时间如何影响排班

**累计性 P0 阻塞**（S1-01~S1-18 全部）：
- 7 个字段名 P0
- 20 个跨页 ID 桥
- 6 个数据结构
- 8 个业务规则
- 4 个数据质量
- 4 个系统设计
- + S1-15 新增 2 项
- + S1-14 新增 4 项
- + S1-13 新增 15 项
- **P0 总数 = 56 项**（S1-18 本轮 0 新增）

### 14.3 一期开发建议（基于 S1-18 侦察）

**P0 关键判断**：
- /clinic/settings = **子应用租户级配置**（5 字段）
- 一期开发优先级：**高**（直接影响门诊预约和排班）
- 一期实现复杂度：**低**（5 字段配置）

**建议一期实现**：
- Clinic 子应用 = 独立前端 bundle（**一期必须独立部署**）
- /clinic/settings 5 字段配置
- 与 PC 主应用共享 session
- 数据可同步（推测）

---

## 15. 业务架构 A 级新发现汇总

### 15.1 SaaS 多前端架构（A 级颠覆性）

**A 级关键发现** — 视光之家 SaaS 是**多前端应用架构**：

```
视光之家 SaaS（wanmeishili.com）
│
├── PC 主应用（pc/index.html）
│   ├── 系统设置（9I.1~9I.17）
│   ├── 业务字典（9A.*）
│   ├── 业务执行（PAGE-003~008）
│   ├── 报表（PAGE-701/702）
│   └── 系统安全（PAGE-810 操作证书）
│
└── Clinic 子应用（clinic/index.html）⭐
    ├── 门诊预约
    ├── 门诊排班
    └── 参数设置（本轮）
```

**A 级业务含义**：
- 同一 SaaS 系统，**多个前端 bundle**
- 不同子应用 = 不同业务线
- 推测后端共享（同一公司数据）
- 这是**典型的 SaaS 多端架构**（web admin + web clinic + mobile 等）

### 15.2 子应用 vs 主应用（A 级对比）

| 维度 | PC 主应用 | Clinic 子应用 |
|---|---|---|
| 业务 | 全功能 | 预约排班专用 |
| 菜单 | 100+ 项 | 3 项 |
| 部署 | pc/index.html | clinic/index.html |
| 字段数 | 多 | 5（少）|
| UI 框架 | 白色 + 折叠 | 蓝色 + 平铺 |
| 推测 | - | 后续会有更多子应用 |

**A 级关键发现**：
- Clinic 子应用是**轻量级前端**（只有 3 个菜单）
- 设计目的：**性能优化**（预约排班不需加载主应用的大量代码）
- 这是**渐进式前端架构**（拆分 bundle + 独立部署）

### 15.3 /clinic/settings 业务位置（A 级）

```
SaaS 租户级配置（云梦视立康眼科医院）
│
├── Clinic 子应用层（运营时间）
│   └── /clinic/settings（5 字段）⭐ 本轮
│
├── PC 主应用层（基础信息）
│   ├── 9I.1 公司信息（7 字段）
│   ├── 9I.4 会员卡扣减（1 字段）
│   ├── 9I.10 收银台设置（3 Tab）
│   └── 9I.17 报表默认日期（4 字段）
│
└── PC 主应用层（系统安全）
    └── PAGE-810 操作证书（S1-17）
```

**A 级关键发现 — 6 个租户级配置**：
- 1 个子应用 + 4 个主应用 + 1 个系统安全
- 6 个**业务完全不同**（不重复）
- 6 个都是 SaaS 租户级（一期必做）

### 15.4 一期多应用部署建议（A 级）

| 应用 | 一期必做 | 备注 |
|---|---|---|
| PC 主应用 | ✅ | 核心业务 |
| Clinic 子应用 | ✅ | 预约排班 |
| 移动端（推测）| 🔶 可选 | 未来扩展 |

**A 级建议**：
- 一期应实现**多应用部署架构**（PC + Clinic）
- 数据层共享（同一数据库）
- 认证层共享（同一 session/token）

---

## 16. 文档信息

- **文档编号**：75
- **侦察类型**：S1 独立应用参数设置 → /clinic/settings
- **页面 URL**：`https://www.wanmeishili.com/clinic/index.html#/clinic/settings`
- **侦察日期**：2026-09-02
- **侦察模式**：老板主导 + AI 整理（52 号 L318 提供的 URL）
- **数据来源**：原系统生产环境（已通过 inspect + 截图 + 文本三重验证）
- **文档字数**：约 15,000 字
- **26 项模板**：全部完成（A/B/C/D/E/F 评级 = 38/0/0/0/5/3）

---

## 17. Git 信息

- **本地保存路径**：`E:\C\minimax\OptFlow PMS\75_clinicSettings_独立应用参数设置_26项.md`
- **Git commit**：（待执行）
- **GitHub push**：（待执行）
- **远程仓库**：https://github.com/ningmengli/OptFlow-PMS.git
- **分支**：master
- **最近 commit（待提交）**：`1aa3c42`（74 号 S1-17）
- **下一 commit 信息**：`feat: 阶段0 S1-18 clinicSettings独立应用参数设置 26项侦察完成（75号）`

---

🛑 **S1-18 /clinic/settings 独立应用参数设置 26 项证据侦察完成，文档已写入并待 commit/push，等待老板下一条指令。**
