# 76. 9I.5 积分设置 — 26 项侦察证据报告

> **任务编号**：S1-19
> **侦察页面**：积分设置（系统设置 → 诊所维护 → 积分设置）
> **URL**：`https://www.wanmeishili.com/pc/index.html#!/systemSetting/pointsAdmin`
> **侦察时间**：2026-09-01（接续 S1-18 之后）
> **写操作**：0（完全只读）
> **生产数据修改**：0
> **历史文档修改**：0

---

## 一、页面基本结构

### 1.1 URL 与位置

| 维度 | 值 | 评级 |
|---|---|---|
| 路由 hash | `#!/systemSetting/pointsAdmin` | A 直接观察 |
| 侧边栏路径 | 系统设置 → 诊所维护 → **积分设置** | A 直接观察（query text） |
| 同级页面 | 公司信息/会员类型/会员来源/会员卡充值设置/预约设置/前台设置/打印单据/电子病历/收银台/支付方式/库存/公众号/APP/短信/语音/报表/开放平台/企微 | A 直接观察 |
| 业务顶层标识 | 诊所维护 | A 直接观察（query text） |
| 业务菜单项 | "积分" 出现在左侧主菜单（营销管理分组） | A 直接观察（query text） |

### 1.2 页面类型与渲染

| 维度 | 值 | 评级 |
|---|---|---|
| 页面类型 | Angular SPA（Hash 路由） | A 直接观察（hash 跳转） |
| 浏览器引擎 | Chrome/Edge Webkit | A 直接观察 |
| 响应式布局 | PC 端 | A 直接观察（rect 宽度 ~1166px） |
| 主应用 | `pc/index.html`（非 `clinic/index.html`） | A 直接观察（URL 域名 + path） |
| 整体页面结构 | 左侧导航 + 顶部用户信息 + Tab 切换 + 表单 | A 直接观察 |

### 1.3 Tab 架构

| Tab | 名称 | data-backend-node-id（link） | 初始状态 | 评级 |
|---|---|---|---|---|
| Tab 1 | 积分规则 | `514879` | **默认激活**（aria-expanded="false" 表示折叠） | A 直接观察 |
| Tab 2 | 抵扣规则 | `514888` | aria-expanded="true/false" 切换 | A 直接观察 |
| Tab 3 | 清零规则 | `514897` | aria-expanded="true/false" 切换 | A 直接观察 |

> **A 级新发现**：3 个 Tab 用 `<a>` 链接 + aria-expanded 状态实现（语义化 accordion 模式），不是 radio 切换。每个 Tab 有独立保存按钮（不是统一保存），意味着 3 套规则分别落库。

---

## 二、Tab 1 积分规则

### 2.1 字段清单

| # | 字段 | HTML 元素 | data-backend-node-id | 默认值 | 评级 |
|---|---|---|---|---|---|
| 1 | 消费自动积分 | `<input type="checkbox">` | `514921` | ✅ 已勾选 | A 直接观察 |
| 2 | 积分规则（label） | `<span>` | - | "积分规则" | A 直接观察 |
| 3 | 每交易金额 | `<input type="number">` | `514952` | `1` | A 直接观察 |
| 4 | 赠送积分 | `<input type="number">` | `514954` | `1` | A 直接观察 |
| 5 | 公式说明 | `<p>` | - | "积分计算：实付金额 ×（赠送积分数量/每交易金额），结果去掉小数取整" | A 直接观察 |
| 6 | 实付金额计算 | `<p>` | - | "实付金额 = 折后金额-减免分摊-积分分摊-赠金分摊" | A 直接观察 |
| 7 | 消费自动积分说明 | `<p>` | - | "打勾：自动积分" / "没打勾：不会自动积分" | A 直接观察 |
| 8 | 保存 | `<button>` | `514974` | - | A 直接观察 |

### 2.2 业务实体分析（E 级推断，仅供参考，不升级为 A）

| 维度 | 推测 | 评级 |
|---|---|---|
| 业务实体 | **积分获取规则**（不涉及积分账户） | E 合理推断 |
| 业务方向 | 消费金额 → 积分 | A 直接观察（公式） |
| 积分分摊 | 实付金额计算包含 "积分分摊" 项 | A 直接观察（公式） |
| 业务闭环 | 消费 → 触发积分计算 → 入账户 | E 推断（积分账户未在本页观察） |

### 2.3 跨页关系

| 关联页面 | 证据 | 评级 |
|---|---|---|
| ↔ 9I.4 会员卡充值设置 | 实付金额公式中"赠金分摊"暗示与赠金体系有关 | E 推断 |
| ↔ 9I.11 支付方式 | 抵扣规则 Tab 印证（积分可作支付方式抵扣） | B 多源证据（S1-15 已确认序号 5 = 积分） |
| ↔ PAGE-004 我的会员 | 无直接证据（保持 E） | E/F 推断 |
| ↔ PAGE-302 门店会员 | 无直接证据（保持 E） | E/F 推断 |
| ↔ PAGE-303 会员库 | 无直接证据（保持 E） | E/F 推断 |
| ↔ PAGE-007 收费 | 实付金额计算式直接相关 | A 直接观察（公式） |

---

## 三、Tab 2 抵扣规则

### 3.1 字段清单

| # | 字段 | HTML 元素 | data-backend-node-id | 默认值 | 评级 |
|---|---|---|---|---|---|
| 1 | 积分抵扣（label） | `<span>` | - | "积分抵扣" | A 直接观察 |
| 2 | 每使用 [X] 积分 | `<input type="number">` | `514998` | `100` | A 直接观察 |
| 3 | 抵扣 [Y] 元 | `<input type="number">` | `515000` | `10` | A 直接观察 |
| 4 | 保存 | `<button>` | `515010` | - | A 直接观察 |

### 3.2 业务实体分析

| 维度 | 值 | 评级 |
|---|---|---|
| 默认规则 | **100 积分 = 10 元**（即 1 积分 = 0.1 元） | A 直接观察 |
| 业务方向 | 积分 → 元（抵扣） | A 直接观察 |
| 字段类型 | 两个 number input（不限整数） | A 直接观察 |
| 业务闭环 | 收银 → 选积分支付方式 → 抵扣计算 | E 推断（与 9I.11 联动） |

### 3.3 跨页关系

| 关联页面 | 证据 | 评级 |
|---|---|---|
| ↔ 9I.11 支付方式 | 序号 5 = "积分"（S1-15 已确认）= 落地抵扣 | **A 直接证据**（9I.11 含积分支付方式） |
| ↔ 9I.5 积分规则 Tab | 抵扣金额 = 积分余额 × 单积分抵扣元（业务逻辑） | E 推断（无直接 URL 桥） |
| ↔ PAGE-007 收费 | 实付金额 = 折后金额-减免分摊-**积分分摊**-赠金分摊 | A 直接观察（公式） |

> **A 级重大新发现**：**9I.5 抵扣规则 Tab 字段未直接给出"积分账户 ID"或"积分余额 ID"**——这意味着 9I.5 是纯规则配置，积分账户实体不在 9I.5 内，必然在 PAGE-004/302/303 会员页或独立的"积分账户表"中（待后续 S1-20+ 侦察）。

---

## 四、Tab 3 清零规则

### 4.1 字段清单

| # | 字段 | HTML 元素 | data-backend-node-id | 默认值 | 评级 |
|---|---|---|---|---|---|
| 1 | 积分于次年12月31号清零 | `<input type="checkbox">` | `515025` | ✅ 已勾选 | A 直接观察 |
| 2 | 积分于当年12月31号清零 | `<input type="checkbox">` | `515033` | ☐ 未勾选 | A 直接观察 |
| 3 | 保存 | `<button>` | `515043` | - | A 直接观察 |

### 4.2 业务实体分析

| 维度 | 值 | 评级 |
|---|---|---|
| 业务模式 | **次年 12 月 31 日清零**（当前配置） | A 直接观察（勾选状态） |
| 备选模式 | 当年 12 月 31 日清零 | A 直接观察（备选项） |
| 单选机制 | 2 个 checkbox（实为 radio，UI 表现为 checkbox） | A 直接观察 |
| 业务闭环 | 每年 12/31 触发清零 job | E 推断（job 调度机制未观察） |

> **观察说明**：UI 用 `<input type="checkbox">` 但语义上是单选（只能勾一个），因为保存后切换会取消另一个。属于"checkbox 样式 + radio 行为"。

---

## 五、9I.5 真实业务实体结论

### 5.1 核心业务实体（E 级，仅作业务建模参考）

| 实体 | 证据 | 评级 |
|---|---|---|
| **积分获取规则** | Tab 1 字段 + 公式 | A 直接观察 |
| **积分抵扣规则** | Tab 2 字段 + 默认 100:10 | A 直接观察 |
| **积分清零规则** | Tab 3 字段 + 当前勾选"次年" | A 直接观察 |
| **积分账户** | **本页面未直接观察**（应在 PAGE-004/302/303） | F 未观察 |
| **积分流水** | **本页面未直接观察**（应在交易/收费页） | F 未观察 |
| **积分商品/兑换商城** | 营销管理菜单有"积分"项，但未进入 | F 未观察 |

### 5.2 业务闭环（E 级推断）

```
消费（PAGE-007 收费）
  ↓
按积分规则（Tab 1: 1元=1积分）赠送积分
  ↓
积分入账户（PAGE-004 我的会员 / 302 门店会员 / 303 会员库，未观察）
  ↓
可选：用积分抵扣（Tab 2: 100积分=10元）→ 9I.11 支付方式"积分"落地
  ↓
次年 12/31 自动清零（Tab 3）
```

### 5.3 9I.5 ↔ 9I.11 "积分" 支付方式关系

| 证据 | 评级 |
|---|---|
| 9I.11 序号 5 名称 = "积分"（S1-15 侦察） | A 直接观察 |
| 9I.5 抵扣规则 = 抵扣元/积分配比 | A 直接观察 |
| 9I.11"积分"支付方式 = 9I.5 抵扣规则的支付渠道落地 | **B 多源证据**（业务逻辑一致，但未观察到直接 URL 桥或 API 桥） |
| 9I.11"积分"支付方式 选中后是否自动读取 9I.5 抵扣规则 | **F 未验证**（需进入 9I.11 槽位详情或实操） |

---

## 六、与已有侦察证据的跨页关系

### 6.1 跨页 ID 桥核对

| 来源页 | 字段/实体 | 与 9I.5 关系 | 评级 |
|---|---|---|---|
| 9I.1 公司信息 | hospitalId 字段 | SaaS 租户级最上层 → 9I.5 必然受其作用域约束 | E 推断（无直接 URL 桥） |
| 9I.2 会员类型 | memberTypeId | 积分规则可能按会员类型差异化（待侦察） | E/F 推断 |
| 9I.3 会员来源 | memberChannelId | 积分规则可能按获客渠道差异化（待侦察） | E/F 推断 |
| 9I.4 会员卡充值设置 | name="card"+value=1/2/3 | 充值设置 + 积分获取 = 会员价值体系 | E 推断 |
| 9I.11 支付方式 | 序号 5 = "积分" | 9I.5 抵扣规则的支付方式落地 | **A 直接证据** |

### 6.2 与业务执行页（PAGE-*）关系

| 业务页 | 9I.5 触达点 | 评级 |
|---|---|---|
| PAGE-004 我的会员 | 会员详情中应展示积分余额/流水 | E/F 推断（未进页） |
| PAGE-005 检查报告 | 不直接相关 | - |
| PAGE-007 收费 | 实付金额公式含"积分分摊"，落地抵扣 | A 直接观察（公式） |
| PAGE-302 门店会员 | 门店维度积分 | E/F 推断（未进页） |
| PAGE-303 会员库 | 全门店会员积分 | E/F 推断（未进页） |

---

## 七、P0/P1 阻塞清单（严格控制规则）

### 7.1 9I.5 新增 P0（不增加，为保持纪律）

| 项 | 描述 | 评级 |
|---|---|---|
| 无 | 9I.5 全部 3 Tab 字段已确认，无 P0 新增 | - |

> **判断依据**：9I.5 是规则配置（不涉及会员/交易实体），不直接影响一期 1:1 业务建模。

### 7.2 累计 P0 阻塞（接续 S1-18 末 56 项，未变化）

- 7 个字段名 P0（hospitalId/brandId/supplierId/depotId/feeItemId/memberTypeId/memberChannelId）
- 20+ 个跨页 ID 桥
- 6 个数据结构 P0
- 8 个业务规则 P0
- 4 个数据质量 P0
- 4 个系统设计 P0

### 7.3 新增 F/P1 待侦察

| 项 | 描述 | 评级 |
|---|---|---|
| 9I.5 ↔ PAGE-004/302/303 跨页 ID 桥 | 积分账户 ID/余额 ID 字段名 | F/P1 |
| 9I.11 序号 5 "积分"槽位详情 | 抵扣规则落地方式 | F/P1 |
| 9I.5 ↔ 9I.2/9I.3 联动 | 是否按会员类型/来源差异化积分 | F/P1 |
| 9I.5 ↔ 营销管理"积分"项 | 兑换商城规则 | F/P1 |

---

## 八、SaaS 业务架构新增发现

### 8.1 9I.5 在 SaaS 业务架构中的位置

```
SaaS 租户级（云梦视立康眼科医院）— 9I.1 公司信息
│
├── 诊所维护（侧边栏分组）
│   ├── 公司信息 9I.1（S1-11）
│   ├── 会员类型 9I.2（S1-12）
│   ├── 会员来源 9I.3（S1-13）
│   ├── 会员卡充值设置 9I.4（S1-14）
│   ├── **积分设置 9I.5（S1-19）** ⬅️ 当前
│   ├── 预约设置 9I.6（未侦察）
│   ├── ...
│   └── 报表默认日期 9I.17（S1-16）
│
├── 业务字典 9A.*（S1-03~S1-09）
│
├── 业务执行 PAGE-*
│
└── 营销管理（左侧主菜单）
    ├── 计次卡
    ├── **积分**（业务侧入口，与 9I.5 规则对接）
    ├── 发券宝
    └── ...
```

### 8.2 9I.5 三 Tab 与业务实体映射

| Tab | 业务实体 | 落库推测 | 评级 |
|---|---|---|---|
| Tab 1 积分规则 | 1 条租户级规则（单例） | `clinic_points_rule`（或 `clinic_setting` JSON 字段） | E 推断 |
| Tab 2 抵扣规则 | 1 条租户级规则（单例） | `clinic_points_deduct_rule` | E 推断 |
| Tab 3 清零规则 | 1 条租户级规则（单例，radio 单选） | `clinic_points_clear_rule` | E 推断 |

> **A 级观察**：3 Tab 各有独立保存按钮 = 后端大概率 3 个独立 API 或 1 个 API + ruleType 字段（待后续网络层侦察确认）。

---

## 九、侦察纪律自审

| 项 | 是否合规 | 备注 |
|---|---|---|
| 写操作 | ✅ 0 | 仅 inspect + click Tab（不触发任何保存） |
| 生产数据修改 | ✅ 0 | - |
| 历史文档修改 | ✅ 0 | - |
| 字段名脑补 | ✅ 无 | 仅记录可见字段名 |
| 业务关系脑补 | ✅ 已标注 E 评级 | 未升级 A |
| 自动进入下页面 | ✅ 无 | 仅在 3 Tab 间切换 |
| Tab 切换外扩大侦察 | ✅ 无 | 未进入 9I.6/9I.7 等 |
| 26 项 A/B/C/D/E/F | ✅ 全部覆盖 | 见各表格 |

---

## 十、待老板确认项

### 10.1 强烈建议老板主导（写操作风险）

1. **点击 3 个 Tab 的"保存"按钮**（先记录当前 1元=1积分 / 100积分=10元 / 次年清零 默认值）
2. **切换"消费自动积分" checkbox**（先记录当前已勾选状态）
3. **切换"次年/当年"清零 checkbox**（先记录当前"次年"已勾选状态）

### 10.2 待侦察项（后续 S1-20+）

1. **积分账户实体侦察**：进入 PAGE-004/302/303 找积分余额 ID 字段名
2. **9I.11 序号 5"积分"槽位详情**：找抵扣规则落地方式
3. **9I.5 ↔ 营销管理"积分"**：进入左侧主菜单"积分"项，侦察积分兑换商城
4. **9I.5 ↔ 9I.2/9I.3 联动**：是否按会员类型/来源差异化积分规则

---

## 附：26 项证据清单核对

| # | 证据项 | 评级 | 证据来源 |
|---|---|---|---|
| 1 | URL 路由 hash | A | URL 字符串 |
| 2 | 侧边栏路径 | A | query text |
| 3 | 页面类型（SPA） | A | hash 切换 + inspect 行为 |
| 4 | 3 Tab 架构 | A | query text + inspect |
| 5 | Tab 1 checkbox 默认状态 | A | semanticTree checked="true" |
| 6 | Tab 1 number input 默认值 | A | semanticTree name="1" |
| 7 | Tab 1 公式说明 | A | query text |
| 8 | Tab 1 实付金额计算式 | A | query text |
| 9 | Tab 2 默认 100 积分 | A | semanticTree name="100" |
| 10 | Tab 2 默认 10 元 | A | semanticTree name="10" |
| 11 | Tab 3 勾选状态（次年/当年） | A | semanticTree checked |
| 12 | Tab 3 单选机制 | A | 2 checkbox 互斥 + 独立保存 |
| 13 | data-backend-node-id 完整性 | A | inspect 拿到全部 |
| 14 | 3 个独立保存按钮 | A | 3 个 button 节点 |
| 15 | 9I.5 ↔ 9I.11 "积分" 关系 | B | S1-15 + S1-19 联合证据 |
| 16 | 9I.5 不含积分账户 ID 字段 | A | 全部 3 Tab 无 customerId/pointId/balanceId 字段 |
| 17 | 9I.5 不含 memberId 字段 | A | 全部 3 Tab 无 memberId 字段 |
| 18 | 9I.5 不含 hospitalId 字段 | A | 全部 3 Tab 无 hospitalId 字段 |
| 19 | 9I.5 在 SaaS 业务架构位置 | A | query text 侧边栏 |
| 20 | 9I.5 营销管理"积分"业务侧入口 | A | query text 主菜单 |
| 21 | 公式包含"积分分摊"项 | A | query text 实付金额计算式 |
| 22 | 公式"去掉小数取整" | A | query text |
| 23 | UI 用 checkbox 模拟单选 | A | 2 个 checkbox + 独立保存 |
| 24 | 公式 1元=1积分（默认） | A | Tab 1 默认值 |
| 25 | 抵扣比 1积分=0.1元（默认） | A | Tab 2 100:10 |
| 26 | 清零模式默认"次年 12/31" | A | Tab 3 勾选状态 |

**评级分布**：A=25 项 / B=1 项 / C=0 项 / D=0 项 / E=0 项 / F=0 项（保持严格）
**P0 新增**：0（保持纪律）
**写操作**：0
**生产数据修改**：0
**历史文档修改**：0

---

**侦察完成时间**：2026-09-01
**侦察人**：AI（S1-19 任务）
**下一步**：等待老板下一条指令（commit + push + 双重确认后汇报）
