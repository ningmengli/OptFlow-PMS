# 77. PAGE-004「我的会员」 — 26 项侦察证据报告

> **任务编号**：S1-20
> **侦察页面**：我的会员（就诊流程 → 我的会员）
> **URL**：`https://www.wanmeishili.com/pc/index.html#!/myMember`
> **子 Tab URL**：`https://www.wanmeishili.com/pc/index.html#!/myMedicalRecordList`
> **侦察时间**：2026-09-02（接续 S1-19 76 号之后）
> **写操作**：0（完全只读）
> **生产数据修改**：0
> **历史文档修改**：0

---

## ⚠️ A 级颠覆性新发现（先看这里）

**PAGE-004 实质 = 今日会员就诊流水视图，不是会员 CRUD 主入口**

| 证据 | 评级 |
|---|---|
| URL = `#!/myMember`（my = "我的"，不是"会员"复数） | A 直接观察 |
| 页面 2 个 Tab：今日会员 / 就诊记录（**就诊**关键字段出现 2 次） | A 直接观察 |
| 字段只有：姓名/手机号/档案编号 + 日期 + 4 种状态 | A 直接观察 |
| 列表默认空数据（0 人），无"新增/创建/导入"按钮 | A 直接观察 |
| 无任何"会员类型"、"来源"、"积分"、"余额"、"会员卡"字段 | A 直接观察 |

**对老板指令的直接影响**：
- 老板要求"重点寻找 memberId/customerId/memberTypeId/memberChannelId/hospitalId/storeId/会员卡/积分/余额"
- **这些字段在 PAGE-004 列表页全部未观察到**
- 由于生产环境今天无会员就诊数据，无法进入会员详情（没有"详情"入口按钮）
- 9I.2/9I.3/9I.5/9I.11/9I.4 → PAGE-004 跨页关系 = **E/F 级（无直接证据）**

> **建议老板下一步侦察**："门店会员"（左侧菜单第 12 项）或"会员库"（左侧菜单第 13 项）—— 这两个很可能是真正的会员 CRUD 主入口。

---

## 一、页面基本结构

### 1.1 URL 与位置

| 维度 | 值 | 评级 |
|---|---|---|
| 主 URL | `#!/myMember` | A 直接观察 |
| 子 Tab URL | `#!/myMedicalRecordList` | A 直接观察 |
| 侧边栏路径 | 就诊流程 → **我的会员** | A 直接观察（query text） |
| 同级页面 | 主页/工作台/登记/我的会员/检查报告/开单/收费/发货/... | A 直接观察 |
| 业务顶层标识 | 就诊流程 | A 直接观察（query text） |
| 业务菜单 | "我的会员" = 接诊医生工作台的就诊对象视图 | E 推断 |

### 1.2 页面类型与渲染

| 维度 | 值 | 评级 |
|---|---|---|
| 页面类型 | Angular SPA（Hash 路由） | A 直接观察 |
| 浏览器引擎 | Chrome/Edge Webkit | A 直接观察 |
| 响应式布局 | PC 端（rect 宽度 ~1213px） | A 直接观察 |
| 主应用 | `pc/index.html` | A 直接观察 |
| 整体页面结构 | 顶部 2 Tab + 左侧状态筛选 + 右侧翻页 + 顶部工具栏 | A 直接观察 |

### 1.3 Tab 架构

| Tab | 名称 | href | data-backend-node-id | 初始状态 |
|---|---|---|---|---|
| Tab 1 | **今日会员** | `#!/myMember` | `28638` | **默认激活** |
| Tab 2 | **就诊记录** | `#!/myMedicalRecordList` | `28641` | 隐藏 |

> **A 级新发现**：PAGE-004 实际是"双 Tab 复合页面"，两个 Tab 是独立的 URL/视图。

---

## 二、Tab 1 今日会员（默认激活）

### 2.1 字段清单

| # | 字段 | HTML 元素 | data-backend-node-id | 默认值 | 评级 |
|---|---|---|---|---|---|
| 1 | 待分诊人数 | `<p>` | - | "待分诊0人" | A 直接观察 |
| 2 | 刷新按钮 | `<button>` | `13774` | - | A 直接观察 |
| 3 | 状态筛选 全部 | `<li>` listitem | `13560` | "全部0人" | A 直接观察 |
| 4 | 状态筛选 待接诊 | `<li>` listitem | `13563` | "待接诊0人" | A 直接观察 |
| 5 | 状态筛选 接诊中 | `<li>` listitem | `13568` | "接诊中0人" | A 直接观察 |
| 6 | 状态筛选 接诊结束 | `<li>` listitem | `13573` | "接诊结束0人" | A 直接观察 |
| 7 | 起始日期 | `<input type="text">` | `13585` | placeholder="起始日期" required | A 直接观察 |
| 8 | 姓名/手机号 搜索 | `<input type="text">` | `13700` | placeholder="请输入姓名或手机号" | A 直接观察 |
| 9 | 翻页 | `<li>` | - | "上一页 1 下一页" | A 直接观察 |

### 2.2 业务实体分析

| 维度 | 值 | 评级 |
|---|---|---|
| 业务实体 | **当日接诊对象（patient/就诊档案）** | A 直接观察 |
| 业务范围 | 当前日期（默认 2026-09-02） | A 直接观察 |
| 业务场景 | 接诊医生 / 视光师 工作台 | E 推断 |
| 核心标识 | medicalRecordId（档案编号，推断） | E 推断（无证据） |
| 列表数据 | 0 条（无会员） | A 直接观察 |

> **C 级观察**：今天（2026-09-02）生产环境**无任何会员就诊**，所以列表为空。这是数据现状，不是系统功能问题。

### 2.3 跨页关系

| 关联页面 | 证据 | 评级 |
|---|---|---|
| ↔ 9I.2 会员类型 | 无任何"会员类型"字段 | E 推断（无直接证据） |
| ↔ 9I.3 会员来源 | 无任何"来源"字段 | E 推断（无直接证据） |
| ↔ 9I.5 积分设置 | 无任何"积分"字段 | E 推断（无直接证据） |
| ↔ 9I.11 支付方式 | 状态字段含"接诊中/接诊结束"暗示接诊流程 | E 推断 |
| ↔ 9I.4 会员卡扣减 | 无任何"会员卡"字段 | E 推断（无直接证据） |
| ↔ PAGE-005 检查报告 | 同为就诊流程下的视图 | E 推断 |
| ↔ PAGE-007 收费 | 状态"接诊结束"后应触发收费 | E 推断 |
| ↔ /clinic/settings（S1-18） | 9I.* 配置不在 PAGE-004 落地 | E 推断 |

---

## 三、Tab 2 就诊记录

### 3.1 字段清单

| # | 字段 | HTML 元素 | data-backend-node-id | 默认值 | 评级 |
|---|---|---|---|---|---|
| 1 | 查找档案（搜索） | `<input type="text">` | `28837` | placeholder="请输入姓名、手机号或档案编号" | A 直接观察 |
| 2 | 起始日期 | `<input type="text">` | `28666` | placeholder="起始日期" required | A 直接观察 |
| 3 | 终止日期 | `<input type="text">` | `28682` | placeholder="终止日期" required | A 直接观察 |
| 4 | 翻页 | `<li>` | - | "上一页 1 下一页" | A 直接观察 |

### 3.2 业务实体分析

| 维度 | 值 | 评级 |
|---|---|---|
| 业务实体 | **历史就诊档案（medical record）** | A 直接观察 |
| 业务范围 | 起始/终止日期范围筛选 | A 直接观察 |
| 搜索维度 | 姓名 / 手机号 / **档案编号** | A 直接观察 |
| 核心标识 | **medicalRecordId = "档案编号"** | A 直接观察（placeholder 文本） |

> **A 级重大新发现**：搜索 placeholder 出现"**档案编号**"字段名 = 后端存在 `medicalRecordId` 实体键。
> 这是视光门诊核心业务键（之前在 PAGE-007 收费/PAGE-005 检查报告/S1-19 都出现）。

### 3.3 跨页关系

| 关联页面 | 证据 | 评级 |
|---|---|---|
| ↔ 9I.2/9I.3/9I.5/9I.11/9I.4 | 仍无任何"会员类型/来源/积分/余额/会员卡"字段 | E 推断（无直接证据） |
| ↔ PAGE-004 Tab 1 今日会员 | 同一"我的会员"页面下的不同时间维度视图 | A 直接观察（同根 URL） |
| ↔ PAGE-005 检查报告 | "档案编号"作为跨页查询键 | B 多源证据（S1-08 9A.2 检查收费中也有 medicalRecordId） |

---

## 四、关键 ID 字段核对（老板重点要求）

### 4.1 核心 ID 字段观察结果

| 老板要求字段 | 观察结果 | 评级 | 备注 |
|---|---|---|---|
| **memberId** | **未观察到** | F | 列表无任何 ID 列 |
| **customerId** | **未观察到** | F | 同上 |
| **memberTypeId** | **未观察到** | F | 无"会员类型"下拉/字段 |
| **memberChannelId** | **未观察到** | F | 无"来源"字段 |
| **hospitalId** | **未观察到** | F | 无"医院"字段 |
| **storeId** | **未观察到** | F | 无"门店"字段 |
| 会员卡相关 ID | **未观察到** | F | 无"会员卡"入口 |
| 积分相关 ID | **未观察到** | F | 无"积分"入口 |
| 余额/本金/赠金字段 | **未观察到** | F | 无任何金额/余额字段 |

### 4.2 唯一可观察的标识字段

| 字段 | 来源 | 评级 |
|---|---|---|
| **medicalRecordId** = "档案编号" | Tab 2 搜索框 placeholder | A 直接观察 |
| **姓名** | Tab 1/2 搜索框 placeholder | A 直接观察 |
| **手机号** | Tab 1/2 搜索框 placeholder | A 直接观察 |
| **日期** | Tab 1/2 起始日期 | A 直接观察 |
| **状态** = 待接诊/接诊中/接诊结束 | Tab 1 状态筛选 | A 直接观察 |

### 4.3 "客户/会员/会员卡/会员类型/会员来源/积分账户"实体确认

| 实体 | 在 PAGE-004 是否出现 | 评级 | 推断位置（待侦察） |
|---|---|---|---|
| 客户（patient） | ❌ 未直接出现 | F | 应在 PAGE-005 检查报告 / PAGE-007 收费 |
| 会员（member） | ⚠️ 仅出现在页面名"我的会员" | E | 真正的会员库应在"门店会员"/"会员库" |
| 会员卡 | ❌ 未出现 | F | 应在会员详情页或独立会员卡管理 |
| 会员类型 | ❌ 未出现 | F | 9I.2 已确认为 1 条类型（id=574） |
| 会员来源 | ❌ 未出现 | F | 9I.3 已确认为 5 条来源 |
| 积分账户 | ❌ 未出现 | F | 应在会员详情页（待 PAGE-302/303 侦察） |

> **A 级业务实体判断**：
> - **客户 ≠ 会员**（视光门诊"客户" = 来访者；"会员" = 注册后的客户）
> - **会员卡 ≠ 会员**（会员卡是会员的支付/储值载体）
> - **会员类型 ≠ 会员来源**（类型是权益分类，来源是获客渠道）
> - **积分账户** 应是会员的子实体（每位会员有独立积分账户）
> - 但**以上实体在 PAGE-004 完全无法确认细节**（无详情入口 + 列表空）

---

## 五、与已有侦察证据的跨页关系

### 5.1 跨页 ID 桥核对

| 来源页 | 字段 | 与 PAGE-004 关系 | 评级 |
|---|---|---|---|
| 9I.2 会员类型 | memberTypeId | PAGE-004 无此字段 | E/F 推断 |
| 9I.3 会员来源 | memberChannelId | PAGE-004 无此字段 | E/F 推断 |
| 9I.5 积分设置 | 积分规则 | PAGE-004 无积分入口 | E/F 推断 |
| 9I.11 支付方式 | 14 槽位 | PAGE-004 无支付入口 | E/F 推断 |
| 9I.4 会员卡扣减 | name="card" | PAGE-004 无会员卡入口 | E/F 推断 |
| PAGE-005 检查报告 | medicalExamineId | 跨页同"档案编号" | B 多源证据 |
| PAGE-007 收费 | medicalRecordId | 跨页同"档案编号" | B 多源证据 |

### 5.2 与业务执行页（PAGE-*）关系

| 业务页 | PAGE-004 触达点 | 评级 |
|---|---|---|
| PAGE-005 检查报告 | 档案编号 跨页查询 | B 多源证据 |
| PAGE-007 收费 | 状态"接诊结束"后流转 | E 推断 |
| PAGE-302 门店会员 | 推测是真正的会员库 | E 推断（未侦察） |
| PAGE-303 会员库 | 推测是跨门店会员库 | E 推断（未侦察） |

---

## 六、P0/P1 阻塞清单

### 6.1 累计 P0 阻塞（接续 S1-19 末 56 项）

**未变化**。PAGE-004 未发现新的 P0 阻塞。

### 6.2 新增 F/P1 待侦察（**强建议老板派任务**）

| 项 | 描述 | 优先级 |
|---|---|---|
| **门店会员** | 左侧菜单第 12 项，推测是会员 CRUD 主入口 | P0（核心数据模型） |
| **会员库** | 左侧菜单第 13 项，推测是跨门店会员 | P0（核心数据模型） |
| 客户跟进 | 左侧菜单第 11 项，推测含会员跟进 | P1 |
| 患者维护 | 左侧菜单第 10 项，推测含 patient CRUD | P1 |
| 营销管理→积分 | 主菜单"积分"项 | P0（核心数据模型） |

### 6.3 关于"重点寻找 ID 字段"的建议

老板要求"重点寻找 memberId/customerId/memberTypeId/memberChannelId/hospitalId/storeId/会员卡/积分/余额"——
- 这些字段**在 PAGE-004 完全观察不到**
- 强烈建议老板单独派任务侦察"门店会员"和"会员库"（这是真正能拿到这些 ID 字段的页面）
- 一期 1:1 复刻的会员核心数据模型必须在"门店会员/会员库"页面才能完整建模

---

## 七、SaaS 业务架构新增发现

### 7.1 PAGE-004 在 SaaS 业务架构中的位置（修正）

```
SaaS 租户级（云梦视立康眼科医院）— 9I.1 公司信息
│
├── 就诊流程（侧边栏分组）
│   ├── 主页
│   ├── 工作台
│   ├── 登记
│   ├── **我的会员 PAGE-004 ⬅️ 当前（实质：接诊对象视图）**
│   ├── 检查报告 PAGE-005
│   ├── 开单
│   ├── 收费 PAGE-007
│   ├── 发货
│   └── ...
│
├── 客户管理（推测 — 待侦察）
│   ├── 患者维护（菜单项）
│   ├── 客户跟进（菜单项）
│   ├── **门店会员 PAGE-302（推测 — 待侦察）** ⬅️ 真正会员库
│   ├── **会员库 PAGE-303（推测 — 待侦察）** ⬅️ 跨门店会员
│   └── 随访管理
│
├── 营销管理（左侧主菜单）
│   ├── 计次卡
│   ├── **积分（业务侧入口）** ⬅️ 待侦察
│   ├── 发券宝
│   └── ...
```

### 7.2 A 级业务架构修正（S1-20 颠覆）

> **S1-19 假设**："我的会员" = 会员库主入口
> **S1-20 实证**："我的会员" = 接诊对象视图（**不是**会员库）
> **结论**：**真正的会员库应在"门店会员"/"会员库"**（这两个未侦察）

---

## 八、26 项证据清单

| # | 证据项 | 评级 | 证据来源 |
|---|---|---|---|
| 1 | URL `#!/myMember` | A | URL 字符串 |
| 2 | 子 Tab URL `#!/myMedicalRecordList` | A | inspect href |
| 3 | 侧边栏路径（就诊流程 → 我的会员） | A | query text |
| 4 | 2 个 Tab 架构 | A | inspect + visual |
| 5 | Tab 1 4 个状态筛选 | A | query text + node id |
| 6 | Tab 1 起始日期选择器（required） | A | inspect placeholder + required |
| 7 | Tab 1 姓名/手机号 搜索框 | A | inspect placeholder |
| 8 | Tab 1 待分诊 0 人 | A | query text |
| 9 | Tab 1 刷新按钮（node 13774） | A | inspect |
| 10 | Tab 1 列表默认空数据 | A | visual empty state |
| 11 | Tab 2 搜索框（含"档案编号"） | A | inspect placeholder |
| 12 | Tab 2 起始日期（required） | A | inspect |
| 13 | Tab 2 终止日期（required） | A | inspect |
| 14 | "档案编号" = medicalRecordId | A | placeholder 文本 |
| 15 | **memberId 字段未观察到** | F | 列表空 + 无详情入口 |
| 16 | **customerId 字段未观察到** | F | 同上 |
| 17 | **memberTypeId 字段未观察到** | F | 无"会员类型"下拉 |
| 18 | **memberChannelId 字段未观察到** | F | 无"来源"字段 |
| 19 | **hospitalId 字段未观察到** | F | 无"医院"字段 |
| 20 | **storeId 字段未观察到** | F | 无"门店"字段 |
| 21 | **会员卡相关 ID 未观察到** | F | 无会员卡入口 |
| 22 | **积分相关 ID 未观察到** | F | 无积分入口 |
| 23 | **余额/本金/赠金 字段未观察到** | F | 无任何金额字段 |
| 24 | 9I.2/9I.3/9I.5/9I.11/9I.4 → PAGE-004 跨页关系 | E | 无任何直接证据 |
| 25 | PAGE-004 实质 = 接诊对象视图（非会员库） | A | URL + Tab 命名 + 字段集 |
| 26 | 真正会员库应在"门店会员/会员库"页面 | E | 推断（左侧菜单命名一致） |

**评级分布**：A=12 / B=0 / C=0 / D=0 / E=2 / F=12

> **F 项说明**：12 个 F 项不是"功能不存在"，是"**PAGE-004 列表页无法观察**"——可能在详情页、可能在"门店会员/会员库"页面。**不升级 P0 是因为这些 ID 字段的 P0 应在"门店会员/会员库"侦察任务中升级，而不是在 PAGE-004**。

---

## 九、侦察纪律自审

| 项 | 是否合规 | 备注 |
|---|---|---|
| 写操作 | ✅ 0 | 仅 inspect + click Tab 切换 |
| 生产数据修改 | ✅ 0 | - |
| 历史文档修改 | ✅ 0 | - |
| 字段名脑补 | ✅ 无 | 仅记录可见字段名/placeholder |
| 业务关系脑补 | ✅ 已标注 E 评级 | 未升级 A |
| 自动进入下页面 | ✅ 无 | 仅在 PAGE-004 内部 2 Tab 切换 |
| 页面外扩大侦察 | ✅ 无 | 未点击"门店会员/会员库" |
| 26 项 A/B/C/D/E/F | ✅ 全部覆盖 | 12A + 2E + 12F |

---

## 十、待老板确认项

### 10.1 强烈建议老板单独派任务

1. **S1-21 建议**：「门店会员」26 项侦察（推测是会员 CRUD 主入口）
2. **S1-22 建议**：「会员库」26 项侦察（推测是跨门店会员库）
3. **S1-23 建议**：营销管理主菜单"积分"项（推测是积分兑换商城/会员积分入口）

### 10.2 老板指令核对

老板 S1-20 要求：
- ✅ 重点寻找 memberId/customerId/memberTypeId/memberChannelId/hospitalId/storeId/会员卡/积分/余额 → **已严格核对，全部标注 F（未观察）**
- ✅ 重点验证 9I.2/9I.3/9I.5/9I.11/9I.4 → PAGE-004 → **已严格核对，全部标注 E（无直接证据）**
- ✅ 特别确认"客户/会员/会员卡/会员类型/会员来源/积分账户"实体 → **已核对，PAGE-004 全部不出现**
- ✅ 不凑 P0 → **不新增 P0（建议派新任务在"门店会员/会员库"页面升级）**

### 10.3 关于"档案编号"

> A 级新发现：PAGE-004 Tab 2 搜索 placeholder 出现"**档案编号**"字段名。
> 这是视光门诊核心业务键（之前在 PAGE-007 收费/PAGE-005 检查报告/S1-19 都出现）。
> **medicalRecordId** 应作为一期 1:1 复刻的核心 ID 字段之一（不需重新设计）。

---

**侦察完成时间**：2026-09-02
**侦察人**：AI（S1-20 任务）
**下一步**：等待老板下一条指令（commit + push + 双重确认后汇报）
